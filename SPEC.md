# Fourth Wall Phone — 功能规格（SPEC）

> 本文档是 `moli-ovo1/fourth-wall-phone` 的持续设计基线。
>
> **维护规则：** 后续每确认一个新功能、修改一条既有规则或废弃一个方案，都同步更新本文件。未确认的想法应标记为“待讨论”，不能悄悄当成既定需求实现。

## 1. 项目定位

将 LittleWhiteBox 的“第四面墙聊天”核心能力独立出来，并扩展成一个 **iPhone / 微信式的场外通讯系统**。

它不是正文聊天的一部分。场外私聊、群聊、自动吐槽、自动点评等内容默认都不写入酒馆正文历史，也不占正文楼层。

最终系统包含：

- 通讯录 / 人格
- 私聊
- 群聊
- 第四面墙人格
- 编剧、攻略及未来其他场外人格
- 自动吐槽
- 自动点评
- 独立场外 API
- 后续的“采纳”机制

UI 最终朝 iPhone / 微信式体验制作，但 **美化最后做**。当前优先保证数据结构、聊天逻辑、触发逻辑和生成链稳定。

---

## 2. 已有基础

当前已有 `第四面墙聊天 Lite v1.2` 原型，其稳定成果包括：

- 可拖动悬浮球入口
- 点击悬浮球打开聊天小窗
- 读取最近正文
- 场外聊天历史与正文分离
- 基础消息发送

后续开发应尽量保留已经稳定的悬浮球启动、挂载、定位方式，不因聊天功能重构破坏入口。

---

## 3. 第四面墙人格

### 3.1 来源

第四面墙联系人是系统默认内置人格。

其默认系统提示词 / Meta Protocol 以 LittleWhiteBox 原版第四面墙提示词为基线，而不是 Lite 版的简化提示词。

提示词允许用户编辑，并提供“恢复 LittleWhiteBox 默认提示词”的能力。

### 3.2 LittleWhiteBox 第四面墙能力取舍

除明确排除项外，原版第四面墙聊天能力均计划保留/恢复。

**需要：**

- 原版完整 Meta Protocol / 系统提示词机制
- 正常第四面墙私聊
- 正文上下文读取与清理
- 独立第四面墙聊天历史
- 真实时间信息
- 消息之间的时间间隔进入模型上下文
- 自动吐槽
- 正文生成完成后的主动反应
- 检测用户编辑自己的正文消息并可吐槽
- 检测用户编辑 AI 正文消息并可吐槽
- 自动吐槽写入场外聊天历史
- 自动吐槽冷却机制
- 重新生成场外回复
- 停止正在生成
- 流式输出
- 自定义提示词
- 恢复默认提示词

**明确不要：**

- 全屏聊天模式
- 图片生成
- TTS / 语音
- AI 回复图片增强
- AI 回复语音增强

---

## 4. 通讯录 / 人格系统

### 4.1 核心原则

“人格”和“聊天会话”必须分离。

通讯录保存人格定义；私聊/群聊只引用人格，而不是复制人格提示词。

因此，同一个人格可以同时存在于：

- 自己的私聊
- 一个群聊
- 多个不同群聊

修改通讯录里的人格提示词后，后续各会话均引用更新后的定义。

### 4.2 联系人内容

通讯录联系人至少包含：

- 唯一 ID
- 名称
- 头像
- 简介 / 备注
- 人格系统提示词
- 人格类型（用于组织和默认模板，不作为硬性能力限制）

计划人格包括但不限于：

- 第四面墙（默认内置）
- 编剧
- 攻略
- 红笔编辑（后续）
- NPC / 角色
- 用户自定义人格

第四面墙默认提示词先按 LittleWhiteBox 原版实现；编剧、攻略及其他人格的正式提示词 **最后再专项创作**。

### 4.3 不需要的联系人权限

不设置“是否允许加入群聊”。

通讯录中的联系人默认均可被用户通过聊天页面的群聊入口拉入群聊。

### 4.4 API 不属于联系人资料（当前阶段）

当前不为每个人格单独配置 API。

后期可以考虑联系人级模型/API覆盖，因为不同模型能力不同；但这属于未来扩展，当前不实现。

---

## 5. 会话系统

### 5.1 私聊

每个联系人可以拥有独立私聊。

私聊拥有自己的：

- 消息历史
- 自动吐槽设置
- 自动吐槽触发状态 / 冷却状态

**私聊不提供“自动点评”。**

编剧、攻略等人格在单独聊天框中不会因为正文生成而自动点评；用户主动找它们聊天时正常回复。

### 5.2 群聊

模拟微信逻辑。

聊天页面右上角提供添加/建立群聊入口：

1. 打开通讯录选择器
2. 勾选想拉入群聊的人格
3. 建立群聊 / 添加群成员

无需预先在联系人资料中设置群聊权限。

群聊拥有独立消息历史及独立自动行为配置。

---

## 6. 自动吐槽（Commentary）

### 6.1 定位

自动吐槽是 **概率型主动行为**。

目标是模拟场外人格看到正文变化后的即时、简短、偏情绪化反应，而不是正式剧情分析。

自动吐槽与自动点评是两套独立机制。

### 6.2 设置位置

自动吐槽的具体配置放在 **每个聊天框自己的设置** 中，而不是通讯录联系人资料中。

系统设置只提供自动吐槽的全局总开关。

### 6.3 私聊设置

私聊提供：

- 自动吐槽：开 / 关
- 触发概率：0–100%

不提供用户可设置的冷却时间。

### 6.4 群聊设置

群聊提供：

- 自动吐槽：开 / 关
- 触发概率：0–100%

不选择吐槽对象。

当群聊的自动吐槽概率命中后，将当前群成员交给普通群聊发言编排器，由模型判断本轮哪些成员适合开口。

不是所有群成员都必须吐槽。

### 6.5 触发事件

第四面墙原版相关触发能力需要保留，包括：

- 正文 AI 生成完成
- 用户编辑自己的正文消息
- 用户编辑 AI 的正文消息

一次正文事件发生后，系统检查所有开启了自动吐槽的会话。

每个会话独立进行自己的概率判定。

例如：

- 第四面墙私聊：50%
- 群聊 A：30%
- 群聊 B：100%

三者互不影响，可以在同一层正文分别命中。

### 6.6 不使用业务冷却时间

自动吐槽不再设置“冷却时间”。

触发频率只由：

- 开关
- 触发概率

共同决定。

例如触发概率设为 100% 时，正常理解应为每次符合条件的正文事件都允许触发，而不应再被额外的几分钟冷却拦截。

### 6.7 技术防重复

虽然没有业务意义上的冷却时间，但底层必须保留 **事件去重机制**。

如果酒馆因为事件监听等原因，对同一个正文变化重复广播多次，只允许该会话处理一次。

该机制属于防 Bug 逻辑：

- 不暴露给用户设置
- 不改变正常概率
- 不阻止下一次真正的新正文事件

### 6.8 独立状态

每个会话独立保存：

- 自动吐槽开关
- 触发概率
- 已处理正文事件标识等必要状态

一个会话触发吐槽，不影响其他会话。

---

## 7. 自动点评（Review）

### 7.1 定位

自动点评是 **周期型群聊行为**。

用于当前群内的场外人格围绕最新正文进行分析、判断、建议、争论和补充。

它与自动吐槽完全独立：

- 自动吐槽：概率触发、即时反应
- 自动点评：固定 N 回合触发、完整讨论

### 7.2 只属于群聊

自动点评只存在于群聊。

单独与第四面墙、编剧、攻略或其他人格私聊时，不提供自动点评设置。

### 7.3 群聊设置

群聊提供：

- 自动点评：开 / 关
- 每 N 个正文回合触发一次

不选择点评对象。

### 7.4 全员必须参与

当自动点评触发时，**当前群聊中的每一个人格都必须至少参与一次本轮点评**。

自动点评不使用普通群聊的“AI 判断谁要不要说”规则。

因此：

**普通群聊：AI 决定谁参与。**

**自动点评：全员参与。**

### 7.5 发言顺序

自动点评触发后：

1. 第一位发言者随机产生；
2. 第一位阅读最新正文并发表自己的点评；
3. 后续尚未发言的成员必须看到前面本轮已经产生的群消息；
4. 后续成员根据正文与已有讨论继续接话；
5. 可以赞同、反驳、补充、吐槽或改变讨论重点；
6. 直到所有当前群成员都至少发言一次。

不能把每个人的点评作为彼此不可见的独立回答并行生成。

### 7.6 允许二次发言

“每人至少一次”不等于“每人只能一次”。

例如：

编剧先提出一个判断，
攻略反驳，
第四面墙补充，
随后编剧认为有必要，可以再次回应攻略。

因此自动点评允许部分人格二次发言，使讨论更接近真实群聊。

### 7.7 防无限对话

为了避免场外人格无限自行聊天，自动点评存在一个 **隐藏的单轮安全上限**。

默认规则：

`单轮最大场外消息数 = 当前群成员数 × 2`

例如：

- 2 人群：最多 4 条
- 3 人群：最多 6 条
- 4 人群：最多 8 条

该数字属于内部安全机制，不作为普通用户设置项。

正常情况下，所有成员各说一次即可结束；只有存在明显接话价值时才继续二次发言。

### 7.8 正文回合定义

自动点评中的“N 回合”按 **有效的 AI 正文回复** 计算。

一次 AI 完成正文回复：

`+1 正文回合`

用户消息本身不单独计算为一个自动点评回合。

### 7.9 删除与重 Roll

自动点评计数必须能跟随当前正文状态变化，而不是只使用一个永远递增的简单计数器。

规则：

- 删除一条有效 AI 正文：对应正文回合回退
- 删除多条正文：按实际剩余正文状态回退
- Re-roll / 重生成同一条 AI 正文：仍属于原来的同一个正文回合，不额外 +1
- 如果被重 Roll 的正好是一个应触发自动点评的第 N 回合，则允许针对新的正文重新产生这一轮点评

实现时应尽量根据当前有效正文状态判断回合位置，避免计数与实际聊天记录失同步。
---

## 8. 系统级自动行为总开关

系统设置提供：

- 自动吐槽：全局总开关
- 自动点评：全局总开关

系统总开关只负责“一键全部暂停/恢复”，**不负责选择联系人、不负责设置具体概率或 N 层**。

关闭总开关时，各聊天原有配置应保留。重新打开后，恢复各聊天自己的设置，无需重新配置。

---

## 9. 同一正文层的多会话触发

**允许。**

同一层正文结束后，多个不同会话可以分别触发自己的自动行为。

例如：

- 第四面墙私聊概率命中，产生一条自动吐槽；同时
- 某群聊刚好达到“每 3 层自动点评”，群里开始一轮点评。

系统不应为了避免同时发生而暗中拦截其中一个。

---

## 10. 群聊发言编排器

群聊存在两种不同的发言模式：

1. 普通群聊模式
2. 自动点评模式

二者不能混为一套“所有人必须回复”的规则。

### 10.1 普通群聊

用户主动在群聊中发送消息时，进入普通群聊模式。

不人为规定：

- 最少必须几个人回答
- 最多只能 2 人
- 最多只能 3 人

参与人数的自然上限就是当前群成员数量。

例如群里有 4 个人格，本轮：

- 可以只有 1 人说
- 可以 2 人说
- 可以 3 人说
- 也可以 4 人全部都说

由模型根据当前话题和各人格立场判断。

### 10.2 第一位发言者

普通群聊的第一位发言者 **优先按照内容相关度选择**，而不是纯随机。

模型需要判断：

- 谁与当前话题最相关
- 谁最有理由先回应
- 谁的人格立场最容易首先产生反应

### 10.3 未被 @ 的成员可以插话

没有被用户 `@` 的人格依然可以参与。

只要模型认为：

- 他对此有明显意见
- 他会自然插一句
- 他想反驳别人
- 他能补充有价值的信息
- 根据人格关系，他不太可能保持沉默

就可以加入本轮对话。

### 10.4 @ 指定人格

用户 `@某人格` 时：

被点名人格必须参与本轮回复。

但 `@` 不代表其他人必须沉默。

其他成员仍然可以根据相关度和群聊语境插话。

### 10.5 后发言者必须阅读前面的新消息

同一轮群聊中：

第一位先生成。

第二位在生成前必须获得：

- 用户刚刚发送的消息
- 第一位刚刚生成的回复

第三位则获得：

- 用户消息
- 第一位回复
- 第二位回复

以此类推。

因此后面的发言应该表现成真正的群聊：

- 接话
- 赞同
- 反驳
- 补充
- 调侃
- 改变讨论方向

而不是每个人都像没看见别人一样，从头独立回答用户。

### 10.6 自动吐槽使用普通群聊参与规则

群聊自动吐槽概率命中后：

不强制全员发言。

仍然由普通群聊编排器判断当前有哪些人格适合产生即时反应。

所以：

**自动吐槽 = 概率决定“这个群要不要动”。**

**群聊编排器 = 决定“群里谁动”。**

### 10.7 自动点评使用特殊全员模式

自动点评不使用“谁适合就谁说”的参与规则。

自动点评一旦达到 N 回合触发条件：

- 当前群所有人格必须至少发言一次
- 第一位随机
- 后面成员必须读取前面的最新发言
- 允许部分成员二次接话
- 整轮受到内部最大消息数限制

详细规则见第 7 节。

### 10.8 核心原则

普通群聊追求：

> 有话就说，没话可以不说。

自动点评追求：

> 所有人都要参与，但不能像排队交作业，而要形成真正的连续讨论。
---

## 11. 独立场外 API

### 11.1 与正文主 API 分离

场外通讯系统使用独立的 API 配置。

包括：

- 私聊
- 群聊
- 自动吐槽
- 自动点评
- 后续编剧 / 攻略 / 编辑等场外人格功能

都通过场外 API 层生成。

场外系统不应直接继承正文当前正在使用的 API 配置，也不应依赖正文生成队列才能工作。

### 11.2 统一 API 适配层

参考 LittleWhiteBox 的 Agent / API 架构思想：

不要让第四面墙、编剧、攻略、群聊等模块各自直接实现自己的网络请求。

统一结构应为：

`场外功能 → 统一 Generation / Agent 层 → Provider Adapter → 实际 API`

聊天模块只负责准备：

- 人格提示词
- 当前正文上下文
- 当前会话历史
- 群聊本轮新增消息
- 生成参数

真正如何调用不同模型，由 API 适配层处理。

### 11.3 当前版本使用一套全局场外 API

第一阶段所有场外人格共用同一套系统级场外 API 配置。

系统设置预计包含：

- Provider / API 类型
- API 地址
- API Key
- 模型
- 必要生成参数
- 连接测试

具体 UI 字段后续单独设计。

### 11.4 Provider 兼容方向

场外 API 适配层的设计应能容纳：

- OpenAI
- OpenAI-compatible
- Claude
- Gemini

具体第一版一次实现哪些 Provider，可在正式编写 API 模块时进一步确认。

但底层接口从一开始就不能写死为单一 OpenAI 请求格式。

### 11.5 流式与中断

统一生成层需要支持：

- 流式回复
- Abort / 停止生成
- 生成错误处理
- 重新生成

这些能力由统一 Generation / Agent 层提供，而不是每个联系人重复实现。

### 11.6 为未来联系人级 API 覆盖预留结构

当前 UI 不提供每个联系人单独 API。

但底层调用关系应允许未来增加：

`联系人自己的模型/API覆盖 > 系统默认场外 API`

例如以后可以做到：

- 第四面墙使用模型 A
- 编剧使用更聪明的模型 B
- 普通 NPC 使用便宜模型 C

当前阶段只预留架构，不实际开放设置。
---

## 12. 上下文与时间感

场外模型需要能够理解当前正文，但场外消息不能写回正文。

需要恢复/完善：

- 读取指定范围的真实正文楼层
- 清理正文中的 thinking / system / meta 等不应进入场外聊天的内容
- 正确区分正文双方身份
- 将最近正文作为场外模型上下文
- 每条场外消息保存真实时间
- 将消息时间及相邻消息间隔提供给模型
- 各会话历史彼此独立

UI 上“显示了时间”不等于模型拥有时间感；时间信息必须真正进入生成上下文。

---

## 13. 场外回复控制

计划恢复 LittleWhiteBox 原版相关能力：

- 流式输出
- 停止当前生成
- 重新生成上一条场外回复

重新生成不能简单追加一条新回复，需要正确处理当前会话历史。

---

## 14. 提示词管理

通讯录人格保存自己的系统提示词。

第四面墙：

- 默认使用 LittleWhiteBox 原版 Meta Protocol
- 可编辑
- 可恢复默认

编剧、攻略及其他场外人格：

- 先完成通讯录和聊天能力
- 正式提示词最后专项设计
- 后续会参考“酒馆小狸 Live”中用户认可的编剧 / 攻略 / 其他场外人格思路，但不得仅凭加载器猜测其未公开实现

---

## 15. “采纳”机制（已纳入方向，细节待讨论）

参考“酒馆小狸 Live”：场外人格的建议默认不进入主线；用户主动点击“采纳”后，才将选定建议送入下一轮正文生成上下文。

本项目计划保留这一方向，尤其适用于：

- 编剧建议
- 攻略建议
- 编辑建议

具体采纳数据格式、有效时长、一次性/持续性等规则 **尚未确认**。

---

## 16. UI / 美化

### 16.1 最终方向

最终模拟 iPhone / 微信式通讯体验，包括通讯录、聊天列表、私聊、群聊等。

聊天页右上角提供类似微信的群聊/成员管理入口。

### 16.2 开发顺序

**UI 美化最后做。**

当前不要为了动画、主题、头像布局等视觉细节阻碍核心功能开发。

优先顺序：

1. 数据结构与通讯录
2. 第四面墙核心聊天能力
3. 自动吐槽
4. 自动点评
5. 群聊发言编排
6. 独立场外 API
7. 提示词管理
8. 采纳等扩展功能
9. 最终 iPhone / 微信 UI 美化

---

## 17. 当前明确废弃的旧方案

以下方案在讨论中提出过，但已被后续设计取代，不应按旧方案实现：

- ❌ 在系统设置里为自动吐槽/点评统一选择联系人  
  改为：具体自动行为设置属于每个会话；系统设置只保留总开关。

- ❌ 在每个联系人资料里设置自动吐槽/自动点评资格  
  改为：通讯录只定义人格；自动行为属于会话。

- ❌ 在联系人资料里设置“是否允许加入群聊”  
  改为：直接从聊天页选择通讯录联系人拉群。

- ❌ 自动点评使用百分比概率  
  改为：每 N 层正文固定触发。

- ❌ 自动吐槽与自动点评合并成同一个自动行为  
  改为：两套独立触发机制，共用需要的群聊基础设施。

- ❌ 每个场外人格当前单独配置 API  
  改为：当前统一使用系统级场外 API；仅为未来保留扩展空间。

---

## 18. 尚待继续讨论

以下问题还没有最终拍板：

1. 通讯录的完整数据结构。
2. 通讯录、聊天列表、联系人详情等按钮和入口的具体放置位置。
3. 联系人新增、编辑、删除以及头像设置方式。
4. “采纳”建议进入正文的具体注入方式和生命周期。
5. 独立场外 API 设置页面的具体字段与交互。
6. 第一版实际优先支持哪些 Provider。
7. 编剧、攻略、红笔编辑等正式人格提示词。
8. 最终 iPhone / 微信 UI 的具体视觉与交互细节。

---

## 19. 文档维护约定

每次设计讨论出现明确结论后：

1. 更新本 `SPEC.md`；
2. 新结论写入对应章节；
3. 被推翻的旧方案应删除或移入“明确废弃的旧方案”；
4. 尚未拍板的内容只能放入“待讨论”，不得伪装成确定需求；
5. 实现代码应以最新 `main` 分支的 `SPEC.md` 为需求基线。
