# Fourth Wall Phone — 功能规格（SPEC）

> 本文档是 `moli-ovo1/fourth-wall-phone` 的持续设计基线。
>
> **维护规则：** 后续每确认一个新功能、修改一条既有规则或废弃一个方案，都同步更新本文件。未确认的想法应标记为“待讨论”，不能悄悄当成既定需求实现。

## 1. 项目定位

将 LittleWhiteBox 的“第四面墙聊天”核心能力独立出来，并扩展成一个 **iPhone / 微信式的场外通讯系统**。

它不是正文聊天的一部分。场外私聊、群聊、自动吐槽、自动点评等内容默认都不写入酒馆正文历史，也不占正文楼层。

最终系统包含：

- 通讯录 / 人格
- 私聊
- 群聊
- 第四面墙人格
- 编剧、攻略及未来其他场外人格
- 自动吐槽
- 自动点评
- 独立场外 API
- 后续的“采纳”机制

UI 最终朝 iPhone / 微信式体验制作，但 **美化最后做**。当前优先保证数据结构、聊天逻辑、触发逻辑和生成链稳定。

---

## 2. 已有基础

当前已有 `第四面墙聊天 Lite v1.2` 原型，其稳定成果包括：

- 可拖动悬浮球入口
- 点击悬浮球打开聊天小窗
- 读取最近正文
- 场外聊天历史与正文分离
- 基础消息发送

后续开发应尽量保留已经稳定的悬浮球启动、挂载、定位方式，不因聊天功能重构破坏入口。

---

## 3. 第四面墙人格

### 3.1 来源

第四面墙联系人是系统默认内置人格。

其默认系统提示词 / Meta Protocol 以 LittleWhiteBox 原版第四面墙提示词为基线，而不是 Lite 版的简化提示词。

提示词允许用户编辑，并提供“恢复 LittleWhiteBox 默认提示词”的能力。

### 3.2 LittleWhiteBox 第四面墙能力取舍

除明确排除项外，原版第四面墙聊天能力均计划保留/恢复。

**需要：**

- 原版完整 Meta Protocol / 系统提示词机制
- 正常第四面墙私聊
- 正文上下文读取与清理
- 独立第四面墙聊天历史
- 真实时间信息
- 消息之间的时间间隔进入模型上下文
- 自动吐槽
- 正文生成完成后的主动反应
- 检测用户编辑自己的正文消息并可吐槽
- 检测用户编辑 AI 正文消息并可吐槽
- 自动吐槽写入场外聊天历史
- 自动吐槽冷却机制
- 重新生成场外回复
- 停止正在生成
- 流式输出
- 自定义提示词
- 恢复默认提示词

**明确不要：**

- 全屏聊天模式
- 图片生成
- TTS / 语音
- AI 回复图片增强
- AI 回复语音增强

---

## 4. 通讯录 / 人格系统

### 4.1 核心原则

“人格”和“聊天会话”必须分离。

通讯录保存人格定义；私聊/群聊只引用人格，而不是复制人格提示词。

因此，同一个人格可以同时存在于：

- 自己的私聊
- 一个群聊
- 多个不同群聊

修改通讯录里的人格提示词后，后续各会话均引用更新后的定义。

### 4.2 联系人内容

通讯录联系人至少包含：

- 唯一 ID
- 名称
- 头像
- 简介 / 备注
- 人格系统提示词
- 人格类型（用于组织和默认模板，不作为硬性能力限制）

计划人格包括但不限于：

- 第四面墙（默认内置）
- 编剧
- 攻略
- 红笔编辑（后续）
- NPC / 角色
- 用户自定义人格

第四面墙默认提示词先按 LittleWhiteBox 原版实现；编剧、攻略及其他人格的正式提示词 **最后再专项创作**。

### 4.3 不需要的联系人权限

不设置“是否允许加入群聊”。

通讯录中的联系人默认均可被用户通过聊天页面的群聊入口拉入群聊。

### 4.4 API 不属于联系人资料（当前阶段）

当前不为每个人格单独配置 API。

后期可以考虑联系人级模型/API覆盖，因为不同模型能力不同；但这属于未来扩展，当前不实现。

---

## 5. 会话系统

### 5.1 私聊

每个联系人可以拥有独立私聊。

私聊拥有自己的：

- 消息历史
- 自动吐槽设置
- 自动吐槽触发状态 / 冷却状态

**私聊不提供“自动点评”。**

编剧、攻略等人格在单独聊天框中不会因为正文生成而自动点评；用户主动找它们聊天时正常回复。

### 5.2 群聊

模拟微信逻辑。

聊天页面右上角提供添加/建立群聊入口：

1. 打开通讯录选择器
2. 勾选想拉入群聊的人格
3. 建立群聊 / 添加群成员

无需预先在联系人资料中设置群聊权限。

群聊拥有独立消息历史及独立自动行为配置。

---

## 6. 自动吐槽（Commentary）

### 6.1 定位

自动吐槽是 **概率型主动行为**。

目标是模拟场外人格看到正文变化后的即时、简短、偏情绪化反应，而不是正式剧情分析。

### 6.2 设置位置

自动吐槽的具体配置放在 **每个聊天框自己的设置** 中，而不是通讯录联系人资料中，也不在系统设置里选择联系人。

系统设置只提供自动吐槽的全局总开关。

### 6.3 私聊设置

私聊提供：

- 自动吐槽：开 / 关
- 触发概率：0–100%
- 冷却时间

### 6.4 群聊设置

群聊提供：

- 自动吐槽：开 / 关
- 触发概率：0–100%
- 冷却时间

**不选择吐槽对象。**

当前群成员就是可参与群聊发言编排的成员池，由群聊发言规则决定实际谁开口。

### 6.5 触发事件

第四面墙原版相关触发能力需要保留，包括：

- 正文 AI 生成完成
- 用户编辑自己的正文消息
- 用户编辑 AI 的正文消息

具体事件是否对所有未来人格采用同样规则，可在实现阶段继续细化；不能因此删除第四面墙原版能力。

### 6.6 独立状态

每个会话独立保存：

- 概率设置
- 冷却时间
- 上次触发时间
- 必要的触发状态

一个会话触发吐槽，不应使另一个会话进入冷却。

---

## 7. 自动点评（Review）

### 7.1 定位

自动点评是 **周期型群聊行为**，用于编剧、攻略及其他场外人格围绕正文进行分析、判断、建议和讨论。

它与自动吐槽是两个独立功能，不合并为同一个开关或同一种触发机制。

### 7.2 只属于群聊

自动点评只存在于群聊设置。

单独与编剧/攻略私聊时，不提供自动点评开关，也不会自动点评正文。

### 7.3 群聊设置

群聊提供：

- 自动点评：开 / 关
- 每 N 层正文触发一次

**不选择点评对象。**

当前群成员进入群聊发言编排逻辑，由编排器决定本轮实际参与者和顺序。

### 7.4 触发方式

自动点评采用固定轮数，不采用百分比。

例如：

- 每 1 层点评
- 每 2 层点评
- 每 3 层点评

每个群聊独立维护自己的正文轮数计数。

---

## 8. 系统级自动行为总开关

系统设置提供：

- 自动吐槽：全局总开关
- 自动点评：全局总开关

系统总开关只负责“一键全部暂停/恢复”，**不负责选择联系人、不负责设置具体概率或 N 层**。

关闭总开关时，各聊天原有配置应保留。重新打开后，恢复各聊天自己的设置，无需重新配置。

---

## 9. 同一正文层的多会话触发

**允许。**

同一层正文结束后，多个不同会话可以分别触发自己的自动行为。

例如：

- 第四面墙私聊概率命中，产生一条自动吐槽；同时
- 某群聊刚好达到“每 3 层自动点评”，群里开始一轮点评。

系统不应为了避免同时发生而暗中拦截其中一个。

---

## 10. 群聊发言编排器

### 10.1 参考与改进方向

参考“酒馆小狸 Live”已知设计：开启多个场外人格后，正文出现时可以轮流发言；第一位开口者可随机，后说者需要承接前面已经说过的话。

本项目在此基础上进一步追求自然群聊感，不要求每次所有成员机械排队回复。

### 10.2 普通群聊消息

用户在群里发送普通消息后：

1. 系统判断当前哪些人格有必要 / 有意愿发言；
2. 选择 1～N 个实际发言者；
3. 第一位发言者根据内容相关性等因素选择（具体算法待继续确认）；
4. 后续发言者必须看到并理解前面本轮已经产生的群消息；
5. 后续人格可以赞同、补充、反驳、吐槽，而不是各自独立回答用户原问题；
6. 不强制所有群成员每次都发言。

### 10.3 @ 指定人格

用户 `@某人格` 时，被点名人格必须进入本轮发言者集合。

其他成员是否加入，由群聊编排规则决定。

### 10.4 自动点评与群聊编排

自动点评本身就是群聊行为。

达到 N 层触发条件时，相当于系统主动发起一次“围绕刚刚正文进行讨论”的群聊回合，然后交给同一套群聊发言编排器决定实际发言者、数量和顺序。

因此自动点评 **不是** 绕开群聊编排器另造一套独立回复系统。

### 10.5 自动吐槽与群聊编排

群聊自动吐槽触发后，同样使用当前群成员和群聊编排机制产生自然的场外反应，而不是在设置里预选某个固定人格。

---

## 11. 独立场外 API

### 11.1 必须与正文主 API 分离

场外通讯系统需要自己的 API 设置，避免自动吐槽、自动点评、群聊和场外私聊持续占用/干扰正文主 API 的调用额度、Token 与生成流程。

### 11.2 设置位置

API 设置统一放在 **系统设置**。

当前阶段所有场外人格共用一套场外 API，不在联系人资料里逐人配置。

基础设置预计包括：

- API 地址
- API Key
- 模型
- 连接测试
- 生成所需的基础参数（具体字段随实现阶段确认）

### 11.3 未来扩展

底层不要把模型/API与人格逻辑写死。

未来可能增加：

- 某联系人覆盖默认模型
- 某联系人使用另一套 API
- 不同场外人格使用不同模型

但当前版本不实现这些 UI 和配置。

---

## 12. 上下文与时间感

场外模型需要能够理解当前正文，但场外消息不能写回正文。

需要恢复/完善：

- 读取指定范围的真实正文楼层
- 清理正文中的 thinking / system / meta 等不应进入场外聊天的内容
- 正确区分正文双方身份
- 将最近正文作为场外模型上下文
- 每条场外消息保存真实时间
- 将消息时间及相邻消息间隔提供给模型
- 各会话历史彼此独立

UI 上“显示了时间”不等于模型拥有时间感；时间信息必须真正进入生成上下文。

---

## 13. 场外回复控制

计划恢复 LittleWhiteBox 原版相关能力：

- 流式输出
- 停止当前生成
- 重新生成上一条场外回复

重新生成不能简单追加一条新回复，需要正确处理当前会话历史。

---

## 14. 提示词管理

通讯录人格保存自己的系统提示词。

第四面墙：

- 默认使用 LittleWhiteBox 原版 Meta Protocol
- 可编辑
- 可恢复默认

编剧、攻略及其他场外人格：

- 先完成通讯录和聊天能力
- 正式提示词最后专项设计
- 后续会参考“酒馆小狸 Live”中用户认可的编剧 / 攻略 / 其他场外人格思路，但不得仅凭加载器猜测其未公开实现

---

## 15. “采纳”机制（已纳入方向，细节待讨论）

参考“酒馆小狸 Live”：场外人格的建议默认不进入主线；用户主动点击“采纳”后，才将选定建议送入下一轮正文生成上下文。

本项目计划保留这一方向，尤其适用于：

- 编剧建议
- 攻略建议
- 编辑建议

具体采纳数据格式、有效时长、一次性/持续性等规则 **尚未确认**。

---

## 16. UI / 美化

### 16.1 最终方向

最终模拟 iPhone / 微信式通讯体验，包括通讯录、聊天列表、私聊、群聊等。

聊天页右上角提供类似微信的群聊/成员管理入口。

### 16.2 开发顺序

**UI 美化最后做。**

当前不要为了动画、主题、头像布局等视觉细节阻碍核心功能开发。

优先顺序：

1. 数据结构与通讯录
2. 第四面墙核心聊天能力
3. 自动吐槽
4. 自动点评
5. 群聊发言编排
6. 独立场外 API
7. 提示词管理
8. 采纳等扩展功能
9. 最终 iPhone / 微信 UI 美化

---

## 17. 当前明确废弃的旧方案

以下方案在讨论中提出过，但已被后续设计取代，不应按旧方案实现：

- ❌ 在系统设置里为自动吐槽/点评统一选择联系人  
  改为：具体自动行为设置属于每个会话；系统设置只保留总开关。

- ❌ 在每个联系人资料里设置自动吐槽/自动点评资格  
  改为：通讯录只定义人格；自动行为属于会话。

- ❌ 在联系人资料里设置“是否允许加入群聊”  
  改为：直接从聊天页选择通讯录联系人拉群。

- ❌ 自动点评使用百分比概率  
  改为：每 N 层正文固定触发。

- ❌ 自动吐槽与自动点评合并成同一个自动行为  
  改为：两套独立触发机制，共用需要的群聊基础设施。

- ❌ 每个场外人格当前单独配置 API  
  改为：当前统一使用系统级场外 API；仅为未来保留扩展空间。

---

## 18. 尚待继续讨论

以下问题还没有最终拍板：

1. 群聊编排器如何精确判断“谁有话说”。
2. 一轮群聊最多允许多少人格发言。
3. 第一位发言者是纯随机、相关度优先，还是混合算法。
4. 普通群聊中未被 @ 的其他成员加入回复的概率/判断机制。
5. 自动点评触发后，是允许 1 人点评还是至少 2 人形成讨论。
6. 自动吐槽在群聊里一次触发允许几个人接话。
7. “采纳”建议进入正文的具体注入方式和生命周期。
8. 独立 API 的首批兼容协议与具体参数。
9. 编剧、攻略、红笔编辑等正式人格提示词。
10. 最终 iPhone / 微信 UI 的具体视觉与交互细节。

---

## 19. 文档维护约定

每次设计讨论出现明确结论后：

1. 更新本 `SPEC.md`；
2. 新结论写入对应章节；
3. 被推翻的旧方案应删除或移入“明确废弃的旧方案”；
4. 尚未拍板的内容只能放入“待讨论”，不得伪装成确定需求；
5. 实现代码应以最新 `main` 分支的 `SPEC.md` 为需求基线。
