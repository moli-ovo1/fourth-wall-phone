# moli70 / v0.4.14

- 自建 custom 角色「角色资料条目」升级为轻量角色世界书：每条可选「常驻」或「关键词触发」。
- 关键词支持逗号、中文逗号、分号、竖线或换行分隔；任一关键词命中本轮可见上下文即激活。关键词模式未填写关键词时不注入，避免误把条件条目当常驻资料。
- 私聊会扫描近期手机消息、可读正文与手机记忆；群聊使用当前群历史、群记忆、围读会正文/Review target 作为触发扫描文本。角色闲聊仍不会因为该机制额外读取正文。
- 只有本轮激活的条目进入模型请求；旧条目自动兼容为「常驻」，不破坏 moli69 已有数据。
- 第一版刻意只实现 OR 关键词触发；AND/NOT、概率、位置/深度等高级世界书条件暂不加入，避免复制一套过重的 SillyTavern 世界书系统。


## v0.4.13 / moli69
- 默认头像本地化：皮下、小上帝、moli 三张头像随扩展打包；moli 使用确认后的左右翻转版本，不依赖外部图床。
- 聊天列表支持长按置顶/取消置顶；非系统默认会话支持彻底删除。删除会同时移除聊天、手机记忆与 Conversation 状态，并要求确认按钮连续点击两次。
- 皮下、小上帝、moli 三个默认私聊以及默认「围读会」不可删除。
- 默认「围读会」固定 reading + 跟随正文时间，设置页不再重复提供模式/时间选择；自建群仍保留模式选择。
- 自动围读旧文案清理，删除“群聊不提供自动吐槽”。
- 群聊手机记忆继续使用现有 Conversation memory，并在手机记忆页按 reading / role-chat 显示和编辑近期记忆与长期总结。
- 自建 custom 角色新增多条「角色资料条目」：可新增、编辑、删除、启用/停用；启用条目作为角色专属资料注入私聊和群聊生成。

# moli小手机 SPEC

> GitHub 仓库技术名：`fourth-wall-phone`  
> 正式产品名称：`moli小手机`  
> 产品形态：SillyTavern 前端 Extension  
> 当前仓库源码是代码执行基线，本文件是产品设计基线。  
> “第四面墙”只是系统内置联系人之一，不是整个产品名称。  
> 早期 Fourth Wall Lite / 酒馆助手脚本只作为历史逻辑参考，不作为当前实现基线。  
> 原则：本文件只记录当前确认后的最终规则。旧方案若被推翻，直接以本文件最新内容为准。

---

## 1. 项目定位

`moli小手机` 是一个面向 SillyTavern 的前端扩展，是独立于正文主剧情之外的场外通讯系统。

它不是正文聊天本身，而是一个独立于主剧情之外的手机式通讯层，用来让：

- 第四面墙人格
- 编剧
- 攻略
- 红笔编辑
- 酒馆角色
- 用户自定义联系人
- 多人群聊

在“正文之外”与用户交流、吐槽、点评、讨论剧情。

核心体验目标：

1. 像微信一样管理私聊和群聊。
2. 场外内容默认不进入正文。
4. 酒馆角色必须尽量保持正文中的人设、关系状态、语言风格与长期记忆。
5. 场外系统使用独立 API，不干扰正文主 API。
6. 多存档严格隔离，避免不同剧情档互相串线。

---

## 2. 当前实现基线

当前正式实现是 SillyTavern 前端 Extension。

当前仓库已经具备部分基础能力，包括但不限于：

- 可拖动悬浮球
- 悬浮球位置持久化
- 可拖动手机面板
- 面板位置持久化
- 点击面板外关闭
- 点击悬浮球打开 / 关闭
- 微信式聊天首页基础结构
- 内置联系人
- 自定义联系人
- 酒馆角色来源资料
- 群聊基础结构
- 群成员增加 / 删除
- 群头像拼接
- 部分聊天信息页
- 当前档会话隔离基础

每次开发仍必须以本次最新仓库源码为准。

早期 `Fourth Wall Lite` / 酒馆助手脚本只允许作为历史逻辑参考，禁止把旧脚本重新当成当前代码基线。

已验收的悬浮球、手机面板拖动以及移动端触摸防穿透逻辑，除非专门修复相关 Bug，否则不得在后续重构中随意替换。

---

## 3. 首页与入口

### 3.1 悬浮球

点击悬浮球后，进入微信式“聊天列表首页”。

### 3.2 首页顶部

顶部不放搜索按钮。

右上角只保留两个入口：

#### `＋`

弹出：

- 同步酒馆角色
- 添加联系人
- 发起群聊

#### `设置`

进入全局设置。

当前至少包含：

- 场外 API
- 自动吐槽全局总开关
- 自动点评全局总开关
- Markdown 渲染全局开关
- 提示词与预设

后续全局功能继续从这里扩展。

---

## 4. 内置联系人

以下联系人首次启用插件时自动存在：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

它们：

- 自动进入全局通讯录
- 自动为当前酒馆存档创建私聊会话
- 自动出现在当前存档的聊天首页

内置联系人不需要用户手动添加。

### 4.1 内置联系人删除规则

内置联系人不做真正物理删除。

可以：

- 恢复默认提示词

但不能像普通联系人一样彻底删除系统定义。

---

### 4.1 第一版删除与状态规则

内置联系人不可物理删除。

第一版暂不实现“隐藏 / 停用”，因为这两个行为的业务语义尚未正式设计。

内置人格可以恢复默认人格提示词。

---

## 5. 自定义联系人

### 5.1 创建方式

`＋ → 添加联系人 → 新建空白人物`

不设置联系人类型。

原因：

- 人格提示词已经定义“这个人是谁”
- 不需要额外再用“NPC / 陪聊 / 作者 / 其他”等标签限制行为

### 5.2 新建字段

新建空白联系人时只需要：

- 名称
- 头像
- 简介 / 一句话描述
- 人格提示词

不提供：

- 联系人类型
- 是否允许加入群聊
- 是否读取正文

### 5.3 上下文读取

所有联系人默认自动读取当前存档上下文。

不同联系人根据自身数据结构自动获得不同上下文包，不要求用户手动配置“能不能看正文”。

创建成功后：

- 加入全局通讯录
- 自动为当前存档创建私聊
- 自动进入当前聊天首页

---

## 6. 同步酒馆角色

### 6.1 添加流程

`＋ → 同步酒馆角色`

弹出当前酒馆角色选择列表。

支持：

- 多选
- 已添加角色标记
- 点击“添加”后批量导入

添加完成后：

- 进入全局通讯录
- 自动为当前存档创建私聊
- 直接出现在当前聊天首页

### 6.2 不采用自动全量同步删除

不能因为酒馆角色卡被删除，就自动删除手机联系人。

源角色不存在时：

- 联系人继续保留
- 所有场外聊天历史继续保留
- 显示“来源失效 / 来源角色不可用”
- 后续可重新绑定角色

### 6.3 自动跟随刷新

酒馆联系人会自动跟随刷新其“来源数据”，包括：

- 酒馆角色原名
- 原角色头像
- 角色设定
- 性格
- 场景
- Example Dialogue
- 其他角色卡相关数据

但是自动同步只能更新来源层。

不能覆盖用户手动层。

---

## 7. 联系人资料模型

酒馆角色联系人同时保存：

- 联系人 ID
- 酒馆来源角色 ID / 可稳定引用信息
- 酒馆角色原名
- 酒馆原头像
- 用户备注名
- 用户自定义头像（可选）
- 用户简介 / 一句话描述
- 人格提示词相关配置
- 来源状态
- 最近同步状态

### 7.1 名称显示规则

优先级：

`用户备注名 > 联系人显示名 > 酒馆角色原名`

酒馆角色原名永远保留，不会因为用户改备注而丢失。

### 7.2 头像显示规则

优先级：

`用户自定义头像 > 酒馆来源头像`

如果用户手动换过头像：

- 酒馆角色卡以后换头像，不覆盖用户自定义头像

用户可选择：

- 恢复跟随角色卡头像

### 7.3 联系人详情页

入口：

`私聊右上角 … → 聊天信息 / 联系人资料`

建议结构：

- 头像
- 当前显示名
- 原角色名（酒馆角色）
- 简介
- 备注名
- 头像
- 简介
- 人格与提示词
- 酒馆角色信息
- 来源状态
- 重新绑定角色
- 恢复跟随头像 / 名称
- 删除联系人（非内置）

---

## 8. 联系人与会话分离

这是核心数据规则。

### 8.1 联系人是全局的

以下内容跨酒馆存档共享：

- 联系人本体
- 内置人格
- 自定义联系人
- 酒馆联系人来源信息
- 联系人备注
- 用户自定义头像
- 联系人人格 Prompt
- 提示词配置

### 8.2 会话按酒馆存档隔离

以下内容必须按当前酒馆聊天 / 存档隔离：

- 私聊历史
- 群聊
- 群聊历史
- 自动吐槽状态
- 自动点评状态
- 自动点评正文回合计数
- 未读消息
- 当前档会话列表
- 与当前档相关的角色动态状态
- 当前档场外上下文

### 8.3 多存档规则

同一个酒馆角色可以在多个存档中存在不同场外聊天。

例如：

- 祈年 / A 档 → A 档私聊
- 祈年 / B 档 → B 档私聊

两者绝不共享聊天记录。

### 8.4 切换酒馆存档

切换酒馆存档后：

- 手机聊天首页切换到该存档自己的会话空间
- 通讯录仍然全局存在

如果新档从未和某联系人聊过：

- 他仍在通讯录
- 不自动出现在新档聊天首页
- 用户从通讯录点进去后，才建立该档的新私聊

### 8.5 内置人格例外

内置人格：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

在每个新存档第一次进入时，都自动创建当前档私聊并显示在首页。

---
\n## 8A. Scope 生命周期与异步安全\n\n联系人 / 人格配置属于全局数据。私聊会话、群聊、消息历史、未读、置顶、自动吐槽状态、自动点评状态与计数、当前档上下文及当前档相关异步生成任务属于当前 SillyTavern 聊天存档 Scope，不同存档不得共享。\n\n### 8A.1 稳定 Scope\n\n只有取得能够稳定区分当前 SillyTavern 存档的信息后，当前 Scope 才可以正式持久化。稳定 Scope 由统一的 `tavern-scope` 模块生成，优先使用当前角色 / 群组稳定标识与当前 `chatId` / 聊天文件稳定标识。其他业务模块不得自行拼接 Scope Key。\n\n### 8A.2 Fallback Scope\n\n初始化期间暂时无法取得稳定 `chatId` 时，可以产生临时 fallback scope，用于暂时显示 UI、构造临时内存对象、等待上下文准备和防止初始化报错。Fallback Scope 不得持久化正式消息、正式群聊、自动行为进度，不得作为永久存档 ID，也不得创建会被用户视为正式数据的持久化会话。\n\n**fallback 可以有临时内存状态，但不能产生之后看起来“突然消失”的正式用户数据。** 一旦取得稳定 Scope，应切换到稳定 Scope。\n\n### 8A.3 Scope Change Contract\n\n当 SillyTavern 当前聊天 / 存档切换时：\n\n1. 获取新的稳定 Scope Key；\n2. 页面回到聊天列表首页；\n3. 当前联系人 / 群聊选择清空；\n4. 首页重新加载新 Scope 会话；\n5. 未读、置顶、自动行为计数等全部切换；\n6. 旧 Scope AI 请求应尽可能 Abort，但不得依赖 Abort 一定成功；\n7. 每个异步任务启动时保存 `requestScopeKey`；\n8. 异步结果写入前再次核对原始 Scope；\n9. 旧请求绝不能写入新 Scope；\n10. 无法中止或晚到的旧请求最多只能写回原 Scope。\n\n### 8A.4 UI 临时状态隔离\n\n`currentContactId`、`currentConversationId`、当前群聊选择、当前引用消息、当前多选集合、当前会话临时操作状态以及未来可能存在的草稿，不得直接跨 Scope 复用。切换 Scope 时要么清理，要么明确按 Scope 独立保存。\n\n### 8A.5 打开手机的页面规则\n\n每次重新打开 `moli小手机` 默认进入当前 Scope 聊天列表首页，不恢复上一次联系人 / 群聊子页面。已验收的移动端触摸防穿透必须保留：打开悬浮球的同一次 pointer / touch / click 事件不得穿透到刚出现的聊天列表项目。\n\n---\n\n## 8B. 公共数据契约\n\nContact、Conversation、Message、Scope 属于跨模块公共契约，功能节点不得自行建立平行结构。\n\n### 8B.1 ID 类型严格区分\n\n- `contactId`：联系人 / 人格\n- `conversationId`：私聊或群聊会话\n- `scopeKey`：SillyTavern 聊天存档数据空间\n- `messageId`：场外消息\n\n禁止混用。\n\n### 8B.2 Conversation ID\n\n跨模块传递聊天会话统一使用 `conversationId`。私聊使用 `private:<contactId>` 命名空间，例如 `builtin:meta` 对应 `private:builtin:meta`；群聊使用独立命名空间，例如 `group:<stable-id>`。\n\n### 8B.3 Conversation Schema\n\n公共 Schema 规定字段含义和能力，不强制不同会话保存无意义的相同设置字段。示意：\n\n```js\n{\n  id: "private:..." | "group:...",\n  type: "private" | "group",\n  contactId: null,\n  memberIds: [],\n  title: "",\n  pinned: false,\n  unreadCount: 0,\n  settings: {},\n  messages: []\n}\n```\n\n私聊 `settings` 可包含自动吐槽；群聊可包含自动吐槽与自动点评。具体默认值以对应业务章节为准。\n\n### 8B.4 Message Schema\n\n正式消息至少需要能够表达：\n\n```js\n{\n  id: "...",\n  senderType: "user" | "contact" | "system",\n  senderId: null,\n  content: "",\n  ts: 0,\n  source: "manual" | "generation" | "commentary" | "review" | "forward" | "system",\n  senderSnapshot: { name: "", avatarRef: null },\n  quote: null,\n  forward: null\n}\n```\n\n`quote` / `forward` 子结构由对应消息节点设计，新增时不得破坏旧消息兼容。\n\n### 8B.5 senderSnapshot\n\n联系人产生消息时保存足以还原发送当时身份的快照，至少能还原当时显示名和头像的可恢复引用 / 信息。不要求每条消息复制完整 base64；可使用 `avatarRef`、`avatarUrl`、`avatarKey` 或其他可恢复引用。联系人删除、踢出群聊、改名、换头像或来源失效后，旧消息仍须知道当时是谁发送的。\n\n历史消息的 `senderSnapshot` 不被联系人后续名称、备注或头像修改反向改写。\n\n### 8B.6 连续发送与空输入触发回复\n\n私聊和群聊统一采用：**连续发送，空输入触发回复**。\n\n- 输入框非空时点击“发送”：只追加一条独立 user 消息，不调用 AI；\n- 用户可以连续发送多条 user 消息；\n- 输入框为空时点击“发送”：才触发一次 AI 生成；\n- 生成时应一次读取自上一条 AI 回复之后累计的全部 user 消息；\n- 生成失败或被停止时，已经发送的 user 消息必须保留，用户可以再次空输入重试；\n- 不提供“重新生成”按钮。\n\n生成过程中发送按钮切换为“停止”；停止只取消当前请求，不删除触发它的用户消息。\n\n---\n\n## 8C. 存储版本与迁移契约\n\n### 8C.1 Data Store 是业务持久化统一入口\n\n除 Storage / Data Store 自身外，UI、API / Generation、Automation、Group Orchestrator、Prompt、Role Fidelity、Integrations 不得直接依赖具体 localStorage Key 或底层布局。业务持久化统一通过 Data Store / Storage 接口，以便未来从 localStorage 迁移到 IndexedDB 等实现而无需整体重写上层。\n\n### 8C.2 schemaVersion\n\n主要持久化数据结构必须保存明确 `schemaVersion`；联系人全局数据也必须有明确版本规则。禁止只靠 Storage Key 名称猜版本。\n\n### 8C.3 Migration Chain\n\n数据升级使用明确迁移链，例如 `v1 → v2 → v3`，禁止假设旧数据天然符合最新 Schema。\n\n### 8C.4 数据兼容铁律\n\n修改 scopeKey、contactId、conversationId、Storage Key、Contact / Conversation / Message / Group Schema 时，必须检查真实旧数据并提供兼容读取或一次性迁移。禁止内部重构导致已有联系人、私聊、群聊、消息或设置看起来突然消失。\n\n### 8C.5 保留未知字段\n\nStorage 层读取再保存对象时必须保留未知字段，例如：\n\n```js\n{ ...storedData, conversations: storedData.conversations || {} }\n```\n\n而不是只重建当前认识的字段，以免未来的 `schemaVersion`、`scopeMeta`、`eventLedger` 等被旧读写逻辑静默删除。\n\n---\n
## 9. 聊天首页

首页采用微信式聊天列表。

每个聊天项显示：

- 头像 / 群头像
- 联系人名 / 群名
- 最后一条消息预览
- 最后消息时间
- 未读数字 / 红点
- 置顶状态

排序：

1. 置顶聊天优先
2. 其余按最新消息时间降序

### 9.1 未读

后台产生的：

- 自动吐槽
- 自动点评
- 其他自动场外消息

如果当前会话没有打开，则计入未读。

当前正在打开的会话：

- 不增加未读

打开聊天后：

- 清空该会话未读

---

## 10. 私聊

### 10.1 基本布局

采用微信式私聊布局。

顶部：

- 返回
- 联系人备注名 / 显示名
- 右上角 `…`

不额外在顶部放联系人头像。

底部输入区：

- `＋`
- 输入框
- 发送

`＋` 第一版只保留扩展入口，具体小功能待后续确定。

生成过程中：

- 发送按钮可切换为停止按钮
- 用户可随时取消当前场外生成请求

### 10.2 消息显示

- 用户与人格消息均采用聊天气泡
- AI 回复支持 Markdown
- 是否渲染 Markdown 由全局设置控制
- 底层始终保存原始 Markdown 文本
- 切换开关后旧消息也能重新按当前显示模式渲染
- 酒馆角色默认 Prompt 应约束少用 Markdown，使其更像即时通讯
- 编剧 / 攻略等工具型人格可自然使用 Markdown

### 10.3 引用

支持回复 / 引用消息。

输入时：

- 输入框上方显示当前引用对象与内容摘要

发送后：

- 消息气泡中显示被引用内容的小块

### 10.4 自动点评

私聊不提供自动点评。

### 10.5 自动吐槽

私聊设置：

- 自动吐槽：开 / 关
- 触发概率：0–100%

## 11. 群聊

### 11.1 创建

`＋ → 发起群聊`

用户选择联系人创建。

群名：

- 用户自定义

群头像：

- 模拟微信群头像
- 将所有当前群成员头像拼接成九宫格 / 多人格头像组合

### 11.2 群成员

支持：

- 后续加人
- 踢人

群成员排序：

- 不作为 AI 发言权重
- 无业务意义

### 11.3 群聊消息 UI

采用真实微信式群聊布局：

- 每条人格消息显示头像
- 每条人格消息显示昵称
- 显示消息气泡
- 连续同一人发言也不合并头像和昵称

### 11.4 删除群成员

成员离开 / 被踢出后：

- 过去的群聊历史仍保留其旧消息
- 不清理历史记录

---

## 12. 群聊发言编排器

群聊分成两种模式：

1. 普通群聊
2. 自动点评

### 12.1 普通群聊

用户主动发送群聊消息时：

- 不强制所有人回复
- 不设置固定 1～3 人上限
- 最大自然上限就是群成员数量

AI 根据：

- 当前话题相关度
- 角色立场
- 人格关系
- 是否被 @
- 是否有明显插话价值

判断谁说话。

### 12.2 第一位发言者

普通群聊第一位：

- 按相关度决定
- 不纯随机

### 12.3 @

用户 @ 某人格：

- 被 @ 人格必须参与

但：

- 未被 @ 的其他成员仍可自然插话

### 12.4 后续成员必须接前文

同一轮内：

第二位看到：

- 用户消息
- 第一位的新回复

第三位看到：

- 用户消息
- 第一位
- 第二位

以此类推。

允许：

- 赞同
- 反驳
- 补充
- 调侃
- 转移话题
- 自然接话

禁止所有人彼此不可见地独立答题。

---

## 12A. 聊天信息 / 当前会话设置页

### 12A.1 私聊聊天信息

从私聊右上角 `…` 进入。

顶部显示：

- 当前联系人头像
- 联系人名
- `＋` 加人入口

`＋` 的作用：

- 从通讯录选择其他联系人
- 创建一个包含当前联系人在内的新群聊

酒馆角色 / 自定义联系人私聊设置包含：

- 自动聊天 / 主动私聊开 / 关
- 自动聊天百分比 0–100%
- 自动吐槽正文开 / 关
- 自动吐槽百分比 0–100%
- 查找聊天记录
- 置顶聊天
- 联系人资料
- 人格与提示词
- 清空聊天记录
- 删除联系人

不加入“消息免打扰”；需要时后续再增加。

### 12A.2 群聊聊天信息

顶部：

- 当前群成员头像
- `＋` 增加成员
- `－` 踢出成员
- 群聊名称

设置包含：

- 自动点评开 / 关
- 点评间隔：每 N 个正文回合
- 查找聊天记录
- 置顶聊天
- 群成员管理
- 清空聊天记录
- 删除并退出群聊

### 12A.3 自动点评暂停规则

关闭自动点评：

- 暂停计数
- 不清零当前进度
- 关闭期间产生的正文不计入该点评周期

重新开启后：

- 从关闭前的进度继续累计

例如每 5 回合点评一次，关闭时为 `3/5`：

- 关闭期间不计数
- 重新开启后仍从 `3/5` 开始
- 再经过 2 个有效 AI 正文回合触发点评

### 12A.4 清空聊天记录

清空当前聊天：

只删除：

- 当前会话场外聊天历史
- 当前未读计数

保留：

- 联系人
- 人格 Prompt
- 自动吐槽设置
- 自动点评设置
- 其他会话配置

### 12A.5 删除规则

私聊：

- “删除联系人”属于全局联系人删除，按联系人删除规则执行

群聊：

- “删除并退出群聊”只删除当前存档中的该群聊及其群聊历史
- 不删除任何群成员联系人

### 12A.6 删除普通联系人对所有存档的影响

删除普通联系人属于全局删除动作：删除全局 Contact；删除其在所有 Scope 下的私聊；从所有 Scope 现存群聊的当前 `memberIds` 中移除；但不删除过去已经产生的群消息。历史群消息依靠 `senderSnapshot` 保留当时姓名、头像身份、文本和时间。

删除后若群剩余成员不足以继续作为正常群聊，如何处理由群聊模块另行确认，Storage 层不得自行决定。内置联系人第一版不可删除，因此不适用此规则。

---

## 13. 群聊 API 生成策略

采用：

**轻编排 + 逐人生成**

### 13.1 编排请求

先发一次轻量请求。

只负责决定：

- 本轮谁说
- 发言顺序

编排阶段不加载每个人完整角色卡，只给足够判断相关度的短描述。

例如：

- 名称
- 简短身份
- 简短性格
- 与当前话题关系
- 是否被 @

### 13.2 发言生成

真正轮到某人格说话时：

单独发一次生成请求。

该请求只加载该人格自己的完整上下文。

例如：

`Speaker 1 → 完整人格包 → 生成`

然后：

`Speaker 2 → 自己的完整人格包 + Speaker 1 新消息 → 生成`

依次继续。

### 13.3 目的

该方案优先保证：

- 人格隔离
- 角色声线稳定
- 不串人设
- 后发言者真正读取前面新消息
- 酒馆角色尽量像正文里的本人

不采用“一次请求同时扮演所有群成员”作为默认模式。

---

## 14. 自动吐槽（Commentary）

### 14.1 定位

自动吐槽是概率型主动行为。

特点：

- 短
- 即时
- 情绪化
- 像看到正文后的第一反应

不是正式剧情分析。

### 14.2 全局开关

系统设置提供：

- 自动吐槽总开关

关闭总开关：

- 只暂停
- 不删除每个会话自己的配置

重新开启：

- 恢复原配置

### 14.3 每会话配置

酒馆角色 / 自定义联系人的私聊分别独立保存：

- 自动吐槽开关
- 触发概率 0–100%

### 14.4 不使用业务冷却

不提供用户可设置的冷却时间。

100% 应意味着：

- 每个符合条件的新事件都允许触发

### 14.5 技术去重

底层保留短暂同事件去重锁：

- 只防止同一个酒馆事件被重复广播
- 不属于“冷却”
- 不暴露设置

### 14.6 触发事件

至少包括：

- AI 正文生成完成
- 用户编辑自己的正文消息
- 用户编辑 AI 正文消息

每次正文事件后：

- 每个开启自动吐槽的会话独立进行概率判定

同一正文事件可以同时触发多个符合条件的私聊。群聊不参与自动吐槽。

### 14.7 群聊自动吐槽（已废弃）

群聊不提供自动吐槽；群聊自动行为只保留自动点评。

---

## 15. 自动点评（Review）

### 15.1 定位

自动点评是周期型群聊行为。

特点：

- 每 N 个正文回合触发
- 全员参与
- 更完整、更分析型
- 与自动吐槽完全独立

### 15.2 只存在于群聊

私聊没有自动点评。

### 15.3 群聊设置

每个群聊保存：

- 自动点评开 / 关
- 每 N 个正文回合触发一次

### 15.4 全员必须参与

自动点评触发后：

- 当前群内每一个人格至少发言一次

### 15.5 发言顺序

第一位：

- 随机

后续：

- 必须看见前面的本轮新消息
- 根据已有讨论继续接话

允许：

- 赞同
- 反驳
- 补充
- 改变讨论重点
- 二次发言

### 15.6 二次发言

每人至少一次，但不限制恰好一次。

如果讨论自然需要：

- 已经发言过的人可以再次回应

### 15.7 单轮安全上限

隐藏规则：

`单轮最大场外消息数 = 当前群成员数 × 2`

不作为用户设置项。

### 15.8 正文回合定义

一个正文回合 = 一次有效 AI 正文回复。

用户消息本身：

- 不单独计一个自动点评回合

### 15.9 删除与重 Roll

自动点评计数根据当前有效正文状态判断。

规则：

- 删除有效 AI 正文 → 回合回退
- 删除多条 → 按实际状态回退
- 重 Roll 同一条 AI 正文 → 不额外 +1
- 如果重 Roll 的位置正好位于 N 边界，可针对新版本正文重新触发对应点评

---

## 16. 不提供“重新生成”按钮

场外聊天 UI 不提供：

- AI 消息重新生成
- 群聊整轮重新生成
- 自动点评重新生成按钮

这是明确取消的功能。

---

## 17. 消息长按菜单

长按消息支持：

- 引用
- 复制
- 转发
- 多选
- 删除

明确不提供：

- 采纳
- 重新生成

### 17.1 转发

转发时：

- 弹出聊天列表
- 可转发到其他私聊 / 群聊
- 单条转发按普通文字消息进入目标会话，不显示额外来源卡片
- 多条消息使用“聊天记录”合并转发卡片，聊天页只做紧凑预览，点击后进入独立聊天记录详情页
- 多条消息可采用合并转发形式

### 17.2 多选

多选模式至少支持：

- 批量删除
- 批量转发

## 18. 删除与清空规则

### 18.1 删除联系人

删除普通联系人时：

同时删除：

- 联系人资料
- 与该联系人相关的所有存档私聊数据
- 与其专属联系人数据

必须二次确认。

警告文案应明确：

“将同时删除该联系人在所有存档中的场外聊天记录，无法恢复。”

### 18.2 群聊历史中的旧成员消息

删除联系人 / 踢出群成员：

- 不自动删除其过去群聊消息

### 18.3 清空聊天记录

清空某个聊天记录时：

只删除：

- 当前会话历史

保留：

- 联系人
- 人格 Prompt
- 自动吐槽设置
- 自动点评设置
- 其他会话配置

### 18.4 恢复默认人格

“恢复默认人格 / 恢复默认提示词”只重置人格配置。

不会删除：

- 已有场外聊天历史
- 会话
- 群聊历史

例如：

第四面墙可恢复 LittleWhiteBox 默认 Meta Protocol。

自定义空白联系人没有“系统默认模板”，因此不提供这一恢复项。

---

## 18A. 通讯录页面

通讯录采用独立分组。

### 18A.1 顶部

包含：

- 搜索框
- 当前存档群聊入口

不在通讯录重复提供“新建联系人”按钮。

创建 / 同步入口统一从聊天首页右上角 `＋` 进入。

### 18A.2 内置人格分组

固定置顶显示：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

不与普通联系人混入字母排序。

### 18A.3 普通联系人

酒馆角色与自定义联系人混合显示，不额外按来源分组。

按当前显示名称进行字母排序。

排序名称优先级：

`备注名 > 联系人显示名 > 酒馆角色原名`

### 18A.4 点击联系人

点击联系人：

- 当前存档已有私聊 → 直接打开
- 当前存档没有私聊 → 创建该存档的新私聊并打开

### 18A.5 群聊入口

通讯录顶部“群聊”显示：

- 当前酒馆存档已有的群聊

切换存档后：

- 群聊列表随存档切换
- 全局联系人列表不变

## 19. 提示词与预设

系统设置提供：

`提示词与预设`

入口分为：

- 全局生成框架
- 联系人配置

不做“预览本次上下文”。

### 19.1 联系人配置列表

按组展示：

#### 内置人格

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

#### 酒馆角色

- 当前已同步的角色

#### 自定义联系人

- 用户新建人物

从：

`设置 → 提示词与预设 → 联系人配置 → 某联系人`

与：

`联系人资料 → 人格与提示词`

打开的是同一份数据。

不能复制两份配置。

### 19.2 第四面墙配置

至少包括：

- Meta Protocol
- 普通聊天 Prompt
- 自动吐槽 Prompt
- 时间感规则
- 正文上下文开关
- 柏宝书长期记忆开关
- 场外历史开关
- 恢复 LittleWhiteBox 默认

### 19.3 编剧 / 攻略 / 红笔编辑

至少包括：

- 人格 Prompt
- 输出风格 Prompt
- 自动点评 Prompt
- 正文上下文开关
- 柏宝书长期记忆开关
- 场外历史开关
- 恢复默认

### 19.4 酒馆角色配置

来源均提供独立开关，默认开启：

- 角色卡
- 性格
- 场景
- Example Dialogue
- 世界书
- 柏宝书长期记忆
- 最近正文
- 场外聊天历史
- 自定义附加 Prompt

不单独增加“最近角色语言样本”。

角色语言风格直接从：

- Example Dialogue
- 最近正文中该角色的实际台词

共同获得，避免重复发送同一批正文浪费 token。

### 19.5 世界书控制

- 世界书默认全量开启
- 点击进入后显示世界书条目列表
- 用户可逐条关闭不希望该联系人读取的条目
- 条目开关状态按联系人独立保存

### 19.6 柏宝书控制

- 柏宝书长期记忆默认开启
- 提供明确开 / 关按钮
- 只读取长期记忆
- 不读取当前状态快照、变量、物品、NPC、计划等其他柏宝书数据
- 未检测到柏宝书时自动退回最近正文机制

### 19.7 自定义联系人配置

至少包括：

- 人格 Prompt
- 简介
- 柏宝书长期记忆
- 最近正文
- 场外聊天历史
- 自定义附加 Prompt

## 20. 全局生成框架

默认逻辑块：

1. 场外系统协议
2. 当前任务类型
3. 用户 Persona
4. 当前发言人格
5. 角色保真包（酒馆角色专用）
6. 当前世界 / 相关世界书
7. 当前存档正文上下文
8. 时间与消息间隔
9. 当前场外会话历史
10. 本轮用户消息 / 正文触发事件
11. 群聊本轮已产生的新消息
12. 输出规则

这些逻辑块：

- 可查看
- 可开关
- 可拖动排序
- 可点击编辑
- 可恢复默认

核心系统项可以关闭，但不允许轻易物理删除。

核心系统项不能轻易物理删除。

### 20.1 同一联系人跨会话上下文

同一个 `contactId` 在当前 Scope 内可以同时出现在私聊和一个或多个群聊中。它们仍然是彼此独立的 Conversation，不合并聊天历史，也不把其他会话消息伪装成当前会话消息。

Generation / Prompt 层允许按同一 `contactId` 读取其其他会话上下文，并必须带明确的会话来源标签。例如：群聊中的蒋郁文可以知道“这是你与用户私聊中发生过的内容”，但不能误认为这些话发生在群聊中，也不能默认其他群成员知道私聊内容。

跨会话互通规则：

- 身份匹配只认稳定 `contactId`，禁止按显示名猜测；
- 当前会话上下文优先；
- 其他会话上下文作为独立来源块提供；
- 读取互通，不自动把消息复制 / 写入另一会话历史；
- 转发记录若保存了 `senderId`，可用于识别“转发内容中的这个人就是当前群成员中的同一联系人”；
- 不同 SillyTavern Scope 之间仍然严格隔离，不跨存档互通。

---

## 21. 酒馆角色保真（Role Fidelity Pack）

目标：

**手机里的酒馆角色，要像正文里那个正在活着的角色。**

不是只读取静态角色卡。

### 21.1 核心来源

酒馆角色生成时优先组合：

1. 角色身份与硬设定
2. 当前存档剧情事实 / 关系状态
3. 当前角色动态状态
4. Example Dialogue / 角色语言示例
5. 最近正文（同时承担近期剧情与近期角色语言参考）
6. 当前场外聊天历史
7. 本轮消息

### 21.2 角色卡与当前剧情关系

角色卡负责：

- 这个人是谁

当前正文 / 记忆负责：

- 这个人现在变成了什么样
- 当前关系走到哪一步
- 当前有哪些承诺、冲突、秘密、状态

### 21.3 语言声纹

不额外复制一份“最近角色语言样本”。

系统在读取最近正文时，同时将其中该角色真实说过的话作为语言风格参考，以减少重复 token。

重点参考：

- 用词习惯
- 句子长短
- 称呼
- 标点
- 是否常反问
- 情绪表达强弱
- 是否主动解释
- 是否回避直白表达
- 幽默方式
- 线上说话相对现实是否更松弛

要求：

**学习正文中的角色声纹，但不要照抄正文的第三人称小说叙述格式。**

### 21.4 Example Dialogue

角色卡的 Example Dialogue 作为高价值角色语言来源。

角色设定说明“他是什么样的人”。

Example Dialogue 更直接说明“他具体怎么说话”。

---

## 22. 柏宝书 Memory Provider

支持可选接入：

`ST-BaiBai-Book`

仓库：

https://github.com/baibai-git/ST-BaiBai-Book

### 22.1 定位

柏宝书在本项目中只负责：

**补充最近正文窗口之外的长期剧情记忆。**

不能作为硬依赖。

### 22.2 检测与降级

如果检测到柏宝书公开 API：

- 读取长期记忆

如果不存在或读取失败：

- 插件继续正常运行
- 使用自己的最近正文机制
- 不影响聊天

### 22.3 读取范围

只读取柏宝书长期记忆。

不读取：

- 当前状态快照
- 变量
- 物品
- NPC
- 计划
- 其他状态型数据

### 22.4 推荐组合

上下文优先采用：

`柏宝书长期记忆 → 最近正文原文 → 当前场外聊天`

柏宝书负责“过去发生过什么”。

最近正文负责“现在正在发生什么”。

尽量避免同一段近期剧情以“摘要 + 原文”重复发送。

### 22.5 覆盖完整性

如果柏宝书公开接口返回记忆覆盖不完整的信息，不能假设其记忆毫无缺口。

最近正文始终作为近期上下文兜底。

## 23. 不同联系人上下文组装

所有联系人默认读取当前存档上下文，但按类型自动组装。

### 23.1 酒馆角色

可使用：

- 角色卡
- 性格
- 场景
- Example Dialogue
- 相关世界书
- 柏宝书记忆
- 当前存档最近正文（同时作为近期角色语言参考）
- 当前场外历史

### 23.2 第四面墙

使用：

- LittleWhiteBox Meta Protocol
- 当前存档长期记忆
- 最近正文
- 当前场外历史
- 当前触发任务

### 23.3 编剧 / 攻略 / 红笔编辑

使用：

- 各自专属 Prompt
- 当前存档剧情摘要 / 长期记忆
- 最近正文
- 当前场外历史
- 当前任务

### 23.4 自定义联系人

使用：

- 自定义人格提示词
- 柏宝书长期记忆（可用时）
- 最近正文
- 当前场外历史

---

## 24. 场外 API

### 24.1 API 来源

支持两种模式：

1. 独立场外 API（默认）
2. 使用酒馆当前 API（备用 / 兼容模式）

默认必须选择：

**独立场外 API**

“使用酒馆当前 API”用于：

- 新用户快速使用
- 不想额外配置 API 的场景
- 兼容 / 备用

### 24.2 覆盖范围

场外 API 层统一服务于：

- 私聊
- 群聊
- 自动吐槽
- 自动点评
- 第四面墙
- 编剧
- 攻略
- 红笔编辑
- 酒馆角色

### 24.3 统一适配层

结构：

`场外功能 → Generation / Agent 层 → Provider Adapter → 实际 API`

不同功能不各自直接写网络请求。

### 24.4 联系人级 API

第一版：

- 所有联系人共用全局场外 API

底层预留：

`联系人 API / 模型覆盖 > 系统默认场外 API`

但第一版 UI 不开放联系人级 API。

### 24.5 独立 API 设置页

至少包含：

- API 来源
- Provider / API 类型
- API 地址
- API Key
- 模型
- 刷新模型列表
- 手动填写模型 ID
- 流式输出
- 测试连接

API Key：

- 只保存在本地配置
- 默认隐藏显示
- 不进入 Prompt
- 不进入聊天记录
- 不随联系人配置导出

模型：

- 支持从 Provider 拉取列表
- 即使模型列表拉取失败，也允许用户手动输入模型 ID

测试连接：

- 不产生聊天消息
- 显示连接成功或具体失败原因


实现约束：

- Provider 模型列表与测试连接统一经过 Provider Adapter，不由设置页直接拼不同 Provider 请求；
- API 地址留空时允许 Adapter 使用该 Provider 的官方默认地址；
- 模型列表拉取失败不得阻止用户手动填写模型 ID；
- 测试连接不写入场外聊天历史。

### 24.6 Provider

第一版优先考虑：

- OpenAI Compatible
- Claude
- Gemini

其中 OpenAI Compatible 用于覆盖：

- OpenAI 风格接口
- 常见中转站
- 本地兼容服务

具体首发 Provider 可在实现阶段按工作量调整，但底层不能写死单一请求格式。

### 24.7 流式与停止

- 流式输出为全局开关
- 生成过程中始终支持停止
- 停止不是用户可关闭的设置项
- 发送按钮可在生成时切换为停止按钮
- 统一 Generation 层负责请求取消与错误处理

### 24.8 高级采样参数

第一版不在普通设置页加入大量：

- Temperature
- Top P
- Top K
- Frequency Penalty
- 其他 Provider 特有参数

后续如有需要，可增加“高级 API 参数”。

## 25. 第四面墙来源

第四面墙人格基于 LittleWhiteBox 的第四面墙设计。

需要保留：

- 完整 Meta Protocol
- 原版自动 commentary 思路
- 时间与消息间隔
- 流式
- 停止
- 上下文清洗
- 提示词可编辑
- 恢复 LittleWhiteBox 默认 Prompt

明确不做：

- LittleWhiteBox 的 180 秒用户级 commentary 冷却
- 图片生成
- TTS / Voice
- Fullscreen
- 图像增强
- Voice enhancer

---

## 27. 第三方参考与发布边界

项目计划公开发布，因此从现在起按可发布标准开发。

### 27.1 LittleWhiteBox

仓库：

https://github.com/RT15548/LittleWhiteBox

其项目使用 Apache License 2.0，并额外要求 attribution。

如果项目引用、修改或分发 LittleWhiteBox 文件，应在 README / Credits 中加入原作者署名。

建议：

`Based on LittleWhiteBox by biex`

发布前还需要整理：

- License
- NOTICE
- attribution

### 27.2 Acsus-Paws-Puffs

仓库：

https://github.com/Hmkovo/Acsus-Paws-Puffs

其许可证为 All Rights Reserved，并明确限制：

- 未授权修改
- 二次开发
- 分发
- 把其代码用于其他项目

因此公开版本中：

**不能直接复制其代码、CSS、默认提示词等实现。**

可以：

- 研究功能设计
- 研究交互流程
- 理解算法思想
- 然后按本项目自己的架构重新实现

### 27.3 柏宝书

仓库：

https://github.com/baibai-git/ST-BaiBai-Book

本项目只计划调用其公开只读 API：

`globalThis.STBaiBaiBook`

优先使用公开文档中支持的接口，不依赖其内部私有实现。

---

## 28. 暂不纳入第一版的功能

不优先做：

- 钱包
- 转账
- 会员
- 模拟生活
- 天气生活系统
- 复杂图片系统
- 表情包生态
- Fullscreen
- TTS
- 图片生成
- 聊天背景深度美化
- 大量 QQ / 微信附加玩法

项目不能膨胀成完整手机模拟器。

核心仍然是：

**场外角色通讯 + 群聊讨论 + 自动吐槽 + 自动点评 + 角色保真。**

---

## 29. 当前开发顺序

1. 补全 Scope 生命周期与公共数据契约
2. 联系人 / 通讯录 / 酒馆角色同步完善
3. 聊天信息页完善
4. 私聊消息操作完善
5. API / Generation 公共层
6. 第四面墙完整 Prompt
7. 自动吐槽
8. Role Fidelity
9. 世界书 / 柏宝书长期记忆
10. 提示词与预设
11. 群聊轻编排 + 逐人生成
12. 自动点评
13. 统一 QA
14. 最终 iPhone / 微信 UI 美化
15. 发布整理

若后续出现明确依赖，可以调整实际施工顺序；但依赖统一 Generation Contract 的复杂 AI 功能，不应先各自实现临时生成接口。

---

## 30. 尚待继续讨论

当前未最终拍板：

1. `＋` 扩展菜单未来加入哪些小功能
2. 转发卡片最终视觉样式
3. 联系人详情页最终视觉美化
4. API 首发 Provider 的最终实现范围
5. 编剧正式 Prompt
6. 攻略正式 Prompt
7. 红笔编辑正式 Prompt
8. 自定义联系人 Prompt 编辑体验的最终视觉细节
9. 全局生成框架各条目的最终编辑 UI
10. 第一版最终 iPhone / 微信 UI 美化
11. README / Credits / LICENSE / NOTICE 的最终发布整理

---

## 31. 维护约定

从现在开始：

- 已确认功能进入 `SPEC.md`
- 后续如果规则被推翻，直接修改 `SPEC.md`
- 不在代码里偷偷保留与 SPEC 冲突的旧逻辑
- 开发时以最新 `SPEC.md` 为唯一设计源
- 大功能确认后再集中更新 SPEC，避免每个小问题都改一次文件


---

## 追加确认：moli 默认线上聊天预设（moli30）

### 1. 模式边界

moli小手机当前聊天模式统一为：**线上即时通讯**。

不拆分“聊天 / 通话 / 线下”三套全局预设。系统设置提供“线上聊天预设”总开关。

全局线上预设负责“手机消息应该怎样说”；联系人资料负责“这个人是谁、允许读取哪些角色资料”；具体 Conversation 负责“这一个聊天实例处于什么世界 / 时间 / 自动行为环境”。

### 2. 默认线上预设条目

默认完整保留 8 个可独立开关、可编辑的 Prompt 条目：

1. 📱 线上聊天协议
2. 🫧 活人聊天行为
3. 💬 语言自然度
4. ⏱️ 时间与消息间隔
5. 🧠 手机记忆
6. 🌍 正文与世界上下文
7. 📤 输出协议
8. 📚 上下文组装规则

完整默认文本以 `src/storage/prompt-settings.js` 中 `DEFAULT_ONLINE_PROMPT_BLOCKS` 为产品默认值，不得在实现中擅自缩写成摘要版。

### 3. 多气泡输出

模型以 `<message>...</message>` 标记独立聊天气泡。标签不向用户展示；一个生成结果可以落成多个真实 assistant 消息。

解析失败或模型未使用标签时，必须安全退化为单气泡，而不是丢弃回复。换行本身不作为气泡边界。

### 4. 手机记忆可编辑性

手机记忆概念层：

`原始聊天记录 → 最近聊天 → 日记 → 长期总结`

其中：

- 最近聊天：来自真实聊天历史的近期上下文窗口，不另做重复的“记忆查看页”，不可作为 AI 摘要文本编辑；原始消息仍可在聊天中查看。
- 日记：可查看、可编辑、可删除，允许用户纠正 AI 总结偏差。
- 长期总结：可查看、可编辑，允许用户纠正 AI 总结偏差。
- 柏宝书长期记忆：外部来源，moli 内只读；moli 不编辑柏宝书内容。

柏宝书与手机记忆不是同一层：柏宝书补正文世界的长期过去；手机日记/总结记录场外线上关系自身发生的事情。

### 5. 最近聊天上下文窗口

“保存多少聊天记录”与“每次生成读取多少聊天记录”必须分离。

聊天记录可长期完整保存；生成时使用可配置的“最近消息读取上限”。设计值：默认 100 条，允许 10～9999 条。9999 是用户允许的最大读取窗口，不代表无条件把 9999 条原文全部发送给模型；Context Builder 仍需受 Provider 上下文/token 容量保护。

该设置属于 Conversation 实例，而不是 Contact。相同联系人不同私聊实例可以使用不同读取窗口。

### 6. 内置人格数量

内置人格最终保留几个目前暂不锁定。第四面墙 / 编剧 / 攻略 / 红笔编辑的最终数量与各自专属配置放到后续阶段决定；当前线上全局预设实现不得依赖“固定必须有四个内置人格”。

---

## 追加确认：Conversation 2.0 第一阶段（moli31）

### 1. 线上聊天预设允许用户新增自定义条目

系统默认 8 个线上聊天 Prompt 条目继续作为 moli 默认预设存在。

用户可通过：

`设置 → 提示词与预设 → 线上聊天预设 → ＋ 添加自定义条目`

新增自己的 Prompt 条目。

自定义条目支持：

- 自定义名称
- 编辑 Prompt 内容
- 独立开关
- 删除
- 与默认条目一起参与实际生成注入

恢复 moli 默认预设时：

- 默认 8 个系统条目恢复默认内容与开启状态
- 用户自定义条目不因“恢复默认”被自动删除

默认系统条目不得因为用户可自定义条目而缩写或移除。

### 2. Contact 与 Conversation 进一步分离

同一个联系人允许拥有多个彼此独立的私聊 Conversation。

Contact 继续表示：

**这个人是谁。**

Conversation 表示：

**这一次聊天发生在哪个世界 / 时间 / 上下文里。**

新的私聊实例拥有独立：

- conversation id / key
- 聊天历史
- 置顶状态
- 未读状态
- 会话标题
- 归属模式
- 后续 Conversation 级上下文设置

### 3. Conversation 归属模式

新私聊支持：

#### 随当前正文

- 绑定当前 SillyTavern chat / scope
- 只在该正文 / 存档激活时出现
- 切走后隐藏
- 切回后原聊天与历史恢复
- 默认时间模式：跟随正文
- 默认读取当前正文：开启

#### 全局陪伴

- 不绑定某一个 SillyTavern chat / scope
- 切换不同正文后仍保留同一份聊天与历史
- 适合现实时间陪伴
- 默认时间模式：现实世界时间
- 默认读取当前正文：关闭

全局陪伴不得默认把当前屏幕上任意一个正文当成自身时间线，以避免不同存档 / 时间线互相污染。

### 4. 旧私聊兼容

旧结构中以 `contactId` 作为会话 key 的既有私聊不得丢失。

旧私聊继续原位读取，并按 Conversation 2.0 规则视为：

- `scopeMode = current`
- 默认跟随正文时间
- 默认读取正文
- 最近聊天读取上限默认 100

不得为了升级多私聊结构而清空、重建或隐藏旧聊天历史。

### 5. 同联系人多私聊默认不互读

同一个 `contactId` 只代表身份相同，不代表所有 Conversation 自动共享记忆。

在尚未提供显式“跨聊天记忆 / 场外历史共享”开关前：

- 现实陪伴聊天不得自动读取剧情沉浸聊天
- 剧情沉浸聊天不得自动读取现实陪伴聊天
- 不再默认把同联系人其他私聊作为 Generation 上下文注入

后续如果允许跨 Conversation 读取，必须由 Conversation 设置显式开启并标明来源。

### 6. 当前阶段 UI

当前私聊聊天信息页增加：

- 当前聊天名称 / 归属显示
- `＋ 新建另一个聊天`

新建聊天时选择：

- 随当前正文
- 全局陪伴

允许填写可选聊天名称，例如：

- 日常
- 2018正文
- IF线

首页与聊天标题使用“联系人名 · 聊天名”区分同一联系人的多个私聊。

### 7. Conversation 2.0 后续阶段

下一阶段继续把以下设置正式放入 Conversation 设置页：

- 时间模式
- 是否读取当前正文
- 最近聊天读取上限（默认 100，可设置 10～9999）
- 自动吐槽
- 自动聊天
- 手机记忆来源
- 后续当前聊天外观覆盖

本阶段只建立多私聊与归属地基，不提前实现未确认的自动聊天触发规则。

---

## 追加确认：Conversation 2.0 第二阶段（moli32）

当前私聊实例拥有独立的“当前聊天设置”。这些设置属于 Conversation，不属于联系人本体；同一个联系人建立的多个私聊可以拥有完全不同的设置，互不覆盖。

### 当前聊天设置入口

私聊 `聊天信息` 中提供：

- `当前聊天设置`

进入后至少包含：

1. 聊天名称
2. 时间模式
3. 读取当前正文
4. 最近聊天读取上限

### 聊天名称

- 仅修改当前 Conversation 的显示名称。
- 不修改联系人姓名 / 备注名。
- 同一个联系人不同聊天可以使用不同名称，例如 `日常`、`2018正文`。

### 时间模式

每个私聊独立保存，允许：

- `跟随正文时间`：以剧情 / 正文时间为准，不自动把现实经过时长套入故事。
- `现实世界时间`：使用现实日期、时刻与真实消息间隔，适合日常陪伴。
- `无时间感`：除非聊天或正文明确提到，否则不主动推断时间经过。

`随当前正文` 新建聊天默认使用 `跟随正文时间`。

`全局陪伴` 新建聊天默认使用 `现实世界时间`。

默认值只是创建时的初始配置，之后用户可以按 Conversation 单独修改。

### 读取当前正文

每个私聊独立保存：

- 开启：生成时允许读取当前 SillyTavern 最近正文。
- 关闭：当前正文不进入这个聊天实例的生成上下文。

`随当前正文` 新建聊天默认开启。

`全局陪伴` 新建聊天默认关闭，避免用户切换不同正文 / 世界线时自动污染陪伴聊天。

用户仍可手动给全局陪伴开启正文读取；此时正文是当前可见参考上下文，不应自动被解释为该陪伴聊天自身的唯一时间线。

### 最近聊天读取上限

- 默认：`100`
- 用户可设置：`10～9999`
- 小于 10 自动收敛到 10。
- 大于 9999 自动收敛到 9999。

这一数值只控制生成时优先送入模型的近期原始手机消息数量，不删除完整聊天记录，也不限制界面能够保存 / 查看多少条历史。

后续手机记忆系统仍按：

`近期原始聊天窗口 → 日记 → 长期总结`

分层处理，不能把“最近聊天读取上限”误当成聊天记录保存上限。

### 隔离规则

修改当前聊天设置只影响当前 `conversationId`。

不得因为两个 Conversation 指向同一个 `contactId`，就同步覆盖：

- 时间模式
- 正文读取开关
- 最近聊天读取上限
- 聊天名称
- 后续自动吐槽 / 自动聊天 / 手机记忆等会话配置

---

# moli33 决策补充：内置人格 / API 预设 / 记忆 / 状态栏 / 自动聊天

> 本节为 2026-09-13 后续确认，若与前文旧“编剧 / 攻略 / 红笔编辑”“日记”等描述冲突，以本节为准。

## 1. 内置人格暂定更新

保留既有“第四面墙”。原 `builtin:writer / builtin:guide / builtin:redpen` 为兼容旧聊天 ID 不做物理更换，但默认显示身份迁移为：

- `上帝`：上帝视角评价正文好坏、判断角色与 NPC、分析局势并给用户行动 / 攻略建议。
- `人类`：站在用户角度的姐妹 / 闺蜜 / 同伙；可磕 CP、替用户说话、讨伐角色，也能认真讨论下一步行动与剧情分支。
- `吃瓜观察员`：活跃气氛，观察暧昧、修罗场、权力关系、NPC 八卦等；最终名称仍允许后续调整。

上述人格的私聊不具备“自动吐槽”或“自动点评”。它们可正常私聊、参加群聊，并可在后续开启“自动聊天”。

## 2. API 配置采用酒馆用户熟悉的预设方式

- 全局 API 设置继续保留“跟随酒馆当前 API / 独立 API”。
- 独立 API 可保存为本机 `API 预设`，保存 API 类型、地址、Key、模型、流式设置。
- 用户可选择预设后“应用”，也可另存和删除预设。
- 后续 Contact 与 Conversation 的独立 API UI 都应复用同一预设库，不复制发明另一套 Profile 管理器。
- 最终优先级仍为：`当前 Conversation 独立 API > Contact 独立 API > 全局 API`，但 UI 不向用户暴露 Resolver 等工程术语。

## 3. 手机记忆命名与触发

旧文档中的“日记”统一改称 **近期记忆**，避免与其他手机扩展的“日记”概念过度相似。

记忆链：

`近期原始聊天窗口 → 近期记忆 → 长期总结`

- 近期记忆：按新增消息条数触发；建议默认 30 条，可配置。
- 长期总结：按新增近期记忆数量触发；建议默认每 5 份近期记忆更新一次，可配置。
- 两者均允许人工编辑；失败时应支持“生成到最新 / 重试失败部分”。
- 柏宝书长期记忆仍是独立只读来源，不等同于手机长期总结。

## 4. 自动聊天

自动聊天不以固定间隔 / 随机概率作为主要触发逻辑，而由角色自主判断是否适合主动联系用户。判断可参考最近聊天、未完话题、关系状态、正文事件、所选时间模式、距离上次互动的时间与近期主动频率。

自动聊天与自动吐槽 / 自动点评是不同系统。

## 5. 状态栏

状态栏参考章鱼手机的成熟功能结构，但 moli 独立实现：

- 全局状态栏预设库：预设名称、Prompt Suffix、Regex Pattern、HTML Template、测试文本、匹配结果、实时预览。
- Contact：启用状态栏、快速填充预设，并允许覆盖 Prompt / Regex / HTML。
- Conversation：保存本聊天实际状态历史，避免同一 Contact 的不同世界线串状态。
- 生成上下文只读取最近 N 个状态；完整状态历史不因此删除。


---

# moli34 确认：主 API 设置按酒馆手机扩展习惯重做 + Contact 独立 API 第一阶段

> 本节覆盖 moli33 中仍然偏“自建 API Profile 管理器”的 UI 方案。后续 API 页面不得再自行发明另一套交互。

## 1. 主设置 API 页面

主设置中的 API 页面采用酒馆用户熟悉的手机扩展结构：

1. `API 来源`
   - `跟随酒馆设置（默认）`
   - `自定义 API`
2. `流式生成`
3. 自定义 API 下选择 `API 类型`
4. 按 API 类型显示对应密钥 / 反向代理 / 模型区域
5. 反向代理支持单独的代理预设：读取、保存、删除
6. OpenRouter 使用自己的 Key 区域
7. 自定义 OpenAI 兼容端点使用 Base URL + 可选 Key + 模型
8. 非自定义端点使用模型选择 + 刷新 + 手动模型
9. 提供测试连接
10. API 配置本身仍支持保存 / 读取 / 删除预设，供 Contact / Conversation 独立 API 复用

`跟随酒馆设置`不是假选项：生成时调用 SillyTavern 当前 API。

当前 moli 独立实现，不直接依赖其他扩展运行时；但交互结构保持酒馆用户熟悉的形式。

## 2. API 数据兼容

旧 `moli-phone:api-settings:v1` 不物理删除。

读取旧配置时兼容迁移：

- 旧 `source=tavern` → 新 `source=default`
- 旧 `source=independent` → 新 `source=custom`
- 旧 Claude → `format=claude`
- 旧 Gemini → `format=makersuite`
- 旧 OpenAI Compatible：有自定义 Base URL 时迁到 `format=custom`，否则迁到 `format=openai`

API 预设同样做惰性兼容读取，不让 moli33 已保存配置消失。

## 3. Contact 独立 API

联系人资料正式拆出 `独立 API`。

规则：

- 默认关闭：跟随主设置 API。
- 开启：当前 Contact 保存自己的 API 配置副本。
- 可以从主 API 预设库“读取配置”。
- 读取预设是**复制当前配置**，不是永久绑定；之后修改主预设不能偷偷改变该联系人。
- Contact 独立 API 已接入私聊生成。
- 后续 Conversation 独立 API 的优先级高于 Contact；尚未开启 Conversation override 时，不得越级发明。

最终优先级仍为：

`Conversation 独立 API > Contact 独立 API > 主设置 API`

## 4. 联系人资料入口

私聊 `聊天信息` 中正式拆出：

- 人格与提示词
- 独立 API
- 状态栏
- 手机记忆
- 当前聊天设置 / 新建聊天

其中：

- `人格与提示词` 已成为独立编辑页。
- `独立 API` 已成为独立编辑页并接入生成。
- `状态栏` 与 `手机记忆` 入口已固定，不允许后续再次遗漏；完整功能继续按既定节点实现。

## 5. 既有命名继续有效

手机记忆仍统一称：

`近期原始聊天 → 近期记忆 → 长期总结`

不得再把“近期记忆”改回“日记”。

---

# moli35 决策补充：联系人 API 引用与人格来源分型

> 本节为 2026-09-13 确认。与 moli34 中“联系人读取后复制 API 配置”以及旧的统一人格提示词 UI 冲突时，以本节为准。

## 1. 联系人独立 API 只引用主设置已保存配置

- 主设置是 API 配置内容的唯一维护入口。
- Contact 资料页不重复展示或保存 API Key、API URL、反向代理密码、模型、流式参数等账号/连接字段。
- Contact 只保存：是否启用独立 API + 所选主设置 API 配置的 `presetId`。
- 关闭独立 API：跟随主设置当前 API。
- 开启独立 API：必须选择一个主设置中已保存的 API 配置。
- 生成时按 `presetId` 动态读取配置，因此主设置中修改同一配置后，引用它的联系人自动使用最新内容。
- 若所选配置已被删除，生成时明确提示用户重新选择，不静默切换到错误模型。
- 兼容 moli34 已产生的旧 `apiOverride.config` 副本：旧数据可继续读取，但新 UI 不再创建配置副本。

## 2. 人格与提示词按联系人来源分型

### 酒馆角色

酒馆角色不额外要求“人格 Prompt”。角色身份由酒馆来源资料决定。资料页只提供可选的 **自定义附加 Prompt**，用于补充约束而不是重新定义角色。

酒馆角色来源结构继续按既定设计逐步接入：角色卡、性格、场景、Example Dialogue、世界书、柏宝书长期记忆、最近正文、场外聊天历史、自定义附加 Prompt。

### 自定义联系人

自定义联系人没有酒馆角色卡，因此保留 **人格 Prompt**，它是主要人格来源；同时保留简介。

### 内置人格

第四面墙 / 上帝 / 人类 / 吃瓜观察员使用系统内置人格 Prompt。允许用户编辑，并在默认 Prompt 完整落地后提供“恢复默认”；不把它们当作酒馆角色来源读取。

## 3. 联系人资料主入口

联系人资料继续固定保留：

- 人格与提示词
- 独立 API
- 状态栏
- 手机记忆
- 聊天 / 当前聊天设置相关入口

下一功能节点：按已确认的章鱼式结构实现状态栏本体。

# moli37 现行规则索引：Conversation 校准

> 本节用于消除历史开发章节中的旧表述冲突。若前文与本节冲突，以本节及其后续明确确认内容为准；历史章节保留作为开发记录，不再作为现行实现依据。

## Conversation 当前规则

- Contact 表示“这个人是谁”；Conversation 表示“这一次聊天发生在哪个世界 / 时间 / 上下文里”。
- 同一 Contact 可以拥有多个彼此独立的私聊 Conversation，默认不互读历史或记忆。
- `当前聊天` 必须可选择：在当前联系人已有 Conversation 之间切换当前编辑 / 打开的聊天实例，而不是固定展示文本。
- `归属` 必须可修改：`随当前正文` / `全局陪伴`。
- 修改归属属于**移动同一 Conversation**：保持同一个 conversation key/id、完整消息历史、标题、时间模式、正文开关、近期聊天上限及未知扩展字段；不得复制第二份、不得清空历史。
- 改为 `随当前正文` 时绑定当前稳定 scope；改为 `全局陪伴` 时解除正文 scope 绑定并进入全局 Conversation store。
- 归属变化不会自动重置时间模式、正文读取开关等已经由用户明确设置的 Conversation 配置。`随当前正文→默认正文时间/正文开启`、`全局陪伴→默认现实时间/正文关闭`只属于**新建时默认值**。
- 时间模式始终是 Conversation 级可编辑设置：跟随正文 / 现实世界 / 无时间感。
- 当前聊天名称、正文读取开关、最近聊天读取上限也均为 Conversation 级设置。

## 已废弃/被后续设计替代的旧术语索引

- 旧内置人格“编剧 / 攻略 / 红笔编辑”已被后续“上帝 / 人类 / 吃瓜观察员”替代；第四面墙仍独立。
- 手机记忆旧称“日记”已替换为“近期记忆”；现行链路为：`原始聊天记录 → 最近聊天上下文窗口 → 近期记忆 → 长期总结`。
- 同 Contact 的其他 Conversation **默认不得**作为生成上下文自动注入；未来只有显式跨聊天共享设置才能开启。
- API 现行方向为主设置保存 API 配置，Contact/未来 Conversation 只选择引用；不得要求在资料卡重复填写 Key、URL、模型和密码。
- 酒馆角色不需要额外“人格 Prompt”覆盖角色卡；其人格来源应由角色卡/性格/场景/Example Dialogue/世界书/柏宝书/最近正文/场外聊天历史等来源开关与可选自定义附加 Prompt 组成。

## 审计后待补齐功能（不代表已实现）

1. 酒馆角色人格来源开关与世界书地基。
2. 柏宝书长期正文记忆接入。
3. 内置人格正式 Prompt / Generation 接入。
4. 群聊轻编排 + 逐人生成。
5. 自动吐槽 / 群自动点评。
6. 角色自主判断式自动聊天。
7. 手机近期记忆 / 长期总结。
8. 状态栏完整生成、提取、历史与展示链。

不得因为功能已写入 SPEC 就宣称代码已经实现；每个节点必须以真实仓库代码和测试结果为准。


# moli38 现行修正：Conversation 四概念拆分与状态栏恢复

> 本节为 2026-09-13 校准。与 moli37 及更早章节中“随当前正文 / 全局陪伴”的归属 UI 文案、把 Conversation 选择器塞入“当前聊天设置”页等冲突时，以本节为准。

## 1. Conversation 四个概念必须独立

1. **当前聊天**：选择同一 Contact 下当前正在查看/编辑的 Conversation。
2. **归属**：只表示 Conversation 存储/绑定范围，现行 UI 文案为 `当前存档 / 全局`。旧称“随当前正文 / 全局陪伴”仅保留为历史开发术语。
3. **时间模式**：`跟随正文时间 / 现实世界时间 / 无时间感`，属于 Conversation 独立设置。
4. **读取当前正文**：独立布尔开关，决定生成时是否读取当前 SillyTavern 正文。

四者不得互相冒充。关闭“读取当前正文”不会改变归属；修改归属不会重置时间模式或正文读取开关。

## 2. 资料卡与当前聊天设置职责

联系人资料卡外层直接提供：
- 当前聊天选择器；
- 归属选择器（当前存档 / 全局）；
- 当前时间模式摘要；
- 读取当前正文：开启 / 关闭。

`当前聊天设置` 内部只保留：
- 聊天名称；
- 时间模式；
- 读取当前正文；
- 最近聊天读取上限。

不得在该子页再次重复“当前聊天”和“归属”选择器。

## 3. 归属修改

归属修改即时移动同一 Conversation；保持 conversation key/id、消息、标题、时间模式、正文读取、近期消息上限与未知扩展字段，不复制、不清空。

## 4. 状态栏回归规则

moli36 已实现的“全局状态栏预设 + Contact 状态栏默认配置”属于已完成能力，不得因后续 Conversation 校准而消失。moli38 恢复该 UI 与读写链。Conversation 状态历史/提取/展示仍属于后续阶段，不在本节宣称完成。


# moli39 现行规则：角色资料卡 = Contact 来源控制中心

> 本节正式取代历史章节中把“最近正文 / 场外聊天历史”列为 Contact 人格来源开关、以及给 Tavern 角色再写一份“大人格 Prompt”的旧表述。

## 1. 三层职责

- **Contact**：这个人是谁，以及允许哪些稳定角色来源参与生成。
- **Conversation**：这一次聊天发生在哪个世界/时间线，是否读取当前正文，以及读取多少当前聊天历史。
- **Role Fidelity / Sources**：Tavern 角色生成时从角色卡哪些字段还原人格。

角色资料卡不是第二张角色卡。Tavern 角色的人格 Source of Truth 始终是 SillyTavern 角色卡及其关联资料；moli小手机只负责读取、筛选、补充与 Conversation 级上下文拼装。

## 2. Tavern「角色资料与提示词」

Tavern 联系人不显示竞争性的“大人格 Prompt”。其页面包含独立来源开关：

- Description / 角色设定
- Personality / 性格
- Scenario / 场景
- Example Dialogue
- System Prompt
- Post-History Instructions

默认全部开启。关闭仅表示生成时不注入；不得删除 `contact.source.roleFidelity` 中已保存的快照。每项可查看当前保存的原文，只读；来源角色存在时自动刷新，来源丢失时保留最后一次同步快照。

System Prompt 与 Post-History Instructions 必须单独暴露。即使开启，其中小说正文格式、第三人称、篇幅等要求也不得覆盖 moli小手机的线上聊天输出协议。

## 3. 动态上下文归 Conversation

以下内容不得再作为 Contact 来源开关：

- 最近正文：由 Conversation 的“读取当前正文”控制；
- 当前场外聊天历史：由当前 Conversation 的 messages 与“最近聊天读取上限”控制；
- 时间模式、当前存档/全局归属：均属于 Conversation。

## 4. Intro 与附加 Prompt 语义

- Tavern 联系人的 `intro` 是联系人简介/展示信息，默认不注入模型；
- 真正要影响 Tavern 角色生成的用户补充统一写入“自定义附加 Prompt”；
- 自定义联系人没有角色卡，仍保留人格 Prompt；
- 内置人格仍使用其内置人格 Prompt 体系。

## 5. 世界书与长期剧情记忆边界

本节点只固定 UI/架构位置，不宣称已经接通世界书或柏宝书。

- 世界书资料卡开关未来表示“允许使用哪些条目”的白名单；
- 本轮实际注入仍需按常驻、关键词、当前正文/聊天等触发规则激活，禁止把全部勾选条目每轮无条件塞入 API；
- 柏宝书属于 Contact 的长期剧情记忆来源，具体 Provider 接入另行实现。

# moli40 现行修正：资料卡减法、状态栏延期、世界书读取地基

> 本节为 2026-09-13 现行规则。与 moli36～moli39 中状态栏已完成/必须保留、资料卡外层显示归属/时间/正文摘要等冲突时，以本节为准。

## 1. 私聊资料卡只保留一个 Conversation 快捷入口

资料卡外层只显示 `当前聊天` 选择器。选项文本同时展示聊天名称与归属，例如 `日常 · 当前存档`、`陪伴 · 全局`。

资料卡外层不再提供独立 `归属` 按钮，也不重复显示 `时间模式`、`读取当前正文`。这些设置统一进入 `当前聊天设置`：聊天名称、时间模式、读取当前正文、最近聊天读取上限。

`scopeMode / boundScopeKey` 仍可作为 Conversation 底层数据存在，但不再作为资料卡外层的独立操作控件。新建聊天时仍可决定创建为当前存档或全局聊天。

## 2. 状态栏整体延期

当前阶段从产品 UI 与运行链移除状态栏：主设置不显示状态栏预设，Contact 资料卡不显示状态栏入口，不继续实现状态生成/Regex/HTML/历史链。

历史状态栏设计章节保留作为开发记录，但不代表现行已完成功能。以后若重新启用，必须重新确认需求后作为独立节点接入，不得影响当前 Contact / Conversation / Role Fidelity / World Book 主线。

为避免破坏用户旧数据，历史 localStorage/Contact 中已经存在的状态栏字段不做主动清洗；现行代码不读取、不编辑、不注入这些字段。

## 3. Tavern 世界书第一阶段：真实读取 + 白名单

世界书属于 Contact 的角色资料来源，但“允许使用”与“本轮激活”必须分开。

第一阶段使用 SillyTavern Extension Context 的正式世界书能力读取资料：`loadWorldInfo()` 读取角色 `extensions.world` 关联的世界书；角色卡内嵌 `character_book` 使用 `convertCharacterBook()` 兼容转换。资料卡可以看到检测到的世界书与条目，并逐条设置 moli 白名单。

白名单规则：默认允许所有条目；只持久化用户明确关闭的条目。关闭条目只影响 moli小手机，不修改 SillyTavern 原世界书，也不删除原始资料。

本阶段**不把所有允许条目直接注入 Prompt**。下一阶段必须继续对齐 SillyTavern 世界书语义，根据常驻、关键词、可选关键词、递归、概率/分组、扫描上下文等规则决定“本轮激活条目”，再与白名单取交集后注入。不得以“勾选 = 每轮全文注入”替代触发机制。

# moli41 现行修正：角色归属在添加好友时确定 + 世界书本轮激活

> 本节为 2026-09-13 校准；与 moli40 及更早章节冲突时，以本节为准。

## 1. 资料卡不再承担 Conversation 归属选择

- 内置人格与用户创建联系人默认属于当前存档；资料卡不显示无意义的“当前聊天/归属”下拉框。
- Tavern 角色的“正文角色 / 全局角色”归属也不在角色资料卡选择。
- 聊天信息页继续保留“当前聊天设置”，用于聊天名称、时间模式、正文读取、最近聊天读取上限；不重复展示归属选择器。
- 同一 Contact 的不同 Conversation 作为聊天列表中的独立实例进入，不依赖资料卡下拉框切换。

## 2. Tavern 角色归属在“同步酒馆角色”创建流程确定

`添加好友 → 同步酒馆角色 → 选择角色 → 添加` 后进入一次角色类型选择：

- `正文角色`：创建当前存档 Conversation；默认正文时间、默认读取当前正文。
- `全局角色`：创建全局 Conversation；默认现实时间、默认不读取当前正文。

“正文角色已在当前聊天列表中”之类提示不再显示。角色已经存在但不在当前 Scope 时仍可显示简短“已添加”，但归属不是资料卡编辑项。

## 3. 世界书第二阶段：白名单后的本轮激活

moli40 的世界书条目开关继续只表示 Contact 白名单。moli41 在生成前增加本轮激活层：扫描当前 Conversation 的近期消息；仅当该 Conversation 开启正文读取时，最近正文也进入扫描材料。

激活层只处理当前 Tavern Contact 自己关联/内嵌的世界书，先排除 SillyTavern 原本禁用条目和 moli 白名单关闭条目，再按常驻、主关键词、次关键词逻辑、正则关键词、概率与有限递归决定本轮条目。激活内容作为“本轮激活的世界书”进入 Prompt Builder。

不得直接调用当前 SillyTavern 页面角色的世界书结果冒充任意 moli Contact 的世界书；不同 Tavern Contact 的世界书必须保持来源隔离。

复杂的 SillyTavern 高级世界书能力（例如 inclusion group、sticky/cooldown、outlet、精确 token budget/插入位置等）仍属于后续兼容增强，不在本节点伪装为已完整复刻。


# moli42 现行修正：同步角色可重复创建 Conversation + 柏宝书长期剧情记忆

> 本节为 2026-09-13 现行规则；与早期“已添加角色不可再次选择”以及资料卡“＋ 新建另一个聊天”的历史 UI 冲突时，以本节为准。

## 1. 同步酒馆角色的选择状态不是永久状态

- “已添加”只表示该 Tavern Contact 已经存在于 moli 通讯录，不代表该角色永久不可选择。
- 同步列表中的勾选属于一次性的 UI 临时状态；完成添加后必须立即清空并刷新。
- 已存在的 Tavern Contact 仍可再次勾选，并通过“正文角色 / 全局角色”创建一个新的独立 Conversation。
- 因此不得再用“已在当前聊天列表”把 checkbox 设为 checked + disabled。

## 2. 删除资料卡“＋ 新建另一个聊天”

资料卡不再提供“＋ 新建另一个聊天”。Tavern 角色需要再次建立独立聊天时，统一从“同步酒馆角色”流程选择已有 Contact 并创建新的 Conversation。Contact 本体继续复用，不重复复制角色资料。

## 3. 柏宝书长期剧情记忆第一阶段接入

- 只使用 ST-BaiBai-Book 公共只读 API v1：`globalThis.STBaiBaiBook`。
- 读取 `getInjectedHistory()`，使用与柏宝书正常记忆注入一致、已经避开其当前滑动窗口的长期历史剧情。
- 不读取 snapshot、state、vars、items、plans、scenes、npcs、itemLog 等状态型资料。
- Tavern Contact 的 `roleSources.longTermMemory` 控制是否允许使用；关闭只停止注入，不修改柏宝书数据。
- 当前 Conversation 关闭“读取当前正文”时，不注入柏宝书这一动态正文世界来源，避免全局/脱离正文聊天被当前存档长期剧情污染。
- 柏宝书不存在、API 版本不兼容或读取失败时静默降级；最近正文机制继续正常工作。
- 若 API `coverage.complete === false`，Prompt 明确把长期记忆视为可能有缺口，近期事实继续以最近正文为准。

## 4. 上下文顺序

正文世界动态上下文保持：`柏宝书长期剧情记忆 → 最近正文原文 → 本轮激活世界书/当前场外聊天`。柏宝书用于补较早历史，最近正文负责当前发生的事情；不得把柏宝书其他状态账本混入这一层。


# moli44 现行规则：手机 Conversation 记忆数据层

> 本节为 2026-09-13 现行规则。手机记忆与柏宝书正文长期记忆是两条完全独立的记忆链。

## 1. 数据归属

手机记忆属于 **Conversation**，不是 Contact。每个私聊实例独立保存 `memory.recent` 与 `memory.longTermSummary`；同一 Contact 的其他 Conversation 默认不得读取。旧 Conversation 通过惰性默认值获得空记忆，不进行破坏性迁移。

## 2. 记忆链与优先级

场外聊天链固定为：`原始聊天记录 → 最近聊天上下文窗口 → 近期记忆 → 长期总结`。生成时优先级为：当前明确消息 / 最近原始聊天 > 当前正文事实 > 近期记忆 > 长期总结；旧记忆不得覆盖更新事实。柏宝书继续只负责正文世界的长期剧情，不属于手机记忆。

## 3. 当前阶段 UI 与编辑

`聊天信息 → 手机记忆` 正式读取当前 Conversation 的记忆数据。近期记忆和长期总结均可直接编辑、删除/清空并保存；清空聊天原始记录不会偷偷删除用户已经保存的记忆。

## 4. Prompt 接入

当前 Conversation 的手机记忆在生成时作为独立“场外记忆”块注入。近期记忆与长期总结不会替代最近原始消息，也不会自动跨 Conversation 共享。

## 5. 自动生成边界

本节点建立稳定数据结构、人工编辑入口与 Prompt 消费接口，并预留 `lastCondensedMessageId / lastSummarizedAt` 进度字段。自动把旧原始消息压缩为近期记忆、再把较旧近期记忆压缩为长期总结，属于下一阶段；在触发规则正式实现前不得伪装成已经自动记忆。

## 6. 世界书兼容热修

世界书激活层读取 `worldBookPolicy.disabledEntries` 时同时兼容旧数组格式与 moli40 现行 object-map 格式，确保用户在资料卡关闭的条目不会因数据格式差异重新进入本轮激活。

# moli45 现行规则：手机记忆自动压缩链

> 本节为 2026-09-13 现行规则，建立在 moli44 Conversation 记忆数据层之上。

## 1. 低频增量压缩

自动记忆不得每轮调用 API。只有已经离开当前 `recentChatLimit` 原始聊天窗口、且自 `lastCondensedMessageId` 之后累计至少 100 个未压缩的完整 AI 交互轮次时，才取最早 100 轮生成一段自动近期记忆。一个完整轮次定义为：用户可连续发送 1..N 个气泡，随后通过空输入触发一次 AI，AI 可返回 1..N 个角色气泡；无论这一轮拆成多少气泡，都只计 1 轮；同一次 AI 调用返回的所有角色气泡共享 `generationTurnId`。再次空输入触发新的 AI 调用时会得到新的轮次 ID，因此仍独立算 1 轮。moli45 以前没有轮次 ID 的旧记录按“连续用户块 + 连续角色块”兼容推断。只有整轮全部离开 `recentChatLimit` 原始聊天窗口后才有资格进入压缩，绝不从一轮中间切开。

## 2. 成功后才推进游标

压缩 API 成功且返回非空结果后，才写入近期记忆并推进 `lastCondensedMessageId / lastCondensedAt`。失败、超时、空回复均不得推进游标；失败只记录 `lastAutoError`，不影响正常聊天回复。

## 3. 近期记忆到长期总结

自动近期记忆累计到 8 段时，取最旧 5 段再压缩为一个长期沉淀段，并从近期记忆中移除这 5 个自动段。人工编辑/创建的近期记忆标记为 manual，不参与自动淘汰。

长期沉淀只追加到现有 `longTermSummary`，不得重写用户已经人工维护的既有长期总结。这样自动链可以继续增长，但不会用一次模型改写粗暴覆盖人工记忆。

## 4. 触发时机

自动检查发生在一次角色回复已经成功落盘之后。未达到阈值时不发起额外 API 请求；达到阈值时才进行压缩。自动压缩失败不回滚或删除本轮正常聊天消息。

## 5. Conversation 隔离与 API

压缩进度、近期记忆、长期总结继续严格属于当前 Conversation。同一 Contact 的其他 Conversation 不共享游标或记忆。记忆压缩沿用该 Contact 当前实际生效的 API（Contact 独立配置优先，否则主 API）；酒馆当前 API 使用公开的 `generateRaw` 能力。

# moli46 现行规则：内置人格正式私聊生成 + 更新可用提示

> 本节建立在 moli45 自动手机记忆链之上。内置人格进入真实 Generation，但不把 Tavern 角色资料来源错误复用给内置人格。

## 1. 四个内置人格进入正式私聊 Generation

- `第四面墙 / 上帝 / 人类 / 吃瓜观察员` 均拥有独立的系统默认人格 Prompt，并可使用现有私聊 Generation。
- 继续沿用 moli小手机统一的线上聊天预设、`<message>` 多气泡协议、流式/停止、时间模式、当前 Conversation 原始历史与手机记忆。
- 内置人格不读取 Tavern Role Fidelity、Tavern 世界书白名单或 Tavern 角色卡字段。
- Conversation 开启“读取当前正文”时，内置人格可读取最近正文；柏宝书存在时可作为较早正文剧情记忆补充。关闭正文读取时，两者都不进入本轮动态正文上下文。
- 内置人格的手机近期记忆 / 长期总结仍严格属于当前 Conversation；moli45 自动压缩链现在也允许内置人格正常参与。

## 2. 默认人格 Prompt 与用户编辑

- 内置人格默认 Prompt 由 moli 自己的 `src/prompts/builtin-personas.js` 维护，四个身份互不共用。
- 联系人“人格与提示词”页可编辑当前内置人格 Prompt，并提供“恢复默认人格 Prompt”。恢复只把编辑框恢复为该身份的系统默认文本，保存后生效；不删除消息、Conversation 或手机记忆。
- 第四面墙本节点先建立可运行的 moli Meta/幕后观察人格基线；历史章节要求的 LittleWhiteBox 原版完整 Meta Protocol / commentary 逐项兼容仍是独立后续增强，不在本节点虚假宣称已经逐字恢复原版。

## 3. 更新按钮状态

- 设置页右上角继续保留扩展更新按钮。
- 手机初始化后以非阻塞方式读取本地 manifest 与 `homePage` 对应 GitHub 仓库的远端 manifest；远端版本高于本地版本时，更新按钮显示红色。
- 没有更新时保持普通样式；检查失败只记录调试信息，不得阻断手机初始化或聊天。
- 点击按钮仍使用 SillyTavern 扩展更新接口执行真正更新；服务器报告已是最新版或更新成功后清除红色状态。

## 4. 回归要求

继续保留：moli45 100 轮自动记忆规则、moli43 悬浮球热修、Tavern 同步角色可重复选择、资料卡无“新建另一个聊天”、Conversation 设置、主/Contact API、Role Fidelity、世界书白名单与激活、柏宝书、消息/转发/搜索。状态栏仍延期，不恢复。

## 5. 下一主节点

内置人格私聊实机确认后，进入 **群聊轻编排 + 逐人生成**。群聊编排不得把所有成员一次塞给一个模型代写；继续采用“先决定本轮谁说话/顺序，再按成员逐人生成，后发言者可看到本轮前面已生成消息”的既定架构。

---

# moli47 修正版：自动行为与第四面墙校准

> 本节是当前最终规则；与前文旧“群聊自动吐槽”或“主动聊天不采用百分比”的描述冲突时，以本节为准。

## 1. 私聊自动行为

仅 **酒馆角色** 与 **自定义联系人** 的私聊提供两套彼此独立的 Conversation 级设置：

- 自动聊天 / 主动私聊：开关 + 0～100% 百分比；百分比表达角色主动联系用户的倾向/触发权重，不是固定分钟定时器。
- 自动吐槽正文：开关 + 0～100% 百分比；它只针对正文事件产生场外吐槽，不等同于主动私聊。

两套状态必须按 Conversation 隔离。同一 Contact 的不同私聊实例可以使用不同配置。

第四面墙、上帝、人类、吃瓜观察员不复用这组“酒馆角色 / 自定义联系人”通用按钮；第四面墙保留自己的 LittleWhiteBox commentary 体系，上帝 / 人类 / 吃瓜观察员仍不拥有自动吐槽 / 自动点评。

## 2. 群聊自动行为

**群聊不提供自动吐槽。** 群聊只提供自动点评：

- 自动点评开 / 关
- 每 N 个有效正文 AI 回合点评

自动点评触发后仍遵守既定“全员参与、逐人生成”的 Review 规则。

## 3. 普通群聊回复规则保持不变

普通群聊采用“轻编排 + 逐人生成”：

- 不强制所有成员回复，也不设固定 1～3 人上限；理论上限为群成员数量。
- 第一位按当前话题相关度决定，不纯随机。
- 被 @ 的成员必须参与，其他成员仍可自然插话。
- 后发言者必须看见本轮用户消息与前面已经产生的新消息。
- 每个成员单独调用自己的完整人格 / Role Fidelity / API 上下文，禁止一次请求让一个模型代写所有成员。

## 4. 第四面墙

第四面墙以 LittleWhiteBox 原版 Fourth Wall 的行为结构为 Source of Truth：皮下语C搭档身份、正文/场外边界、时间与消息间隔、现实碎片、内省后短消息、独立 commentary 模式、上下文清洗、流式/停止及恢复默认入口都必须保留。moli 不再使用“幕后分析助手”作为第四面墙人格。

为避免与 moli 现有消息协议冲突，解析层同时兼容 `<message>` 与 LittleWhiteBox 风格 `<msg>`；`<thinking>` 仅作为内部生成内容，展示时必须被清理。

当前节点先完成默认 Prompt 行为结构、解析兼容和设置持久化；自动吐槽 / 自动点评 / 主动私聊真正后台触发仍必须等待对应 Automation Engine，不能仅凭按钮存在就宣称功能已运行。

---

# moli48：自动行为滑杆 + 群聊轻编排/逐人生成

## 1. 私聊自动行为 UI

酒馆角色 / 自定义联系人继续保存 0～100 的内部概率值，但聊天信息页不再显示数字输入框或百分比数字。`主动私聊频率` 与 `正文吐槽频率` 均使用简约滑动进度条；拖动位置直接保存原有 0～100 Conversation 值，不改变既有数据语义。

## 2. 普通群聊真实生成

普通群聊的空输入触发回复正式接通“轻编排 + 逐人生成”：

- 先进行一次轻量编排，只给成员 ID、名称、短描述、最近群聊与 @ 信息，不加载所有人的完整角色卡；
- 编排器只返回本轮发言成员与顺序，不代写群成员内容；
- 不强制全员，不设固定 1～3 人上限，最多不超过当前成员数量；
- 被 @ 的成员必须进入本轮 speaker 顺序；
- 真正生成时每个 speaker 单独调用；Contact 独立 API 优先，否则跟随主 API；
- Tavern speaker 继续读取自己的 Role Fidelity、允许的世界书、柏宝书等既有上下文；
- 后发言者必须读取本轮前面已经生成的新群消息，因此同轮成员不是彼此隔离的独立答题；
- 每个 speaker 仍使用 moli 线上聊天输出协议，可产生 1～N 个自然气泡；
- 一次用户空输入触发的整轮群聊回复共享同一个 `generationTurnId`。

## 3. 边界

本节点只接通普通群聊主动请求回复。群聊自动点评仍等待 Automation Engine；角色/自创联系人的自动吐槽正文与主动私聊后台触发也仍等待 Automation Engine。群聊仍然没有自动吐槽。

# moli49：滑杆百分比可视反馈 + 群聊自动点评第一阶段

## 1. 滑杆显示规则修正

酒馆角色 / 自定义联系人聊天信息页中的两条自动行为滑杆继续保持简约，不恢复数字输入框；但为了让用户知道当前实际值，滑杆下方中间显示一个小号百分比文本，并在拖动时实时更新：

- 主动私聊频率：滑杆 + 居中 `N%`
- 正文吐槽频率：滑杆 + 居中 `N%`

底层仍保存原有 0～100 Conversation 值，数据语义不变。

## 2. 群聊自动点评开始真正运行

群聊仍然**没有自动吐槽**。本节点只接群聊自动点评 Automation：

- 每个群聊独立保存 `自动点评开 / 关` 与 `每 N 个有效正文 AI 回合`；
- Extension 只在稳定 SillyTavern chat scope 下监测当前正文中的有效 assistant 回合；
- 第一次接管现有存档时只建立基线，不追溯旧正文自动补点评；
- 开关关闭期间更新观测基线，但不累计点评有效回合；重新开启后从续算点继续；
- 正文 assistant 回合增加时累计；删除导致回合数下降时同步回退；
- 到达 / 跨过 N 边界后触发一轮 Review；
- 同一 N 边界发生正文 reroll 且最后一条正文内容变化时，允许针对新版正文重新触发该边界 Review；
- Review 第一位成员随机；当前群所有可用人格至少各生成一次；后说者读取本轮前面已经写入 working history 的点评消息；
- 每个成员仍使用自己的 Contact 人格、Role Fidelity、Worldbook、BaiBai、API 配置；
- Review 消息写入群聊时 `source = review`，并通过统一 Conversation 消息结构保存；
- 后台点评失败不影响正文和已有场外聊天，错误状态保留，避免高频无限重试。

本节点尚未接酒馆角色 / 自定义联系人的正文自动吐槽与主动私聊 SEND/SKIP，它们仍属于后续 Automation 节点。


# moli50：群聊收束 + 自动行为闭环 + 未读/输入反馈

- 普通群聊不以正文存在为发言前提；正文仅作为可用上下文。轻编排通常选择 1～3 名成员，必要时最多 4 名；被 @ 者必须参与，额外插话最多 2 人。同一成员一轮只调用一次，每次最多展示 3 个气泡。
- 群自动点评仍是唯一群后台自动行为：全员逐人一次，每人最多 2 个气泡。群聊不增加自动吐槽。
- Tavern 角色在群聊/点评中必须把正文同名同身份角色认作自己本人，以第一人称本人立场回应，不得默认变成作者、分析员或旁观者。
- 酒馆角色/自创联系人正文吐槽与主动私聊开始进入 Automation：正文新 AI 回合按吐槽百分比获得机会；主动聊天按频率获得机会后仍由角色 SEND/SKIP 自主判断，不使用固定定时发言。
- 后台自动行为按“自动轮次”计算未读：连续 3 个自动轮次未打开对应 Conversation，则该 Conversation 的后台自动行为休眠；用户打开后清零并恢复。气泡数量不等于自动轮次数。手动群聊/手动请求回复不计入三轮休眠。
- 任意 Conversation 有未读时，moli 悬浮球显示小红点；全部已读后同步消失。当前打开的 Conversation 收到后台消息时直接视为已读。
- 当前打开会话只要仍有 Generation/Automation 在运行，输入栏上方显示“对方正在输入•••”；整个私聊回复或整轮群聊/点评结束、停止或失败后才消失。该文字是纯前端 UI 状态，不进入 Prompt，不消耗模型 token。


# moli51 现行规则：群聊轮换、未读双层红点、跨面板 Generation 状态

- 普通群聊每轮重新独立编排；上一轮发言者仅作为去重复参考，不得形成固定组合。若成员充足且编排连续返回完全相同的多人组合，在不破坏 @ 强制参与的前提下替换一名非强制成员，防止长期锁死。相关度仍高于机械平均轮换。
- Tavern 角色继续遵守“正文中的同名同身份角色就是本人”的本人视角铁律。
- 未读状态同时显示在聊天列表头像右上角与 moli 悬浮球；两处读取同一 Conversation unreadCount。打开对应 Conversation 后清零。
- Generation 运行状态提升为跨 PhonePanel 生命周期的运行时状态。关闭面板不等于停止生成；重新打开同一 Conversation 时，标题位置显示“对方正在输入中…”，发送键恢复“停止”，直到整轮结束/Abort/错误。
- 不再在输入框上方额外占一行显示输入状态；生成时直接用“对方正在输入中…”临时替换聊天标题，模拟手机聊天。


# moli52：通知链、群聊短消息与跨会话连续性
- 未读红点继续以 Conversation.unreadCount 为唯一真值，同时驱动聊天列表头像/数字与悬浮球；悬浮球红点样式增加 WebView 显示加固。
- 群聊普通回复每位入选成员严格 1 个气泡，单条最多约 80 字；自动点评每人 1 个气泡，最多约 100 字。
- 群聊角色生成接入其同 scope 私聊近期上下文；私聊角色生成接入其参与群聊的近期上下文，实现转发与群/私话题连续性。仅共享该角色本人可合理知晓的会话，不把其他角色私聊泄漏给它。
- 群聊成员消息头像可直接跳转该成员私聊。
- 世界书来源对内容完全相同的 linked / embedded 来源去重，保留真正不同的同名世界书。
- 自动 Review 继续由正文 assistant turn 监测驱动；本节点保留后续实机诊断入口。
- 参考边界：章鱼喷墨机/OVO、Acsus-Paws-Puffs 仅学习产品与交互思想；LittleWhiteBox 按其许可证边界；酒馆小狸公开仓库不可确认时不猜测私有实现。


# moli53 节点：群聊模式 / 群记忆 / Review 目标锁定

- 群聊新增 Conversation 级 `groupMode`：`reading`（围读会）与 `role-chat`（角色闲聊），同一个群可随时切换，不把模式永久绑定为群类型。旧群默认迁移为围读会，以保持既有行为。
- 围读会：可读取当前正文；自动 Review 仅在此模式累计/触发。角色闲聊：生成层强制关闭当前正文与柏宝书，不累计正文 Review；仍保留成员自己的必要身份资料/世界书、该成员私聊连续性、当前群聊天和群手机记忆。
- 群聊正式拥有 Conversation Memory（近期记忆 / 长期总结）并复用现有 100 轮自动压缩链；群使用主 API。近期记忆记录 `sourceMode`；群长期总结按 `reading / roleChat` 分槽保存。角色闲聊不会注入围读会近期记忆或围读会长期总结，避免分析口吻污染日常群聊。
- 群资料页新增“手机记忆”“当前聊天设置”；当前聊天设置可切换围读会/角色闲聊、正文时间/现实时间、正文读取和近期聊天上限。角色闲聊时正文读取强制关闭。
- Review Automation 在触发瞬间抓取精确的正文 assistant turn 快照，并作为 `PRIMARY REVIEW TARGET` 传入逐人生成；群聊天、群记忆和其他正文片段仅作为 SUPPORTING CONTEXT，不能再成为误点评对象。
- 聊天列表删除右侧未读数字，只保留头像红点；悬浮球红点继续使用同一 `unreadCount` 真值。
- 时间模式 UI 移除“无时间感”，仅保留“跟随正文时间 / 现实世界时间”；旧 `none` 数据惰性迁移为当前 scope 的默认时间模式。微信式时间标签留到后续独立节点。
- `manifest.json` 版本提升到 `0.3.8`。

## moli53 回归要求

- 不回退 moli52 普通群聊轻编排/逐人生成、每人单气泡、私聊/群聊桥接、世界书去重、主动私聊/正文吐槽 Automation、未读休眠、跨面板 Generation 状态。
- `角色闲聊` 中不得出现最近正文或柏宝书注入；`围读会` 切回后恢复正文能力。
- Review 必须以触发时正文快照为主要点评对象；切到角色闲聊后不得累计/触发 Review。


### Runtime generation error visibility (moli54)
Generation, private automation, and group Review errors must be visible in the affected Conversation, not console-only. The chat error banner persists until the user resumes interaction (typing or clicking). Group Review isolates per-member failures: one member timeout must not cancel successful replies from other members.


## moli55：群聊单次批量生成（2026-09-13）

- 普通群聊由“编排器 1 次 + 入选成员逐人 N 次”改为 **一轮仅 1 次主 API 请求**：同一次请求完成发言者选择与 1～3 个成员的气泡生成，再按 `speakerId` 拆回独立气泡。
- 自动点评同样改为 **一轮仅 1 次主 API 请求**，一次生成当前围读会全部成员的点评，再拆成各自气泡。
- 群聊批量请求统一使用主 API；Contact 独立 API 继续用于私聊。原因是“一轮只调用一次”无法同时调用多个不同 Contact API。
- 批量 Prompt 中每位成员拥有独立 `MEMBER PRIVATE ZONE`：角色卡/人格资料、本成员世界书、本成员自己的轻量手机私聊连续性。其他成员严禁引用该区内容，除非信息已由用户转发或出现在群公共历史。
- 围读会只共享一份当前正文辅助上下文；不再为每个成员重复注入成员个人正文历史或柏宝书。角色闲聊仍完全禁止正文与柏宝书。
- moli55 为累计补丁：包含 moli54 的 Review 错误提示、失败可见性与请求稳定性修复，因此从未安装 moli54 的 moli53/当前完整仓库可直接覆盖 moli55。


## moli56：群聊单成员单气泡硬约束 + 微信式时间标签
- 自动点评：同一轮按 contact id 去重，每个成员最多落地 1 条气泡；四名成员最多四条。普通群聊继续选择 1～3 人，每人最多一条。
- 约束同时存在于 Prompt、批量解析器和 Review 落地层，避免模型重复 speakerId 时产生额外气泡。
- 私聊桥按成员 contact id/成员映射隔离，不写死任何角色名字。
- Conversation 时间显示采用稀疏微信式标签：现实时间按消息 ts，间隔 >=5 分钟或跨日显示；正文时间从最新正文顶部可确认的日期/时刻做消息快照，同一时刻连续气泡只显示一次，无法确认时不编造。

---

## moli57：第四面墙 LittleWhiteBox 恢复第一阶段

第四面墙（`builtin:meta`）开始使用专用 Prompt / 上下文处理路径，而不是只作为普通内置联系人套用通用聊天模板。

本阶段确认：

- 新增 `src/prompts/fourth-wall.js`，集中维护第四面墙 Meta Protocol、Commentary Protocol、皮下历史清洗与时间/间隔格式化。
- 第四面墙仍复用 moli 已有 Conversation、近期记忆/长期总结、正文读取、API、流式、停止与 `<msg>` 解析链，不另建竞争性存储或 Generation 系统。
- 现实时间模式：皮下历史消息使用真实 `ts`；用户消息可携带“距上次第四面墙回复”的时间间隔。
- 正文时间模式：优先使用消息落地时保存的 `storyTime` 快照；没有明确剧情时间时不编造精确时刻。
- 第四面墙历史/其他会话/正文聊天内容进入 Prompt 前，清理历史中遗留的 `<think>` / `<thinking>` / `<system>` / `<meta>` / `<meta_protocol>` / `<instructions>` 包裹内容，避免把旧输出中的隐藏结构重新当成当前指令。
- 恢复默认人格 Prompt 时，第四面墙恢复到新的专用 Meta Protocol。
- 本阶段不引入 LittleWhiteBox 的图片生成、TTS、Fullscreen、图片增强、Voice enhancer、180 秒业务冷却。
- Commentary 的专用协议已集中到第四面墙模块，但事件级自动 commentary 触发/编辑事件监听继续留在后续阶段，不在本阶段伪装成已完成。

---

## moli58：皮下（LittleWhiteBox 四次元壁核心复刻第一阶段）

### 1. 产品身份

`builtin:meta` 对用户显示为 **皮下**，辅助小字为 **入戏…**。它不是普通联系人，不使用普通私聊/群聊的全局线上人格预设；它拥有独立 Fourth Wall Protocol，同时复用 moli 的 API、模型参数、超时与流式底座。

### 2. 头像

皮下头像没有独立持久化真值。每次渲染时读取当前 SillyTavern 正文 char 的头像；切换正文角色后，皮下聊天列表、资料页和 AI 气泡头像随之变化。

### 3. Fourth Wall Context Stack

皮下请求严格区分：

- `chat_history`：当前正文，属于双方共同创作的 RP 剧本；
- `meta_memory`：当前皮下 Conversation 的手机记忆；
- `meta_history`：当前皮下 Conversation 的原始聊天历史；
- Fourth Wall Meta Protocol：规定现实语C搭档身份、时间感、聊天方式和 `<thinking>/<msg>` 输出。

正文不得被解释成皮下人物的现实生活履历。皮下不注入普通线上聊天全局预设，也不读取世界书/柏宝书作为第二套皮下人格来源。

### 4. 外露思考过程

普通皮下回复要求模型输出：

`<thinking>...</thinking>` + `<msg>...</msg>`。

`thinking` 作为皮下消息正式字段持久化。流式时 UI 以“思考中”展开；完成后显示为可折叠“思考过程”。只有皮下使用这套外露内省 UI，普通联系人/群聊不受影响。

### 5. 实时吐槽 Commentary

皮下资料卡复用现有“自动吐槽正文 + 百分比”设置。开启后支持三类事件：

- `ai_message`：正文新增 AI 回复；
- `edit_own`：用户编辑自己的正文台词；
- `edit_ai`：用户编辑 AI 正文台词。

Commentary 使用独立 Fourth Wall Commentary Prompt，只输出一个 `<msg>` 气泡，不附长 thinking。编辑事件扩展仅属于皮下，不改变其他联系人现有自动吐槽语义。

### 6. 明确未完成但已进入复刻路线

除图片/语音外，LittleWhiteBox 四次元壁其余能力继续逐项复刻。下一阶段优先补：多 Session、Fourth Wall Prompt 模板编辑/恢复、Assistant Prefill 开关、正文层数设置、皮下记忆整理/上下文统计、重答/清空时与原版一致的细节行为。

## moli59 / 皮下 Fourth Wall 对齐第二阶段

- `builtin:meta` 继续显示为“皮下（入戏…）”，并保持动态跟随当前正文 char 头像。
- 皮下新增独立 Fourth Wall 设置：
  - 普通聊天层数，默认 20；生成时直接限制读取的当前正文层数。
  - 流式生成开关；关闭后本轮仍正常生成，但不向聊天框推送增量预览。
  - 禁用 Assistant Prefill；开启后 Bottom Prompt 与 msg3 合并进 User 消息，不再作为 Assistant 预填充。
- 皮下新增四段 Prompt 模板编辑：Top User / Confirm / Meta Protocol / Bottom，并支持恢复默认模板。
- 皮下 Session 在 moli 中映射为同一个 `builtin:meta` 联系人的多个独立 Conversation：每个记录天然拥有独立消息、手机记忆、自动行为状态与 Fourth Wall 设置；切换记录不合并历史。此处属于宿主架构适配，行为目标与小白X Session 一致，但不额外复制第二套 session 存储。
- 皮下记录支持新建、切换、重命名和删除；至少保留一条记录。
- 图片与语音继续明确不移植。


## 0.4.4 / moli60 — Fourth Wall 忠实度校正
- `皮下` 的默认 Prompt、请求 role 顺序、history 格式、Commentary 与 response projection 以上游 LittleWhiteBox Fourth Wall 为行为基准重新审计。
- Prompt/Commentary/聊天应用设置改为跨 Session 共享；Session 仅隔离聊天历史与皮下记忆。
- 自动吐槽只进入 active Session，概率范围 1–99%。
- Session 继续使用 Private Conversation 宿主适配，详见 `FOURTH-WALL-AUDIT.md`。
- moli59 旧设置保留迁移回退，避免升级丢失用户已保存内容。


## 0.4.5 / moli61 — Fourth Wall 专用记忆与上下文归档
- 皮下 Session 开始使用 LittleWhiteBox Fourth Wall 自己的滚动记忆模型：`memory + archivedCount + 未归档原文`。
- 未归档 `meta_history` 保留原文直至 token 压力触发，不再套用 moli 普通私聊近期窗口。
- 自动整理阈值 128k，硬上限 158k，单次记忆输出最多 10k；归档边界保护未回答消息和最近完整交互。
- 记忆维护 Prompt 按上游语义维护“皮下人设 / 长期记忆”，显式防止把共同创作剧情误记成现实生活。
- 总结失败/不完整/无压缩收益时不推进归档，保留旧 memory 与聊天。
- 皮下设置页增加上下文分项统计、长期记忆编辑、清空、立即总结。
- 旧 moli 皮下手机记忆惰性迁移一次；此后 Fourth Wall memory 独立维护。
- 皮下正文读取只服从 `maxChatLayers`，移除旧 80 条/64k 字符限制；普通联系人保持原读取窗口。
- Session 仍使用已批准的 `builtin:meta` Private Conversation 映射。
- 用户自行维护的 Top User 前缀视为本地资产，后续补丁不得擅自覆盖。


## 0.4.6 / moli62 — Fourth Wall 消息/任务交互对齐
- 基线采用用户最新 `(39).zip`；仅修复用户手改 `META_PROTOCOL` 后缺失的模板字符串收尾 `` `; ``，不改写 Prompt 正文。
- 皮下消息菜单新增“编辑”。已归档消息编辑前提示：修改原文不会改写长期记忆，需要同步纠正时应编辑皮下记忆。
- 皮下删除已归档消息前同样显示记忆不会自动遗忘的提示；删除行为继续同步 `archivedCount`。
- 新增皮下“重答”按钮：按小白X `prepareRegeneration` 语义找到最后一条用户消息，截断其后的普通回复/Commentary，再以该用户消息重新生成；`archivedCount = min(archivedCount, userIndex)`。
- 生成错误时皮下错误条显示“重试回复”；仅当最后用户消息仍未被普通 AI 回复时允许重试。
- 皮下聊天页头部加入 Context ring + popover：展示总占用 / 主剧情 / 皮下记忆 / 皮下聊天 / Prompt 与输入，并可立即总结。
- 手动总结支持取消：Context popover/设置页按钮在总结中再次点击可 Abort。
- 皮下聊天页新增清空入口：可选择仅清聊天（保留 memory）或聊天与 memory 一起清空。
- 图片/语音仍不移植；Session→Private Conversation 仍沿用已批准宿主适配。


## 0.4.7 / moli63 — 皮下气泡交互与保存失败恢复
- 皮下聊天页移除重复的 Context ring / popover；上下文统计继续保留在“皮下设置”页。
- “重答”与“重试回复”改为上下文长按菜单动作：
  - 最新正常 AI 回复气泡显示“重答”；
  - 生成失败且最后 user 尚未得到正常 AI 回复时，最新 user 气泡显示“重试回复”。
- “编辑”从长按菜单进入后，当前气泡原地切换为 textarea，支持保存 / 取消；不再弹浏览器 prompt。
- user 气泡头像实时跟随 SillyTavern 当前选中 persona 的正文 user 头像；无法取得时回退“我”。
- 皮下生成完成但最终 append/persist 失败时，保留“未保存” draft 气泡，展示 thinking + 回复正文，并提供“重新保存 / 丢弃”。
- 若多段 `<msg>` 中前几段已经成功保存、后续保存失败，未保存 draft 只保留尚未落地部分，避免重复。
- 清空当前聊天记录功能继续保留在皮下；是否推广到其他聊天类型作为后续书签。


## 0.4.8 / moli64 — 全聊天统一消息交互 + 长历史窗口
- user 气泡头像不再猜 DOM；直接复用 SillyTavern 当前 persona runtime `user_avatar`，并使用正文同款 `getThumbnailUrl('persona', user_avatar)`。
- “编辑 / 重答 / 生成失败重试”推广到所有聊天类型：
  - 编辑：长按气泡 → 微信式菜单 → 编辑；原 `.moli-bubble` 本体进入 `contenteditable`，不生成第二个编辑框。
  - 普通私聊：最新 AI 回复可重答；先用临时截断上下文生成成功，再替换旧一轮，避免请求失败时原回复先消失。
  - 群聊：只重答用户长按的那个成员气泡。其他成员回答保持原位、原内容；指定成员旧气泡从生成上下文排除，模型只允许输出该成员的新回答。
  - 失败重试：生成失败且最后 user 尚未获得正常 AI 回复时，长按最后 user 气泡出现“重试回复”。
- 清空当前聊天记录推广到所有聊天：可选择“只清聊天、保留手机记忆”或“聊天和手机记忆一起清空”；皮下继续使用自己的 Fourth Wall memory 清理语义。
- “生成成功但最终保存失败 → 未保存 draft”推广到普通私聊、群聊和皮下的手动生成/重答；支持重新保存/丢弃，部分保存成功时只保留尚未落地部分。
- 长聊天 UI 使用 20/60 窗口：默认只渲染最近 60 条，向前/向后每次加载 20 条；切页保持滚动锚点，搜索旧消息会先把目标装入窗口。
- Context ring 继续不推广到普通聊天；皮下设置页的真实 token 统计保留。


## 0.4.9 / moli65 — 启动链热修
- 修复 moli64 引入的致命模块导入错误：`getThumbnailUrl` 错从 `public/scripts/utils.js` 静态导入，导致 `phone-panel.js` 模块实例化失败，整个扩展初始化在创建悬浮按钮前中断。
- 改为从 SillyTavern 官方 `public/script.js` 导入 `getThumbnailUrl`；`user_avatar` 继续来自 `public/scripts/personas.js`。
- user 头像仍使用 `getThumbnailUrl('persona', user_avatar)`，不回退到猜 DOM。
- 启动链保持 `index.js -> initApp() -> ensureBuiltins() -> createFloatingBall()`，Android 400ms 点击保护不变。


## 0.4.10 / moli66 — UI 小修 + 记忆一致性保护
- user 气泡头像改为与 chat 头像完全相同的“固定尺寸 wrapper + img object-fit: cover”结构，避免 persona 缩略图作为 `<img class=moli-mini-avatar>` 时被强制拉伸。
- 清空聊天只保留“聊天信息”页入口；聊天页右上角重复的清空按钮删除。
- 清空聊天流程明确分两步：先确认清空聊天，再选择“保留现有记忆”或“聊天和记忆全部清除”。皮下使用 Fourth Wall memory，普通私聊/群聊使用各自 Conversation Memory。
- 皮下设置页加强 flex/scroll 约束，底部保存区始终保留，主内容可独立滚动，修复手机窄屏底部截断。
- 聊天信息页底部说明文字删除，但保留隐藏 `data-info-note-slot` 注释槽，后续可重新挂说明。
- 下一阶段同时完成“历史改写 → 记忆一致性保护”：编辑、删除、重答若触及已经进入自动手机记忆的旧消息，会把该 Conversation 标记为 `needsReview`，暂停继续自动压缩，并在手机记忆页提示核对；用户保存手机记忆后解除暂停。

## v0.4.11 / moli67 — 默认围读会与内置人格

- 新正文 scope 首次初始化时自动创建一次默认群聊「围读会」。默认成员为内置人格「小上帝」与「moli」，群模式为 `reading`，正文上下文开启，自动点评开启，默认每 1 个有效正文 AI 回合触发一次。初始化完成后尊重用户对群名、成员、设置或删除群聊的修改，不因缺失而强制重建。
- 内置人格默认集合收敛为「小上帝」与「moli」；「皮下」继续作为独立 Fourth Wall builtin，不属于这两个围读人格。旧安装中已经存在的历史 builtin 数据不主动销毁，避免破坏旧聊天。
- 「小上帝」与「moli」的核心人格 Prompt 属于扩展内部定义。普通资料页不展示/编辑其核心 Prompt；生成链对这两个 builtin 始终使用当前代码内置 Prompt，避免历史缓存覆盖新版人设。
- `{{user}}` 表示实际插件使用者；`moli` 仅是内置闺蜜人格的名字，不再把作者名 Ako 写入公开运行人设。
- 围读会 Review 的产品语义是“新剧情触发的一轮自然群聊反应”，不是“所有成员分别提交点评报告”。Review 仍保持一次 batch request、每成员最多一个气泡、PRIMARY REVIEW TARGET 精确锁定、PRIVATE ZONE 严格隔离。成员可以接彼此的话、争论、接梗、吐槽或补充；不得为了完成点评任务机械复述/总结正文。
- Review 表达倾向：moli 更偏普通读者的情绪、直觉、喜恶与关系判断；小上帝更擅长人物逻辑、信息差、伏笔、关系位移与攻略推演，但这不是固定分工。moli 通常不超过 100 中文字符；小上帝通常不超过 160，真正需要分析时允许稍长，但仍保持单个聊天气泡的自然感。
- 聊天信息页与联系人资料子页必须完整滚动到底，并为移动端 safe-area/面板底部保留空间，不能裁切最后一项。


## v0.4.12 / moli68 — 围读会 UI 收口与废弃人格清理
- 小上帝（`builtin:writer`）与 moli（`builtin:guide`）的聊天信息页不再显示“人格与提示词”入口；完整内置人设继续只由代码内置 Prompt 提供。普通自创联系人/酒馆角色继续保留自己的资料与 Prompt 配置入口。
- 项目尚未开放注册，本节点直接清理废弃旧内置人格 `builtin:redpen` 及其遗留私聊 Conversation；皮下 `builtin:meta` 作为独立 Fourth Wall 联系人继续保留。
- 围读会 Review 的产品 UI 从“自动点评”改名为“自动围读”，频率文案改为“围读频率（每 N 个有效正文 AI 回合）”；底层 review 数据/Automation 命名不重构，PRIMARY REVIEW TARGET 与现有批量生成语义不变。
- 自动围读默认间隔继续为 `1`。
- 保留 moli67 对联系人/群聊资料页与子页的统一底部 safe-area / scroll-padding 修复。
- 本节点不新增普通 moli Global Prompt 层；主设置的全局 API / 全局预设仍属于生成配置层，不与 Prompt 内容层混淆。
