
### Character Identity × Conversation / World Instance 边界（v0.5.14 固化）

- User 一旦添加 Contact，该人物是持久手机资产；退出/切换 SillyTavern 正文不得让联系人或已建立的私聊实例从手机消失。
- 角色卡、人设、角色世界书等身份资料跟 Contact Identity；正文/全局只是 Conversation Scope，不能决定“有没有角色卡”。
- 同一 Contact 可同时存在正文与全局 Conversation。任何会写入聊天、记忆、事件或行动结果的操作，不能只凭名字/contactId 猜目标，必须先解析到明确 Conversation / World Instance。
- 「当前角色世界」是公共 App 的 World Target 选择器。选择时必须区分“正文/全局”；进入社区后 @/邀请/转发 UI 可只显示自然角色名，但底层必须继承已选 World Target，不能再次混入另一 scope 的同名角色。
- Current Tavern Page 是环境输入，不是 Contact/Conversation 的存在性过滤器。

# moli70 / v0.4.14

- 自建 custom 角色「角色资料条目」升级为轻量角色世界书：每条可选「常驻」或「关键词触发」。
- 关键词支持逗号、中文逗号、分号、竖线或换行分隔；任一关键词命中本轮可见上下文即激活。关键词模式未填写关键词时不注入，避免误把条件条目当常驻资料。
- 私聊会扫描近期手机消息、可读正文与手机记忆；群聊使用当前群历史、群记忆、围读会正文/Review target 作为触发扫描文本。角色闲聊仍不会因为该机制额外读取正文。
- 只有本轮激活的条目进入模型请求；旧条目自动兼容为「常驻」，不破坏 moli69 已有数据。
- 第一版刻意只实现 OR 关键词触发；AND/NOT、概率、位置/深度等高级世界书条件暂不加入，避免复制一套过重的 SillyTavern 世界书系统。


## v0.4.13 / moli69
- 默认头像本地化：皮下、小上帝、moli 三张头像随扩展打包；moli 使用确认后的左右翻转版本，不依赖外部图床。
- 聊天列表支持长按置顶/取消置顶；非系统默认会话支持彻底删除。删除会同时移除聊天、手机记忆与 Conversation 状态，并要求确认按钮连续点击两次。
- 皮下、小上帝、moli 三个默认私聊以及默认「围读会」不可删除。
- 默认「围读会」固定 reading + 跟随正文时间，设置页不再重复提供模式/时间选择；自建群仍保留模式选择。
- 自动围读旧文案清理，删除“群聊不提供自动吐槽”。
- 群聊手机记忆继续使用现有 Conversation memory，并在手机记忆页按 reading / role-chat 显示和编辑近期记忆与长期总结。
- 自建 custom 角色新增多条「角色资料条目」：可新增、编辑、删除、启用/停用；启用条目作为角色专属资料注入私聊和群聊生成。

# moli小手机 SPEC

> GitHub 仓库技术名：`fourth-wall-phone`  
> 正式产品名称：`moli小手机`  
> 产品形态：SillyTavern 前端 Extension  
> 当前仓库源码是代码执行基线，本文件是产品设计基线。  
> “第四面墙”只是系统内置联系人之一，不是整个产品名称。  
> 早期 Fourth Wall Lite / 酒馆助手脚本只作为历史逻辑参考，不作为当前实现基线。  
> 原则：本文件只记录当前确认后的最终规则。旧方案若被推翻，直接以本文件最新内容为准。

---

## 1. 项目定位

`moli小手机` 是一个面向 SillyTavern 的前端扩展，是独立于正文主剧情之外的场外通讯系统。

它不是正文聊天本身，而是一个独立于主剧情之外的手机式通讯层，用来让：

- 第四面墙人格
- 编剧
- 攻略
- 红笔编辑
- 酒馆角色
- 用户自定义联系人
- 多人群聊

在“正文之外”与用户交流、吐槽、点评、讨论剧情。

核心体验目标：

1. 像微信一样管理私聊和群聊。
2. 场外内容默认不进入正文。
4. 酒馆角色必须尽量保持正文中的人设、关系状态、语言风格与长期记忆。
5. 场外系统使用独立 API，不干扰正文主 API。
6. 多存档严格隔离，避免不同剧情档互相串线。

---

## 2. 当前实现基线

当前正式实现是 SillyTavern 前端 Extension。

当前仓库已经具备部分基础能力，包括但不限于：

- 可拖动悬浮球
- 悬浮球位置持久化
- 可拖动手机面板
- 面板位置持久化
- 点击面板外关闭
- 点击悬浮球打开 / 关闭
- 微信式聊天首页基础结构
- 内置联系人
- 自定义联系人
- 酒馆角色来源资料
- 群聊基础结构
- 群成员增加 / 删除
- 群头像拼接
- 部分聊天信息页
- 当前档会话隔离基础

每次开发仍必须以本次最新仓库源码为准。

早期 `Fourth Wall Lite` / 酒馆助手脚本只允许作为历史逻辑参考，禁止把旧脚本重新当成当前代码基线。

已验收的悬浮球、手机面板拖动以及移动端触摸防穿透逻辑，除非专门修复相关 Bug，否则不得在后续重构中随意替换。

---

## 3. 首页与入口

### 3.1 悬浮球

点击悬浮球后，进入微信式“聊天列表首页”。

### 3.2 首页顶部

顶部不放搜索按钮。

右上角只保留两个入口：

#### `＋`

弹出：

- 同步酒馆角色
- 添加联系人
- 发起群聊

#### `设置`

进入全局设置。

当前至少包含：

- 场外 API
- 自动吐槽全局总开关
- 自动点评全局总开关
- Markdown 渲染全局开关
- 提示词与预设

后续全局功能继续从这里扩展。

---

## 4. 内置联系人

以下联系人首次启用插件时自动存在：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

它们：

- 自动进入全局通讯录
- 自动为当前酒馆存档创建私聊会话
- 自动出现在当前存档的聊天首页

内置联系人不需要用户手动添加。

### 4.1 内置联系人删除规则

内置联系人不做真正物理删除。

可以：

- 恢复默认提示词

但不能像普通联系人一样彻底删除系统定义。

---

### 4.1 第一版删除与状态规则

内置联系人不可物理删除。

第一版暂不实现“隐藏 / 停用”，因为这两个行为的业务语义尚未正式设计。

内置人格可以恢复默认人格提示词。

---

## 5. 自定义联系人

### 5.1 创建方式

`＋ → 添加联系人 → 新建空白人物`

不设置联系人类型。

原因：

- 人格提示词已经定义“这个人是谁”
- 不需要额外再用“NPC / 陪聊 / 作者 / 其他”等标签限制行为

### 5.2 新建字段

新建空白联系人时只需要：

- 名称
- 头像
- 简介 / 一句话描述
- 人格提示词

不提供：

- 联系人类型
- 是否允许加入群聊
- 是否读取正文

### 5.3 上下文读取

所有联系人默认自动读取当前存档上下文。

不同联系人根据自身数据结构自动获得不同上下文包，不要求用户手动配置“能不能看正文”。

创建成功后：

- 加入全局通讯录
- 自动为当前存档创建私聊
- 自动进入当前聊天首页

---

## 6. 同步酒馆角色

### 6.1 添加流程

`＋ → 同步酒馆角色`

弹出当前酒馆角色选择列表。

支持：

- 多选
- 已添加角色标记
- 点击“添加”后批量导入

添加完成后：

- 进入全局通讯录
- 自动为当前存档创建私聊
- 直接出现在当前聊天首页

### 6.2 不采用自动全量同步删除

不能因为酒馆角色卡被删除，就自动删除手机联系人。

源角色不存在时：

- 联系人继续保留
- 所有场外聊天历史继续保留
- 显示“来源失效 / 来源角色不可用”
- 后续可重新绑定角色

### 6.3 自动跟随刷新

酒馆联系人会自动跟随刷新其“来源数据”，包括：

- 酒馆角色原名
- 原角色头像
- 角色设定
- 性格
- 场景
- Example Dialogue
- 其他角色卡相关数据

但是自动同步只能更新来源层。

不能覆盖用户手动层。

---

## 7. 联系人资料模型

酒馆角色联系人同时保存：

- 联系人 ID
- 酒馆来源角色 ID / 可稳定引用信息
- 酒馆角色原名
- 酒馆原头像
- 用户备注名
- 用户自定义头像（可选）
- 用户简介 / 一句话描述
- 人格提示词相关配置
- 来源状态
- 最近同步状态

### 7.1 名称显示规则

优先级：

`用户备注名 > 联系人显示名 > 酒馆角色原名`

酒馆角色原名永远保留，不会因为用户改备注而丢失。

### 7.2 头像显示规则

优先级：

`用户自定义头像 > 酒馆来源头像`

如果用户手动换过头像：

- 酒馆角色卡以后换头像，不覆盖用户自定义头像

用户可选择：

- 恢复跟随角色卡头像

### 7.3 联系人详情页

入口：

`私聊右上角 … → 聊天信息 / 联系人资料`

建议结构：

- 头像
- 当前显示名
- 原角色名（酒馆角色）
- 简介
- 备注名
- 头像
- 简介
- 人格与提示词
- 酒馆角色信息
- 来源状态
- 重新绑定角色
- 恢复跟随头像 / 名称
- 删除联系人（非内置）

---

## 8. 联系人与会话分离

这是核心数据规则。

### 8.1 联系人是全局的

以下内容跨酒馆存档共享：

- 联系人本体
- 内置人格
- 自定义联系人
- 酒馆联系人来源信息
- 联系人备注
- 用户自定义头像
- 联系人人格 Prompt
- 提示词配置

### 8.2 会话按酒馆存档隔离

以下内容必须按当前酒馆聊天 / 存档隔离：

- 私聊历史
- 群聊
- 群聊历史
- 自动吐槽状态
- 自动点评状态
- 自动点评正文回合计数
- 未读消息
- 当前档会话列表
- 与当前档相关的角色动态状态
- 当前档场外上下文

### 8.3 多存档规则

同一个酒馆角色可以在多个存档中存在不同场外聊天。

例如：

- 祈年 / A 档 → A 档私聊
- 祈年 / B 档 → B 档私聊

两者绝不共享聊天记录。

### 8.4 切换酒馆存档

切换酒馆存档后：

- 手机聊天首页切换到该存档自己的会话空间
- 通讯录仍然全局存在

如果新档从未和某联系人聊过：

- 他仍在通讯录
- 不自动出现在新档聊天首页
- 用户从通讯录点进去后，才建立该档的新私聊

### 8.5 内置人格例外

内置人格：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

在每个新存档第一次进入时，都自动创建当前档私聊并显示在首页。

---
\n## 8A. Scope 生命周期与异步安全\n\n联系人 / 人格配置属于全局数据。私聊会话、群聊、消息历史、未读、置顶、自动吐槽状态、自动点评状态与计数、当前档上下文及当前档相关异步生成任务属于当前 SillyTavern 聊天存档 Scope，不同存档不得共享。\n\n### 8A.1 稳定 Scope\n\n只有取得能够稳定区分当前 SillyTavern 存档的信息后，当前 Scope 才可以正式持久化。稳定 Scope 由统一的 `tavern-scope` 模块生成，优先使用当前角色 / 群组稳定标识与当前 `chatId` / 聊天文件稳定标识。其他业务模块不得自行拼接 Scope Key。\n\n### 8A.2 Fallback Scope\n\n初始化期间暂时无法取得稳定 `chatId` 时，可以产生临时 fallback scope，用于暂时显示 UI、构造临时内存对象、等待上下文准备和防止初始化报错。Fallback Scope 不得持久化正式消息、正式群聊、自动行为进度，不得作为永久存档 ID，也不得创建会被用户视为正式数据的持久化会话。\n\n**fallback 可以有临时内存状态，但不能产生之后看起来“突然消失”的正式用户数据。** 一旦取得稳定 Scope，应切换到稳定 Scope。\n\n### 8A.3 Scope Change Contract\n\n当 SillyTavern 当前聊天 / 存档切换时：\n\n1. 获取新的稳定 Scope Key；\n2. 页面回到聊天列表首页；\n3. 当前联系人 / 群聊选择清空；\n4. 首页重新加载新 Scope 会话；\n5. 未读、置顶、自动行为计数等全部切换；\n6. 旧 Scope AI 请求应尽可能 Abort，但不得依赖 Abort 一定成功；\n7. 每个异步任务启动时保存 `requestScopeKey`；\n8. 异步结果写入前再次核对原始 Scope；\n9. 旧请求绝不能写入新 Scope；\n10. 无法中止或晚到的旧请求最多只能写回原 Scope。\n\n### 8A.4 UI 临时状态隔离\n\n`currentContactId`、`currentConversationId`、当前群聊选择、当前引用消息、当前多选集合、当前会话临时操作状态以及未来可能存在的草稿，不得直接跨 Scope 复用。切换 Scope 时要么清理，要么明确按 Scope 独立保存。\n\n### 8A.5 打开手机的页面规则\n\n每次重新打开 `moli小手机` 默认进入当前 Scope 聊天列表首页，不恢复上一次联系人 / 群聊子页面。已验收的移动端触摸防穿透必须保留：打开悬浮球的同一次 pointer / touch / click 事件不得穿透到刚出现的聊天列表项目。\n\n---\n\n## 8B. 公共数据契约\n\nContact、Conversation、Message、Scope 属于跨模块公共契约，功能节点不得自行建立平行结构。\n\n### 8B.1 ID 类型严格区分\n\n- `contactId`：联系人 / 人格\n- `conversationId`：私聊或群聊会话\n- `scopeKey`：SillyTavern 聊天存档数据空间\n- `messageId`：场外消息\n\n禁止混用。\n\n### 8B.2 Conversation ID\n\n跨模块传递聊天会话统一使用 `conversationId`。私聊使用 `private:<contactId>` 命名空间，例如 `builtin:meta` 对应 `private:builtin:meta`；群聊使用独立命名空间，例如 `group:<stable-id>`。\n\n### 8B.3 Conversation Schema\n\n公共 Schema 规定字段含义和能力，不强制不同会话保存无意义的相同设置字段。示意：\n\n```js\n{\n  id: "private:..." | "group:...",\n  type: "private" | "group",\n  contactId: null,\n  memberIds: [],\n  title: "",\n  pinned: false,\n  unreadCount: 0,\n  settings: {},\n  messages: []\n}\n```\n\n私聊 `settings` 可包含自动吐槽；群聊可包含自动吐槽与自动点评。具体默认值以对应业务章节为准。\n\n### 8B.4 Message Schema\n\n正式消息至少需要能够表达：\n\n```js\n{\n  id: "...",\n  senderType: "user" | "contact" | "system",\n  senderId: null,\n  content: "",\n  ts: 0,\n  source: "manual" | "generation" | "commentary" | "review" | "forward" | "system",\n  senderSnapshot: { name: "", avatarRef: null },\n  quote: null,\n  forward: null\n}\n```\n\n`quote` / `forward` 子结构由对应消息节点设计，新增时不得破坏旧消息兼容。\n\n### 8B.5 senderSnapshot\n\n联系人产生消息时保存足以还原发送当时身份的快照，至少能还原当时显示名和头像的可恢复引用 / 信息。不要求每条消息复制完整 base64；可使用 `avatarRef`、`avatarUrl`、`avatarKey` 或其他可恢复引用。联系人删除、踢出群聊、改名、换头像或来源失效后，旧消息仍须知道当时是谁发送的。\n\n历史消息的 `senderSnapshot` 不被联系人后续名称、备注或头像修改反向改写。\n\n### 8B.6 连续发送与空输入触发回复\n\n私聊和群聊统一采用：**连续发送，空输入触发回复**。\n\n- 输入框非空时点击“发送”：只追加一条独立 user 消息，不调用 AI；\n- 用户可以连续发送多条 user 消息；\n- 输入框为空时点击“发送”：才触发一次 AI 生成；\n- 生成时应一次读取自上一条 AI 回复之后累计的全部 user 消息；\n- 生成失败或被停止时，已经发送的 user 消息必须保留，用户可以再次空输入重试；\n- 不提供“重新生成”按钮。\n\n生成过程中发送按钮切换为“停止”；停止只取消当前请求，不删除触发它的用户消息。\n\n---\n\n## 8C. 存储版本与迁移契约\n\n### 8C.1 Data Store 是业务持久化统一入口\n\n除 Storage / Data Store 自身外，UI、API / Generation、Automation、Group Orchestrator、Prompt、Role Fidelity、Integrations 不得直接依赖具体 localStorage Key 或底层布局。业务持久化统一通过 Data Store / Storage 接口，以便未来从 localStorage 迁移到 IndexedDB 等实现而无需整体重写上层。\n\n### 8C.2 schemaVersion\n\n主要持久化数据结构必须保存明确 `schemaVersion`；联系人全局数据也必须有明确版本规则。禁止只靠 Storage Key 名称猜版本。\n\n### 8C.3 Migration Chain\n\n数据升级使用明确迁移链，例如 `v1 → v2 → v3`，禁止假设旧数据天然符合最新 Schema。\n\n### 8C.4 数据兼容铁律\n\n修改 scopeKey、contactId、conversationId、Storage Key、Contact / Conversation / Message / Group Schema 时，必须检查真实旧数据并提供兼容读取或一次性迁移。禁止内部重构导致已有联系人、私聊、群聊、消息或设置看起来突然消失。\n\n### 8C.5 保留未知字段\n\nStorage 层读取再保存对象时必须保留未知字段，例如：\n\n```js\n{ ...storedData, conversations: storedData.conversations || {} }\n```\n\n而不是只重建当前认识的字段，以免未来的 `schemaVersion`、`scopeMeta`、`eventLedger` 等被旧读写逻辑静默删除。\n\n---\n
## 9. 聊天首页

首页采用微信式聊天列表。

每个聊天项显示：

- 头像 / 群头像
- 联系人名 / 群名
- 最后一条消息预览
- 最后消息时间
- 未读数字 / 红点
- 置顶状态

排序：

1. 置顶聊天优先
2. 其余按最新消息时间降序

### 9.1 未读

后台产生的：

- 自动吐槽
- 自动点评
- 其他自动场外消息

如果当前会话没有打开，则计入未读。

当前正在打开的会话：

- 不增加未读

打开聊天后：

- 清空该会话未读

---

## 10. 私聊

### 10.1 基本布局

采用微信式私聊布局。

顶部：

- 返回
- 联系人备注名 / 显示名
- 右上角 `…`

不额外在顶部放联系人头像。

底部输入区：

- `＋`
- 输入框
- 发送

`＋` 第一版只保留扩展入口，具体小功能待后续确定。

生成过程中：

- 发送按钮可切换为停止按钮
- 用户可随时取消当前场外生成请求

### 10.2 消息显示

- 用户与人格消息均采用聊天气泡
- AI 回复支持 Markdown
- 是否渲染 Markdown 由全局设置控制
- 底层始终保存原始 Markdown 文本
- 切换开关后旧消息也能重新按当前显示模式渲染
- 酒馆角色默认 Prompt 应约束少用 Markdown，使其更像即时通讯
- 编剧 / 攻略等工具型人格可自然使用 Markdown

### 10.3 引用

支持回复 / 引用消息。

输入时：

- 输入框上方显示当前引用对象与内容摘要

发送后：

- 消息气泡中显示被引用内容的小块

### 10.4 自动点评

私聊不提供自动点评。

### 10.5 自动吐槽

私聊设置：

- 自动吐槽：开 / 关
- 触发概率：0–100%

## 11. 群聊

### 11.1 创建

`＋ → 发起群聊`

用户选择联系人创建。

群名：

- 用户自定义

群头像：

- 模拟微信群头像
- 将所有当前群成员头像拼接成九宫格 / 多人格头像组合

### 11.2 群成员

支持：

- 后续加人
- 踢人

群成员排序：

- 不作为 AI 发言权重
- 无业务意义

### 11.3 群聊消息 UI

采用真实微信式群聊布局：

- 每条人格消息显示头像
- 每条人格消息显示昵称
- 显示消息气泡
- 连续同一人发言也不合并头像和昵称

### 11.4 删除群成员

成员离开 / 被踢出后：

- 过去的群聊历史仍保留其旧消息
- 不清理历史记录

---

## 12. 群聊发言编排器

群聊分成两种模式：

1. 普通群聊
2. 自动点评

### 12.1 普通群聊

用户主动发送群聊消息时：

- 不强制所有人回复
- 不设置固定 1～3 人上限
- 最大自然上限就是群成员数量

AI 根据：

- 当前话题相关度
- 角色立场
- 人格关系
- 是否被 @
- 是否有明显插话价值

判断谁说话。

### 12.2 第一位发言者

普通群聊第一位：

- 按相关度决定
- 不纯随机

### 12.3 @

用户 @ 某人格：

- 被 @ 人格必须参与

但：

- 未被 @ 的其他成员仍可自然插话

### 12.4 后续成员必须接前文

同一轮内：

第二位看到：

- 用户消息
- 第一位的新回复

第三位看到：

- 用户消息
- 第一位
- 第二位

以此类推。

允许：

- 赞同
- 反驳
- 补充
- 调侃
- 转移话题
- 自然接话

禁止所有人彼此不可见地独立答题。

---

## 12A. 聊天信息 / 当前会话设置页

### 12A.1 私聊聊天信息

从私聊右上角 `…` 进入。

顶部显示：

- 当前联系人头像
- 联系人名
- `＋` 加人入口

`＋` 的作用：

- 从通讯录选择其他联系人
- 创建一个包含当前联系人在内的新群聊

酒馆角色 / 自定义联系人私聊设置包含：

- 自动聊天 / 主动私聊开 / 关
- 自动聊天百分比 0–100%
- 自动吐槽正文开 / 关
- 自动吐槽百分比 0–100%
- 查找聊天记录
- 置顶聊天
- 联系人资料
- 人格与提示词
- 清空聊天记录
- 删除联系人

不加入“消息免打扰”；需要时后续再增加。

### 12A.2 群聊聊天信息

顶部：

- 当前群成员头像
- `＋` 增加成员
- `－` 踢出成员
- 群聊名称

设置包含：

- 自动点评开 / 关
- 点评间隔：每 N 个正文回合
- 查找聊天记录
- 置顶聊天
- 群成员管理
- 清空聊天记录
- 删除并退出群聊

### 12A.3 自动点评暂停规则

关闭自动点评：

- 暂停计数
- 不清零当前进度
- 关闭期间产生的正文不计入该点评周期

重新开启后：

- 从关闭前的进度继续累计

例如每 5 回合点评一次，关闭时为 `3/5`：

- 关闭期间不计数
- 重新开启后仍从 `3/5` 开始
- 再经过 2 个有效 AI 正文回合触发点评

### 12A.4 清空聊天记录

清空当前聊天：

只删除：

- 当前会话场外聊天历史
- 当前未读计数

保留：

- 联系人
- 人格 Prompt
- 自动吐槽设置
- 自动点评设置
- 其他会话配置

### 12A.5 删除规则

私聊：

- “删除联系人”属于全局联系人删除，按联系人删除规则执行

群聊：

- “删除并退出群聊”只删除当前存档中的该群聊及其群聊历史
- 不删除任何群成员联系人

### 12A.6 删除普通联系人对所有存档的影响

删除普通联系人属于全局删除动作：删除全局 Contact；删除其在所有 Scope 下的私聊；从所有 Scope 现存群聊的当前 `memberIds` 中移除；但不删除过去已经产生的群消息。历史群消息依靠 `senderSnapshot` 保留当时姓名、头像身份、文本和时间。

删除后若群剩余成员不足以继续作为正常群聊，如何处理由群聊模块另行确认，Storage 层不得自行决定。内置联系人第一版不可删除，因此不适用此规则。

---

## 13. 群聊 API 生成策略

采用：

**轻编排 + 逐人生成**

### 13.1 编排请求

先发一次轻量请求。

只负责决定：

- 本轮谁说
- 发言顺序

编排阶段不加载每个人完整角色卡，只给足够判断相关度的短描述。

例如：

- 名称
- 简短身份
- 简短性格
- 与当前话题关系
- 是否被 @

### 13.2 发言生成

真正轮到某人格说话时：

单独发一次生成请求。

该请求只加载该人格自己的完整上下文。

例如：

`Speaker 1 → 完整人格包 → 生成`

然后：

`Speaker 2 → 自己的完整人格包 + Speaker 1 新消息 → 生成`

依次继续。

### 13.3 目的

该方案优先保证：

- 人格隔离
- 角色声线稳定
- 不串人设
- 后发言者真正读取前面新消息
- 酒馆角色尽量像正文里的本人

不采用“一次请求同时扮演所有群成员”作为默认模式。

---

## 14. 自动吐槽（Commentary）

### 14.1 定位

自动吐槽是概率型主动行为。

特点：

- 短
- 即时
- 情绪化
- 像看到正文后的第一反应

不是正式剧情分析。

### 14.2 全局开关

系统设置提供：

- 自动吐槽总开关

关闭总开关：

- 只暂停
- 不删除每个会话自己的配置

重新开启：

- 恢复原配置

### 14.3 每会话配置

酒馆角色 / 自定义联系人的私聊分别独立保存：

- 自动吐槽开关
- 触发概率 0–100%

### 14.4 不使用业务冷却

不提供用户可设置的冷却时间。

100% 应意味着：

- 每个符合条件的新事件都允许触发

### 14.5 技术去重

底层保留短暂同事件去重锁：

- 只防止同一个酒馆事件被重复广播
- 不属于“冷却”
- 不暴露设置

### 14.6 触发事件

至少包括：

- AI 正文生成完成
- 用户编辑自己的正文消息
- 用户编辑 AI 正文消息

每次正文事件后：

- 每个开启自动吐槽的会话独立进行概率判定

同一正文事件可以同时触发多个符合条件的私聊。群聊不参与自动吐槽。

### 14.7 群聊自动吐槽（已废弃）

群聊不提供自动吐槽；群聊自动行为只保留自动点评。

---

## 15. 自动点评（Review）

### 15.1 定位

自动点评是周期型群聊行为。

特点：

- 每 N 个正文回合触发
- 全员参与
- 更完整、更分析型
- 与自动吐槽完全独立

### 15.2 只存在于群聊

私聊没有自动点评。

### 15.3 群聊设置

每个群聊保存：

- 自动点评开 / 关
- 每 N 个正文回合触发一次

### 15.4 全员必须参与

自动点评触发后：

- 当前群内每一个人格至少发言一次

### 15.5 发言顺序

第一位：

- 随机

后续：

- 必须看见前面的本轮新消息
- 根据已有讨论继续接话

允许：

- 赞同
- 反驳
- 补充
- 改变讨论重点
- 二次发言

### 15.6 二次发言

每人至少一次，但不限制恰好一次。

如果讨论自然需要：

- 已经发言过的人可以再次回应

### 15.7 单轮安全上限

隐藏规则：

`单轮最大场外消息数 = 当前群成员数 × 2`

不作为用户设置项。

### 15.8 正文回合定义

一个正文回合 = 一次有效 AI 正文回复。

用户消息本身：

- 不单独计一个自动点评回合

### 15.9 删除与重 Roll

自动点评计数根据当前有效正文状态判断。

规则：

- 删除有效 AI 正文 → 回合回退
- 删除多条 → 按实际状态回退
- 重 Roll 同一条 AI 正文 → 不额外 +1
- 如果重 Roll 的位置正好位于 N 边界，可针对新版本正文重新触发对应点评

---

## 16. 不提供“重新生成”按钮

场外聊天 UI 不提供：

- AI 消息重新生成
- 群聊整轮重新生成
- 自动点评重新生成按钮

这是明确取消的功能。

---

## 17. 消息长按菜单

长按消息支持：

- 引用
- 复制
- 转发
- 多选
- 删除

明确不提供：

- 采纳
- 重新生成

### 17.1 转发

转发时：

- 弹出聊天列表
- 可转发到其他私聊 / 群聊
- 单条转发按普通文字消息进入目标会话，不显示额外来源卡片
- 多条消息使用“聊天记录”合并转发卡片，聊天页只做紧凑预览，点击后进入独立聊天记录详情页
- 多条消息可采用合并转发形式

### 17.2 多选

多选模式至少支持：

- 批量删除
- 批量转发

## 18. 删除与清空规则

### 18.1 删除联系人

删除普通联系人时：

同时删除：

- 联系人资料
- 与该联系人相关的所有存档私聊数据
- 与其专属联系人数据

必须二次确认。

警告文案应明确：

“将同时删除该联系人在所有存档中的场外聊天记录，无法恢复。”

### 18.2 群聊历史中的旧成员消息

删除联系人 / 踢出群成员：

- 不自动删除其过去群聊消息

### 18.3 清空聊天记录

清空某个聊天记录时：

只删除：

- 当前会话历史

保留：

- 联系人
- 人格 Prompt
- 自动吐槽设置
- 自动点评设置
- 其他会话配置

### 18.4 恢复默认人格

“恢复默认人格 / 恢复默认提示词”只重置人格配置。

不会删除：

- 已有场外聊天历史
- 会话
- 群聊历史

例如：

第四面墙可恢复 LittleWhiteBox 默认 Meta Protocol。

自定义空白联系人没有“系统默认模板”，因此不提供这一恢复项。

---

## 18A. 通讯录页面

通讯录采用独立分组。

### 18A.1 顶部

包含：

- 搜索框
- 当前存档群聊入口

不在通讯录重复提供“新建联系人”按钮。

创建 / 同步入口统一从聊天首页右上角 `＋` 进入。

### 18A.2 内置人格分组

固定置顶显示：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

不与普通联系人混入字母排序。

### 18A.3 普通联系人

酒馆角色与自定义联系人混合显示，不额外按来源分组。

按当前显示名称进行字母排序。

排序名称优先级：

`备注名 > 联系人显示名 > 酒馆角色原名`

### 18A.4 点击联系人

点击联系人：

- 当前存档已有私聊 → 直接打开
- 当前存档没有私聊 → 创建该存档的新私聊并打开

### 18A.5 群聊入口

通讯录顶部“群聊”显示：

- 当前酒馆存档已有的群聊

切换存档后：

- 群聊列表随存档切换
- 全局联系人列表不变

## 19. 提示词与预设

系统设置提供：

`提示词与预设`

入口分为：

- 全局生成框架
- 联系人配置

不做“预览本次上下文”。

### 19.1 联系人配置列表

按组展示：

#### 内置人格

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

#### 酒馆角色

- 当前已同步的角色

#### 自定义联系人

- 用户新建人物

从：

`设置 → 提示词与预设 → 联系人配置 → 某联系人`

与：

`联系人资料 → 人格与提示词`

打开的是同一份数据。

不能复制两份配置。

### 19.2 第四面墙配置

至少包括：

- Meta Protocol
- 普通聊天 Prompt
- 自动吐槽 Prompt
- 时间感规则
- 正文上下文开关
- 柏宝书长期记忆开关
- 场外历史开关
- 恢复 LittleWhiteBox 默认

### 19.3 编剧 / 攻略 / 红笔编辑

至少包括：

- 人格 Prompt
- 输出风格 Prompt
- 自动点评 Prompt
- 正文上下文开关
- 柏宝书长期记忆开关
- 场外历史开关
- 恢复默认

### 19.4 酒馆角色配置

来源均提供独立开关，默认开启：

- 角色卡
- 性格
- 场景
- Example Dialogue
- 世界书
- 柏宝书长期记忆
- 最近正文
- 场外聊天历史
- 自定义附加 Prompt

不单独增加“最近角色语言样本”。

角色语言风格直接从：

- Example Dialogue
- 最近正文中该角色的实际台词

共同获得，避免重复发送同一批正文浪费 token。

### 19.5 世界书控制

- 世界书默认全量开启
- 点击进入后显示世界书条目列表
- 用户可逐条关闭不希望该联系人读取的条目
- 条目开关状态按联系人独立保存

### 19.6 柏宝书控制

- 柏宝书长期记忆默认开启
- 提供明确开 / 关按钮
- 只读取长期记忆
- 不读取当前状态快照、变量、物品、NPC、计划等其他柏宝书数据
- 未检测到柏宝书时自动退回最近正文机制

### 19.7 自定义联系人配置

至少包括：

- 人格 Prompt
- 简介
- 柏宝书长期记忆
- 最近正文
- 场外聊天历史
- 自定义附加 Prompt

## 20. 全局生成框架

默认逻辑块：

1. 场外系统协议
2. 当前任务类型
3. 用户 Persona
4. 当前发言人格
5. 角色保真包（酒馆角色专用）
6. 当前世界 / 相关世界书
7. 当前存档正文上下文
8. 时间与消息间隔
9. 当前场外会话历史
10. 本轮用户消息 / 正文触发事件
11. 群聊本轮已产生的新消息
12. 输出规则

这些逻辑块：

- 可查看
- 可开关
- 可拖动排序
- 可点击编辑
- 可恢复默认

核心系统项可以关闭，但不允许轻易物理删除。

核心系统项不能轻易物理删除。

### 20.1 同一联系人跨会话上下文

同一个 `contactId` 在当前 Scope 内可以同时出现在私聊和一个或多个群聊中。它们仍然是彼此独立的 Conversation，不合并聊天历史，也不把其他会话消息伪装成当前会话消息。

Generation / Prompt 层允许按同一 `contactId` 读取其其他会话上下文，并必须带明确的会话来源标签。例如：群聊中的蒋郁文可以知道“这是你与用户私聊中发生过的内容”，但不能误认为这些话发生在群聊中，也不能默认其他群成员知道私聊内容。

跨会话互通规则：

- 身份匹配只认稳定 `contactId`，禁止按显示名猜测；
- 当前会话上下文优先；
- 其他会话上下文作为独立来源块提供；
- 读取互通，不自动把消息复制 / 写入另一会话历史；
- 转发记录若保存了 `senderId`，可用于识别“转发内容中的这个人就是当前群成员中的同一联系人”；
- 不同 SillyTavern Scope 之间仍然严格隔离，不跨存档互通。

---

## 21. 酒馆角色保真（Role Fidelity Pack）

目标：

**手机里的酒馆角色，要像正文里那个正在活着的角色。**

不是只读取静态角色卡。

### 21.1 核心来源

酒馆角色生成时优先组合：

1. 角色身份与硬设定
2. 当前存档剧情事实 / 关系状态
3. 当前角色动态状态
4. Example Dialogue / 角色语言示例
5. 最近正文（同时承担近期剧情与近期角色语言参考）
6. 当前场外聊天历史
7. 本轮消息

### 21.2 角色卡与当前剧情关系

角色卡负责：

- 这个人是谁

当前正文 / 记忆负责：

- 这个人现在变成了什么样
- 当前关系走到哪一步
- 当前有哪些承诺、冲突、秘密、状态

### 21.3 语言声纹

不额外复制一份“最近角色语言样本”。

系统在读取最近正文时，同时将其中该角色真实说过的话作为语言风格参考，以减少重复 token。

重点参考：

- 用词习惯
- 句子长短
- 称呼
- 标点
- 是否常反问
- 情绪表达强弱
- 是否主动解释
- 是否回避直白表达
- 幽默方式
- 线上说话相对现实是否更松弛

要求：

**学习正文中的角色声纹，但不要照抄正文的第三人称小说叙述格式。**

### 21.4 Example Dialogue

角色卡的 Example Dialogue 作为高价值角色语言来源。

角色设定说明“他是什么样的人”。

Example Dialogue 更直接说明“他具体怎么说话”。

---

## 22. 柏宝书 Memory Provider

支持可选接入：

`ST-BaiBai-Book`

仓库：

https://github.com/baibai-git/ST-BaiBai-Book

### 22.1 定位

柏宝书在本项目中只负责：

**补充最近正文窗口之外的长期剧情记忆。**

不能作为硬依赖。

### 22.2 检测与降级

如果检测到柏宝书公开 API：

- 读取长期记忆

如果不存在或读取失败：

- 插件继续正常运行
- 使用自己的最近正文机制
- 不影响聊天

### 22.3 读取范围

只读取柏宝书长期记忆。

不读取：

- 当前状态快照
- 变量
- 物品
- NPC
- 计划
- 其他状态型数据

### 22.4 推荐组合

上下文优先采用：

`柏宝书长期记忆 → 最近正文原文 → 当前场外聊天`

柏宝书负责“过去发生过什么”。

最近正文负责“现在正在发生什么”。

尽量避免同一段近期剧情以“摘要 + 原文”重复发送。

### 22.5 覆盖完整性

如果柏宝书公开接口返回记忆覆盖不完整的信息，不能假设其记忆毫无缺口。

最近正文始终作为近期上下文兜底。

## 23. 不同联系人上下文组装

所有联系人默认读取当前存档上下文，但按类型自动组装。

### 23.1 酒馆角色

可使用：

- 角色卡
- 性格
- 场景
- Example Dialogue
- 相关世界书
- 柏宝书记忆
- 当前存档最近正文（同时作为近期角色语言参考）
- 当前场外历史

### 23.2 第四面墙

使用：

- LittleWhiteBox Meta Protocol
- 当前存档长期记忆
- 最近正文
- 当前场外历史
- 当前触发任务

### 23.3 编剧 / 攻略 / 红笔编辑

使用：

- 各自专属 Prompt
- 当前存档剧情摘要 / 长期记忆
- 最近正文
- 当前场外历史
- 当前任务

### 23.4 自定义联系人

使用：

- 自定义人格提示词
- 柏宝书长期记忆（可用时）
- 最近正文
- 当前场外历史

---

## 24. 场外 API

### 24.1 API 来源

支持两种模式：

1. 独立场外 API（默认）
2. 使用酒馆当前 API（备用 / 兼容模式）

默认必须选择：

**独立场外 API**

“使用酒馆当前 API”用于：

- 新用户快速使用
- 不想额外配置 API 的场景
- 兼容 / 备用

### 24.2 覆盖范围

场外 API 层统一服务于：

- 私聊
- 群聊
- 自动吐槽
- 自动点评
- 第四面墙
- 编剧
- 攻略
- 红笔编辑
- 酒馆角色

### 24.3 统一适配层

结构：

`场外功能 → Generation / Agent 层 → Provider Adapter → 实际 API`

不同功能不各自直接写网络请求。

### 24.4 联系人级 API

第一版：

- 所有联系人共用全局场外 API

底层预留：

`联系人 API / 模型覆盖 > 系统默认场外 API`

但第一版 UI 不开放联系人级 API。

### 24.5 独立 API 设置页

至少包含：

- API 来源
- Provider / API 类型
- API 地址
- API Key
- 模型
- 刷新模型列表
- 手动填写模型 ID
- 流式输出
- 测试连接

API Key：

- 只保存在本地配置
- 默认隐藏显示
- 不进入 Prompt
- 不进入聊天记录
- 不随联系人配置导出

模型：

- 支持从 Provider 拉取列表
- 即使模型列表拉取失败，也允许用户手动输入模型 ID

测试连接：

- 不产生聊天消息
- 显示连接成功或具体失败原因


实现约束：

- Provider 模型列表与测试连接统一经过 Provider Adapter，不由设置页直接拼不同 Provider 请求；
- API 地址留空时允许 Adapter 使用该 Provider 的官方默认地址；
- 模型列表拉取失败不得阻止用户手动填写模型 ID；
- 测试连接不写入场外聊天历史。

### 24.6 Provider

第一版优先考虑：

- OpenAI Compatible
- Claude
- Gemini

其中 OpenAI Compatible 用于覆盖：

- OpenAI 风格接口
- 常见中转站
- 本地兼容服务

具体首发 Provider 可在实现阶段按工作量调整，但底层不能写死单一请求格式。

### 24.7 流式与停止

- 流式输出为全局开关
- 生成过程中始终支持停止
- 停止不是用户可关闭的设置项
- 发送按钮可在生成时切换为停止按钮
- 统一 Generation 层负责请求取消与错误处理

### 24.8 高级采样参数

第一版不在普通设置页加入大量：

- Temperature
- Top P
- Top K
- Frequency Penalty
- 其他 Provider 特有参数

后续如有需要，可增加“高级 API 参数”。

## 25. 第四面墙来源

第四面墙人格基于 LittleWhiteBox 的第四面墙设计。

需要保留：

- 完整 Meta Protocol
- 原版自动 commentary 思路
- 时间与消息间隔
- 流式
- 停止
- 上下文清洗
- 提示词可编辑
- 恢复 LittleWhiteBox 默认 Prompt

明确不做：

- LittleWhiteBox 的 180 秒用户级 commentary 冷却
- 图片生成
- TTS / Voice
- Fullscreen
- 图像增强
- Voice enhancer

---

## 27. 第三方参考与发布边界

项目计划公开发布，因此从现在起按可发布标准开发。

### 27.1 LittleWhiteBox

仓库：

https://github.com/RT15548/LittleWhiteBox

其项目使用 Apache License 2.0，并额外要求 attribution。

如果项目引用、修改或分发 LittleWhiteBox 文件，应在 README / Credits 中加入原作者署名。

建议：

`Based on LittleWhiteBox by biex`

发布前还需要整理：

- License
- NOTICE
- attribution

### 27.2 Acsus-Paws-Puffs

仓库：

https://github.com/Hmkovo/Acsus-Paws-Puffs

其许可证为 All Rights Reserved，并明确限制：

- 未授权修改
- 二次开发
- 分发
- 把其代码用于其他项目

因此公开版本中：

**不能直接复制其代码、CSS、默认提示词等实现。**

可以：

- 研究功能设计
- 研究交互流程
- 理解算法思想
- 然后按本项目自己的架构重新实现

### 27.3 柏宝书

仓库：

https://github.com/baibai-git/ST-BaiBai-Book

本项目只计划调用其公开只读 API：

`globalThis.STBaiBaiBook`

优先使用公开文档中支持的接口，不依赖其内部私有实现。

---

## 28. 暂不纳入第一版的功能

不优先做：

- 钱包
- 转账
- 会员
- 模拟生活
- 天气生活系统
- 复杂图片系统
- 表情包生态
- Fullscreen
- TTS
- 图片生成
- 聊天背景深度美化
- 大量 QQ / 微信附加玩法

项目不能膨胀成完整手机模拟器。

核心仍然是：

**场外角色通讯 + 群聊讨论 + 自动吐槽 + 自动点评 + 角色保真。**

---

## 29. 当前开发顺序

1. 补全 Scope 生命周期与公共数据契约
2. 联系人 / 通讯录 / 酒馆角色同步完善
3. 聊天信息页完善
4. 私聊消息操作完善
5. API / Generation 公共层
6. 第四面墙完整 Prompt
7. 自动吐槽
8. Role Fidelity
9. 世界书 / 柏宝书长期记忆
10. 提示词与预设
11. 群聊轻编排 + 逐人生成
12. 自动点评
13. 统一 QA
14. 最终 iPhone / 微信 UI 美化
15. 发布整理

若后续出现明确依赖，可以调整实际施工顺序；但依赖统一 Generation Contract 的复杂 AI 功能，不应先各自实现临时生成接口。

---

## 30. 尚待继续讨论

当前未最终拍板：

1. `＋` 扩展菜单未来加入哪些小功能
2. 转发卡片最终视觉样式
3. 联系人详情页最终视觉美化
4. API 首发 Provider 的最终实现范围
5. 编剧正式 Prompt
6. 攻略正式 Prompt
7. 红笔编辑正式 Prompt
8. 自定义联系人 Prompt 编辑体验的最终视觉细节
9. 全局生成框架各条目的最终编辑 UI
10. 第一版最终 iPhone / 微信 UI 美化
11. README / Credits / LICENSE / NOTICE 的最终发布整理

---

## 31. 维护约定

从现在开始：

- 已确认功能进入 `SPEC.md`
- 后续如果规则被推翻，直接修改 `SPEC.md`
- 不在代码里偷偷保留与 SPEC 冲突的旧逻辑
- 开发时以最新 `SPEC.md` 为唯一设计源
- 大功能确认后再集中更新 SPEC，避免每个小问题都改一次文件


---

## 追加确认：moli 默认线上聊天预设（moli30）

### 1. 模式边界

moli小手机当前聊天模式统一为：**线上即时通讯**。

不拆分“聊天 / 通话 / 线下”三套全局预设。系统设置提供“线上聊天预设”总开关。

全局线上预设负责“手机消息应该怎样说”；联系人资料负责“这个人是谁、允许读取哪些角色资料”；具体 Conversation 负责“这一个聊天实例处于什么世界 / 时间 / 自动行为环境”。

### 2. 默认线上预设条目

默认完整保留 8 个可独立开关、可编辑的 Prompt 条目：

1. 📱 线上聊天协议
2. 🫧 活人聊天行为
3. 💬 语言自然度
4. ⏱️ 时间与消息间隔
5. 🧠 手机记忆
6. 🌍 正文与世界上下文
7. 📤 输出协议
8. 📚 上下文组装规则

完整默认文本以 `src/storage/prompt-settings.js` 中 `DEFAULT_ONLINE_PROMPT_BLOCKS` 为产品默认值，不得在实现中擅自缩写成摘要版。

### 3. 多气泡输出

模型以 `<message>...</message>` 标记独立聊天气泡。标签不向用户展示；一个生成结果可以落成多个真实 assistant 消息。

解析失败或模型未使用标签时，必须安全退化为单气泡，而不是丢弃回复。换行本身不作为气泡边界。

### 4. 手机记忆可编辑性

手机记忆概念层：

`原始聊天记录 → 最近聊天 → 日记 → 长期总结`

其中：

- 最近聊天：来自真实聊天历史的近期上下文窗口，不另做重复的“记忆查看页”，不可作为 AI 摘要文本编辑；原始消息仍可在聊天中查看。
- 日记：可查看、可编辑、可删除，允许用户纠正 AI 总结偏差。
- 长期总结：可查看、可编辑，允许用户纠正 AI 总结偏差。
- 柏宝书长期记忆：外部来源，moli 内只读；moli 不编辑柏宝书内容。

柏宝书与手机记忆不是同一层：柏宝书补正文世界的长期过去；手机日记/总结记录场外线上关系自身发生的事情。

### 5. 最近聊天上下文窗口

“保存多少聊天记录”与“每次生成读取多少聊天记录”必须分离。

聊天记录可长期完整保存；生成时使用可配置的“最近消息读取上限”。设计值：默认 100 条，允许 10～9999 条。9999 是用户允许的最大读取窗口，不代表无条件把 9999 条原文全部发送给模型；Context Builder 仍需受 Provider 上下文/token 容量保护。

该设置属于 Conversation 实例，而不是 Contact。相同联系人不同私聊实例可以使用不同读取窗口。

### 6. 内置人格数量

内置人格最终保留几个目前暂不锁定。第四面墙 / 编剧 / 攻略 / 红笔编辑的最终数量与各自专属配置放到后续阶段决定；当前线上全局预设实现不得依赖“固定必须有四个内置人格”。

---

## 追加确认：Conversation 2.0 第一阶段（moli31）

### 1. 线上聊天预设允许用户新增自定义条目

系统默认 8 个线上聊天 Prompt 条目继续作为 moli 默认预设存在。

用户可通过：

`设置 → 提示词与预设 → 线上聊天预设 → ＋ 添加自定义条目`

新增自己的 Prompt 条目。

自定义条目支持：

- 自定义名称
- 编辑 Prompt 内容
- 独立开关
- 删除
- 与默认条目一起参与实际生成注入

恢复 moli 默认预设时：

- 默认 8 个系统条目恢复默认内容与开启状态
- 用户自定义条目不因“恢复默认”被自动删除

默认系统条目不得因为用户可自定义条目而缩写或移除。

### 2. Contact 与 Conversation 进一步分离

同一个联系人允许拥有多个彼此独立的私聊 Conversation。

Contact 继续表示：

**这个人是谁。**

Conversation 表示：

**这一次聊天发生在哪个世界 / 时间 / 上下文里。**

新的私聊实例拥有独立：

- conversation id / key
- 聊天历史
- 置顶状态
- 未读状态
- 会话标题
- 归属模式
- 后续 Conversation 级上下文设置

### 3. Conversation 归属模式

新私聊支持：

#### 随当前正文

- 绑定当前 SillyTavern chat / scope
- 只在该正文 / 存档激活时出现
- 切走后隐藏
- 切回后原聊天与历史恢复
- 默认时间模式：跟随正文
- 默认读取当前正文：开启

#### 全局陪伴

- 不绑定某一个 SillyTavern chat / scope
- 切换不同正文后仍保留同一份聊天与历史
- 适合现实时间陪伴
- 默认时间模式：现实世界时间
- 默认读取当前正文：关闭

全局陪伴不得默认把当前屏幕上任意一个正文当成自身时间线，以避免不同存档 / 时间线互相污染。

### 4. 旧私聊兼容

旧结构中以 `contactId` 作为会话 key 的既有私聊不得丢失。

旧私聊继续原位读取，并按 Conversation 2.0 规则视为：

- `scopeMode = current`
- 默认跟随正文时间
- 默认读取正文
- 最近聊天读取上限默认 100

不得为了升级多私聊结构而清空、重建或隐藏旧聊天历史。

### 5. 同联系人多私聊默认不互读

同一个 `contactId` 只代表身份相同，不代表所有 Conversation 自动共享记忆。

在尚未提供显式“跨聊天记忆 / 场外历史共享”开关前：

- 现实陪伴聊天不得自动读取剧情沉浸聊天
- 剧情沉浸聊天不得自动读取现实陪伴聊天
- 不再默认把同联系人其他私聊作为 Generation 上下文注入

后续如果允许跨 Conversation 读取，必须由 Conversation 设置显式开启并标明来源。

### 6. 当前阶段 UI

当前私聊聊天信息页增加：

- 当前聊天名称 / 归属显示
- `＋ 新建另一个聊天`

新建聊天时选择：

- 随当前正文
- 全局陪伴

允许填写可选聊天名称，例如：

- 日常
- 2018正文
- IF线

首页与聊天标题使用“联系人名 · 聊天名”区分同一联系人的多个私聊。

### 7. Conversation 2.0 后续阶段

下一阶段继续把以下设置正式放入 Conversation 设置页：

- 时间模式
- 是否读取当前正文
- 最近聊天读取上限（默认 100，可设置 10～9999）
- 自动吐槽
- 自动聊天
- 手机记忆来源
- 后续当前聊天外观覆盖

本阶段只建立多私聊与归属地基，不提前实现未确认的自动聊天触发规则。

---

## 追加确认：Conversation 2.0 第二阶段（moli32）

当前私聊实例拥有独立的“当前聊天设置”。这些设置属于 Conversation，不属于联系人本体；同一个联系人建立的多个私聊可以拥有完全不同的设置，互不覆盖。

### 当前聊天设置入口

私聊 `聊天信息` 中提供：

- `当前聊天设置`

进入后至少包含：

1. 聊天名称
2. 时间模式
3. 读取当前正文
4. 最近聊天读取上限

### 聊天名称

- 仅修改当前 Conversation 的显示名称。
- 不修改联系人姓名 / 备注名。
- 同一个联系人不同聊天可以使用不同名称，例如 `日常`、`2018正文`。

### 时间模式

每个私聊独立保存，允许：

- `跟随正文时间`：以剧情 / 正文时间为准，不自动把现实经过时长套入故事。
- `现实世界时间`：使用现实日期、时刻与真实消息间隔，适合日常陪伴。
- `无时间感`：除非聊天或正文明确提到，否则不主动推断时间经过。

`随当前正文` 新建聊天默认使用 `跟随正文时间`。

`全局陪伴` 新建聊天默认使用 `现实世界时间`。

默认值只是创建时的初始配置，之后用户可以按 Conversation 单独修改。

### 读取当前正文

每个私聊独立保存：

- 开启：生成时允许读取当前 SillyTavern 最近正文。
- 关闭：当前正文不进入这个聊天实例的生成上下文。

`随当前正文` 新建聊天默认开启。

`全局陪伴` 新建聊天默认关闭，避免用户切换不同正文 / 世界线时自动污染陪伴聊天。

用户仍可手动给全局陪伴开启正文读取；此时正文是当前可见参考上下文，不应自动被解释为该陪伴聊天自身的唯一时间线。

### 最近聊天读取上限

- 默认：`100`
- 用户可设置：`10～9999`
- 小于 10 自动收敛到 10。
- 大于 9999 自动收敛到 9999。

这一数值只控制生成时优先送入模型的近期原始手机消息数量，不删除完整聊天记录，也不限制界面能够保存 / 查看多少条历史。

后续手机记忆系统仍按：

`近期原始聊天窗口 → 日记 → 长期总结`

分层处理，不能把“最近聊天读取上限”误当成聊天记录保存上限。

### 隔离规则

修改当前聊天设置只影响当前 `conversationId`。

不得因为两个 Conversation 指向同一个 `contactId`，就同步覆盖：

- 时间模式
- 正文读取开关
- 最近聊天读取上限
- 聊天名称
- 后续自动吐槽 / 自动聊天 / 手机记忆等会话配置

---

# moli33 决策补充：内置人格 / API 预设 / 记忆 / 状态栏 / 自动聊天

> 本节为 2026-09-13 后续确认，若与前文旧“编剧 / 攻略 / 红笔编辑”“日记”等描述冲突，以本节为准。

## 1. 内置人格暂定更新

保留既有“第四面墙”。原 `builtin:writer / builtin:guide / builtin:redpen` 为兼容旧聊天 ID 不做物理更换，但默认显示身份迁移为：

- `上帝`：上帝视角评价正文好坏、判断角色与 NPC、分析局势并给用户行动 / 攻略建议。
- `人类`：站在用户角度的姐妹 / 闺蜜 / 同伙；可磕 CP、替用户说话、讨伐角色，也能认真讨论下一步行动与剧情分支。
- `吃瓜观察员`：活跃气氛，观察暧昧、修罗场、权力关系、NPC 八卦等；最终名称仍允许后续调整。

上述人格的私聊不具备“自动吐槽”或“自动点评”。它们可正常私聊、参加群聊，并可在后续开启“自动聊天”。

## 2. API 配置采用酒馆用户熟悉的预设方式

- 全局 API 设置继续保留“跟随酒馆当前 API / 独立 API”。
- 独立 API 可保存为本机 `API 预设`，保存 API 类型、地址、Key、模型、流式设置。
- 用户可选择预设后“应用”，也可另存和删除预设。
- 后续 Contact 与 Conversation 的独立 API UI 都应复用同一预设库，不复制发明另一套 Profile 管理器。
- 最终优先级仍为：`当前 Conversation 独立 API > Contact 独立 API > 全局 API`，但 UI 不向用户暴露 Resolver 等工程术语。

## 3. 手机记忆命名与触发

旧文档中的“日记”统一改称 **近期记忆**，避免与其他手机扩展的“日记”概念过度相似。

记忆链：

`近期原始聊天窗口 → 近期记忆 → 长期总结`

- 近期记忆：按新增消息条数触发；建议默认 30 条，可配置。
- 长期总结：按新增近期记忆数量触发；建议默认每 5 份近期记忆更新一次，可配置。
- 两者均允许人工编辑；失败时应支持“生成到最新 / 重试失败部分”。
- 柏宝书长期记忆仍是独立只读来源，不等同于手机长期总结。

## 4. 自动聊天

自动聊天不以固定间隔 / 随机概率作为主要触发逻辑，而由角色自主判断是否适合主动联系用户。判断可参考最近聊天、未完话题、关系状态、正文事件、所选时间模式、距离上次互动的时间与近期主动频率。

自动聊天与自动吐槽 / 自动点评是不同系统。

## 5. 状态栏

状态栏采用已确认的成熟功能结构，由 moli 独立实现：

- 全局状态栏预设库：预设名称、Prompt Suffix、Regex Pattern、HTML Template、测试文本、匹配结果、实时预览。
- Contact：启用状态栏、快速填充预设，并允许覆盖 Prompt / Regex / HTML。
- Conversation：保存本聊天实际状态历史，避免同一 Contact 的不同世界线串状态。
- 生成上下文只读取最近 N 个状态；完整状态历史不因此删除。


---

# moli34 确认：主 API 设置按酒馆手机扩展习惯重做 + Contact 独立 API 第一阶段

> 本节覆盖 moli33 中仍然偏“自建 API Profile 管理器”的 UI 方案。后续 API 页面不得再自行发明另一套交互。

## 1. 主设置 API 页面

主设置中的 API 页面采用酒馆用户熟悉的手机扩展结构：

1. `API 来源`
   - `跟随酒馆设置（默认）`
   - `自定义 API`
2. `流式生成`
3. 自定义 API 下选择 `API 类型`
4. 按 API 类型显示对应密钥 / 反向代理 / 模型区域
5. 反向代理支持单独的代理预设：读取、保存、删除
6. OpenRouter 使用自己的 Key 区域
7. 自定义 OpenAI 兼容端点使用 Base URL + 可选 Key + 模型
8. 非自定义端点使用模型选择 + 刷新 + 手动模型
9. 提供测试连接
10. API 配置本身仍支持保存 / 读取 / 删除预设，供 Contact / Conversation 独立 API 复用

`跟随酒馆设置`不是假选项：生成时调用 SillyTavern 当前 API。

当前 moli 独立实现，不直接依赖其他扩展运行时；但交互结构保持酒馆用户熟悉的形式。

## 2. API 数据兼容

旧 `moli-phone:api-settings:v1` 不物理删除。

读取旧配置时兼容迁移：

- 旧 `source=tavern` → 新 `source=default`
- 旧 `source=independent` → 新 `source=custom`
- 旧 Claude → `format=claude`
- 旧 Gemini → `format=makersuite`
- 旧 OpenAI Compatible：有自定义 Base URL 时迁到 `format=custom`，否则迁到 `format=openai`

API 预设同样做惰性兼容读取，不让 moli33 已保存配置消失。

## 3. Contact 独立 API

联系人资料正式拆出 `独立 API`。

规则：

- 默认关闭：跟随主设置 API。
- 开启：当前 Contact 保存自己的 API 配置副本。
- 可以从主 API 预设库“读取配置”。
- 读取预设是**复制当前配置**，不是永久绑定；之后修改主预设不能偷偷改变该联系人。
- Contact 独立 API 已接入私聊生成。
- 后续 Conversation 独立 API 的优先级高于 Contact；尚未开启 Conversation override 时，不得越级发明。

最终优先级仍为：

`Conversation 独立 API > Contact 独立 API > 主设置 API`

## 4. 联系人资料入口

私聊 `聊天信息` 中正式拆出：

- 人格与提示词
- 独立 API
- 状态栏
- 手机记忆
- 当前聊天设置 / 新建聊天

其中：

- `人格与提示词` 已成为独立编辑页。
- `独立 API` 已成为独立编辑页并接入生成。
- `状态栏` 与 `手机记忆` 入口已固定，不允许后续再次遗漏；完整功能继续按既定节点实现。

## 5. 既有命名继续有效

手机记忆仍统一称：

`近期原始聊天 → 近期记忆 → 长期总结`

不得再把“近期记忆”改回“日记”。

---

# moli35 决策补充：联系人 API 引用与人格来源分型

> 本节为 2026-09-13 确认。与 moli34 中“联系人读取后复制 API 配置”以及旧的统一人格提示词 UI 冲突时，以本节为准。

## 1. 联系人独立 API 只引用主设置已保存配置

- 主设置是 API 配置内容的唯一维护入口。
- Contact 资料页不重复展示或保存 API Key、API URL、反向代理密码、模型、流式参数等账号/连接字段。
- Contact 只保存：是否启用独立 API + 所选主设置 API 配置的 `presetId`。
- 关闭独立 API：跟随主设置当前 API。
- 开启独立 API：必须选择一个主设置中已保存的 API 配置。
- 生成时按 `presetId` 动态读取配置，因此主设置中修改同一配置后，引用它的联系人自动使用最新内容。
- 若所选配置已被删除，生成时明确提示用户重新选择，不静默切换到错误模型。
- 兼容 moli34 已产生的旧 `apiOverride.config` 副本：旧数据可继续读取，但新 UI 不再创建配置副本。

## 2. 人格与提示词按联系人来源分型

### 酒馆角色

酒馆角色不额外要求“人格 Prompt”。角色身份由酒馆来源资料决定。资料页只提供可选的 **自定义附加 Prompt**，用于补充约束而不是重新定义角色。

酒馆角色来源结构继续按既定设计逐步接入：角色卡、性格、场景、Example Dialogue、世界书、柏宝书长期记忆、最近正文、场外聊天历史、自定义附加 Prompt。

### 自定义联系人

自定义联系人没有酒馆角色卡，因此保留 **人格 Prompt**，它是主要人格来源；同时保留简介。

### 内置人格

第四面墙 / 上帝 / 人类 / 吃瓜观察员使用系统内置人格 Prompt。允许用户编辑，并在默认 Prompt 完整落地后提供“恢复默认”；不把它们当作酒馆角色来源读取。

## 3. 联系人资料主入口

联系人资料继续固定保留：

- 人格与提示词
- 独立 API
- 状态栏
- 手机记忆
- 聊天 / 当前聊天设置相关入口

下一功能节点：按已确认的状态栏结构实现状态栏本体。

# moli37 现行规则索引：Conversation 校准

> 本节用于消除历史开发章节中的旧表述冲突。若前文与本节冲突，以本节及其后续明确确认内容为准；历史章节保留作为开发记录，不再作为现行实现依据。

## Conversation 当前规则

- Contact 表示“这个人是谁”；Conversation 表示“这一次聊天发生在哪个世界 / 时间 / 上下文里”。
- 同一 Contact 可以拥有多个彼此独立的私聊 Conversation，默认不互读历史或记忆。
- `当前聊天` 必须可选择：在当前联系人已有 Conversation 之间切换当前编辑 / 打开的聊天实例，而不是固定展示文本。
- `归属` 必须可修改：`随当前正文` / `全局陪伴`。
- 修改归属属于**移动同一 Conversation**：保持同一个 conversation key/id、完整消息历史、标题、时间模式、正文开关、近期聊天上限及未知扩展字段；不得复制第二份、不得清空历史。
- 改为 `随当前正文` 时绑定当前稳定 scope；改为 `全局陪伴` 时解除正文 scope 绑定并进入全局 Conversation store。
- 归属变化不会自动重置时间模式、正文读取开关等已经由用户明确设置的 Conversation 配置。`随当前正文→默认正文时间/正文开启`、`全局陪伴→默认现实时间/正文关闭`只属于**新建时默认值**。
- 时间模式始终是 Conversation 级可编辑设置：跟随正文 / 现实世界 / 无时间感。
- 当前聊天名称、正文读取开关、最近聊天读取上限也均为 Conversation 级设置。

## 已废弃/被后续设计替代的旧术语索引

- 旧内置人格“编剧 / 攻略 / 红笔编辑”已被后续“上帝 / 人类 / 吃瓜观察员”替代；第四面墙仍独立。
- 手机记忆旧称“日记”已替换为“近期记忆”；现行链路为：`原始聊天记录 → 最近聊天上下文窗口 → 近期记忆 → 长期总结`。
- 同 Contact 的其他 Conversation **默认不得**作为生成上下文自动注入；未来只有显式跨聊天共享设置才能开启。
- API 现行方向为主设置保存 API 配置，Contact/未来 Conversation 只选择引用；不得要求在资料卡重复填写 Key、URL、模型和密码。
- 酒馆角色不需要额外“人格 Prompt”覆盖角色卡；其人格来源应由角色卡/性格/场景/Example Dialogue/世界书/柏宝书/最近正文/场外聊天历史等来源开关与可选自定义附加 Prompt 组成。

## 审计后待补齐功能（不代表已实现）

1. 酒馆角色人格来源开关与世界书地基。
2. 柏宝书长期正文记忆接入。
3. 内置人格正式 Prompt / Generation 接入。
4. 群聊轻编排 + 逐人生成。
5. 自动吐槽 / 群自动点评。
6. 角色自主判断式自动聊天。
7. 手机近期记忆 / 长期总结。
8. 状态栏完整生成、提取、历史与展示链。

不得因为功能已写入 SPEC 就宣称代码已经实现；每个节点必须以真实仓库代码和测试结果为准。


# moli38 现行修正：Conversation 四概念拆分与状态栏恢复

> 本节为 2026-09-13 校准。与 moli37 及更早章节中“随当前正文 / 全局陪伴”的归属 UI 文案、把 Conversation 选择器塞入“当前聊天设置”页等冲突时，以本节为准。

## 1. Conversation 四个概念必须独立

1. **当前聊天**：选择同一 Contact 下当前正在查看/编辑的 Conversation。
2. **归属**：只表示 Conversation 存储/绑定范围，现行 UI 文案为 `当前存档 / 全局`。旧称“随当前正文 / 全局陪伴”仅保留为历史开发术语。
3. **时间模式**：`跟随正文时间 / 现实世界时间 / 无时间感`，属于 Conversation 独立设置。
4. **读取当前正文**：独立布尔开关，决定生成时是否读取当前 SillyTavern 正文。

四者不得互相冒充。关闭“读取当前正文”不会改变归属；修改归属不会重置时间模式或正文读取开关。

## 2. 资料卡与当前聊天设置职责

联系人资料卡外层直接提供：
- 当前聊天选择器；
- 归属选择器（当前存档 / 全局）；
- 当前时间模式摘要；
- 读取当前正文：开启 / 关闭。

`当前聊天设置` 内部只保留：
- 聊天名称；
- 时间模式；
- 读取当前正文；
- 最近聊天读取上限。

不得在该子页再次重复“当前聊天”和“归属”选择器。

## 3. 归属修改

归属修改即时移动同一 Conversation；保持 conversation key/id、消息、标题、时间模式、正文读取、近期消息上限与未知扩展字段，不复制、不清空。

## 4. 状态栏回归规则

moli36 已实现的“全局状态栏预设 + Contact 状态栏默认配置”属于已完成能力，不得因后续 Conversation 校准而消失。moli38 恢复该 UI 与读写链。Conversation 状态历史/提取/展示仍属于后续阶段，不在本节宣称完成。


# moli39 现行规则：角色资料卡 = Contact 来源控制中心

> 本节正式取代历史章节中把“最近正文 / 场外聊天历史”列为 Contact 人格来源开关、以及给 Tavern 角色再写一份“大人格 Prompt”的旧表述。

## 1. 三层职责

- **Contact**：这个人是谁，以及允许哪些稳定角色来源参与生成。
- **Conversation**：这一次聊天发生在哪个世界/时间线，是否读取当前正文，以及读取多少当前聊天历史。
- **Role Fidelity / Sources**：Tavern 角色生成时从角色卡哪些字段还原人格。

角色资料卡不是第二张角色卡。Tavern 角色的人格 Source of Truth 始终是 SillyTavern 角色卡及其关联资料；moli小手机只负责读取、筛选、补充与 Conversation 级上下文拼装。

## 2. Tavern「角色资料与提示词」

Tavern 联系人不显示竞争性的“大人格 Prompt”。其页面包含独立来源开关：

- Description / 角色设定
- Personality / 性格
- Scenario / 场景
- Example Dialogue
- System Prompt
- Post-History Instructions

默认全部开启。关闭仅表示生成时不注入；不得删除 `contact.source.roleFidelity` 中已保存的快照。每项可查看当前保存的原文，只读；来源角色存在时自动刷新，来源丢失时保留最后一次同步快照。

System Prompt 与 Post-History Instructions 必须单独暴露。即使开启，其中小说正文格式、第三人称、篇幅等要求也不得覆盖 moli小手机的线上聊天输出协议。

## 3. 动态上下文归 Conversation

以下内容不得再作为 Contact 来源开关：

- 最近正文：由 Conversation 的“读取当前正文”控制；
- 当前场外聊天历史：由当前 Conversation 的 messages 与“最近聊天读取上限”控制；
- 时间模式、当前存档/全局归属：均属于 Conversation。

## 4. Intro 与附加 Prompt 语义

- Tavern 联系人的 `intro` 是联系人简介/展示信息，默认不注入模型；
- 真正要影响 Tavern 角色生成的用户补充统一写入“自定义附加 Prompt”；
- 自定义联系人没有角色卡，仍保留人格 Prompt；
- 内置人格仍使用其内置人格 Prompt 体系。

## 5. 世界书与长期剧情记忆边界

本节点只固定 UI/架构位置，不宣称已经接通世界书或柏宝书。

- 世界书资料卡开关未来表示“允许使用哪些条目”的白名单；
- 本轮实际注入仍需按常驻、关键词、当前正文/聊天等触发规则激活，禁止把全部勾选条目每轮无条件塞入 API；
- 柏宝书属于 Contact 的长期剧情记忆来源，具体 Provider 接入另行实现。

# moli40 现行修正：资料卡减法、状态栏延期、世界书读取地基

> 本节为 2026-09-13 现行规则。与 moli36～moli39 中状态栏已完成/必须保留、资料卡外层显示归属/时间/正文摘要等冲突时，以本节为准。

## 1. 私聊资料卡只保留一个 Conversation 快捷入口

资料卡外层只显示 `当前聊天` 选择器。选项文本同时展示聊天名称与归属，例如 `日常 · 当前存档`、`陪伴 · 全局`。

资料卡外层不再提供独立 `归属` 按钮，也不重复显示 `时间模式`、`读取当前正文`。这些设置统一进入 `当前聊天设置`：聊天名称、时间模式、读取当前正文、最近聊天读取上限。

`scopeMode / boundScopeKey` 仍可作为 Conversation 底层数据存在，但不再作为资料卡外层的独立操作控件。新建聊天时仍可决定创建为当前存档或全局聊天。

## 2. 状态栏整体延期

当前阶段从产品 UI 与运行链移除状态栏：主设置不显示状态栏预设，Contact 资料卡不显示状态栏入口，不继续实现状态生成/Regex/HTML/历史链。

历史状态栏设计章节保留作为开发记录，但不代表现行已完成功能。以后若重新启用，必须重新确认需求后作为独立节点接入，不得影响当前 Contact / Conversation / Role Fidelity / World Book 主线。

为避免破坏用户旧数据，历史 localStorage/Contact 中已经存在的状态栏字段不做主动清洗；现行代码不读取、不编辑、不注入这些字段。

## 3. Tavern 世界书第一阶段：真实读取 + 白名单

世界书属于 Contact 的角色资料来源，但“允许使用”与“本轮激活”必须分开。

第一阶段使用 SillyTavern Extension Context 的正式世界书能力读取资料：`loadWorldInfo()` 读取角色 `extensions.world` 关联的世界书；角色卡内嵌 `character_book` 使用 `convertCharacterBook()` 兼容转换。资料卡可以看到检测到的世界书与条目，并逐条设置 moli 白名单。

白名单规则：默认允许所有条目；只持久化用户明确关闭的条目。关闭条目只影响 moli小手机，不修改 SillyTavern 原世界书，也不删除原始资料。

本阶段**不把所有允许条目直接注入 Prompt**。下一阶段必须继续对齐 SillyTavern 世界书语义，根据常驻、关键词、可选关键词、递归、概率/分组、扫描上下文等规则决定“本轮激活条目”，再与白名单取交集后注入。不得以“勾选 = 每轮全文注入”替代触发机制。

# moli41 现行修正：角色归属在添加好友时确定 + 世界书本轮激活

> 本节为 2026-09-13 校准；与 moli40 及更早章节冲突时，以本节为准。

## 1. 资料卡不再承担 Conversation 归属选择

- 内置人格与用户创建联系人默认属于当前存档；资料卡不显示无意义的“当前聊天/归属”下拉框。
- Tavern 角色的“正文角色 / 全局角色”归属也不在角色资料卡选择。
- 聊天信息页继续保留“当前聊天设置”，用于聊天名称、时间模式、正文读取、最近聊天读取上限；不重复展示归属选择器。
- 同一 Contact 的不同 Conversation 作为聊天列表中的独立实例进入，不依赖资料卡下拉框切换。

## 2. Tavern 角色归属在“同步酒馆角色”创建流程确定

`添加好友 → 同步酒馆角色 → 选择角色 → 添加` 后进入一次角色类型选择：

- `正文角色`：创建当前存档 Conversation；默认正文时间、默认读取当前正文。
- `全局角色`：创建全局 Conversation；默认现实时间、默认不读取当前正文。

“正文角色已在当前聊天列表中”之类提示不再显示。角色已经存在但不在当前 Scope 时仍可显示简短“已添加”，但归属不是资料卡编辑项。

## 3. 世界书第二阶段：白名单后的本轮激活

moli40 的世界书条目开关继续只表示 Contact 白名单。moli41 在生成前增加本轮激活层：扫描当前 Conversation 的近期消息；仅当该 Conversation 开启正文读取时，最近正文也进入扫描材料。

激活层只处理当前 Tavern Contact 自己关联/内嵌的世界书，先排除 SillyTavern 原本禁用条目和 moli 白名单关闭条目，再按常驻、主关键词、次关键词逻辑、正则关键词、概率与有限递归决定本轮条目。激活内容作为“本轮激活的世界书”进入 Prompt Builder。

不得直接调用当前 SillyTavern 页面角色的世界书结果冒充任意 moli Contact 的世界书；不同 Tavern Contact 的世界书必须保持来源隔离。

复杂的 SillyTavern 高级世界书能力（例如 inclusion group、sticky/cooldown、outlet、精确 token budget/插入位置等）仍属于后续兼容增强，不在本节点伪装为已完整复刻。


# moli42 现行修正：同步角色可重复创建 Conversation + 柏宝书长期剧情记忆

> 本节为 2026-09-13 现行规则；与早期“已添加角色不可再次选择”以及资料卡“＋ 新建另一个聊天”的历史 UI 冲突时，以本节为准。

## 1. 同步酒馆角色的选择状态不是永久状态

- “已添加”只表示该 Tavern Contact 已经存在于 moli 通讯录，不代表该角色永久不可选择。
- 同步列表中的勾选属于一次性的 UI 临时状态；完成添加后必须立即清空并刷新。
- 已存在的 Tavern Contact 仍可再次勾选，并通过“正文角色 / 全局角色”创建一个新的独立 Conversation。
- 因此不得再用“已在当前聊天列表”把 checkbox 设为 checked + disabled。

## 2. 删除资料卡“＋ 新建另一个聊天”

资料卡不再提供“＋ 新建另一个聊天”。Tavern 角色需要再次建立独立聊天时，统一从“同步酒馆角色”流程选择已有 Contact 并创建新的 Conversation。Contact 本体继续复用，不重复复制角色资料。

## 3. 柏宝书长期剧情记忆第一阶段接入

- 只使用 ST-BaiBai-Book 公共只读 API v1：`globalThis.STBaiBaiBook`。
- 读取 `getInjectedHistory()`，使用与柏宝书正常记忆注入一致、已经避开其当前滑动窗口的长期历史剧情。
- 不读取 snapshot、state、vars、items、plans、scenes、npcs、itemLog 等状态型资料。
- Tavern Contact 的 `roleSources.longTermMemory` 控制是否允许使用；关闭只停止注入，不修改柏宝书数据。
- 当前 Conversation 关闭“读取当前正文”时，不注入柏宝书这一动态正文世界来源，避免全局/脱离正文聊天被当前存档长期剧情污染。
- 柏宝书不存在、API 版本不兼容或读取失败时静默降级；最近正文机制继续正常工作。
- 若 API `coverage.complete === false`，Prompt 明确把长期记忆视为可能有缺口，近期事实继续以最近正文为准。

## 4. 上下文顺序

正文世界动态上下文保持：`柏宝书长期剧情记忆 → 最近正文原文 → 本轮激活世界书/当前场外聊天`。柏宝书用于补较早历史，最近正文负责当前发生的事情；不得把柏宝书其他状态账本混入这一层。


# moli44 现行规则：手机 Conversation 记忆数据层

> 本节为 2026-09-13 现行规则。手机记忆与柏宝书正文长期记忆是两条完全独立的记忆链。

## 1. 数据归属

手机记忆属于 **Conversation**，不是 Contact。每个私聊实例独立保存 `memory.recent` 与 `memory.longTermSummary`；同一 Contact 的其他 Conversation 默认不得读取。旧 Conversation 通过惰性默认值获得空记忆，不进行破坏性迁移。

## 2. 记忆链与优先级

场外聊天链固定为：`原始聊天记录 → 最近聊天上下文窗口 → 近期记忆 → 长期总结`。生成时优先级为：当前明确消息 / 最近原始聊天 > 当前正文事实 > 近期记忆 > 长期总结；旧记忆不得覆盖更新事实。柏宝书继续只负责正文世界的长期剧情，不属于手机记忆。

## 3. 当前阶段 UI 与编辑

`聊天信息 → 手机记忆` 正式读取当前 Conversation 的记忆数据。近期记忆和长期总结均可直接编辑、删除/清空并保存；清空聊天原始记录不会偷偷删除用户已经保存的记忆。

## 4. Prompt 接入

当前 Conversation 的手机记忆在生成时作为独立“场外记忆”块注入。近期记忆与长期总结不会替代最近原始消息，也不会自动跨 Conversation 共享。

## 5. 自动生成边界

本节点建立稳定数据结构、人工编辑入口与 Prompt 消费接口，并预留 `lastCondensedMessageId / lastSummarizedAt` 进度字段。自动把旧原始消息压缩为近期记忆、再把较旧近期记忆压缩为长期总结，属于下一阶段；在触发规则正式实现前不得伪装成已经自动记忆。

## 6. 世界书兼容热修

世界书激活层读取 `worldBookPolicy.disabledEntries` 时同时兼容旧数组格式与 moli40 现行 object-map 格式，确保用户在资料卡关闭的条目不会因数据格式差异重新进入本轮激活。

# moli45 现行规则：手机记忆自动压缩链

> 本节为 2026-09-13 现行规则，建立在 moli44 Conversation 记忆数据层之上。

## 1. 低频增量压缩

自动记忆不得每轮调用 API。只有已经离开当前 `recentChatLimit` 原始聊天窗口、且自 `lastCondensedMessageId` 之后累计至少 100 个未压缩的完整 AI 交互轮次时，才取最早 100 轮生成一段自动近期记忆。一个完整轮次定义为：用户可连续发送 1..N 个气泡，随后通过空输入触发一次 AI，AI 可返回 1..N 个角色气泡；无论这一轮拆成多少气泡，都只计 1 轮；同一次 AI 调用返回的所有角色气泡共享 `generationTurnId`。再次空输入触发新的 AI 调用时会得到新的轮次 ID，因此仍独立算 1 轮。moli45 以前没有轮次 ID 的旧记录按“连续用户块 + 连续角色块”兼容推断。只有整轮全部离开 `recentChatLimit` 原始聊天窗口后才有资格进入压缩，绝不从一轮中间切开。

## 2. 成功后才推进游标

压缩 API 成功且返回非空结果后，才写入近期记忆并推进 `lastCondensedMessageId / lastCondensedAt`。失败、超时、空回复均不得推进游标；失败只记录 `lastAutoError`，不影响正常聊天回复。

## 3. 近期记忆到长期总结

自动近期记忆累计到 8 段时，取最旧 5 段再压缩为一个长期沉淀段，并从近期记忆中移除这 5 个自动段。人工编辑/创建的近期记忆标记为 manual，不参与自动淘汰。

长期沉淀只追加到现有 `longTermSummary`，不得重写用户已经人工维护的既有长期总结。这样自动链可以继续增长，但不会用一次模型改写粗暴覆盖人工记忆。

## 4. 触发时机

自动检查发生在一次角色回复已经成功落盘之后。未达到阈值时不发起额外 API 请求；达到阈值时才进行压缩。自动压缩失败不回滚或删除本轮正常聊天消息。

## 5. Conversation 隔离与 API

压缩进度、近期记忆、长期总结继续严格属于当前 Conversation。同一 Contact 的其他 Conversation 不共享游标或记忆。记忆压缩沿用该 Contact 当前实际生效的 API（Contact 独立配置优先，否则主 API）；酒馆当前 API 使用公开的 `generateRaw` 能力。

# moli46 现行规则：内置人格正式私聊生成 + 更新可用提示

> 本节建立在 moli45 自动手机记忆链之上。内置人格进入真实 Generation，但不把 Tavern 角色资料来源错误复用给内置人格。

## 1. 四个内置人格进入正式私聊 Generation

- `第四面墙 / 上帝 / 人类 / 吃瓜观察员` 均拥有独立的系统默认人格 Prompt，并可使用现有私聊 Generation。
- 继续沿用 moli小手机统一的线上聊天预设、`<message>` 多气泡协议、流式/停止、时间模式、当前 Conversation 原始历史与手机记忆。
- 内置人格不读取 Tavern Role Fidelity、Tavern 世界书白名单或 Tavern 角色卡字段。
- Conversation 开启“读取当前正文”时，内置人格可读取最近正文；柏宝书存在时可作为较早正文剧情记忆补充。关闭正文读取时，两者都不进入本轮动态正文上下文。
- 内置人格的手机近期记忆 / 长期总结仍严格属于当前 Conversation；moli45 自动压缩链现在也允许内置人格正常参与。

## 2. 默认人格 Prompt 与用户编辑

- 内置人格默认 Prompt 由 moli 自己的 `src/prompts/builtin-personas.js` 维护，四个身份互不共用。
- 联系人“人格与提示词”页可编辑当前内置人格 Prompt，并提供“恢复默认人格 Prompt”。恢复只把编辑框恢复为该身份的系统默认文本，保存后生效；不删除消息、Conversation 或手机记忆。
- 第四面墙本节点先建立可运行的 moli Meta/幕后观察人格基线；历史章节要求的 LittleWhiteBox 原版完整 Meta Protocol / commentary 逐项兼容仍是独立后续增强，不在本节点虚假宣称已经逐字恢复原版。

## 3. 更新按钮状态

- 设置页右上角继续保留扩展更新按钮。
- 手机初始化后以非阻塞方式读取本地 manifest 与 `homePage` 对应 GitHub 仓库的远端 manifest；远端版本高于本地版本时，更新按钮显示红色。
- 没有更新时保持普通样式；检查失败只记录调试信息，不得阻断手机初始化或聊天。
- 点击按钮仍使用 SillyTavern 扩展更新接口执行真正更新；服务器报告已是最新版或更新成功后清除红色状态。

## 4. 回归要求

继续保留：moli45 100 轮自动记忆规则、moli43 悬浮球热修、Tavern 同步角色可重复选择、资料卡无“新建另一个聊天”、Conversation 设置、主/Contact API、Role Fidelity、世界书白名单与激活、柏宝书、消息/转发/搜索。状态栏仍延期，不恢复。

## 5. 下一主节点

内置人格私聊实机确认后，进入 **群聊轻编排 + 逐人生成**。群聊编排不得把所有成员一次塞给一个模型代写；继续采用“先决定本轮谁说话/顺序，再按成员逐人生成，后发言者可看到本轮前面已生成消息”的既定架构。

---

# moli47 修正版：自动行为与第四面墙校准

> 本节是当前最终规则；与前文旧“群聊自动吐槽”或“主动聊天不采用百分比”的描述冲突时，以本节为准。

## 1. 私聊自动行为

仅 **酒馆角色** 与 **自定义联系人** 的私聊提供两套彼此独立的 Conversation 级设置：

- 自动聊天 / 主动私聊：开关 + 0～100% 百分比；百分比表达角色主动联系用户的倾向/触发权重，不是固定分钟定时器。
- 自动吐槽正文：开关 + 0～100% 百分比；它只针对正文事件产生场外吐槽，不等同于主动私聊。

两套状态必须按 Conversation 隔离。同一 Contact 的不同私聊实例可以使用不同配置。

第四面墙、上帝、人类、吃瓜观察员不复用这组“酒馆角色 / 自定义联系人”通用按钮；第四面墙保留自己的 LittleWhiteBox commentary 体系，上帝 / 人类 / 吃瓜观察员仍不拥有自动吐槽 / 自动点评。

## 2. 群聊自动行为

**群聊不提供自动吐槽。** 群聊只提供自动点评：

- 自动点评开 / 关
- 每 N 个有效正文 AI 回合点评

自动点评触发后仍遵守既定“全员参与、逐人生成”的 Review 规则。

## 3. 普通群聊回复规则保持不变

普通群聊采用“轻编排 + 逐人生成”：

- 不强制所有成员回复，也不设固定 1～3 人上限；理论上限为群成员数量。
- 第一位按当前话题相关度决定，不纯随机。
- 被 @ 的成员必须参与，其他成员仍可自然插话。
- 后发言者必须看见本轮用户消息与前面已经产生的新消息。
- 每个成员单独调用自己的完整人格 / Role Fidelity / API 上下文，禁止一次请求让一个模型代写所有成员。

## 4. 第四面墙

第四面墙以 LittleWhiteBox 原版 Fourth Wall 的行为结构为 Source of Truth：皮下语C搭档身份、正文/场外边界、时间与消息间隔、现实碎片、内省后短消息、独立 commentary 模式、上下文清洗、流式/停止及恢复默认入口都必须保留。moli 不再使用“幕后分析助手”作为第四面墙人格。

为避免与 moli 现有消息协议冲突，解析层同时兼容 `<message>` 与 LittleWhiteBox 风格 `<msg>`；`<thinking>` 仅作为内部生成内容，展示时必须被清理。

当前节点先完成默认 Prompt 行为结构、解析兼容和设置持久化；自动吐槽 / 自动点评 / 主动私聊真正后台触发仍必须等待对应 Automation Engine，不能仅凭按钮存在就宣称功能已运行。

---

# moli48：自动行为滑杆 + 群聊轻编排/逐人生成

## 1. 私聊自动行为 UI

酒馆角色 / 自定义联系人继续保存 0～100 的内部概率值，但聊天信息页不再显示数字输入框或百分比数字。`主动私聊频率` 与 `正文吐槽频率` 均使用简约滑动进度条；拖动位置直接保存原有 0～100 Conversation 值，不改变既有数据语义。

## 2. 普通群聊真实生成

普通群聊的空输入触发回复正式接通“轻编排 + 逐人生成”：

- 先进行一次轻量编排，只给成员 ID、名称、短描述、最近群聊与 @ 信息，不加载所有人的完整角色卡；
- 编排器只返回本轮发言成员与顺序，不代写群成员内容；
- 不强制全员，不设固定 1～3 人上限，最多不超过当前成员数量；
- 被 @ 的成员必须进入本轮 speaker 顺序；
- 真正生成时每个 speaker 单独调用；Contact 独立 API 优先，否则跟随主 API；
- Tavern speaker 继续读取自己的 Role Fidelity、允许的世界书、柏宝书等既有上下文；
- 后发言者必须读取本轮前面已经生成的新群消息，因此同轮成员不是彼此隔离的独立答题；
- 每个 speaker 仍使用 moli 线上聊天输出协议，可产生 1～N 个自然气泡；
- 一次用户空输入触发的整轮群聊回复共享同一个 `generationTurnId`。

## 3. 边界

本节点只接通普通群聊主动请求回复。群聊自动点评仍等待 Automation Engine；角色/自创联系人的自动吐槽正文与主动私聊后台触发也仍等待 Automation Engine。群聊仍然没有自动吐槽。

# moli49：滑杆百分比可视反馈 + 群聊自动点评第一阶段

## 1. 滑杆显示规则修正

酒馆角色 / 自定义联系人聊天信息页中的两条自动行为滑杆继续保持简约，不恢复数字输入框；但为了让用户知道当前实际值，滑杆下方中间显示一个小号百分比文本，并在拖动时实时更新：

- 主动私聊频率：滑杆 + 居中 `N%`
- 正文吐槽频率：滑杆 + 居中 `N%`

底层仍保存原有 0～100 Conversation 值，数据语义不变。

## 2. 群聊自动点评开始真正运行

群聊仍然**没有自动吐槽**。本节点只接群聊自动点评 Automation：

- 每个群聊独立保存 `自动点评开 / 关` 与 `每 N 个有效正文 AI 回合`；
- Extension 只在稳定 SillyTavern chat scope 下监测当前正文中的有效 assistant 回合；
- 第一次接管现有存档时只建立基线，不追溯旧正文自动补点评；
- 开关关闭期间更新观测基线，但不累计点评有效回合；重新开启后从续算点继续；
- 正文 assistant 回合增加时累计；删除导致回合数下降时同步回退；
- 到达 / 跨过 N 边界后触发一轮 Review；
- 同一 N 边界发生正文 reroll 且最后一条正文内容变化时，允许针对新版正文重新触发该边界 Review；
- Review 第一位成员随机；当前群所有可用人格至少各生成一次；后说者读取本轮前面已经写入 working history 的点评消息；
- 每个成员仍使用自己的 Contact 人格、Role Fidelity、Worldbook、BaiBai、API 配置；
- Review 消息写入群聊时 `source = review`，并通过统一 Conversation 消息结构保存；
- 后台点评失败不影响正文和已有场外聊天，错误状态保留，避免高频无限重试。

本节点尚未接酒馆角色 / 自定义联系人的正文自动吐槽与主动私聊 SEND/SKIP，它们仍属于后续 Automation 节点。


# moli50：群聊收束 + 自动行为闭环 + 未读/输入反馈

- 普通群聊不以正文存在为发言前提；正文仅作为可用上下文。轻编排通常选择 1～3 名成员，必要时最多 4 名；被 @ 者必须参与，额外插话最多 2 人。同一成员一轮只调用一次，每次最多展示 3 个气泡。
- 群自动点评仍是唯一群后台自动行为：全员逐人一次，每人最多 2 个气泡。群聊不增加自动吐槽。
- Tavern 角色在群聊/点评中必须把正文同名同身份角色认作自己本人，以第一人称本人立场回应，不得默认变成作者、分析员或旁观者。
- 酒馆角色/自创联系人正文吐槽与主动私聊开始进入 Automation：正文新 AI 回合按吐槽百分比获得机会；主动聊天按频率获得机会后仍由角色 SEND/SKIP 自主判断，不使用固定定时发言。
- 后台自动行为按“自动轮次”计算未读：连续 3 个自动轮次未打开对应 Conversation，则该 Conversation 的后台自动行为休眠；用户打开后清零并恢复。气泡数量不等于自动轮次数。手动群聊/手动请求回复不计入三轮休眠。
- 任意 Conversation 有未读时，moli 悬浮球显示小红点；全部已读后同步消失。当前打开的 Conversation 收到后台消息时直接视为已读。
- 当前打开会话只要仍有 Generation/Automation 在运行，输入栏上方显示“对方正在输入•••”；整个私聊回复或整轮群聊/点评结束、停止或失败后才消失。该文字是纯前端 UI 状态，不进入 Prompt，不消耗模型 token。


# moli51 现行规则：群聊轮换、未读双层红点、跨面板 Generation 状态

- 普通群聊每轮重新独立编排；上一轮发言者仅作为去重复参考，不得形成固定组合。若成员充足且编排连续返回完全相同的多人组合，在不破坏 @ 强制参与的前提下替换一名非强制成员，防止长期锁死。相关度仍高于机械平均轮换。
- Tavern 角色继续遵守“正文中的同名同身份角色就是本人”的本人视角铁律。
- 未读状态同时显示在聊天列表头像右上角与 moli 悬浮球；两处读取同一 Conversation unreadCount。打开对应 Conversation 后清零。
- Generation 运行状态提升为跨 PhonePanel 生命周期的运行时状态。关闭面板不等于停止生成；重新打开同一 Conversation 时，标题位置显示“对方正在输入中…”，发送键恢复“停止”，直到整轮结束/Abort/错误。
- 不再在输入框上方额外占一行显示输入状态；生成时直接用“对方正在输入中…”临时替换聊天标题，模拟手机聊天。


# moli52：通知链、群聊短消息与跨会话连续性
- 未读红点继续以 Conversation.unreadCount 为唯一真值，同时驱动聊天列表头像/数字与悬浮球；悬浮球红点样式增加 WebView 显示加固。
- 群聊普通回复每位入选成员严格 1 个气泡，单条最多约 80 字；自动点评每人 1 个气泡，最多约 100 字。
- 群聊角色生成接入其同 scope 私聊近期上下文；私聊角色生成接入其参与群聊的近期上下文，实现转发与群/私话题连续性。仅共享该角色本人可合理知晓的会话，不把其他角色私聊泄漏给它。
- 群聊成员消息头像可直接跳转该成员私聊。
- 世界书来源对内容完全相同的 linked / embedded 来源去重，保留真正不同的同名世界书。
- 自动 Review 继续由正文 assistant turn 监测驱动；本节点保留后续实机诊断入口。
- 外部实现边界：仅保留可独立验证的产品需求与本项目自己的实现决策；涉及第三方代码时必须遵守对应许可证，无法确认公开实现时不猜测私有实现。


# moli53 节点：群聊模式 / 群记忆 / Review 目标锁定

- 群聊新增 Conversation 级 `groupMode`：`reading`（围读会）与 `role-chat`（角色闲聊），同一个群可随时切换，不把模式永久绑定为群类型。旧群默认迁移为围读会，以保持既有行为。
- 围读会：可读取当前正文；自动 Review 仅在此模式累计/触发。角色闲聊：生成层强制关闭当前正文与柏宝书，不累计正文 Review；仍保留成员自己的必要身份资料/世界书、该成员私聊连续性、当前群聊天和群手机记忆。
- 群聊正式拥有 Conversation Memory（近期记忆 / 长期总结）并复用现有 100 轮自动压缩链；群使用主 API。近期记忆记录 `sourceMode`；群长期总结按 `reading / roleChat` 分槽保存。角色闲聊不会注入围读会近期记忆或围读会长期总结，避免分析口吻污染日常群聊。
- 群资料页新增“手机记忆”“当前聊天设置”；当前聊天设置可切换围读会/角色闲聊、正文时间/现实时间、正文读取和近期聊天上限。角色闲聊时正文读取强制关闭。
- Review Automation 在触发瞬间抓取精确的正文 assistant turn 快照，并作为 `PRIMARY REVIEW TARGET` 传入逐人生成；群聊天、群记忆和其他正文片段仅作为 SUPPORTING CONTEXT，不能再成为误点评对象。
- 聊天列表删除右侧未读数字，只保留头像红点；悬浮球红点继续使用同一 `unreadCount` 真值。
- 时间模式 UI 移除“无时间感”，仅保留“跟随正文时间 / 现实世界时间”；旧 `none` 数据惰性迁移为当前 scope 的默认时间模式。微信式时间标签留到后续独立节点。
- `manifest.json` 版本提升到 `0.3.8`。

## moli53 回归要求

- 不回退 moli52 普通群聊轻编排/逐人生成、每人单气泡、私聊/群聊桥接、世界书去重、主动私聊/正文吐槽 Automation、未读休眠、跨面板 Generation 状态。
- `角色闲聊` 中不得出现最近正文或柏宝书注入；`围读会` 切回后恢复正文能力。
- Review 必须以触发时正文快照为主要点评对象；切到角色闲聊后不得累计/触发 Review。


### Runtime generation error visibility (moli54)
Generation, private automation, and group Review errors must be visible in the affected Conversation, not console-only. The chat error banner persists until the user resumes interaction (typing or clicking). Group Review isolates per-member failures: one member timeout must not cancel successful replies from other members.


## moli55：群聊单次批量生成（2026-09-13，历史节点）

- 该节点首次把普通群聊从“编排器 1 次 + 入选成员逐人 N 次”改为 **一轮仅 1 次主 API 请求**。当时协议限制 1～3 个成员；**moli83 已保留单次批量调用架构，但废弃 1～3 人 / 每人单气泡限制，改为整轮总气泡范围。**
- 自动围读同样保持 **一轮仅 1 次主 API 请求**；moli83 起不再要求全部成员各出一条。
- 群聊批量请求统一使用主 API；Contact 独立 API 继续用于私聊。原因是“一轮只调用一次”无法同时调用多个不同 Contact API。
- 批量 Prompt 中每位成员拥有独立 `MEMBER PRIVATE ZONE`：角色卡/人格资料、本成员世界书、本成员自己的轻量手机私聊连续性。其他成员严禁引用该区内容，除非信息已由用户转发或出现在群公共历史。
- 围读会只共享一份当前正文辅助上下文；不再为每个成员重复注入成员个人正文历史或柏宝书。角色闲聊仍完全禁止正文与柏宝书。
- moli55 为累计补丁：包含 moli54 的 Review 错误提示、失败可见性与请求稳定性修复，因此从未安装 moli54 的 moli53/当前完整仓库可直接覆盖 moli55。


## moli56：群聊单成员单气泡硬约束 + 微信式时间标签（历史节点，气泡约束已被 moli83 废弃）
- 当时自动围读按 contact id 去重、普通群聊限制 1～3 人且每人最多一条。**moli83 已明确撤销这一产品约束**：现在按群聊整轮总气泡范围生成，同一 speakerId 可重复、成员可沉默。
- 原来的 Prompt / 解析器去重限制已在 moli83 同步移除；仅微信式时间标签等不相关行为继续保留。
- 私聊桥按成员 contact id/成员映射隔离，不写死任何角色名字。
- Conversation 时间显示采用稀疏微信式标签：现实时间按消息 ts，间隔 >=5 分钟或跨日显示；正文时间从最新正文顶部可确认的日期/时刻做消息快照，同一时刻连续气泡只显示一次，无法确认时不编造。

---

## moli57：第四面墙 LittleWhiteBox 恢复第一阶段

第四面墙（`builtin:meta`）开始使用专用 Prompt / 上下文处理路径，而不是只作为普通内置联系人套用通用聊天模板。

本阶段确认：

- 新增 `src/prompts/fourth-wall.js`，集中维护第四面墙 Meta Protocol、Commentary Protocol、皮下历史清洗与时间/间隔格式化。
- 第四面墙仍复用 moli 已有 Conversation、近期记忆/长期总结、正文读取、API、流式、停止与 `<msg>` 解析链，不另建竞争性存储或 Generation 系统。
- 现实时间模式：皮下历史消息使用真实 `ts`；用户消息可携带“距上次第四面墙回复”的时间间隔。
- 正文时间模式：优先使用消息落地时保存的 `storyTime` 快照；没有明确剧情时间时不编造精确时刻。
- 第四面墙历史/其他会话/正文聊天内容进入 Prompt 前，清理历史中遗留的 `<think>` / `<thinking>` / `<system>` / `<meta>` / `<meta_protocol>` / `<instructions>` 包裹内容，避免把旧输出中的隐藏结构重新当成当前指令。
- 恢复默认人格 Prompt 时，第四面墙恢复到新的专用 Meta Protocol。
- 本阶段不引入 LittleWhiteBox 的图片生成、TTS、Fullscreen、图片增强、Voice enhancer、180 秒业务冷却。
- Commentary 的专用协议已集中到第四面墙模块，但事件级自动 commentary 触发/编辑事件监听继续留在后续阶段，不在本阶段伪装成已完成。

---

## moli58：皮下（LittleWhiteBox 四次元壁核心复刻第一阶段）

### 1. 产品身份

`builtin:meta` 对用户显示为 **皮下**，辅助小字为 **入戏…**。它不是普通联系人，不使用普通私聊/群聊的全局线上人格预设；它拥有独立 Fourth Wall Protocol，同时复用 moli 的 API、模型参数、超时与流式底座。

### 2. 头像

皮下头像没有独立持久化真值。每次渲染时读取当前 SillyTavern 正文 char 的头像；切换正文角色后，皮下聊天列表、资料页和 AI 气泡头像随之变化。

### 3. Fourth Wall Context Stack

皮下请求严格区分：

- `chat_history`：当前正文，属于双方共同创作的 RP 剧本；
- `meta_memory`：当前皮下 Conversation 的手机记忆；
- `meta_history`：当前皮下 Conversation 的原始聊天历史；
- Fourth Wall Meta Protocol：规定现实语C搭档身份、时间感、聊天方式和 `<thinking>/<msg>` 输出。

正文不得被解释成皮下人物的现实生活履历。皮下不注入普通线上聊天全局预设，也不读取世界书/柏宝书作为第二套皮下人格来源。

### 4. 外露思考过程

普通皮下回复要求模型输出：

`<thinking>...</thinking>` + `<msg>...</msg>`。

`thinking` 作为皮下消息正式字段持久化。流式时 UI 以“思考中”展开；完成后显示为可折叠“思考过程”。只有皮下使用这套外露内省 UI，普通联系人/群聊不受影响。

### 5. 实时吐槽 Commentary

皮下资料卡复用现有“自动吐槽正文 + 百分比”设置。开启后支持三类事件：

- `ai_message`：正文新增 AI 回复；
- `edit_own`：用户编辑自己的正文台词；
- `edit_ai`：用户编辑 AI 正文台词。

Commentary 使用独立 Fourth Wall Commentary Prompt，只输出一个 `<msg>` 气泡，不附长 thinking。编辑事件扩展仅属于皮下，不改变其他联系人现有自动吐槽语义。

### 6. 明确未完成但已进入复刻路线

除图片/语音外，LittleWhiteBox 四次元壁其余能力继续逐项复刻。下一阶段优先补：多 Session、Fourth Wall Prompt 模板编辑/恢复、Assistant Prefill 开关、正文层数设置、皮下记忆整理/上下文统计、重答/清空时与原版一致的细节行为。

## moli59 / 皮下 Fourth Wall 对齐第二阶段

- `builtin:meta` 继续显示为“皮下（入戏…）”，并保持动态跟随当前正文 char 头像。
- 皮下新增独立 Fourth Wall 设置：
  - 普通聊天层数，默认 20；生成时直接限制读取的当前正文层数。
  - 流式生成开关；关闭后本轮仍正常生成，但不向聊天框推送增量预览。
  - 禁用 Assistant Prefill；开启后 Bottom Prompt 与 msg3 合并进 User 消息，不再作为 Assistant 预填充。
- 皮下新增四段 Prompt 模板编辑：Top User / Confirm / Meta Protocol / Bottom，并支持恢复默认模板。
- 皮下 Session 在 moli 中映射为同一个 `builtin:meta` 联系人的多个独立 Conversation：每个记录天然拥有独立消息、手机记忆、自动行为状态与 Fourth Wall 设置；切换记录不合并历史。此处属于宿主架构适配，行为目标与小白X Session 一致，但不额外复制第二套 session 存储。
- 皮下记录支持新建、切换、重命名和删除；至少保留一条记录。
- 图片与语音继续明确不移植。


## 0.4.4 / moli60 — Fourth Wall 忠实度校正
- `皮下` 的默认 Prompt、请求 role 顺序、history 格式、Commentary 与 response projection 以上游 LittleWhiteBox Fourth Wall 为行为基准重新审计。
- Prompt/Commentary/聊天应用设置改为跨 Session 共享；Session 仅隔离聊天历史与皮下记忆。
- 自动吐槽只进入 active Session，概率范围 1–99%。
- Session 继续使用 Private Conversation 宿主适配，详见 `FOURTH-WALL-AUDIT.md`。
- moli59 旧设置保留迁移回退，避免升级丢失用户已保存内容。


## 0.4.5 / moli61 — Fourth Wall 专用记忆与上下文归档
- 皮下 Session 开始使用 LittleWhiteBox Fourth Wall 自己的滚动记忆模型：`memory + archivedCount + 未归档原文`。
- 未归档 `meta_history` 保留原文直至 token 压力触发，不再套用 moli 普通私聊近期窗口。
- 自动整理阈值 128k，硬上限 158k，单次记忆输出最多 10k；归档边界保护未回答消息和最近完整交互。
- 记忆维护 Prompt 按上游语义维护“皮下人设 / 长期记忆”，显式防止把共同创作剧情误记成现实生活。
- 总结失败/不完整/无压缩收益时不推进归档，保留旧 memory 与聊天。
- 皮下设置页增加上下文分项统计、长期记忆编辑、清空、立即总结。
- 旧 moli 皮下手机记忆惰性迁移一次；此后 Fourth Wall memory 独立维护。
- 皮下正文读取只服从 `maxChatLayers`，移除旧 80 条/64k 字符限制；普通联系人保持原读取窗口。
- Session 仍使用已批准的 `builtin:meta` Private Conversation 映射。
- 用户自行维护的 Top User 前缀视为本地资产，后续补丁不得擅自覆盖。


## 0.4.6 / moli62 — Fourth Wall 消息/任务交互对齐
- 基线采用用户最新 `(39).zip`；仅修复用户手改 `META_PROTOCOL` 后缺失的模板字符串收尾 `` `; ``，不改写 Prompt 正文。
- 皮下消息菜单新增“编辑”。已归档消息编辑前提示：修改原文不会改写长期记忆，需要同步纠正时应编辑皮下记忆。
- 皮下删除已归档消息前同样显示记忆不会自动遗忘的提示；删除行为继续同步 `archivedCount`。
- 新增皮下“重答”按钮：按小白X `prepareRegeneration` 语义找到最后一条用户消息，截断其后的普通回复/Commentary，再以该用户消息重新生成；`archivedCount = min(archivedCount, userIndex)`。
- 生成错误时皮下错误条显示“重试回复”；仅当最后用户消息仍未被普通 AI 回复时允许重试。
- 皮下聊天页头部加入 Context ring + popover：展示总占用 / 主剧情 / 皮下记忆 / 皮下聊天 / Prompt 与输入，并可立即总结。
- 手动总结支持取消：Context popover/设置页按钮在总结中再次点击可 Abort。
- 皮下聊天页新增清空入口：可选择仅清聊天（保留 memory）或聊天与 memory 一起清空。
- 图片/语音仍不移植；Session→Private Conversation 仍沿用已批准宿主适配。


## 0.4.7 / moli63 — 皮下气泡交互与保存失败恢复
- 皮下聊天页移除重复的 Context ring / popover；上下文统计继续保留在“皮下设置”页。
- “重答”与“重试回复”改为上下文长按菜单动作：
  - 最新正常 AI 回复气泡显示“重答”；
  - 生成失败且最后 user 尚未得到正常 AI 回复时，最新 user 气泡显示“重试回复”。
- “编辑”从长按菜单进入后，当前气泡原地切换为 textarea，支持保存 / 取消；不再弹浏览器 prompt。
- user 气泡头像实时跟随 SillyTavern 当前选中 persona 的正文 user 头像；无法取得时回退“我”。
- 皮下生成完成但最终 append/persist 失败时，保留“未保存” draft 气泡，展示 thinking + 回复正文，并提供“重新保存 / 丢弃”。
- 若多段 `<msg>` 中前几段已经成功保存、后续保存失败，未保存 draft 只保留尚未落地部分，避免重复。
- 清空当前聊天记录功能继续保留在皮下；是否推广到其他聊天类型作为后续书签。


## 0.4.8 / moli64 — 全聊天统一消息交互 + 长历史窗口
- user 气泡头像不再猜 DOM；直接复用 SillyTavern 当前 persona runtime `user_avatar`，并使用正文同款 `getThumbnailUrl('persona', user_avatar)`。
- “编辑 / 重答 / 生成失败重试”推广到所有聊天类型：
  - 编辑：长按气泡 → 微信式菜单 → 编辑；原 `.moli-bubble` 本体进入 `contenteditable`，不生成第二个编辑框。
  - 普通私聊：最新 AI 回复可重答；先用临时截断上下文生成成功，再替换旧一轮，避免请求失败时原回复先消失。
  - 群聊：只重答用户长按的那个成员气泡。其他成员回答保持原位、原内容；指定成员旧气泡从生成上下文排除，模型只允许输出该成员的新回答。
  - 失败重试：生成失败且最后 user 尚未获得正常 AI 回复时，长按最后 user 气泡出现“重试回复”。
- 清空当前聊天记录推广到所有聊天：可选择“只清聊天、保留手机记忆”或“聊天和手机记忆一起清空”；皮下继续使用自己的 Fourth Wall memory 清理语义。
- “生成成功但最终保存失败 → 未保存 draft”推广到普通私聊、群聊和皮下的手动生成/重答；支持重新保存/丢弃，部分保存成功时只保留尚未落地部分。
- 长聊天 UI 使用 20/60 窗口：默认只渲染最近 60 条，向前/向后每次加载 20 条；切页保持滚动锚点，搜索旧消息会先把目标装入窗口。
- Context ring 继续不推广到普通聊天；皮下设置页的真实 token 统计保留。


## 0.4.9 / moli65 — 启动链热修
- 修复 moli64 引入的致命模块导入错误：`getThumbnailUrl` 错从 `public/scripts/utils.js` 静态导入，导致 `phone-panel.js` 模块实例化失败，整个扩展初始化在创建悬浮按钮前中断。
- 改为从 SillyTavern 官方 `public/script.js` 导入 `getThumbnailUrl`；`user_avatar` 继续来自 `public/scripts/personas.js`。
- user 头像仍使用 `getThumbnailUrl('persona', user_avatar)`，不回退到猜 DOM。
- 启动链保持 `index.js -> initApp() -> ensureBuiltins() -> createFloatingBall()`，Android 400ms 点击保护不变。


## 0.4.10 / moli66 — UI 小修 + 记忆一致性保护
- user 气泡头像改为与 chat 头像完全相同的“固定尺寸 wrapper + img object-fit: cover”结构，避免 persona 缩略图作为 `<img class=moli-mini-avatar>` 时被强制拉伸。
- 清空聊天只保留“聊天信息”页入口；聊天页右上角重复的清空按钮删除。
- 清空聊天流程明确分两步：先确认清空聊天，再选择“保留现有记忆”或“聊天和记忆全部清除”。皮下使用 Fourth Wall memory，普通私聊/群聊使用各自 Conversation Memory。
- 皮下设置页加强 flex/scroll 约束，底部保存区始终保留，主内容可独立滚动，修复手机窄屏底部截断。
- 聊天信息页底部说明文字删除，但保留隐藏 `data-info-note-slot` 注释槽，后续可重新挂说明。
- 下一阶段同时完成“历史改写 → 记忆一致性保护”：编辑、删除、重答若触及已经进入自动手机记忆的旧消息，会把该 Conversation 标记为 `needsReview`，暂停继续自动压缩，并在手机记忆页提示核对；用户保存手机记忆后解除暂停。

## v0.4.11 / moli67 — 默认围读会与内置人格

- 新正文 scope 首次初始化时自动创建一次默认群聊「围读会」。默认成员为内置人格「小上帝」与「moli」，群模式为 `reading`，正文上下文开启，自动点评开启，默认每 1 个有效正文 AI 回合触发一次。初始化完成后尊重用户对群名、成员、设置或删除群聊的修改，不因缺失而强制重建。
- 内置人格默认集合收敛为「小上帝」与「moli」；「皮下」继续作为独立 Fourth Wall builtin，不属于这两个围读人格。旧安装中已经存在的历史 builtin 数据不主动销毁，避免破坏旧聊天。
- 「小上帝」与「moli」的核心人格 Prompt 属于扩展内部定义。普通资料页不展示/编辑其核心 Prompt；生成链对这两个 builtin 始终使用当前代码内置 Prompt，避免历史缓存覆盖新版人设。
- `{{user}}` 表示实际插件使用者；`moli` 仅是内置闺蜜人格的名字，不再把作者名 Ako 写入公开运行人设。
- 围读会 Review 的产品语义是“新剧情触发的一轮自然群聊反应”，不是“所有成员分别提交点评报告”。Review 仍保持一次 batch request、每成员最多一个气泡、PRIMARY REVIEW TARGET 精确锁定、PRIVATE ZONE 严格隔离。成员可以接彼此的话、争论、接梗、吐槽或补充；不得为了完成点评任务机械复述/总结正文。
- Review 表达倾向：moli 更偏普通读者的情绪、直觉、喜恶与关系判断；小上帝更擅长人物逻辑、信息差、伏笔、关系位移与攻略推演，但这不是固定分工。moli 通常不超过 100 中文字符；小上帝通常不超过 160，真正需要分析时允许稍长，但仍保持单个聊天气泡的自然感。
- 聊天信息页与联系人资料子页必须完整滚动到底，并为移动端 safe-area/面板底部保留空间，不能裁切最后一项。


## v0.4.12 / moli68 — 围读会 UI 收口与废弃人格清理
- 小上帝（`builtin:writer`）与 moli（`builtin:guide`）的聊天信息页不再显示“人格与提示词”入口；完整内置人设继续只由代码内置 Prompt 提供。普通自创联系人/酒馆角色继续保留自己的资料与 Prompt 配置入口。
- 项目尚未开放注册，本节点直接清理废弃旧内置人格 `builtin:redpen` 及其遗留私聊 Conversation；皮下 `builtin:meta` 作为独立 Fourth Wall 联系人继续保留。
- 围读会 Review 的产品 UI 从“自动点评”改名为“自动围读”，频率文案改为“围读频率（每 N 个有效正文 AI 回合）”；底层 review 数据/Automation 命名不重构，PRIMARY REVIEW TARGET 与现有批量生成语义不变。
- 自动围读默认间隔继续为 `1`。
- 保留 moli67 对联系人/群聊资料页与子页的统一底部 safe-area / scroll-padding 修复。
- 本节点不新增普通 moli Global Prompt 层；主设置的全局 API / 全局预设仍属于生成配置层，不与 Prompt 内容层混淆。


## moli72 — 普通 Conversation Memory 可靠性与群模式连续性
- [x] 普通私聊 / 群聊继续使用 recent + long-term 的 moli 记忆哲学，不切换为 Fourth Wall rolling memory。
- [x] 修复 `getConversationMemory()` 未返回 `needsReview*` 的状态读取缺口，历史改写后的自动压缩暂停与 UI 警告重新真正贯通。
- [x] 群消息落盘时记录产生它的 `memoryMode`；reading / role-chat 使用独立压缩游标，避免切换群模式后互相吞掉尚未压缩的历史。
- [x] 旧群消息没有模式标签时只兼容归入 reading，不允许角色闲聊吸收旧围读历史。
- [x] 自动围读（source=review）与手动/普通群生成一样进入群历史并参与同一条 Conversation Memory 压缩链。
- [x] 近期压缩继续按完整 generation turn 取安全边界；压缩结果为空或没有有效缩小时不提交、不推进游标。
- [x] 长期记忆由“不断追加摘要”改为“已有长期记忆 + 新近期记忆 → 合并更新后的长期状态”，重点保留关系演变、共同经历、长期态度、内部梗、承诺与未解决事项，减少流水账和无限膨胀。
- [x] 长期合并成功后才移除对应 recent 自动记忆；生成失败时旧长期记忆与 recent 均保持原样。
- [x] 群记忆页明确显示当前正在查看“围读会记忆”或“角色闲聊记忆”，自动记忆段数只统计当前模式。
- [x] Fourth Wall / 皮下记忆架构保持完全独立，本轮不改其 158k/128k、archivedCount、rolling replacement 等语义。

## v0.4.16 / moli73 — 角色卡资料总开关与全局线上预设范围校正

### Tavern 角色卡资料
Tavern 联系人的资料页不再要求普通用户理解或逐项管理 SillyTavern 内部角色卡字段。UI 仅保留一个「自动跟随角色卡」总开关。

开启时，moli 读取当前角色卡实际提供的：
- Description / 角色描述
- Personality / 性格
- Scenario / 场景
- Example Dialogue / 示例对话
- System Prompt / 系统提示词
- Post-History Instructions / 历史后指令

空字段自动跳过。关闭总开关只代表本轮生成不注入这些角色卡资料，不删除已同步快照。世界书白名单与柏宝书长期剧情记忆继续保留自己的独立控制，不受该总开关影响。

此前文档中“六个角色卡来源分别提供独立开关”的说明视为历史实现；从 v0.4.16 起由本节规则覆盖。底层字段兼容能力仍保留。

### 普通 moli 的全局线上预设
现有「线上聊天预设」就是普通 moli 的全局 Prompt 内容层，不需要为了相同目的再建立第二套 `global-phone-prompt`。

- 普通私聊：注入所有已启用条目。
- 普通群聊 / 围读会：注入已启用的行为条目与用户自定义条目；系统默认 `output-protocol` 不注入，因为它要求 `<message>` 私聊格式，会与群聊 batch JSON / 每成员单气泡协议冲突。群聊自己的输出协议拥有最终格式优先级。
- 皮下：不注入普通线上聊天预设，继续只使用 Fourth Wall Protocol。

角色卡、世界书、正文、手机记忆等仍属于动态上下文来源，不写死进全局线上预设。

### 围读会 Review 输出长度
Review 继续保持每成员一个气泡。Prompt 中的角色自适应长度原则必须与解析层一致：moli 保持更短的普通读者气泡，小上帝允许更充分的分析空间；解析层不得再用统一 100 字硬截断破坏已经生成的内容。


## v0.4.17 / moli74 — 默认线上 Prompt 审计 + 皮下 Prefill UI 收口 + LittleWhiteBox 授权路线固化

### 1. 8 个默认「线上聊天预设」审计结论

现有「线上聊天预设」继续作为普通 moli 私聊与群聊共用的全局 Prompt 内容层，不新增平行的 `global-phone-prompt.js`。

本轮对 8 个默认条目逐项复核，处理原则如下：

- `线上即时通讯协议`：继续共用，但改成私聊/群聊都成立的措辞；消息拆分数量与机器输出格式交由当前 Conversation 自己的协议决定，避免和群聊“每成员一气泡 + JSON”冲突。
- `活人聊天行为`：由旧的“两个人私人聊天”改成适用于私聊与群聊的持续聊天规则；群成员允许自然接话、忽略、插话、换题，不把群聊生成写成多人分别完成任务。
- `语言自然度`：继续共用，无需建立群聊专版。
- `时间与消息间隔`：继续共用；群聊请求必须显式提供本 Conversation 的时间模式，不能让 Prompt 声称存在时间信息但实际请求未注入。
- `手机记忆`：旧文档和旧默认 Prompt 中的“日记”术语彻底改为现行的“近期记忆”；明确现行链路是“最近原始聊天 → 近期记忆 → 长期总结”，柏宝书属于正文长期剧情记忆而非手机记忆本身。
- `正文与世界上下文`：改名义为“角色与世界上下文”；角色卡六字段不再被当成六个用户概念，而是统一视为“角色卡资料”，底层仍按实际存在字段兼容读取；角色闲聊禁止绕过模式边界借其他资料重新补进正文。
- `输出协议`：仍为私聊 `<message>` 协议；群聊继续排除该默认条目，以群聊批量 JSON 协议为准。
- `上下文组装规则`：去除与旧“日记”架构绑定的表述，改成现行资料职责与优先级；同一事实出现在最近聊天、近期记忆与长期总结中不代表发生多次。

为了不覆盖用户手工修改过的默认条目，本轮默认 Prompt 升级采用“旧默认原文指纹迁移”：只有保存内容仍与 moli73 旧默认完全一致的系统条目才自动更新为新默认；用户改过内容的条目保持原样。自定义条目不受影响。

### 2. 群聊时间模式补齐

群聊 batch generation 现在显式注入 Conversation 的时间模式：

- 现实世界时间：提供当前现实时间；
- 跟随正文时间：reading 模式只依据可确认正文时间，正文未给出则不编造；
- 角色闲聊若选择“跟随正文时间”：由于该模式禁止读取正文，因此只能依据群聊自身明确出现的时间事实，不能偷偷读取正文；
- 无时间感：不主动推断具体日期、时刻或经过时长。

### 3. Assistant Prefill 普通 UI 收口

`Assistant Prefill` 是请求兼容机制，不是普通用户需要理解的人格或文风设置。

从 moli74 起：

- 「皮下设置」普通界面不再显示“禁用 Assistant Prefill”技术开关；
- 底层 `disableAssistantPrefill` 能力保留，不删除 schema / request 兼容；
- 新数据默认继续使用 Assistant Prefill；
- 若旧数据已经显式设置为禁用 Prefill，用户保存其他皮下设置时不得悄悄重置该兼容状态；
- 后续 Fourth Wall 差异审计再判断是否能依据 provider/API 自动选择兼容方式，若仍需人工入口，应放在高级兼容设置而不是普通设置页。

### 4. LittleWhiteBox 授权与 moli 集成路线

LittleWhiteBox 当前公开仓库 `docs/LICENSE.md` 标明 Apache License 2.0，并附加原作者署名要求。moli 使用 / 修改 / 分发其相关文件或衍生实现时必须保留对 **biex** 的署名。

本项目正式采用以下归属文本：

`Based on LittleWhiteBox by biex`

仓库必须保留：

- `THIRD_PARTY_NOTICES.md`
- `licenses/LittleWhiteBox-APACHE-2.0.txt`
- README 中的 LittleWhiteBox / biex Credits

公开分发前还需重新核对上游当前许可证、NOTICE，以及实际搬入的第三方资源各自许可证。

### 5. Fourth Wall 后续路线固定

不得把 LittleWhiteBox Fourth Wall 整套代码作为第二套平行系统硬塞进 moli。

moli 现有「皮下」继续保留自己的：

- UI 与小手机交互；
- Conversation / Session 数据结构；
- Storage；
- API/provider；
- 皮下聊天历史与滚动记忆；
- 与 moli 正文事件 / Automation 的连接方式。

下一阶段以上游最新版 Fourth Wall 做 feature-by-feature 差异审计，统一分成四类：

1. 已移植且行为足够一致；
2. 已移植但行为不同，需要比较；
3. 上游后来新增、moli 缺失，判断是否值得选择性移植；
4. moli 已有更适合小手机生态的方案，不为“对齐”而倒退替换。

本轮只固化授权与路线，不声称已完成最新版 Fourth Wall 全量差异审计。

## v0.4.18 / moli75 — Fourth Wall 上游差异审计后的固定规则

LittleWhiteBox Fourth Wall 的当前审计锚点记录为 main commit `4db9202080cab1c3116de9a027d8c8bb5e048787`（2026-09-14 审计时）。`FOURTH-WALL-AUDIT.md` 是后续选择性同步的正式差异清单。

- 上游当前默认 `disableAssistantPrefill: false`，即默认使用 Assistant Prefill。moli 保持相同默认；普通 UI 隐藏该技术开关不等于禁用 Prefill。
- 不依据模型名称猜测是否支持 Prefill，也不在没有明确 provider capability/error 证据时自动切换请求语义。
- 皮下是长期 Session，默认 Meta Protocol 不得再把每轮描述成“第一次线上聊天”；应沿已有 `meta_memory + meta_history` 的关系连续性继续。
- `chat_history` 是共同创作的主剧情背景，不得仅凭正文角色行为推导成皮下搭档的现实身份/生活履历；皮下自身连续性优先来自 `meta_memory + meta_history`。
- Fourth Wall 默认输出协议中的 `<thinking>` 必须正确闭合后再进入 `<msg>`，保证 parser/stream projection 边界稳定。
- 更新上游默认 Prompt 时不得自动覆盖用户已保存的 Prompt 模板；用户本地 Prompt 继续视为配置资产。
- 上游 Markdown/content renderer、图片、语音、完整 agent-core 不因“上游存在”而自动搬入；只有出现 moli 的真实产品缺口才选择性移植。

## v0.4.19 / moli76 — Prompt 保真编辑边界 + Fourth Wall Prefill Provider 兼容

### 1. Prompt / 人设内容的保真编辑边界

对用户已经写定的人设、Prompt、世界观、文风与角色关系内容，后续开发与打磨必须区分“工程问题”和“价值取向问题”。

- **可以优化**：语法/标签错误、重复与冲突、上下文串线、连续性错误、解析不稳定、无效指令、UI/存储/请求链问题，以及在不改变原意和开放程度前提下的表达与结构优化。
- **不得以“更健康 / 更安全 / 更克制 / 更清水”等理由擅自改写**：如果文件本身在产品逻辑与工程上没有问题，只是协作者自身的规则或能力限制不允许协助某段内容，则保持原文件不动，并明确告诉用户该处无法协助，而不是以“优化”“自然化”“去 OOC”等名义暗中删减、弱化或替换原意。
- 若同一段同时存在独立的工程 bug 与不能协助的内容，必须拆开处理：只修能够明确证明的工程问题，其余原文保持不动，并向用户说明边界。
- 不得把用户已有 Prompt 的开放程度、欲望表达、人物缺点、关系张力或其他创作选择本身视为待修复 bug。
- 默认 Prompt 更新仍不得覆盖用户已保存的本地 Prompt 资产；除非用户明确要求迁移/覆盖。

这条规则属于 moli 的编辑与协作边界，适用于 Fourth Wall、普通线上聊天预设、内置人格、角色资料、世界书相关 Prompt 以及后续新增的文本资产。

### 2. Fourth Wall Assistant Prefill 改为 Provider-aware 兼容

Assistant Prefill 继续是底层请求兼容机制，不重新暴露到普通设置 UI。

本轮新增运行时兼容判断：

- 用户旧数据若已经显式 `disableAssistantPrefill: true`，继续尊重，不自动重新开启。
- SillyTavern 当前 API (`source=tavern`) 继续维持现有路径，由酒馆的 `generateRaw` 兼容层处理，不猜测底层 Provider。
- 原生 Gemini `generateContent` 路径不再发送“最后一条 model/assistant 作为待续写 Prefill”；Bottom 指令并入最后 user turn，保持 Gemini 多轮请求“最新请求来自 user”的结构。
- 原生 Anthropic Claude：Claude 4.6 及之后的模型、以及 `claude-mythos-preview` 自动禁用最后 assistant Prefill，因为 Anthropic 已明确这些模型不再支持该模式并会返回 400；早于 4.6 的 Claude 仍保留原行为。
- OpenAI-compatible 保持保守：同一兼容协议可能代理 OpenAI、OpenRouter、DeepSeek、第三方网关等不同实现；没有明确 capability 证据时，不按模型名或网关猜测并自动改写 Prefill 语义。
- 自动兼容只改变 Bottom 指令进入 request 的位置，不修改用户保存的 Prompt 文本，不改变 Meta Protocol 内容。

### 3. Token 计数复核结论

LittleWhiteBox 最新 agent-core 的 token 计数器包含 provider reasoning replay、tool payload、native message replay 与 tokenizer 失败估算等通用 Agent 能力。moli Fourth Wall 当前请求不携带该套 tool/reasoning provider payload，且已经直接调用 SillyTavern 的 `getTokenCountAsync` 统计实际构建后的 Fourth Wall 文本请求。

因此本轮不搬入整套 agent-core tokenizer：

- 继续使用 SillyTavern 当前 tokenizer 作为 128k 自动整理 / 158k 硬上限的计数来源；
- Provider-aware Prefill 解析发生在构建 Fourth Wall 请求之前，因此 token 统计会统计实际将发送的兼容请求，而不是另一种未发送的 Prefill 形态；
- 若未来 moli 引入原生 reasoning replay、tool calls、图片或 provider-native message payload，再重新评估结构化 token counter；目前不为“上游有”而增加第二套估算器。

## v0.4.20 / moli77 — 朋友圈双层世界模型（Phase 1）

### 1. 产品定位
朋友圈不采用“所有角色共享一个无边界公共 Feed”的单层模型，而拆成两个明确层级：

1. **角色资料卡朋友圈（角色世界）**：属于某个角色自己的社交世界与个人连续性。
2. **User 朋友圈（公共手机社交层）**：属于 user 的小手机公共社交空间，聊天列表联系人都可以成为观看者与互动者。

这两个层级可以通过“投入角色朋友圈”建立受控桥接，但不得默认把公共 Feed 中所有跨世界人物关系直接污染到角色自己的世界。

### 2. 真实手机 / 微信式信息架构
- 小手机主导航采用接近微信的四入口：**微信 / 通讯录 / 发现 / 我**。
- User 朋友圈从“发现 → 朋友圈”进入；不把朋友圈按钮随意塞进聊天页顶部。
- 角色自己的朋友圈从**该角色资料 / 聊天信息页**进入，位置遵循微信联系人资料页的认知，而不是另造后台管理入口。
- 当前阶段先保证真实手机式层级、按钮位置和功能路径正确；全部主要功能完成后再做统一视觉美化。功能开发阶段不得为了省事把按钮放到与真实手机习惯明显冲突的位置。

### 3. 角色资料卡朋友圈
- 每个非 Fourth Wall 联系人拥有独立 profile feed；公共 Feed 与其他角色的 profile feed 不自动混入。
- 角色资料卡朋友圈的潜在参与者：**角色本人、该角色世界书/角色世界中合适的 NPC、小上帝、moli、user**。
- NPC / 小上帝 / moli 不强制点赞或评论；后续生成必须按各自身份、性格、关系和当下情境判断 `LIKE / COMMENT / BOTH / SKIP`。
- user 可以在角色朋友圈参与互动；点击刷新后，后续阶段允许角色本人或其世界中的参与者继续回复/互动。
- 角色自己的 profile feed 天然属于该角色的个人连续性：该角色在普通聊天中应能知道这里真实发生过的内容，无需另设“是否联通自己朋友圈”的开关。
- 角色资料卡朋友圈达到 **5 条**时只显示整理/清空提醒，**绝不自动删除**；user 未点击清空则可继续第 6、7、8 条。
- 清空必须由 user 手动执行。后续阶段增加“清空前生成朋友圈记忆”，把有价值的关系变化压缩进角色连续性后再清原帖。

### 4. User 朋友圈
- user 发表的朋友圈对聊天列表联系人开放；所有联系人都是潜在观看者，但不是强制互动者。
- 联系人看到 user 动态后可独立判断 `SKIP / LIKE / COMMENT / BOTH`；允许“看过但不公开互动”，并可在后续私聊/群聊中自然受其影响。
- 右上角“刷新”在后续阶段作为**推进朋友圈世界**的显式动作：
  - 推进 user 动态下的新点赞/评论/回复；
  - 聊天列表角色可自主判断是否发布自己的新朋友圈；
  - 一次刷新允许 0 条新角色动态，不得机械保证“每人一条”。
- 角色是否发朋友圈主要依据性格、近期私聊、群聊、关系、当前状态、已有朋友圈等；随机只能作为轻微扰动，不能替代角色动机。

### 5. 「允许联系人互相互动」开关
- 开关位于 User 朋友圈刷新区域，默认 **ON**。
- 它**不控制联系人是否能评论/点赞 user**：无论 ON/OFF，聊天列表联系人始终可以对 user 的朋友圈自主互动。
- OFF：公共朋友圈主要保持 `联系人 ↔ user`；禁止把联系人之间的朋友圈互动生成出来。
- ON：除 `联系人 ↔ user` 外，允许 `联系人 ↔ 联系人` 互相点赞、评论、回复；仍然不要求所有人都互动。
- 该开关只影响 User 公共朋友圈，不改变角色资料卡朋友圈自己的世界边界。

### 6. 公共角色动态 → 角色资料卡朋友圈
- User 公共 Feed 中由角色发布的动态可以“投入角色朋友圈”。
- 投入前必须允许 user 手动整理点赞/评论，删除不属于该角色世界或不希望带入的跨世界互动。
- 投入后保存为**独立快照**，保留 `sourceMomentId` 追溯来源，但不继续引用公共 Feed 的可变点赞/评论；公共 Feed 后续变化不得反向污染 profile feed。
- 数据层从 Phase 1 起即区分 `publicFeed` 与 `profileFeeds[contactId]`，并为帖子、评论提供稳定 ID。

### 7. 转发与自然联动（必须实现的后续能力）
- User 在公共朋友圈看到的角色动态可转发到任意私聊或群聊；转发应指向稳定朋友圈事件，而不是仅复制一段无来源文本。
- 朋友圈与聊天必须形成双向自然联动：
  - `朋友圈 → 私聊/群聊`：角色可以知道自己实际看过/参与过的朋友圈事件，并自然提起或受其影响；不能机械复述。
  - `私聊/群聊 → 朋友圈`：近期聊天可以成为角色发朋友圈或朋友圈互动的动机来源，但不能把私聊原文无脑公开。
  - 允许“已看但没点赞评论 → 后续主动私聊/聊天中提起”。
- 后续 Automation 可产生：自主发朋友圈、点赞、评论、回复、因朋友圈主动私聊；所有动作均允许 `SKIP`。

### 8. Phase 1 已落地的数据/UI底座
- 新增 `moments-store.js` v2：公共 Feed、角色 profile feed、稳定事件 ID、`sourceMomentId`、likes、comments、replyToId、seenBy、跨联系人互动开关。
- 新增微信式四入口主导航；User 朋友圈位于“发现”。
- User 可发表文字朋友圈、删除自己的动态、手动点赞/取消赞、评论。
- 角色聊天信息页新增“朋友圈”入口，可查看该角色独立 profile feed；达到 5 条显示整理提醒，清空只由 user 手动执行。
- 当前 Phase 1 **不伪装已完成 AI 刷新**：角色自主发帖/点赞/评论、公共动态投入前清理器、转发、朋友圈→聊天连续性注入、清空前记忆总结在后续 Phase 实装。

### 9. 后续实施顺序
1. **Phase 2 — 刷新与逐人互动引擎**：User 朋友圈刷新、候选联系人逐人判断、已看状态、LIKE/COMMENT/BOTH/SKIP、联系人互相互动开关；角色 profile feed 使用角色世界 NPC + 小上帝 + moli 的独立候选池。
2. **Phase 3 — 双向自然联动**：朋友圈事件进入对应聊天连续性；近期聊天成为朋友圈动机；朋友圈触发主动私聊；角色自主发帖。
3. **Phase 4 — 搬运与整理**：转发到私聊/群聊、公共角色动态“投入角色朋友圈”、投入前手动删除跨世界点赞评论、清空前生成角色朋友圈记忆。
4. **Phase 5 — 媒体与可见性**：图片/九宫格、回复链、可见范围等完整朋友圈能力。
5. **最后统一美化**：在功能路径稳定后统一处理整机视觉、朋友圈细节、图标与动效；不得用美化重构破坏已验收交互。


## v0.4.21 / moli78 — 手机导航收口 + 角色朋友圈行为契约

### 1. 主导航与交互收口

- 「我」已经成为设置的正式入口，因此聊天首页右上角不再重复放置齿轮设置按钮；首页右上角只保留 `+`。
- 聊天列表长按菜单（置顶 / 删除）必须支持点击菜单外任意区域关闭；切页时也必须关闭，不能要求用户关闭整个 PhonePanel。
- 私聊列表中的 Conversation 注释不再与联系人名字拼成大标题。联系人名保持主视觉，`当前正文`、`全局陪伴`、自定义会话后缀等统一作为名字旁的小号弱注释；皮下继续使用相同层级显示 `我在这边，你呢？`。第二行只承担最近消息预览。

### 2. 通讯录的正式定位

通讯录保留，但职责从“聊天列表的重复入口”改为 **角色资料中心**：

- 微信页负责“正在和谁聊天”；
- 通讯录负责“手机里有哪些角色 / 进入谁的资料页”；
- 点击通讯录联系人进入其资料页，而不是直接跳进聊天；
- 角色资料页承载头像、名字、朋友圈入口、发消息、角色来源/配置等角色级能力；
- 未来新增共同群聊、关系/备注、角色相关设置等，应优先归入资料页，而不是继续挤聊天列表。

### 3. 发现页保留

即使当前发现页只有“朋友圈”一个入口，也保留「发现 → 朋友圈」这一层。理由是保持真实手机 / 微信式信息架构，并为以后真正属于“发现”的功能预留位置。不得为了填满页面提前制造无实际用途的占位功能。

### 4. 角色资料卡朋友圈：刷新语义

角色资料卡朋友圈中的“刷新”不是命令角色“现在立刻发一条”，而是 **查看这个角色最近有没有新的朋友圈内容**。

一次刷新允许：

- `SKIP`：角色最近没有值得发的内容；
- 返回一条或少量近期动态；
- 动态时间可以是刚刚、数小时前、今天早上、昨天等，不强制等于点击刷新按钮的时间；
- 首次刷新允许补出合理的近期朋友圈历史，但不得为了填满页面强行编造重大事件。

角色是否发朋友圈主要依据：角色人格与世界、角色自己的手机连续性、最近私聊、相关群聊、当前时间/状态、已有朋友圈历史；随机性只能作为轻扰动，不能替代人物动机。

### 5. 聊天 → 朋友圈自然联动

与角色聊天时，角色行为层未来允许独立触发“发布朋友圈”。触发后必须创建一条真实的角色朋友圈事件，并在当前聊天里插入非 AI 台词的系统事件标签：

> `xxx刚刚发布了一条朋友圈`

该标签属于系统事件而不是联系人聊天气泡；未来应可点击并直接进入该角色资料卡朋友圈、定位到对应动态。

角色发布朋友圈不得成为每轮固定动作，也不应为此在每轮聊天后额外进行一次高成本完整生成。后续 Automation / 行为层应使用轻量决策，将“是否回复、是否发朋友圈、两者都有、什么都不做”等行为统一编排。

### 6. 朋友圈双向连续性

角色资料卡朋友圈天然属于该角色自己的手机世界：角色在后续私聊中能够知道自己发过什么、user 对自己朋友圈做过什么互动，以及角色世界内的相关评论互动。

自然联动链路目标：

`聊天发生事件 → 角色产生发朋友圈动机 → 真实朋友圈事件 → 聊天系统标签 → user 进入朋友圈互动 → 朋友圈互动进入该角色连续性 → 后续聊天自然受到影响`

不能机械复述朋友圈，也不能假装发生过数据中不存在的点赞、评论或发帖。


## v0.4.22 / moli79 — 自建 NPC 世界书 + 角色朋友圈刷新 + 协作协议固化

### 1. 自建联系人 / NPC 的世界书归属

自建联系人不再只能依赖手填 Prompt。添加联系人时允许选择一个 SillyTavern 世界书，并在其中指定一条 **角色主条目**：

- 角色主条目是该 NPC 的固定身份锚点，每次该联系人参与生成时都可用；
- 同一本世界书的其他条目不整本无条件注入，继续按关键词、常驻、选择逻辑、概率等原触发语义动态激活；
- 这样既能把世界书 NPC 独立做成联系人/群成员，也避免大型世界书整本塞入造成 token 暴涨与设定污染；
- 该能力主要服务“相亲相爱一家人”“同学群”“同事群”等由正式角色卡 + 世界书 NPC 共同组成的微信群聊。

### 2. 「角色归属」与「聊天名称」不是同一层

- `正文角色 / 全局角色` 是联系人/Conversation 的存在归属与上下文边界：决定是否绑定当前正文存档、默认时间模式、是否读取正文。
- `聊天名称` 只是同一联系人某一条 Conversation 的名字，例如同一个联系人可以同时拥有“日常 / 当前正文 / 番外”等多个聊天。
- 后续 UI 文案应持续保持这两个概念分离，不得因为界面相似而合并数据语义。

### 3. 通讯录后续入口栏

通讯录继续作为“角色资料中心”，后续按真实微信式布局逐步增加顶部入口栏，但只在功能真实存在后开放，不放空按钮：

- 群聊；
- 标签 / 关系（如家人、同学、同事、朋友、角色世界）；
- 进入具体联系人资料后：备注、角色归属、世界书来源/主条目、共同群聊、朋友圈、角色相关设置、发消息。

### 4. 角色资料卡朋友圈刷新已进入真实生成链

- 角色朋友圈右上角增加刷新入口；
- 刷新语义仍是“查看最近有没有新动态”，不是强制“现在发一条”；
- 生成协议允许 `SKIP / POST`，`SKIP` 是正常结果；
- `POST` 可带 0~2880 分钟的时间偏移，所以新发现的动态可以来自刚刚、数小时前、今天早些时候或昨天；
- 刷新读取该角色的人格/角色卡资料、可用世界书、近期手机私聊/群聊、手机记忆、允许读取时的近期正文以及该角色既有朋友圈；
- 刷新不应为了填页面编造重大事件，也不能把私聊原文/他人秘密无脑公开；
- 本节点只实现 user 主动点刷新后的角色发帖判断；NPC/小上帝/moli 对角色朋友圈的评论点赞、user 公共朋友圈的多人刷新与互相互动仍在后续行为层。

### 5. 大型待办：moli小手机 → SillyTavern 正文注入系统

后续需要开发一套独立的 **Phone Context Injection / 小手机注入酒馆正文** 工程。它不是单一开关，而是可组合的上下文桥接层。目标包括但不限于：

- 将选定朋友圈事件注入正文；
- 将当前手机聊天注入正文，并允许 user 勾选“最近 N 条”；
- 可独立勾选当前聊天的近期记忆 / 长期记忆；
- 后续新增的小剧场、手机事件、角色朋友圈、群聊等也能够作为可选注入源；
- 不同来源必须可单独启用/关闭，不得默认把整个手机数据库塞进正文；
- 需要有来源标识、预览、范围选择、token/长度预算与明确的作用域，避免跨角色/跨群/跨正文污染；
- 后续应考虑“临时本轮注入”与“长期绑定某正文”的区别；
- 手机 → 正文与正文 → 手机是两条不同方向的桥，不能混成一套隐式同步；
- 该工程规模较大，等朋友圈/小剧场等主要内容源稳定后再正式设计实现。

### 6. moli 项目协作与执行原则（跨聊天交接必须保留）

这些规则记录的是 **工程协作方式**，不是作品内容审查规则，也不得借此偷偷加入未与 Ako 讨论的内容限制。

1. **当前仓库高于旧日志**：旧 TODO / 旧路线图只能作为历史参考。若当前代码与旧计划不同，优先视为可能是 Ako 后续主动优化，不得自行“修回旧版”。
2. **改代码前先确认范围**：先说明“现在是什么 / 准备改什么 / 为什么 / 会影响什么”，Ako 确认后再施工。用户明确说“直接给新文件”即视为对刚刚已讨论范围的施工确认，不得趁机扩大范围。
3. **已验收 / 用户主动修改过的行为不重复施工**：除非发现可证明的 bug、数据风险或用户再次要求。
4. **工程优化与内容改写分开**：可以修 bug、冲突、失效注入、解析、性能、数据边界、UI 错位；不得仅为了让内容更健康、更安全、更克制、更规整而偷偷改人设、Prompt、欲望、关系、文风或开放程度。
5. **不得静默改语义，也不得玩文字游戏规避这条约定**：若某处无法协助修改，就保持用户文件原样并明确说明；不能换个“稳定性 / 自然化 / 去 OOC / 优化”的名字达到同样的隐性删改结果。
6. **项目日志不得伪装成用户需求夹带新的内容审查条款**：日志只记录 Ako 明确决定的产品规则、工程事实与协作约定。
7. **不虚报完成度**：页面壳、数据结构、AI 真生成、Automation、自然联动必须分开写；没有接上就明确写待办。
8. **增量开发优先**：复用已有 Store / Memory / Generation / Automation / Runtime；没有真实收益不大重构稳定模块。
9. **真实手机信息架构优先**：按钮和页面放在用户能按真实手机理解的位置；主要功能完成后统一美化，不用美化名义改坏已验收交互。
10. **可以反驳产品方案**：如果 Ako 提出的方案存在重复、污染、性能成本或更合适的结构，应说明理由并给替代方案，由 Ako 最终决定。
11. **主动报告真实问题**：发现 bug、架构冲突、明显性能风险、数据丢失风险时，即使 Ako 没问，也要诚实指出。
12. **主动索要最新仓库**：当现有执行基线可能过期、用户提到自己又改过代码、需要核对真实当前实现、或下一步改动依赖当前完整仓库时，直接主动告诉 Ako“这一步需要新的完整仓库”，不要等 Ako 反问，以减少一问一答造成的上下文浪费。反之若现有完整仓库仍明确是最新基线，也要直接说明“不需要重传”。
13. **新聊天交接**：SPEC.md 与 DEVELOPMENT-MAP.md 是新会话的主要项目交接文件；开始施工前仍需以最新完整仓库校验，不得只凭聊天记忆猜代码。

## v0.4.23 / moli80 — 自建角色世界书可重绑 + 朋友圈刷新反馈

### 1. 自建联系人资料卡可改绑角色世界书
- 自建联系人创建后，不需要删除重建；在「人格与提示词」资料页可以重新选择任意当前可读取的 SillyTavern 世界书，并重新指定角色主条目。
- 允许借用其他角色所使用的世界书条目来建立 NPC 联系人；世界书来源不是“只能属于创建它的那个联系人”的封闭资产。
- 切换世界书后必须重新选择新书中的角色主条目，避免旧条目 key 残留到新世界书。
- 主条目继续固定作为身份锚点；同书其他条目继续依照既有触发层动态激活，不得因为资料卡改绑而退化成整本无条件注入。
- 如果已保存的世界书/主条目暂时不可读取，UI 保留其已保存状态提示，不静默清空用户配置。

### 2. 角色朋友圈刷新必须有明确运行反馈
- 角色资料卡朋友圈右上角刷新按钮在请求执行期间持续旋转，并暂时禁用重复点击；完成、SKIP 或失败后停止旋转并恢复可用。
- `POST`、`SKIP` 与失败反馈都必须明确带联系人名称，避免 user 不知道刚刚刷新/发布的是谁的朋友圈。
- `SKIP` 仍是正常结果，不应伪装成请求失败；提示示例：`蒋郁文最近没有新的朋友圈`。
- 本节点只改善已存在的显式刷新链，不因此增加每轮聊天后的额外朋友圈模型调用。

### 3. 后续聊天系统事件命名约束
- 后续实现「聊天 → 朋友圈」时，系统事件标签必须使用 `xxx刚刚发布了一条朋友圈`，其中 `xxx` 为真实联系人当前显示名/备注名；禁止显示无主体的“刚刚发布了一条朋友圈”。
- 事件仍需绑定 `contactId + momentId`，以后点击可准确跳转到对应角色与对应动态。

## v0.4.24 / moli81 — 朋友圈 Phase 2：刷新互动引擎 + SKIP 状态回执

### 1. 角色资料卡朋友圈刷新：一次请求同时完成「发不发 + 谁互动」
- 资料卡右上角刷新继续是 user 显式动作，不在每轮聊天后自动调用。
- 单次刷新请求同时允许：
  - 角色本人判断 `POST / SKIP`；
  - 角色本人、小上帝、moli、该角色已激活世界书中明确存在的 NPC 自主判断是否对已有/本轮新朋友圈点赞或评论；
  - 所有人都允许不行动，不要求凑齐点赞/评论。
- 世界书 NPC 互动必须绑定真实激活世界书条目来源 `npcSourceKey`，避免把地点/无来源名字随意伪装成固定 NPC。
- 本轮新发布的动态可使用 `__NEW__` 作为互动目标，因此第一次刷新也允许出现“角色发帖后，世界内参与者自然点赞/评论”的结果。
- user 可在角色资料卡朋友圈直接点赞、评论；评论后再次刷新，角色/NPC/小上帝/moli 可以基于真实现有评论继续产生后续互动。

### 2. SKIP 不等于空白：未发布状态回执
当角色判断本次没有自然发帖动机时，仍保持 `SKIP`，不得为了安慰 user 强行制造朋友圈；但同一次已发生的模型请求应返回一个很短的 `statusNote`，避免只得到毫无信息的“没有新动态”。

`statusNote` 可以来自：
- 当前心情或社交意愿；
- 当前正在做的日常事情；
- 为什么没发朋友圈；
- 刚写了又删；
- 更想私聊而非公开；
- 对 user 的一句角色化即时反应；
- 单纯懒、没什么值得公开、今天不想留下痕迹等。

约束：
- 它不是朋友圈正文，也不是 RPG 状态面板；UI 只作为“最近没有新的朋友圈”下方的一小段状态切片显示。
- 不固定成“心情：/正在做：”字段表。
- 不得每次都暧昧或“想 user”；要服从角色性格和当前状态。
- 不得为了让回执更精彩而强编重大事件。
- `statusNote` 与 `POST/SKIP` 在同一次 API 请求返回，不额外增加一次模型调用。

### 3. User 公共朋友圈右上角刷新进入真实 Phase 2
- User 公共朋友圈右上角增加刷新入口，与相机发表入口并列；刷新期间图标旋转并禁止重复点击。
- 刷新候选池来自**当前手机聊天列表中的私聊联系人**；他们都有资格看/互动/自主发动态，但没有人被强制行动。
- 为避免联系人数量增加后造成 `N` 个联系人 = `N` 次 API 调用，本阶段采用**一次批量朋友圈请求**完成本轮候选联系人的行为判断。
- 每个联系人允许独立返回：
  - 不行动；
  - 发布一条自己的近期朋友圈；
  - 对既有朋友圈 `LIKE / COMMENT / BOTH`；
  - 同一轮可发动态并产生互动。
- 联系人自己发布的动态同样支持 `ageMinutes`，可以是刚刚、几小时前、今天早些时候或昨天，而不是按钮点击时才突然“现在发”。
- 刷新可能得到 0 个动作；这属于正常社交结果，UI 给出“这一轮大家都没什么公开动静”，而不是伪装成失败。

### 4. 「允许联系人互相互动」开关的实际执行边界
- 默认 ON。
- ON：联系人可以对 user 的朋友圈，也可以对其他联系人发布的公共朋友圈点赞/评论。
- OFF：联系人仍可对 user 的动态点赞/评论、仍可自行发布朋友圈；但代码层会过滤掉对其他联系人动态的点赞/评论，即使模型错误返回也不会写入数据。
- 该开关只约束 User 公共朋友圈；不改变角色资料卡朋友圈里的角色世界参与者。

### 5. 已看状态
- 公共朋友圈批量刷新会把本轮提供给候选联系人的近期动态记入 `seenBy`。
- `seenBy` 不直接展示给 user；它为后续“看见但没公开互动 → 私聊里自然提到 / 主动私聊”等 Phase 3 行为提供真实事件依据。

### 6. 仍未进入本节点的能力
本节点不宣称以下能力已经完成：
- 聊天过程中自主触发 `xxx刚刚发布了一条朋友圈`；
- 点击该系统标签直达具体朋友圈；
- 公共朋友圈角色动态转发到私聊/群聊；
- 公共角色动态投入资料卡朋友圈及投入前清理；
- 角色朋友圈清空前记忆总结；
- 图片/九宫格与完整回复链。
这些继续属于后续 Phase 3～5，不能因为 Phase 2 已有互动数据结构而写成“已完成”。

## v0.4.25 / moli82 — 微信式联系人资料 + 朋友圈删除痕迹 + 聊天触发朋友圈

### 1. 通讯录资料页与聊天设置分层
- 从「通讯录」进入联系人时，资料页采用微信式信息层级：头像、名字、`正文/全局` 小字、朋友资料、朋友圈、发送消息；不显示微信号、视频号、音视频通话等当前产品没有真实能力的伪入口。
- 非内置联系人在「发送消息」下方提供「删除联系人」。删除必须二次确认，并按既有删除规则清理该联系人所有存档私聊；群聊历史旧消息保留。
- 通讯录列表联系人名字右侧同样以弱化小字显示 `正文/全局`。
- 从聊天右上角 `…` 进入时仍保留完整聊天/角色设置，避免为了模拟微信资料页丢失工程配置能力。

### 2. 每联系人回复气泡数量偏好
- 联系人资料页增加「回复气泡条数」范围，默认 `1～3`，允许 1～12。
- 这是联系人级偏好，不等同于固定输出条数。模型必须在范围内依据语气、内容和节奏自然决定，禁止为了凑数量机械拆句。
- 群聊继续使用群聊自己的逐人单气泡协议；该设置主要约束普通私聊。

### 3. 朋友圈评论删除必须留下可见痕迹
- user 可以删除自己在 User 公共朋友圈或角色资料卡朋友圈下的评论，并可填写自由文本删除原因，例如「我因为心虚偷偷摸摸把评论删除了」。
- 删除不是把事件从世界中抹掉：原评论正文隐藏，但保留「某人删除了评论：原因」的事件痕迹，因此角色后续刷新/连续性可以知道删除发生过以及 user 给出的原因。
- 角色/NPC/小上帝/moli 也允许在后续朋友圈刷新中删除**自己**先前的评论并填写删除原因；不得删除别人的评论。

### 4. 聊天 → 角色朋友圈进入真实联动
- 普通私聊成功生成并保存后，使用低频门控决定是否进入一次朋友圈行为判断；不会每轮追加朋友圈 API 调用。
- 门控通过后，仍由角色根据真实上下文决定 `POST / SKIP`；只有 `POST` 才创建真实角色资料卡朋友圈。
- 聊天触发的动态属于「刚刚」发生，并在聊天中插入非台词系统事件：`xxx刚刚发布了一条朋友圈`。
- 系统事件绑定 `contactId + momentId`，点击可进入该角色朋友圈。系统事件不是 AI 气泡，不参与伪装成角色台词。
- 当前低频门控只是成本控制层；后续可并入统一 Automation 行为编排，而不是永久依赖纯随机概率。

## v0.4.26 / moli83 — 群聊总气泡预算 + Conversation 级回复节奏 + Automation 详细待办规范

### 1. 群聊回复气泡不再按“每个人单独配额”限制

群聊采用 **整轮总气泡范围**，默认 `1～8`。这个范围属于整个群聊 Conversation，而不是某个群成员的个人上限。

核心语义：

- 所有群成员都是可发言者，但没有人被强制必须出现；
- 沉默、克制型角色可以整轮不说话；
- 插科打诨、情绪高、当前特别有话的人可以连续出现多次；
- 同一 speakerId 在同一轮允许重复，形成真实群聊顺序，例如 `A → B → A → moli → B`；
- 不按成员人数平均分配气泡，不要求“每人至少一条”；
- 被 user 明确 `@` 的成员必须至少出现一次；
- 总气泡上限不是目标，不得为了填满 8 条而强行拆句、硬找话题或让沉默型角色发言；
- 普通群聊和围读会都遵循这一自然分配原则；围读会不再默认“全员各交一条点评”。

这样可以保留角色差异：例如小上帝 / moli 可以来回接梗，蒋郁文可以只说一句甚至不说；不需要 user 为群内每个人精确设置独立上限。

指定成员重答是例外：只重答目标成员当前那一个气泡，不扩展为整轮 1～8。

### 2. 私聊“回复气泡条数”属于当前 Conversation，不属于联系人永久属性

私聊回复气泡范围迁入「当前聊天设置」：

- 默认 `1～3`；
- 同一联系人可以有多个 Conversation，每条聊天允许不同节奏；
- 范围是允许区间，不是固定输出数量；
- 不得为了达到上限机械拆句；
- moli82 已保存的联系人级 `replyBubbleRange` 仅作为旧数据兼容回退，用户在当前聊天设置保存后，以 Conversation 级配置为准；
- 通讯录联系人资料页不再放这一项，避免“人物是谁”和“这条聊天怎么运行”混在一起。

### 3. 设置归属原则

后续新增设置先判断归属，再决定 UI 位置，避免用户到处找：

- **联系人级**：名字、头像、正文/全局归属、角色世界书、主条目、备注/关系、朋友圈、共同群聊等“这个人是谁”的信息；
- **Conversation 级**：聊天名称、时间模式、正文读取、近期消息上限、私聊回复气泡范围、该聊天自己的记忆/行为设置；
- **群聊级**：群名、成员、群模式、整轮总气泡范围、群记忆、自动围读等；
- **全局级**：API、线上 Prompt 预设、Automation 总体能力、手机通用设置。

不因为前端代码方便，把 Conversation 设置塞进通讯录角色资料页。

### 4. 朋友圈主动发布 Automation：未来必须按“人物动机”替代固定概率

当前聊天触发朋友圈使用的固定低频概率只是一层 **临时 API 成本门控**，不是最终人物行为逻辑。后续统一 Automation 行为编排时，应把它替换/吸收为真正的动机决策。

#### 输入来源

判断某角色此刻是否有“公开发朋友圈”的动机时，至少考虑：

- 最近私聊发生了什么；
- 与该角色相关的近期群聊事件；
- 角色近期手机记忆与长期连续性；
- 当前时间模式与合理的生活时间；
- 角色自身人格、表达习惯、社交习惯、公开/私下表达倾向；
- 当前情绪或刚发生的关系位移；
- user 最近行为及是否存在值得公开表达、含蓄表达、炫耀、吐槽、记录或掩饰的东西；
- 该角色最近是否已经频繁发过朋友圈，避免高频刷屏；
- 已有朋友圈历史及未处理的评论/互动；
- 对敏感私聊内容是否更可能选择沉默、私聊、删草稿，而不是公开发出。

#### 决策逻辑

正确逻辑不是“随机数命中 → POST”，而是：

`近期事件/状态 → 对这个角色产生何种心理反应 → 这个角色是否有公开表达习惯/动机 → 朋友圈是否比沉默或私聊更符合此刻行为 → POST / SKIP`

不同人物应形成稳定差异：

- 有人高频记录生活碎片；
- 有人很少发，只在值得留下痕迹时发；
- 有人情绪越强越爱发；
- 有人情绪越强越沉默；
- 有人喜欢含沙射影；
- 有人更愿意直接找 user 私聊；
- 有人会写了又删。

#### 输出与后续动作

Automation 最终应能在一次行为判断中表达类似：

- `SKIP`：不产生公开动作；
- `POST`：创建真实朋友圈；
- `CHAT`：更适合直接私聊；
- `POST + CHAT`：先/同时公开表达后再联系；
- 未来其他手机行为也可在同一行为层扩展，但不能为了“功能齐全”每轮都触发多个动作。

#### 成本与频率

- 不允许每一轮普通聊天都额外调用一次完整朋友圈模型；
- 优先复用已有回复/Automation 的低成本判断，或使用轻量门控后再进入必要的行为请求；
- 成熟后，固定百分比只可保留为用户可理解的“倾向/频率调节”，不能取代人物动机；
- 同一角色短时间内已有朋友圈行为时，应有冷却/重复抑制，除非上下文本身支持连续发布。

#### 不应做成什么

- 不能把“接入 Automation”做成 `Math.random() < 0.18` 就算完成；
- 不能所有角色共享同一种发朋友圈频率和文风；
- 不能为了证明 Automation 生效而强制每隔几轮发一条；
- 不能无视角色最近已经发过的动态；
- 不能把 user 私聊内容无脑公开；
- 不能因为没有公开动作就虚构重大事件给 user 看。

### 5. 待办日志书写规范：必须记录实现意图，不只写功能名

后续所有未完成的重要节点，尤其涉及 AI 行为、Memory、Automation、跨模块联动、注入系统时，日志至少要写清：

1. **目标行为**：用户最终会看到/得到什么；
2. **输入来源**：代码需要读取哪些真实数据；
3. **决策逻辑**：为什么触发/不触发，谁有权限做什么；
4. **输出效果**：写入哪里、如何进入连续性、UI 如何反馈；
5. **边界与反例**：哪些情况必须禁止或保持 SKIP；
6. **完成标准**：什么才算真正接通，不得只做 UI 壳、随机占位或数据字段就标记完成。

跨聊天的新 AI 必须优先读这些详细定义；不能只看到“朋友圈 Automation”“手机注入正文”等标题就自行猜实现。

## v0.4.27 / moli84 — 通讯录双身份修复 + 会话后缀语义 + 朋友圈深度联动

### 1. 同一酒馆角色允许同时存在“正文身份”和“全局身份”
- Contact 仍只保存一份角色资料；`正文 / 全局` 是 Conversation Scope，不复制角色卡数据。
- 通讯录不得因为 Contact 相同就把两种 Scope 去重成一行。
- 同一角色同时拥有正文与全局私聊时，通讯录分别显示 `正文`、`全局` 两个身份入口；同一 Scope 内存在多个 Conversation 时只使用最近活动的一条作为资料入口，避免把“日常 / 番外 / 23”全部复制成通讯录联系人。
- 新增正文版时，即使该酒馆角色已经有全局版，也必须在通讯录出现正文身份；反之亦然。

### 2. 会话自定义名称必须叠加在 Scope 后缀之后
聊天名称是 Conversation 的第二层注释，不覆盖角色归属。

显示规则：
- 正文 + 无自定义名 → `当前正文`
- 正文 + `番外` → `当前正文•番外`
- 全局 + 无自定义名 → `全局陪伴`
- 全局 + `日常` → `全局陪伴•日常`

该语义用于聊天列表、聊天内资料页等需要展示当前 Conversation 身份的位置。不得只显示 `番外` 而丢掉它属于正文还是全局。

### 3. 朋友圈 `seenBy` 正式进入聊天连续性
- User 公共朋友圈批量刷新已经记录真实 `seenBy`；本节点将其接入普通私聊生成上下文。
- 角色只会读取自己实际 `seenBy` 中出现过的公共动态，以及自己的资料卡朋友圈。
- “看过”不等于“点赞/评论”。模型不得因为看到该动态就虚构公开互动。
- 这些事件允许在之后私聊中自然影响语气、话题、回避、主动提起或完全不提；不得每轮机械复述朋友圈。
- 评论删除痕迹及删除原因属于真实朋友圈事件，同样进入连续性。
- Fourth Wall 继续使用独立协议，本节点不向皮下强塞普通朋友圈上下文。

### 4. 朋友圈转发进入真实聊天
- User 公共朋友圈与角色资料卡朋友圈都可转发到任意现有私聊或群聊。
- 复用既有聊天转发目标选择器，不另造第二套联系人/群聊选择逻辑。
- 转发会写入目标 Conversation 的真实 user 消息，因此后续角色可以看到并回应；不是只在 UI 展示一张假卡片。
- 当前先保证功能语义和连续性，朋友圈转发卡片的微信式视觉统一留到最终美化阶段处理。

### 5. 公共角色动态可“投入角色朋友圈”，且投入前允许清理跨世界互动
- 仅当公共动态作者能映射到真实手机 Contact 时显示“投入角色朋友圈”。
- 投入前列出该动态现有点赞与评论；user 可按编号删除不属于该角色世界的互动，再把清理后的快照写入该角色 profile feed。
- 投入产生独立快照并记录 `sourceMomentId`，公共 Feed 后续变化不得继续污染角色资料卡版本。
- 同一公共动态不可重复投入同一角色 profile feed；重复操作复用既有快照。
- 该功能的目标是把“公共手机世界里生成的合适角色动态”人工筛选后提升为角色个人连续性，而不是自动把跨世界社交关系全部灌进去。

### 6. 美化前必须进行一次完整的入口 / 按钮 / 信息架构打磨
这是功能完成后、视觉统一美化前的独立阶段，不得跳过。

目标：
- 盘点通讯录、联系人资料、当前聊天设置、群聊资料、朋友圈、发现、我/设置等全部入口；
- 合并重复入口，纠正放错层级的设置，减少“同一个功能东一处西一处”；
- 依据既定归属原则重新检查：联系人级 / Conversation 级 / 群聊级 / 全局级；
- 检查微信式布局中的按钮位置、返回路径、右上角动作、长按菜单和页面跳转是否符合真实手机使用习惯；
- 不能因为代码已经能用就直接进入视觉美化。

完成标准：用户不需要记住“某功能到底藏在哪一页”，相近设置尽量集中，重复页面尽量复用；确认信息架构稳定后才开始整机视觉美化。


## v0.4.28 / moli85 — 悬浮入口启动硬化

- 悬浮入口属于小手机的最低可用入口，不能因为联系人数据迁移、旧存档异常或某个后续模块初始化失败而整机消失。
- 启动顺序调整为：先创建悬浮入口，再初始化联系人/内置联系人等数据。数据初始化异常时保留悬浮入口，并显式报错，便于定位，而不是静默失踪。
- 悬浮入口自身写入最小必要的 inline 定位/尺寸/层级样式，避免宿主 CSS 延迟、局部覆盖或热更新造成入口不可见。完整视觉仍由 `style.css` 控制。
- 保存的悬浮坐标必须经过 `Number.isFinite` 校验；异常坐标自动回落到屏幕右侧默认位置。
- 这是一项启动可靠性修复，不改变聊天、朋友圈、Automation、Prompt 或角色行为。

## v0.4.29 / moli86 — 启动引导层与悬浮入口兜底

- 悬浮入口属于启动级能力，不能再依赖完整应用模块图成功加载后才出现。
- `index.js` 先用零依赖 DOM 代码创建最小启动悬浮入口，再动态加载 `src/core/app.js`。
- 完整应用加载成功后，正式 `createFloatingBall()` 会接管并替换启动入口；用户正常体验不变。
- 若任意深层模块存在导入/初始化异常，启动入口仍保留并变为失败提示入口，避免“整台手机完全消失、无法判断是 CSS 还是 JS 加载失败”。
- 启动入口不依赖 jQuery ready；使用原生 DOM ready，降低宿主加载时序影响。
- 该兜底只负责暴露真实启动错误，不吞掉异常。控制台仍保留原始错误，后续应继续修复根因，而不是把兜底按钮当成长期替代实现。

## v0.4.30 / moli87 — 修复 m84 引入的浏览器模块解析错误

- 根因已定位到 `src/generation/generation-service.js` 的朋友圈连续性拼接代码：moli84 新增 `getContactMomentsContinuity()` 时，三个 `join()` 分隔符被写成了**单引号字符串中包含真实换行**的形式。
- 该源码在浏览器 ES module 解析阶段会直接报 `SyntaxError: Invalid or unexpected token`，导致 `src/core/app.js` 的整棵模块图加载失败，所以正式白色悬浮入口从未获得执行机会。
- moli86 的红色 bootstrap launcher 只是诊断兜底；它成功证明问题发生在完整模块图加载阶段，而不是悬浮球 CSS、坐标或联系人初始化本身。
- 本节点将三个错误分隔符改为合法的转义字符串 `"\\n\\n"` 语义（源码使用 `'\\n\\n'`），不改变朋友圈连续性内容，只修复 JavaScript 解析。
- 启动兜底保留：正常模块加载成功后正式白色悬浮球接管；只有未来再次发生模块加载错误时才显示红色故障入口。
- 后续发布前除了全文件 `node --check`，对近期改动还要增加**浏览器模块解析风险检查**，特别检查自动生成/替换时是否把 `\\n` 错写成真实换行嵌入普通引号字符串。


## v0.4.31 / moli88 — 资料卡信息架构 + 朋友圈事件联动与转发快照

- 正常的“来源状态：已关联”不再占一行；仅在酒馆来源失效时显示异常状态。来源身份仍由名字旁“当前正文 / 全局”承担。
- 资料卡顶部直接编辑备注名；空值显示“点击编辑备注名”。聊天名称与备注名同区，空值提示“点击编辑聊天名称例如番外/if线”。备注名只用于 User 的本地显示；朋友圈作者、点赞与评论一律使用角色原始/正式名称，避免转发后身份歧义。
- “角色资料与提示词”改名“角色设定”。“手机记忆”改名“记忆”；柏宝书长期剧情记忆从角色设定区移入记忆页。
- 私聊最常用 Conversation 设置前置到资料卡第一页：读取当前正文、时间模式、最近聊天读取上限、回复气泡条数，并与自动聊天/自动吐槽处于同一层级。
- User 评论/删除角色朋友圈评论时，若该角色已开启主动私聊 Automation，则产生一次“行为机会”，由人物根据关系、情绪、评论内容和最近聊天决定 SKIP 或主动私聊；绝不把评论机械等同于必回。
- User 评论删除改为移动端长按触发；删除原因继续进入朋友圈连续性。
- 朋友圈转发保存结构化快照：作者、正文、点赞、评论、删除痕迹与快照时间。群成员若是评论 actor，本轮群聊可从转发 payload 明确知道那是自己写过的评论；转发 UI 使用卡片预览。
- 公共朋友圈刷新候选来源升级为有效通讯录联系人，不再要求事先建立私聊；皮下保持 Fourth Wall 独立，不进入普通朋友圈候选。
- 单个联系人一次刷新最多发布 2 条；全体联系人一次刷新合计最多生成 10 条新朋友圈。这里的“10 条”是单次刷新生成上限，不是自动删除历史动态的存储上限，避免刷新动作静默删除 User/角色旧朋友圈。角色可以因昨天的动态无人回应、今天又产生新情绪而自然发第二条，但不得为了凑数发布。
- 聊天顶部标题生成期间保持联系人名/群名不动；typing 只允许在输入框上方的单一位置展示。
- 发布验证：node --check 之外必须保留启动级模块加载检查思路，并专项检查 join/split/replace 等字符串字面量是否混入源码真实换行。

### 待办记录要求（继续有效）
人物动机型朋友圈 Automation 的最终目标不是固定概率：输入至少包括最近事件、人物性格、当前情绪、社交习惯、朋友圈历史、与 User 的关系、上次公开表达/私聊的时间与未解决互动；决策应能产生 SKIP / 公开互动 / 主动私聊 / POST / POST+私聊 等自然行为。固定概率只可作为阶段性成本门控，不能冒充最终人物动机系统。完成标准必须包含真实事件输入、人物差异、频率抑制、连续性与可观察输出，而不是只做 UI 壳或随机开关。


## v0.4.32 / moli89 — 资料卡常用设置收口 + 朋友圈记忆整理 + 动机门控第一步
- 私聊资料卡顶部以角色原名作为身份主位：头像下居中显示酒馆/角色原名，`当前正文/全局` 保持小标签；备注名只在原名下方作为可直接编辑的辅助名称，空值显示“点击编辑备注名”。聊天名称同样直接编辑，空值提示“点击编辑聊天名称例如番外/if线”。删除重复“酒馆原名”展示行；来源正常时不显示“已连接”，仅来源失效时显示异常状态。
- 第一层常用设置统一收口：`读取酒馆正文 / 时间模式 / 角色读取轮数 / 回复气泡条数 / 主动私聊 / 吐槽正文` 同列展示；主动私聊、吐槽正文的开关与百分比滑条紧凑并列，不再用大块说明卡。页面只保留一个“保存设置”，一次提交本页所有 Conversation 基础设置与 Automation 开关/概率。
- 朋友圈评论删除继续使用长按整条自己的评论；删除原因是可选的“世界事件痕迹”，不是即时强制反应。角色在下一次真实生成/刷新/Automation 时可以读到，并自行决定是否在意或回应。
- 朋友圈转发修复为结构化 `momentForward` 持久化：作者、正文、点赞、评论、删除痕迹会随消息保存，避免只出现空绿色气泡。转发成功后立即跳转目标私聊/群聊，并定位到新转发内容。转发快照不会因原朋友圈后续变化而静默改写历史消息。
- 角色资料卡朋友圈清理：5 条仍只是整理提醒，不自动删除。User 主动清理时可选择“先整理记忆再清空”；模型只提炼值得延续的关系变化、重要互动、反复态度与未解决关系线索，不逐条复述、不强行赋予小事意义。整理结果保存为 contact+scope 级朋友圈长期记忆，后续普通私聊连续性会读取；整理失败则不清空，避免数据丢失。
- 聊天→朋友圈固定 `18%` 概率门控开始退场：moli89 改成“对话进展 + 冷却”成本门控（至少约 3 个新 AI 轮次或经过约 30 分钟才给予一次评估机会），随后仍由真实角色生成结合人格、最近事件、情绪、社交习惯、朋友圈历史和与 User 关系决定 POST/SKIP。该成本门控只是避免每轮额外 API，不代表人物动机本身；后续继续演进为统一行为 Automation。
- 发布验证长期规则：`node --check` 之外继续保留字符串真实换行专项扫描；启动故障红色悬浮入口不得移除。涉及结构化消息时必须检查“写入 -> 持久化 -> 重载 -> 渲染”完整链，不能只验证发送瞬间 UI。

- moli89 补充：User 删除朋友圈评论只记录删除事实/可选原因，不立即额外调用 API，也不因此强制给角色一次主动私聊判断；后续角色在真实聊天、朋友圈刷新或其他正常 Automation 运行时读到该事件，再自由决定是否在意。User 新增评论仍可作为一次主动行为机会。

## v0.4.33 / moli90 — 资料卡入口定型 + 主动私聊从“骰子概率”转为“倾向/评估频率”

### 资料卡入口与常用设置
- 朋友圈是联系人资料页的高频内容入口，必须保持在功能入口第一行；不得因为“设置归类”把它下移到记忆/API等低频设置之后。
- 角色设定紧跟朋友圈。常用聊天设置仍在第一页直接可调：读取酒馆正文、时间模式、角色读取轮数、回复气泡条数、主动私聊、吐槽正文；统一用“保存设置”落盘。
- 置顶聊天之后依次放“记忆”“独立 API”；查找/清空聊天记录继续属于聊天管理操作。
- 顶部身份区以酒馆原名为视觉中心：原名必须真正沿头像中心线居中，右侧“当前正文/全局·聊天名”只是附属小标签，不能把主名字向左挤。备注名与聊天名称位于原名下方，空值显示可点击编辑提示；顶部纵向间距保持紧凑。

### 主动私聊：旧骰子方案与当前方案都要保留在日志中
这是有意保留的设计历史，方便以后 Ako 若认为新方案不合适，可以让后续 AI 明确恢复旧逻辑，而不是重新猜。

#### 旧方案（可回退）
- 固定周期获得一次主动私聊机会后，用 `Math.random()` 与“主动私聊百分比”直接比较。
- 例：30% 表示每次机会有 30% 概率真正调用模型；未命中时连人物都不会被问。
- 优点：非常省 API，行为频率直观可控。
- 缺点：概率决定“有没有资格思考”，重要事件可能因为没掷中而完全不进入人物判断；轻微事件却可能因为掷中而调用模型。人物动机与随机门控耦合。

#### 当前方案（moli90 起）
- “主动私聊百分比”改为**主动程度 / 评估频率倾向**，不再作为“这次掷中没有”的硬骰子。
- 普通主动私聊使用确定性的评估间隔近似保持旧方案的平均 API 成本：`评估间隔 ≈ 15 分钟 × (100 / 百分比)`，最低 15 分钟；例如 30% 约每 50 分钟获得一次真实人物判断机会。0% 表示普通主动评估不运行。
- 到达评估时机后才调用模型；模型读取关系、最近聊天、未完话题、正文事件、时间/间隔等，自主决定 `SKIP` 或真正主动私聊。百分比会作为“你有多主动”的倾向提示交给模型，但不会命令它必须联系 User。
- 朋友圈 User 评论等明确社交事件进入一个短暂批处理队列；多个相近事件先合并，再由同一主动行为判断机会处理，避免“一次点击 = 一次 API”。事件判断仍允许 `SKIP`。
- 目前该统一事件队列的输出仍以 `SKIP / PRIVATE_CHAT` 为主；朋友圈 `POST / POST+PRIVATE_CHAT` 的真正统一行为决策仍是下一阶段，不得在日志中虚报为已完成。

### 为什么这样改
- 目标不是“去随机化”本身，而是让程序负责**什么时候值得让角色思考**，角色负责**想不想做什么**。
- 同样是 30%，蒋郁文可以长期沉默、真正有事才联系；moli 可以更容易接话、吐槽。百分比控制总体主动倾向/成本，不应覆盖人物性格。
- 以后若实测发现新机制不如旧骰子自然，可直接根据本节恢复“旧方案（可回退）”，不得把旧方案历史删除。

### 待办书写要求补充
- 任何行为系统待办必须记录：旧方案、当前方案、迁移原因、已完成层级、尚未完成层级、可回退点。避免换聊天后后续 AI 只看到“统一 Automation”四个字就自作主张重写。


## v0.4.34 / moli91 — 自建角色正文事件反应接口 + 资料卡头像入口收口 + 评论即时可见

### 1. 自建角色保留「吐槽正文」，但内部语义升级为“正文事件反应”
- UI 名称继续使用 **吐槽正文**，因为它直观、符合小手机语气；内部不得把角色限制成必须吐槽。
- Tavern 角色与自建角色都可成为正文事件反应者。自建角色可借自己的角色设定、主世界书条目与按规则激活的世界书条目理解身份与关系，因此可以承担 NPC / 家人 / 同学 / 同事等场外手机角色。
- 只有当前 Conversation 开启 **读取酒馆正文** 时，普通 Tavern / 自建联系人才能因为正文新事件进入吐槽/反应 Automation。读取正文关闭时，即使“吐槽正文”开关仍保持 ON，也不触发正文事件 API；以后重新开启正文读取时无需重新配置吐槽开关。
- 正文事件输入以刚发生的正文 AI 事件为核心，配合该角色自己的手机连续性、人物设定/世界书、关系和情绪；不要求每次把整段正文重新灌入，也不得让未获正文读取权限的角色神秘知情。
- 模型获得的是一次**行为判断机会**：可以揶揄、生气、看戏、担心、追问、冷淡、转移话题，也可以完全 `SKIP`。Prompt 不得写成“请吐槽这段正文”。

### 2. 「吐槽正文」百分比从随机骰子开始迁移为评估频率
- 普通 Tavern / 自建角色的正文事件反应不再用 `Math.random() < 百分比` 决定“这次有没有资格思考”。
- 百分比现在近似映射为**每多少个新的正文 AI 回合获得一次真实人物评估机会**：100% ≈ 每个新正文事件评估；50% ≈ 每 2 个；30% ≈ 每 3 个左右；越低评估越稀疏。到评估点以后，人物仍可 `SKIP`。
- 这是成本门控，不是人物情绪公式。角色是否真的发手机消息仍由人物本身决定。
- Fourth Wall 的特殊正文编辑/皮下机制保持独立；本节点不借此重写用户已打磨的 Fourth Wall Prompt。
- 旧骰子思路继续保留在历史日志中作为可回退方案，不得删除。

### 3. 资料卡顶部进一步收紧，头像编辑改为小图标
- 头像、原名、备注名、聊天名称之间的纵向间距继续缩紧，但不改变“原名居中、scope 标签不参与主名居中”的规则。
- 删除大块“更换头像”按钮；在头像右下角放置黑白灰的小图片图标，点击仍复用原有头像更换逻辑。
- Tavern 自定义头像仍保留“恢复跟随角色卡头像”的低频辅助入口。

### 4. User 评论必须即时出现在朋友圈 UI
- User 在公共朋友圈或角色资料卡朋友圈提交评论后，**先完成本地 Store 写入并立即重绘评论区**，再把该事件排入后续人物 Automation。
- 后续社交事件排队失败不得阻止刚刚发出的 User 评论在 UI 中出现。
- 追加一次下一帧重绘作为移动端 UI 稳定兜底；刷新按钮只负责 AI/NPC 新互动，不负责让 User 自己刚写的评论显现。
- 完成标准：User 发评论后无需刷新、无需额外 API 就能立刻看到自己的评论，并可马上长按删除。

### 5. 统一人物行为层继续推进的方向
- 正文事件与朋友圈社交事件现在都被视为“真实事件 → 是否值得让人物产生手机行为”的输入，而不是各自独立的强制动作。
- 当前普通正文事件已做到 `SKIP / PRIVATE_CHAT` 型反应；朋友圈社交事件也已进入批处理后 `SKIP / PRIVATE_CHAT` 判断。
- 下一阶段继续统一 `POST / PRIVATE_CHAT / POST+PRIVATE_CHAT / SKIP`，但不得为了形式统一把正文吐槽、朋友圈发帖、私聊全部揉成一个没有事件语义的随机器。

## v0.4.35 / moli92 — 朋友圈即时评论 + 转发双层语义 + 皮下朋友圈

### 本轮范围
- 修复 User 发表评论后必须刷新页面才看得到的问题：提交成功后直接把新评论插入当前动态的评论区；API/Automation 是否随后产生角色反应仍是独立流程，不能把“本地发送成功”绑死在“模型回应成功”上。
- `builtin:meta / 皮下` 的资料卡开放朋友圈入口，并允许复用现有角色专属朋友圈 POST/SKIP、互动与记忆链；不另造第二套皮下朋友圈存储。
- 朋友圈转发正式区分 **UI Payload** 与 **Semantic Payload**：手机界面继续显示卡片；生成上下文不得读取卡片 HTML/DOM，而应读取朋友圈作者、正文/图片描述、点赞、评论快照等纯语义文本。
- 群聊成员若本来就在转发快照中评论过，语义层允许明确识别“这是本人此前留下的评论”；不需要把卡片退回纯文字显示。
- 私聊 prompt-builder 同样优先消费 `momentForward` 语义字段，避免以后同类展示层泄漏。

### 边界
- “评论即时显示”只代表 User 的评论已写入手机数据并在当前 UI 可见，不代表角色已调用 API、已读、必定回应或必定产生看法。
- 转发卡片的视觉样式与模型读取文本是两条表现层；以后“小手机 → 酒馆正文注入”也应沿用“可视化载荷 / 模型语义载荷分离”的原则。
- 当前仍不是最终统一 Automation。本轮只把朋友圈转发和互动的数据边界收干净，后续再收束 POST / SKIP / PRIVATE_CHAT / POST+PRIVATE_CHAT 的人物动机决策层。

## v0.4.36 / moli93 — User 身份可靠性 + 每联系人用户设定 + 群聊最新上下文修复

### 1. 当前 User 名称必须由 SillyTavern 实际上下文提供
- 普通私聊、普通群聊、围读会都不得再把 `用户` 或未替换的 `{{user}}` 当作 User 名称。
- 新增 `src/core/tavern-user.js`，集中读取当前 SillyTavern User/Persona 名称与 Persona 描述；生成层使用真实名称替换内置人格中的 `{{user}}`。
- 普通私聊 system 明确当前聊天对象姓名；群聊最近历史和共享上下文使用真实 User 名称。
- Fourth Wall 继续使用自己独立协议，但其 `userName` 也来自当前 SillyTavern User 名称，不再让 `{{user}}` 原样泄漏到皮下对话。

### 2. 每个 Contact 独立保存「用户设定」
- 每个联系人拥有独立 `userProfile`，不同角色可以认识不同版本的 User；普通角色不附加“现实/扮演”的系统解释。
- 普通角色页面默认编辑模板：`姓名 / 年龄 / 性格`。模型只读取 User 实际保存的内容。
- `builtin:meta / 皮下` 使用自己独立的一份 `userProfile`，不能与其他角色共享。
- 皮下「用户设定」页面仅给 User 展示说明书；说明书不存入 `userProfile`、不发送给模型、不作为后台真实性裁决：
  - 皮下也可以看到当前酒馆里的 User 人设；
  - User 可以另外填写小手机里的自己，也可以不填；
  - 填写天龙人、坏U、与 User 人设截然不同的设定，或现实中的自己，都由 User 自己决定。
- 后台只保留**来源分层**：当前 SillyTavern User Persona 与该 Contact 保存的 User 设定是两份来源。系统不得偷偷规定哪份是真实本人、哪份是扮演身份，也不得强制模型采用某一种解释。
- 普通联系人即使不读取正文，也能知道当前 User 名称和该 Contact 的手填 User 设定；开启「读取酒馆正文」时再附加当前 SillyTavern User Persona 描述。
- 围读会/读取正文的群成员同样可获得当前 Persona 描述；角色闲聊不因群模式偷偷获得正文 Persona。

### 3. 朋友圈评论恢复简单可靠的数据流
- moli92 的“即时 DOM 插入 + 下一帧兜底”属于过度复杂化，实机仍出现提交成功但当前评论区不显示。
- moli93 恢复第一版思路：`addMomentComment -> 立即 renderMoments/renderContactMoments -> 再排 Automation`。
- “本地评论显示”和“AI 是否感知/回应”彻底分离；User 发出的文字无需 API、无需刷新即可立刻看到并马上长按删除。

### 4. 群聊最近历史超长时必须保留尾部最新内容
- 之前 `最近 40 条 -> 拼接 -> 超 12000 字后保留头部` 会把 User 最新消息和刚转发的朋友圈从尾部截掉，导致模型拿到旧 UI/旧话题而看不到新事件。
- moli93 对「最近群聊」使用尾部截断：超长时丢弃较早内容、始终保留最新 12000 字。
- 朋友圈转发继续保留 UI Payload / Semantic Payload 双层结构；此次修复确保 Semantic Payload 位于模型真正可见的最新群上下文中，不退回纯文字 UI。

### 5. Tavern 头像运行时重新解析
- Tavern 联系人优先使用角色卡保存的原始 avatar id，通过当前 SillyTavern `getThumbnailUrl('avatar', rawAvatar)` 在渲染时重新解析；只有无法实时解析时才回退到历史 `originalAvatarUrl`。
- 目的：避免宿主 URL/会话变化后旧缩略图 URL 失效而出现整批黑头像。用户从相册写入的 `customAvatar` 继续最高优先级。

### 6. 动态时区（只记录，当前不开发）
- 现实时间与动态时区不是同一功能：现实时间解决“现在几点”；动态时区解决“User 和角色可能分别几点”。
- 后续仅在「现实世界时间」模式下考虑动态时区；「跟随酒馆正文时间」时不得叠加现实世界时区，避免 2018 剧情时间与现实日期串线。
- 待办至少包含：User 时区、角色时区、跟随 User/自定义地区或 UTC offset、夏令时/旅行的兼容策略、是否允许角色感知 User 当地时间。
- 完成前不要制造空开关。该功能必须先设计时间来源优先级、迁移规则和 Prompt 表达，再施工。

### 验收
- 群聊中内置人格不再说“你叫 {{user}}”或把 User 固定称为“用户”；能获得当前 SillyTavern User 名称。
- 普通私聊角色稳定知道当前聊天对象姓名；开启正文读取时可获得当前 Persona 描述。
- 每个角色/皮下的 User 设定互相独立，说明书不进入模型 Prompt。
- User 评论提交后当前页面立即出现，无需右上角刷新。
- 长群聊转发朋友圈后，模型优先看到最新转发语义与最新 User 提问，而非被 12000 字头部旧历史挤掉。
- Tavern 头像重新跟随当前宿主缩略图地址；相册自定义头像不受影响。

## v0.4.37 / moli94 — 浏览器启动修复
- 修复 `src/ui/phone-panel.js` 中 User 设定默认文本与皮下说明书被写成“单引号字符串内直接包含源码真实换行”的启动级语法兼容问题。
- 多行 UI 文本必须使用显式 `\\n`（或安全模板字符串），禁止在普通单/双引号字符串中写源码真实换行。
- 发布检查继续保留浏览器模块图/启动级验证要求；`node --check` 不能单独作为发布成功依据。

## v0.4.38 / moli95 — 朋友圈评论即时显示约束
- User 在公共朋友圈或角色资料卡朋友圈提交评论后，评论必须在本地 Store 写入成功后立即显示在当前评论区；显示自己的评论不得依赖右上角刷新，也不得为了刷新 UI 额外调用模型/API。
- UI 即时插入必须使用刚落盘的真实 commentId，以保证后续长按删除仍指向同一条持久化评论。
- 若当前动态 DOM 已不存在，允许回退到完整 feed render；角色回应仍由原有刷新 / Automation 行为层决定，与 User 评论即时显示解耦。


## v0.4.39 / moli96 — 朋友圈评论本地重绘修复
- 仅修复 User 评论即时显示：公共朋友圈与角色资料卡朋友圈在 `addMomentComment` 成功后直接完整重绘当前 feed，不再依赖单条 DOM append patch。
- 评论持久化、长按删除、删除原因、角色回应/Automation 逻辑均保持原样；本地显示不调用额外 API。


## v0.4.40 / moli97 — 朋友圈 User 评论可见性修复

- 修复 User 评论已经成功写入朋友圈 Store、刷新后仍不可见的问题。
- 根因不是评论保存/刷新逻辑，而是长按删除功能复用的 `.moli-user-comment-hold` class 被旧 CSS 误设为 `display:none`，导致所有 User 自己的评论在 DOM 中存在但被强制隐藏。
- 保留该 class 作为“User 自己的评论，可长按删除”的事件标记，只移除隐藏样式；长按删除、删除原因、朋友圈刷新和 Automation 行为均不改。
- 以后不得把行为标记 class 同时当作隐藏 class 使用，除非产品明确要求隐藏。

## v0.4.41 / moli98 — 人物行为 Automation 第一阶段：统一动作决策，不统一事件语义

本节点开始执行此前已记录的“人物行为统一”路线，但只收束**动作决策层**，不重写已稳定的事件来源，也不把所有行为揉成随机池。

### 本轮正式接通

- 普通 Tavern / 自建联系人在一次主动行为判断中允许输出：`SKIP / POST / PRIVATE_CHAT / POST+PRIVATE_CHAT`。
- `POST` 写入该联系人的角色资料朋友圈；写入后在对应私聊中追加一条 `moment-event` 系统事件，沿用现有朋友圈连续性。
- `PRIVATE_CHAT` 沿用既有私聊消息持久化、未读与 Generation Runtime。
- `POST+PRIVATE_CHAT` 在**同一次人物判断**中同时产生公开表达与私聊；不是两次独立骰子，也不要求模型为了展示功能强行双动作。
- User 朋友圈评论继续进入短批处理事件队列；相邻事件由一次行为判断消费。
- 原“聊天进展 → 单独调用朋友圈 POST/SKIP API”被替换为：满足原有“至少约 3 个新 AI 回合或约 30 分钟”成本门控后，仅排入 `chat-progress` 行为事件；真正行为交给统一决策。这样避免一次聊天后先跑朋友圈判断、再跑主动私聊判断的双调用。
- `chat-progress` 即使主动私聊开关关闭，也仍可提供 `POST/SKIP` 机会，保留此前“自动朋友圈不依赖主动私聊开关”的产品语义；此时不得借机产生 PRIVATE_CHAT。
- 普通主动私聊定时评估仍由现有倾向/评估频率门控触发；到达机会后人物可决定私聊、发朋友圈、两者都做或都不做。

### 明确保留、不在本轮擅自统一的部分

- **吐槽正文**仍保持“正文事件反应 → SKIP / 私聊反应”的既有产品语义；本轮没有把它改成“正文一来就可能发朋友圈”。这是有意的边界，不是遗漏。
- **皮下 Fourth Wall**保持现有 Commentary 语义和 Prompt，不进入普通联系人 POST 决策。
- **群聊围读 / Review Automation**不接入这套私人行为动作决策。
- **公共朋友圈手动刷新**仍使用现有批量朋友圈生成流程，不因本轮被废弃或重写。
- 删除评论/删除原因仍只作为世界事件痕迹；本轮不恢复“删除后立即额外 API 反应”的已废弃方案。

### 成本与反例

- 不恢复旧 `Math.random() < 百分比` 作为最终行为决定。
- 不恢复聊天后固定 18% 单独发朋友圈门控。
- 不允许“一次 UI 点击 = 一次 API”；社交事件继续批处理。
- 不为了证明 Automation 生效而强制 POST、PRIVATE_CHAT 或双动作。
- 不把私聊秘密机械公开到朋友圈。
- 不因“统一 Automation”而丢失事件来源：模型仍能知道这是聊天进展、User 评论还是普通时间机会。

### 下一阶段前必须再次与 User 对齐

下一阶段不得仅凭“统一人物行为”标题继续扩写。开发前必须详细说明：准备接入哪些事件源、每个事件是否允许 POST/PRIVATE_CHAT、如何冷却与去重、哪些旧方案明确不恢复、完成标准是什么；确认后再改代码，避免重新实现已经废弃的设计。


## v0.4.42 / moli99 — 人物行为 Automation 第二阶段：事件治理、聚合与 soft cooldown

本节点收尾普通 Tavern / 自建联系人的人物行为 Automation。目标不是继续扩张朋友圈功能，而是把“人物知道一件事”与“立即调用模型思考一次”正式分开，并让相邻社交事件以一个完整行为上下文进入同一次人物判断。

### 事件语义表

- 普通时间机会：是主动行为机会；允许 `POST / PRIVATE_CHAT / POST+PRIVATE_CHAT / SKIP`，仍受主动私聊倾向/评估频率门控。
- `chat-progress`：是主动行为机会；允许 POST；主动私聊开启时也允许 PRIVATE_CHAT/BOTH。保留既有聊天进展成本门控。
- User 评论角色朋友圈：是主动行为机会；主动私聊开启时允许 POST/PRIVATE_CHAT/BOTH；相邻评论必须批处理，禁止一条评论一次 API。
- User 点赞 / 取消赞：只作为连续性事实，不单独唤醒模型；可在下一次真正行为机会中一并被人物看到。
- User 删除评论 / 删除原因：只作为连续性事实，不单独唤醒模型；若与正在聚合的评论批次相邻，可作为同一批上下文进入判断。
- `seenBy`：继续保持连续性事实，不升级为行为唤醒源。
- 未解决话题 / 长期未互动：不新增一套独立 UI 触发器；继续由普通时间机会结合已有最近聊天、Memory、关系上下文进行判断。
- 正文吐槽、Fourth Wall Commentary、群 Review、公共朋友圈手动刷新：保持独立产品语义，不接入普通联系人统一行为池。

### 聚合规则

- 社交主动事件采用约 2 分钟短批处理窗口。
- 多个相邻事件最多保留最近一组有限语义事件，不因连续点击产生等量 API 调用。
- context-only 事件可以进入等待队列，但**不能单独使队列 ready**；只有真正的 wake event 到达批处理窗口，或后续自然时间机会出现时，才会被一起消费。
- 队列中的社交事实有时效上限，避免几天前的点赞在很久以后被误当成刚发生。

### soft cooldown

- 不使用“发过朋友圈后 X 分钟绝对禁止再次 POST”一类硬封禁。
- 记录最近少量主动动作，并在后续行为 Prompt 中明确告诉人物：刚刚已执行过的 POST/PRIVATE_CHAT 是决策成本；普通小事通常不值得机械重复，但重大关系/剧情变化可以继续行动。
- soft cooldown 只影响人物判断，不覆盖人物动机，不把连续真实行动判为非法。

### Runtime 持久性

Private conversation defaults 必须保留 Automation 运行时字段，包括：待聚合社交事件、最近主动动作、正文吐槽最近评估计数/时间。应用默认值时不得把这些字段静默丢掉，否则刷新/重新读取后事件治理会失效。

### 完成标准

1. User 连续多条评论在短窗口内只形成一次人物行为判断。
2. 单独点赞/取消赞不会直接调用模型。
3. 单独删除评论不会直接调用模型，删除痕迹和原因仍能成为后续上下文事实。
4. `chat-progress` 继续正常产生 POST 机会；主动私聊关闭时不得借机生成 PRIVATE_CHAT。
5. 最近刚 POST/PRIVATE_CHAT 会作为 soft cooldown 信息进入下一次决策，但不存在硬性禁用。
6. `POST+PRIVATE_CHAT` 继续由一次人物判断同时执行。
7. 正文吐槽 / Fourth Wall / 群 Review 不因本节点改变动作边界。
8. Automation runtime 在 conversation defaults / reload 路径中不被静默清空。

### 下一大节点

Automation 第二阶段完成后暂停朋友圈功能扩张。下一工程为 **Phone Context Injection：moli 小手机内容 -> SillyTavern 正文**。必须继续遵守：手机->正文与正文->手机是独立桥；临时本轮注入与持久绑定分开；UI Payload 与 Semantic Payload 分开；需要来源选择、预览、Token 预算与最新内容优先策略。


## moli118 / v0.4.60 · 社区基础修整
- 手机主屏幕新增“（不在正文聊天框时必选）当前角色世界”入口；正文外社区生成可读取所选角色既有资料、已绑定世界书/条目，避免酒馆主页生成脱离角色世界的内容。
- moli社区顶部增加返回主屏幕按钮，移除社区内“收藏”板块；独立收藏 App 另列后续工程。
- 社区推荐刷新保持单次推荐 API：推荐生成的天涯/小红书/知乎内容直接归档到各自板块，不额外分别刷新三个板块。
- 天涯：移除重复浅色“版块推荐”条，保留深色工具条并改名“杂谈”，莲蓬鬼话开关留在该条；移除“红色笑脸常驻”说明；整体放大标题、帖子、楼层与工具栏字体/行高。
- 社区刷新状态跨板块切换持续保存，返回生成中的板块仍显示旋转/刷新中，不再因重渲染造成“看起来停止”。
- 小红书：修复 AI 初始评论 replyTo 分支关系在解析阶段丢失的问题；详情页可显示主评论下的回复；移除帖子图片/瀑布流右下角收藏爱心。
- 已核查：旧版并未真正实现“当前世界”社区顶部选择器，因此没有隐藏残留需要删除。

### 后续已确认待办（本轮不施工）
- 独立“收藏”App：帖子/内容自身保留随手收藏入口，收藏 App 负责统一查看、管理与后续注入；收藏保存独立语义快照，来源聊天/帖子被刷新、清空或删除后收藏仍存在。此前设计的“全局捕捉收藏模式”废弃，不作为收藏方案；但“进入全局捕捉/选取模式”的交互概念很新颖，保留为未来其他工具能力候选。
- 社区各板块主体完成后，把天涯已有“帖子常驻”能力推广到小红书、知乎及未来板块；按钮位置遵循各板块原生 UI。社区推荐主页补充“喜欢的帖子可以设为常驻，不会随刷新消失”一类说明，最终文案后定。
- 长期网友 / Community Memory / 跨帖子身份连续性 / 网友加微信 / 根据社区既有表现自动生成可编辑角色资料卡：作为独立大工程，暂不半做。


## moli119 / v0.4.61 · 社区回复生态细化（2026-09-16）
- 天涯列表与详情标题显示已有 subtitle 分类：`[板块] 标题`；不新增生成限制，只展示生成结果中已有分类。
- 天涯楼层回复恢复线性盖楼关系：回复某楼仍产生新楼层，并显示 `@用户名 #楼层号`；楼层增加“回复”入口。AI 续楼可结构化返回 replyToFloor。
- 小红书专页删除内部重复“小红书”标题。
- 小红书评论支持主评论下多条回复、回复之间继续互相回复；数据可任意深度，视觉保持“主评论 + 回复区”两层；“展开 X 条回复”可真正展开/收起，子回复也可继续回复。
- 小红书新笔记初始评论（主评论+子回复合计）上限由 8 提升到 15。
- 小红书详情页不再把右上角刷新误绑定为整板块刷新；评论胶囊旁增加“新增评论”旋转按钮，只为当前笔记追加 1~6 条评论/回复，不清空旧评论、不刷新其他笔记。

### 社区统一待办补充
- **评论区“新增”能力推广**：小红书完成后，天涯、知乎以及未来社区板块都应在帖子/内容详情页提供符合各自平台语义的“新增回复/新增评论”能力。详情页新增只追加当前内容的互动，不得误触发整个板块刷新；生成中即使切换页面，运行状态也应持续可见。具体按钮位置和视觉遵循各平台原生页面，不机械统一。


## moli120 / v0.4.62 · 社区世界感 + 知乎基础结构（2026-09-16）
- 天涯、小红书、知乎与社区推荐统一强化“故事世界中的互联网”来源：可从当前角色、人物关系、职业环境、社会背景、地点、时代、近期事件和正文剧情自然发散；故事世界相关内容不再只是偶发彩蛋，同时保留普通互联网内容。
- 天涯初始回帖强化线性楼层互相回复倾向：一批帖子中应明显出现 replyTo，但不机械强制每帖；界面继续用真实关系显示 `@用户名 #楼层`。
- 社区评论/回复数量改为疏密分布：冷内容 0~3、普通 4~8、热内容 9~15；解析层统一允许最多 15 条初始互动，避免所有帖子长期固定三四条。
- 知乎底层生成从“问题 + 单回答”升级为 `Question → Answers[] → Answer.comments[]`；每题可生成 1~4 个不同身份/立场的回答，每个回答拥有自己的评论。
- 知乎问题详情移除旧蓝色网页式返回框，仅保留原生返回图标；问题正文下加入 `＋关注问题 / 邀请回答 / 增加回答` 操作区。`关注问题` 复用收藏数据并提示“已投入收藏 App”。
- “邀请回答”与“增加回答”本轮只完成入口与数据结构基础，角色联动不伪装为已完成；下一阶段接入真实行为。

### 新确认待办
- **收藏方案修订**：废弃“捕捉收藏”；帖子/笔记/问题等内容自身恢复随手收藏入口，收藏 App 作为统一管理中心。原“全局捕捉/选取”交互概念保留为未来其他工具候选，不绑定收藏。
- **知乎邀请回答联动**：点击邀请回答 → 弹微信联系人（全部角色）→ 选择角色 → 可编辑或直接发送邀请 → 以结构化 phone event 注入该角色私聊上下文 → 角色自主选择 `ANSWER / MESSAGE / BOTH / SKIP`。不能把邀请伪造成普通 User 聊天消息。
- **社区评论 @角色**：User 在各社区评论/回复输入时可输入 `@` 唤起微信联系人角色列表；被 @ 的角色收到结构化社区 mention 事件，并由角色性格决定是否公开回复、私聊、两者都做或忽略。
- **知乎评论页**：按普通干净评论区实现；评论属于具体 Answer，支持主评论与回复。详情页“查看全部评论”进入/展开该回答自己的评论区。
- **评论区新增能力继续推广**：知乎以及未来每个板块详情页都要有“新增评论/回复”，只追加当前内容，不刷新整个板块。


## moli121 / v0.4.63 社区“发现 → 屯仓 → 生长”
- 社区推荐成为自动生成新帖的唯一刷新入口；一次生成由 8~12 条收敛为 4~6 条，优先保证单帖质量。
- 天涯 / 小红书 / 知乎分板不再提供整板 AI 刷新；推荐生成的内容按 section 自动进入对应分板。
- 每个分板最多保留 10 条非常驻内容；新内容进入后自动淘汰最早的非常驻内容。常驻内容不占 10 条流动槽位。
- 分板内容支持长按删除，用户可主动清理不喜欢的内容。
- 详情页继续负责内容“生长”：评论区右上提供 ↻ 新增评论/回复入口，不刷新帖子本身或整个板块。当前已覆盖天涯、小红书、知乎回答评论。
- 收藏仍以帖子内随手收藏 + 未来独立收藏 App 为方向；旧“捕捉收藏”方案废弃。全局捕捉/选取交互仅保留为未来其他工具能力的创意候选，不再属于收藏方案。
- 后续社区↔微信角色桥：知乎邀请回答，以及 User 在社区评论输入 @ 时弹出微信角色列表；角色可根据自身性格选择公开回应、私聊、两者都做或不回应。


## moli122 / v0.4.64 · 社区列表与身份修复（2026-09-16）

- 知乎分板不再使用天涯式纯标题列表，改为知乎推荐流式问题卡：问题标题、首条回答摘要、赞同与评论信息；详情仍为问题 → 多回答 → 各回答评论。
- 莲蓬鬼话开关从天涯板块移除，移动到「社区推荐」顶部右侧；它仍只控制莲蓬鬼话内容是否参与推荐/展示。
- User 在天涯回复、小红书评论/回复、知乎回答评论时可选择匿名；匿名只改变公开显示名，不改变 User 身份归属。
- 修复 AI 评论冒用 User 名称：公共网络批量生成及天涯/小红书/知乎评论追加在解析落库前都会拦截与当前 User 显示名完全相同的 AI 作者名，并降级为普通网友名。AI 不再能通过刷新伪装成 User。
- 天涯、小红书、知乎分板统一把常驻内容从普通流中分离，并在页面尾端增加「常驻」板块；常驻不占 10 条普通流动仓名额。
- 保留社区推荐 4~6 条发现入口、分板屯仓、详情页生长的 moli121 架构。
- 下一阶段仍为「社区 ↔ 微信角色桥」：知乎邀请回答与评论区 @ 联系人共用结构化社区事件，由角色决定 ANSWER / MESSAGE / BOTH / SKIP；评论区输入 @ 时可从微信角色列表选择。
- 「捕捉收藏」已废弃；仅保留“全局捕捉/选择模式”作为未来其他工具交互候选，不再属于收藏方案。


## moli123 / v0.4.65
- 修复天涯标题重复板块前缀：展示层统一去掉标题自身已有的方括号分类，只保留结构化 subtitle。
- 莲蓬鬼话开关移动到社区推荐标题下方虚线右侧。
- 小红书常驻按钮取消圆形底框，仅显示星标。
- 社区→微信角色桥第一阶段接通：知乎「邀请回答」可选择微信联系人、填写可选邀请语；角色按自身性格决定 ANSWER / MESSAGE / BOTH / SKIP，公开回答写回知乎，私聊写回对应微信会话。
- 下一阶段：评论输入 @ 的联系人选择器与统一 community mention 事件桥，复用同一角色自主决策机制。


## moli124 / v0.4.66
- 完成社区评论 @ 微信角色联动第一版：User 在天涯、小红书、知乎评论/回复输入 @ 时可选择微信角色。
- User 评论先按原平台结构正常落库；被 @ 角色随后自主决定 REPLY / MESSAGE / BOTH / SKIP，不强制回应。
- 公开回应以角色本人身份写回对应社区结构；私聊回应写回该角色微信私聊，禁止模型代 User 发言。
- 保留评论匿名选择。
- 下一阶段切换到微信整体美化：先建立统一视觉规范，再按聊天列表→私聊/群聊→资料/设置→朋友圈逐页处理，不擅自改变既有交互。


## moli124 / v0.4.67 — 微信视觉整理
- 微信主导航精简为「微信 / 通讯录 / 发现」，移除「我」入口；全局设置入口迁至手机主屏幕设置 App。
- 微信聊天列表增加搜索框，调整列表字号、行距、头像与右上角 + 的视觉层级；底部导航改为三项主题化胶囊布局。
- 天涯斑竹显示改为 `{{user}}，{{char}}`，移除 moli。


### v0.4.71 · WeChat visual/wallpaper refinement
- Chat title is a compact liquid-glass capsule; composer is slimmer and keeps ♡ / + frameless.
- Chat wallpaper supports global fallback plus per-conversation override; only the active resolved wallpaper is rendered.
- WeChat list shell stays white while the search field remains light gray.


### v0.4.72 · 角色专属朋友圈整理收口
- 顶部不显示页面标题和条数/整理提醒；右上角为「整理 / 说明书 / 刷新」。
- 保留既有“某角色最近没有新的朋友圈”及其状态注释功能，不改动这块逻辑。
- 可见动态上限为 6 条，新增后最老动态自然退出。
- 「整理」只提交尚未成功整理的动态；长期记忆成功写入后才标记，失败不标记，避免二次整理重复提炼。
- 删除底部“清空本页朋友圈”和空页大字提示；角色专属朋友圈补齐 User「已阅」。


### v0.4.73 朋友圈认知交互补齐
- 角色朋友圈顶部工具固定到右侧：整理 / 说明书 / 刷新。
- 角色朋友圈新增可编辑「{{user}}偷看 N 次」认知；已阅、偷看、删除评论理由均作为角色可知材料，不强制角色行为。
- 朋友圈说明书改为 moli 内置弹层；User 朋友圈发表入口由相机改为 +。
- 主屏幕设置入口使用独立 App 图标。


### v0.4.74 朋友圈细节
- 角色朋友圈工具组右对齐并统一刷新基线；偷看控件增强可见性。
- User 朋友圈浏览痕迹改为与“允许所有角色互动”一致的玻璃信息条。
- 朋友圈文字发布页去除灰色顶栏/输入框描边，新增“照片语义描述”：用户以文字描述附图，动态保存 imageDescription，角色上下文按真实附图语义读取。
- 角色刷新成功的“发现 xx 的一条近期朋友圈”不再占用顶部状态；0 条刷新状态保持原逻辑。
- 设置 App 图标裁去源图多余留白以填满图标格。

### 朋友圈可见范围与浏览痕迹（v0.4.75）
- “看了你的朋友圈 N 次”中的 N 是从上一次公共朋友圈刷新到本次刷新之间的访问次数，不是历史累计；本轮 0 次不显示。
- 公共朋友圈刷新优先只考虑最近有实际私聊的最多 5 个联系人；浏览痕迹区域最多显示 3 条。
- 用户动态可带图片文字描述、所在位置、提醒对象与可见对象。“仅对方可见”是“谁可以看”仅选一人的快捷语义。
- 角色也可按自身性格选择动态仅 User 可见；这些设置提供认知/可见性，不强制角色互动。


### v0.4.76 / moli135
- User 公共朋友圈刷新恢复整个通讯录为候选角色；角色可 0 行动，浏览痕迹最多显示 3 条，次数仅代表本轮刷新区间。
- 公共朋友圈时间线最多保留最近 10 条动态，第 11 条进入时淘汰时间最老的一条。角色个人朋友圈原有规则与‘最近没有新的朋友圈’提示不变。
- User 发朋友圈删除‘提醒谁看’设置行；正文输入 @ 时可从通讯录多选角色；保留并修正‘谁可以看’多选与所在位置。
- 发布页所在位置/谁可以看恢复线性图标。
- 修正设置/我们的墙 App 图标透明背景显示。


### moli136 / v0.4.77
- 通讯录去除顶部“通讯录”标题灰框，增加可展开群聊入口；群名小字号展示，点击直接进入群聊。
- User 朋友圈发布正文不再触发 @ 角色选择；@ 需求保留给评论区后续实现。
- 修复“谁可以看”选择弹层交互并保留公开/单选仅对方/多选仅这些角色。
- 主屏幕“我们的墙 / 设置”图标取消错误二次放大，按圆角卡片填充。

### moli137 visibility / UI correction
- User 发布朋友圈的“谁可以看”使用联系人多选：未选择联系人=公开；选择一个或多个联系人=仅这些联系人可见。
- 角色朋友圈中的 User 名称必须使用当前 SillyTavern User 名称，不向界面暴露 `{{user}}` 模板占位符。


### moli138 / v0.4.79 — 朋友圈互动结算 + 主动私聊语义收口
- 角色专属朋友圈的 User 行为改为“先积累、刷新时一次结算”：已阅、偷看、点赞/取消赞、评论、删除评论都作为事实保留，不再因单次评论/点赞立即唤醒后台 Automation。
- 角色专属朋友圈右上角刷新成为明确的认知结算点：本轮 API 会读取上次结算后积累的 User 互动；刷新成功后这些事实标记为角色已知。
- “知道 ≠ 在意 ≠ 行动”：刷新时角色可只更新/不更新朋友圈，也可在主动私聊权限开启时自主选择 PRIVATE_CHAT / POST+PRIVATE_CHAT / SKIP；不得机械回应每个互动。
- 主动私聊百分比继续定义为人物总体主动倾向，而不是事件触发骰子。普通日常主动评估仍按倾向控制评估频率；朋友圈刷新已经是一次真实人物判断机会，因此不再先用百分比抽签。
- 主动私聊开关与主动倾向语义分离：开关关闭时刷新绝不能产生主动私聊；开关开启时即使倾向为 0%，重大真实动机仍允许在这次已经发生的刷新判断中主动联系。0% 只让普通后台日常主动评估停止。
- 已结算的朋友圈互动保留为近期已知事实，后续私聊仍可自然提起；一次 SKIP 不等于遗忘。待结算事件窗口由 12 条扩到 50 条，降低用户长时间不刷新时丢失互动序列的风险。
- 后续：将同一“事实 → 结算/传播 → 人物判断 → 行动”结构抽象为跨微信/社区的 World Event / Social Event Pipeline；不在本节点贸然把社区事件全部改写。


### moli139 / v0.4.80 — @联动补齐 + 朋友圈可见列表修复 + 聊天气泡工具玻璃化
- 角色专属朋友圈与 User 公共朋友圈评论支持输入 `@` 后选择微信联系人；被 @ 的角色会获得一次明确的 @ 事件判断机会，并可自行选择评论区回复、主动私聊、两者都做或 SKIP。收到 @ 不等于必须回复。
- 社区原有 @ 联动保留；本轮确认天涯/小红书/知乎等社区评论入口继续使用同一“收到但自行决定是否回应”的语义。
- 修复 User 朋友圈发表页“谁可以看”选择器：提高弹层交互层级并补齐联系人选择行样式/点击态；公开与指定联系人互斥，确定后写回 visibility。
- 聊天气泡长按菜单移除内部白色按钮底，统一为半透明液态玻璃。
- 多选底栏改为与聊天输入框一致的液态玻璃胶囊条；删除仍保留危险色，但整体容器不再是白色矩形。
- 下一步：在现有朋友圈互动结算稳定后，把微信/朋友圈/社区事件统一收口到 Social/World Event Pipeline；事实记录、认知结算、人物判断、实际行动继续保持分层。


## moli140 / v0.4.81 — World/Social Event Pipeline 第一纵切
- 朋友圈“谁可以看”不再复用 help-sheet：点击箭头直接进入联系人列表；无人选择=公开，选择一人或多人=仅所选角色可见。
- 新增 `src/storage/world-event-store.js` 作为统一手机世界事实账本 v1：记录 source / actor / action / target / object / content / metadata，并为每个目标人物分别维护 pending/known 认知状态。
- 朋友圈既有 `recordMomentChatEvent` 同步登记 World Event；朋友圈刷新真正把待处理事件交给角色后，同步把对应 World Event 从 pending 标为 known。SKIP 不删除已知事实。
- 主动私聊默认开启：仅对“没有显式保存过开关”的联系人应用；用户明确关闭过的联系人保持关闭。默认主动程度仍为 30。
- 主动程度 0–100 不是随机触发概率。普通后台主动评估在 0 时不自行唤醒；但已经由明确社交事件产生的判断机会，在“允许主动私聊=开启”时仍可由人物自行决定 PRIVATE_CHAT，即使倾向为 0。
- 固定原则：事实 ≠ 认知；知道 ≠ 在意 ≠ 行动；开关是权限，主动程度是人物习惯；@ 属于明确点名，可即时形成判断机会；偷看/已阅/点赞等允许积累到朋友圈刷新统一结算。
- World Event v1 的 source 字段从第一版即支持 `wechat.* / xiaohongshu / zhihu / tianya / tavern / future-app`，避免未来 Phone Context Injection 被写死成微信专用。后续社区迁移必须渐进接入，不推倒现有可用生成链。

## v0.4.82 世界事件与可见范围补充规范
1. User 朋友圈可见范围采用发表页内联纯文字名单，不依赖 modal/help-sheet/page；无选择=公开，有选择=仅所选联系人。
2. 可见性必须约束 AI 上下文，不只是 UI 标签。限定动态不得出现在未获授权角色的共享朋友圈上下文。
3. World Event 三层语义固定：occurred（世界发生）→ pending/known（人物认知）→ action（人物行动）。SKIP 只改变行动，不回滚 known。
4. World Event Store 提供消费标记防止同一新事件被同一判断入口重复当作“刚发生”，但已知事实仍可作为连续性被后续聊天自然引用。
5. @ / 邀请回答等明确点名属于即时认知机会；偷看、已阅、点赞、取消赞、评论、删除评论等朋友圈互动可积累至刷新结算。两类最终都服从人物判断，不强制回复。
6. `允许主动私聊`是权限且新联系人默认开启；`主动程度 0–100`是行为倾向/日常评估频率，不是事件触发概率。
7. `getWorldEventContextSource()` 是未来 Phone Context Injection 的统一手机事件出口；后续 App 应接此模型，不为每个 App 重做一套正文注入。

## 自创社区与世界边界补充（v0.4.83）
1. `自创` 是 moli社区的用户定义信息生态，不是第四个固定平台模板。一个定义至少包含名称、描述、启用状态；描述负责说明世界范围、常见参与者/话题/气质。
2. 社区推荐刷新时，固定生态与启用的自创生态共同作为候选。一次仍只生成 4~6 条，AI按当前世界状态自然选择，不要求每个自创社区每轮出现。
3. 自创内容使用统一 Public Web 数据结构与详情/评论/@桥；评论生长必须读取该自创社区定义，禁止自动天涯化、小红书化、知乎化。
4. 自创社区事件使用 `community.custom`，并保留 `customCommunityId`，使跨 App 连续性与未来世界事件查询可追溯。
5. User朋友圈可见范围：发表页常驻纯文字联系人列表；无人选择=公开；选择联系人=仅这些 contactId 可见。当前版本没有宣称同一 contact 的“正文/全局 Conversation”可被分别授权。
6. “我们的墙”坚持 User 主权：没有自动长期绑定。World Event 只是新的可选素材源，是否跨墙、跨哪些内容，仍由 User 每次选择。


## moli144 / v0.4.84 — 社区信息架构与朋友圈可见性重建
- 朋友圈发表页“谁可以看”不再依赖展开/弹层/hidden toggle，联系人选择区常驻显示；旧展开交互视为废弃实现。
- 天涯斑竹显示真实 `User ♡ 当前角色`，禁止把字面量 `{{user}}` / `{{char}}` 泄漏到界面。
- moli社区新增“自创”；自创首页标题为 `User名小窝`，左栏只负责新增/打开编辑条目，不承担推荐选择。预置入口文案包括：当前角色黑粉匿名楼、学校论坛、同事八卦、如果回到以前、当u去世。已保存条目再次打开必须恢复原文字并可继续编辑。
- “社区推荐”新增“我只想看”：默认全选且帖子条数留空时保持既有默认；可多选天涯/小红书/知乎/自创，自创可进一步展开到已保存条目多选。该选择只决定本次/当前推荐来源，不改变自创条目本身。
- 自创条目是 User 定义的信息环境，不得自动天涯化/小红书化/知乎化。


## moli145 / v0.4.85 · 自创社区收口
- 社区推荐“我只想看”移动到推荐标题下方，与“莲蓬鬼话”同一层级；筛选面板改为竖向结构。
- “自创 >”默认折叠，展开后才显示已保存自创条目，可多选；不展开时不会把大量条目铺满推荐页。
- 自创小窝的条目库默认折叠；展开后提供“全选 / 删除 / ＋新增”。删除后的默认示例不会在下次启动复活。
- 默认示例仅初始化一次：匿名树洞、私密日记；其可编辑原文保留 {{char}} / {{user}}，送入生成器时才解析当前角色/User。
- 新增/编辑不再连续弹多个 prompt：改为单个编辑弹框，名称 + 一块完整自由文本，一次写完并保存；再次打开保留上次文字。
- 社区推荐生成的 section=custom 内容继续归档在公共网络仓，并在“自创”页的“自创内容”区域可见。
- 天涯斑竹优先读取当前 SillyTavern 正文角色，避免 selected world 缺失时落成字面 Char。
- 朋友圈“谁可以看”本轮停止继续叠补丁：保留为明确待办，后续需要独立重建发表页 visibility 模块与世界线级权限；不得为修此项破坏公开发表。


## moli165 / v0.5.05 — World Event Lifecycle 第一阶段闭环
- 继续以 World Event 为事实账本，不删除/替代朋友圈 chatEvents、Behavior Queue 或 community:pending；App Queue 仍负责各自执行。
- Character Decision 现在会读取该人物 `known + 尚未由 character-decision consumed` 的 World Events；无论最终 POST / PRIVATE_CHAT / POST+PRIVATE_CHAT / SKIP，均记录该判断入口已处理这些事实。`consumed` 只表示“这个判断入口处理过”，不删除事件、不撤销 known，SKIP 仍可在未来自然提起。
- 自动行为产生的 POST / PRIVATE_CHAT 等结果会回写新的 `wechat.automation` World Event，并保存 causedByEventIds，开始形成“事实→认知→判断→行动结果→新事实”的闭环。
- 社区普通 User 评论/回复/知乎回答现在会针对可确定的相关角色登记 World Event；提交时为 pending，评论区刷新真正结算后才进入 known。
- 社区 @ / 邀请评论 / 邀请回答继续保留现有 `community:pending` 批处理，不改 CLOSED 的产品交互；其 World Event 改为 pending，刷新结算时进入 known，并在该社区结算入口成功处理后标记 `community-settlement` consumed。
- 角色因社区明确互动产生的公开回答/公开回复（以及公开回复+私聊）会回写角色自己的 World Event，作为后续微信/跨 App 连续性的个人经历。
- 新增按 objectId 对全部目标人物批量 known/consumed 的底层能力，供现有 App Queue 与 World Event 生命周期渐进桥接。
- 边界不变：World Event=事实；Awareness=谁知道；App Pending/Queue=执行；Consumed=某判断入口已处理；Memory/Continuity=仍可记得。禁止把 consumed 当删除或遗忘。
- 社区 Interaction Runtime 继续 CLOSED；本版本不新增社区按钮、不改社区 UI、不改已确认 Prompt/Personality，也不改变 User-controlled“我们的墙”穿墙边界。
- 下一阶段：继续补齐社区生成结果与匿名 identityKnownBy 的 Awareness 传播，统一更多人物 Decision consumer，并审计 Cross-App Character Continuity；在此基础上再完善 World Event 作为“我们的墙”可选 Context Source。


## moli166 / v0.5.06 — Cross-App Character Continuity 第一阶段
- 新增 `character-continuity-store.js`：不复制各 App 原始数据，而是从 World Event 读取同一 contactId 的“亲历事件 + 已知事件”，形成跨 App 人物经历视图。微信私聊生成现在会读取该视图，因此角色在社区公开做过、或已经明确得知的事情可以成为微信中的同一个人的经历。
- 新增匿名身份 Awareness 小账本：系统真相(realContactId)与人物知识(knownBy)分离。角色匿名公开参与时，角色本人确定知道自己的匿名身份；不会因为数据库保存 knownIdentityId 就让其他角色自动全知。
- World Event 增加 Result/Provenance 链接：cause event 可记录 decision 与 resultEventIds，result event 反向记录 causedByEventIds。Automation 与社区 @ 的公开/私聊结果开始写回因果链。
- Cross-App Continuity 只读取“本人亲历”或 Awareness=known 的事实；pending 事实不会因为跨 App 检索而泄露给人物。SKIP 仍不删除事实。
- 本阶段不修改 CLOSED 的社区 UI，不新增匿名猜测机制，不自动把手机事实注入正文；“我们的墙”继续是 User-controlled bridge。


### 特殊联系人单例入口与世界路由（moli172）
- `moli / 皮下 / 小上帝` 是固定系统入口。聊天列表始终每个只显示一个，不把各正文 scope 的系统 Conversation 平铺成多个同名联系人。
- 单例仅作用于 UI/入口路由，不删除、不合并不同世界已经存在的聊天历史。
- 当前处于正文页时，特殊入口优先进入该正文 scope 对应 Conversation；不在正文页时，若主屏幕已选择“当前角色世界”，入口跟随该世界；没有明确世界时优先 global/default。
- 普通角色和 User 自建联系人不适用单例规则；其已建立 Conversation 必须持续存在。

### 世界书条目查看/本地编辑（moli172）
- Tavern Contact 的世界书条目必须允许 User 查看完整正文。
- 编辑操作默认只产生 moli Contact 级本地覆盖，不反向修改 SillyTavern 原世界书，避免手机侧编辑意外污染酒馆资料。
- 本地覆盖只替换已被原触发规则激活条目的正文；不能把未触发条目变成常驻注入。

### Character Identity × Tavern Chat Instance
- 同名/同 Contact 只证明是同一个角色身份，不证明处于同一个世界。
- 一个 Tavern Contact 可以对应多个正文 World Instance；每个具体 Tavern chat 的 `boundScopeKey`（其中包含 chatId）是正文实例边界。
- 角色卡、人设、角色世界书属于 Character Identity，可跨该角色的正文实例共享读取；正文、手机聊天、记忆、Continuity、World Event 属于 World Instance，严禁跨 chatId 混用。
- 添加 Tavern 角色时应直接从 SillyTavern 完整角色列表绑定角色卡并保存可用快照；“必须先点进该角色正文才能读取人设”属于缺陷，不是产品语义。
- 当前角色世界选择器必须能区分同一角色的多个正文实例。显示优先级：用户为手机 Conversation 设置的标题（如“主线/IF线”）> Tavern chatId 可读名 > 正文 fallback；全局实例明确显示“全局”。
- 手机已明确选择 Current World 时，它是手机侧世界路由权威；当前 Tavern 页面只负责提供环境，不得覆盖 User 已选择的 World Instance。

## v0.5.17 World Boundary：正文 / 全局的硬隔离
- **正文页面唯一决定正文世界**：当 SillyTavern 当前存在具体 `:chat:` 正文实例时，手机社区只能属于该正文实例；主屏幕“当前角色世界”入口隐藏，不允许在 A 正文中手动切到 B。
- **正文外社区只属于全局层**：正文外“当前角色世界”只列出全局 Tavern 人物；正文实例不再作为候选。被否决方案：正文外通过 Current World 重新进入 A/B 正文社区。
- **微信可见性与社区隔离不是同一规则**：正文外显示全局人物 + 自创人物 + Global 三人格；正文内显示当前正文人物 + 全局人物 + 自创人物 + 当前正文三人格。其他正文实例人物不得铺到当前聊天列表。
- **认知防火墙**：`读取酒馆正文` 只能读取该 Conversation 自己绑定的 `boundScopeKey`，并且仅当它恰好等于当前具体 Tavern chat scope 时才允许接入动态正文；Global Conversation 以及 A Conversation 在 B 页面都不得读取 B 当前正文。
- **柏宝书同样属于正文世界**：柏宝书动态长期剧情只在上述 exact-bound-world 条件成立时注入，不能把当前页面的柏宝书交给另一条 Conversation。
- **固定人格**：moli / 小上帝 / 皮下仍是单入口；正文内优先当前正文实例，正文外优先 Global。社区不会因为其存在而自主把三人格写成正文原生人物，但 User 主动 @ / 邀请 / 转发 / 自创场景可召入当前世界。
- **可见 ≠ 属于 ≠ 知道**：Global / 自创人物即使在正文内微信可见，也不因此获得当前正文事实。
- 后续待办（不在本节点实现）：删除联系人意味着彻底不要该人物，应删除其微信、朋友圈、社区、Character Continuity / World Event 个人痕迹及“我们的墙”相关人物资产，而不是保留作废历史；“同步酒馆角色”同一角色卡允许反复新建，不显示“已添加”。


## moli175：删除语义与全局角色入口（2026-09-18）
- 正文环境中不显示“当前角色世界”入口；正文外该入口只面向全局人物，候选包括全局 Tavern 人物与自创人物。
- 同一 SillyTavern 角色卡允许重复添加。同步页不再显示“已添加”；每次添加都创建新的 moli Contact/Conversation 实例，但共享同一 Tavern Character Identity 来源。
- “删除联系人”定义为：这个人物在 moli 小手机中彻底清空。删除前先清理该 Contact 绑定的非全局 World Scope：社区全部帖子/自创板块数据、朋友圈、World Event/Continuity 身份认知、我们的墙 pending/history/workspace；同时清理其他持久 scope 中以该 Contact 为作者/目标的社区、朋友圈、World Event 与匿名身份痕迹。随后删除其所有私聊、近期/长期记忆，并从群成员列表移除。
- 对正文人物 A：删除 A 即删除 A World 的所有社区帖子，而不是只删除 A 自己发过的帖子。这里不保留“作废世界历史”。
- 删除联系人与“只删除聊天记录”语义严格不同：若 User 仍需要社区/墙/跨 App 历史，应只清聊天，不应删除 Contact。
- 暂不改变 Global/自创人物的“读取正文”能力；“旁观正文但明确知道正文发生在 User 与另一个角色之间”作为下一阶段单独设计，不在本版本偷偷改语义。


## moli176 / v0.5.19 — 自创人物归属与 NPC 世界绑定

- 自创人物不再默认等同于“正文人物”，创建时明确选择 `Global / NPC`。Global 为跨正文陪伴人物；NPC 必须在具体正文页面创建并绑定该正文 `scopeKey`。
- 正文社区的原生人物池为：当前正文角色 + 绑定该正文的 NPC。NPC 与正文角色拥有相同的自然社区参与权，可以被社区自主提及、发帖或评论。
- moli / 皮下 / 小上帝仍是跨世界可召入固定人格，不属于正文社区原生人物池；只有 User 主动 @、邀请、转发，或自创内容明确召入时才进入该世界。
- 通讯录新增“人物绑定”入口，集中查看自创人物的 Global/NPC 归属。旧自创人物没有显式归属字段时，若已有正文 Conversation，则兼容视作 NPC；已有 Global Conversation 则视作 Global，不强行破坏旧数据。
- 正文内主屏幕“当前角色世界”必须整块隐藏，并在每次进入主屏幕时重新按当前 Tavern 环境计算，避免扩展初始化于正文外、之后进入正文仍残留入口。
- 已废弃旧假设：`自创人物 = 全局人物`；自创只是人物来源，Global/NPC 才是世界归属。

## moli177 / v0.5.20 — NPC 认知边界与人物绑定闭环

- **NPC 属于正文世界，不等于 NPC 全知正文。** 新建 NPC 的“读取酒馆正文”默认关闭；User 有明确玩法目的时仍可在资料卡手动开启完整正文读取。
- NPC 的正常认知目标是“本人明确在场、通过自身感官/对话能够知道的剧情”。后续 Unified Awareness 应把新增正文投影为 NPC 视角事实后写入其 Continuity/认知；不得按名字关键词抓取，也不得把他人私聊、内心、背后提及自动泄露给 NPC。本版本只固化边界，不临时造第二套 NPC 摘要系统。
- 自创 NPC 只能在当前确实存在具体 Tavern `:chat:` 正文实例时创建，并自动绑定该 World Instance；正文外创建只能得到 Global 自创人物。不得使用上一次正文残留 scope 为正文外新人物绑定世界。
- 微信可见性：A-NPC 只在 A 正文出现；B 正文和正文外均不可见。Global 自创人物继续跨正文可见。
- `moli / 皮下 / 小上帝` 是“固定人格”，不是 Global 角色，也不是任一正文的原生人物。聊天列表不再用当前正文标题作为它们的归属后缀；正文实例只决定本次路由/经历容器。
- 通讯录“人物绑定”显示完整世界结构：正文主角色 + 绑定 NPC，以及 Global/固定人格。正文主角色的世界由 Tavern chat 决定不可在此改绑；NPC 可改绑到已有正文 World Instance，改绑时保留其手机 Conversation 数据但完整正文读取重新保持关闭。
- 正文内“当前角色世界”入口必须真正 `display:none`，不能仅依赖 HTML `hidden` 被组件自身 `display:grid` 覆盖。


# moli178：人物正文权限、Global 旁观正文与 NPC Awareness 交接

## 人物正文权限（现行规则）
- 正文酒馆角色：正文属于其 World Instance 的事实环境，必须读取自己绑定的当前正文；资料卡不提供“读取正文”开关。柏宝书长期剧情属于正文角色既有长期剧情来源，资料卡不再把它作为普通聊天开关暴露。
- moli / 皮下 / 小上帝：三者是内部固定人格类型，但 UI 不显示“固定人格”工程后缀。为了围读会、皮下扮演等既有玩法，在具体正文环境中必须能读取正文；不向 User 暴露普通“读取正文/柏宝书”选择。皮下保留自身原有“入戏…”标志。
- NPC：属于某个明确 World Instance。NPC 必须能够读取其绑定世界的当前正文以参与该世界，但资料卡不提供正文开关；这不代表 NPC 自动获得上帝视角长期记忆。NPC 的持久认知必须走后述 Awareness 过滤。NPC 当前不读取柏宝书长期剧情。
- Global 人物（Global Tavern Contact / Global 自创人物）：与正文角色认知隔离。只有这类人物显示“旁观正文”开关，默认关闭；不提供柏宝书开关，也不读取柏宝书。

## Global「旁观正文」
- 旁观是 Observed Context，不是 Experienced Context。系统事实边界高于 User 自定义解释：无论 User 怎样写理解规则，都不得把旁观正文改写成该 Global 人物自己的亲历。
- 开启后只读取 User 当前正在更新的具体正文最近 10 楼；不补以前正文、不读取柏宝书、不利用长期正文摘要补全过去。设计目的就是让陪伴型角色获得有限窗口，可能产生“你在干吗/为什么在和他亲嘴”等疑惑，而不是获得另一世界全知。
- User 可在每 Contact 的「用户设定 → AI理解规则」自由定义该人物如何解释旁观材料，例如“这是 User 在拍电影”“这是 User 在出轨”“这是平行时空”。moli 不替 User 固定这些意义。
- 第一阶段旁观材料只在生成时作为临时 Observed Context；不额外调用 API 自动总结为长期记忆。Observed → Awareness → Memory 留给统一认知层。

## NPC 亲历剧情：后续 Unified Awareness 必做（不要另造临时记忆系统）
NPC 虽能读取绑定世界当前正文用于即时参与，但持久认知不能把整段正文直接写入 NPC 记忆。统一认知层必须做“NPC 视角事实投影”：
1. 输入新增正文事件；
2. 判断 NPC 本人是否明确在场。仅被别人提到名字、别人谈论 NPC、叙述者知道的幕后信息，都不等于 NPC 在场；
3. 若明确在场，再判断 NPC 通过当时感官、对话与可见行为实际能知道什么；
4. 只把 NPC 可知事实写入该 NPC 的 Awareness / Continuity / Memory；角色内心、密谈、幕后动机等不可见信息不得进入；
5. 同一场景允许不同人物形成不同认知投影；
6. 该能力应接入 Unified Character Cognition & Decision / Awareness 主线，与 World Event、Continuity、consumedBy 共用事实—认知边界，不得单独为 NPC 新建第二套摘要/记忆系统。

## UI 收口
- 通讯录删除“人物绑定”栏；World/scope 归属属于后台结构，不直接向普通 User 展示。
- 正文内 Current World 入口已由 moli177 实机验证隐藏；正文外保留。


## moli179 — Tavern Character Card source-of-truth closure
- 酒馆角色的 Character Identity 不依赖当前打开的 Tavern Page。同步添加时，即使角色从未进入过正文，也必须按角色 avatar/source identity 主动读取 `/api/characters/get` 的完整角色卡，再保存 roleFidelity。
- SillyTavern 开启 lazy/shallow character list 时，列表快照可能没有 description/personality/scenario 等；浅快照不得覆盖 moli 已保存的完整角色卡快照。
- 用户可见后缀：普通 Global 人物统一显示「陪伴」；皮下显示「我在这边，你呢？」。底层 scopeMode/global 与固定人格分类不因此改变。


## v0.5.23 / moli180 酒馆角色卡与正文归属修正
- 修正完整角色卡主动读取：SillyTavern POST `/api/characters/get` 需要 CSRF；当宿主未暴露 `getRequestHeaders()` 时，moli 必须自行从 `/csrf-token` 取得 token 后再读取，不能因为角色从未打开过正文就只得到浅角色卡。
- Tavern 角色快照必须保留该角色自己的 `chat` 标识。选择“正文角色”添加时，Conversation 必须绑定到**该角色自己的 sourceId + chat**，严禁把批量添加的其他角色绑定到当前屏幕上正在打开的第三方正文。
- 如果某张角色卡尚不存在任何 Tavern chat，则不能借用当前正文作为其世界；先保留完整角色卡并以陪伴实例承载，待该角色真正产生 Tavern chat 后再解析其正文 World Instance。


## v0.5.24 · 最终 Prompt 人设链修复
- SillyTavern User Persona 必须提取实际文本值；若宿主暴露的是 textarea/input DOM，读取 `.value`，禁止把 DOM 对象字符串化为 `[object HTMLTextAreaElement]`。
- 酒馆 Contact 的角色卡是 Character Identity 的必备生成上下文。私聊生成前若本地仅有浅快照，必须按该 Contact 自己的角色身份主动补取完整角色卡，再进入 Prompt Builder；Global/陪伴身份不得成为跳过角色卡的理由。
- 验收以最终模型实际收到角色卡事实为准，不以资料页可见或本地已缓存为准。
- User 自定义全局 Prompt 属于 User Prompt 层，世界边界/人设链修复不得擅自删除、改写或固化其内容。


## v0.5.25 / moli182 — Identity Context 与角色卡 UI 同源
- User Persona 属于 User Identity Context，不属于“正文读取/旁观正文”的附属资料。私聊与群聊均不得因为 Global/陪伴角色关闭「旁观正文」而删除 User Persona。
- 「旁观正文」只控制最近十楼 Observed Context；不得顺带控制 User Persona。
- 角色设定页与生成链必须共用 Character Identity → 完整 Tavern 角色卡的解析路径。SillyTavern 前端若仅提供 shallow row，角色设定页应主动补取完整角色卡并刷新联系人快照，不得出现“模型已拿到角色卡但 UI 仍称无资料”的分裂状态。
- Current Tavern Page 只描述 User 当前站在哪个页面，不得决定另一个 Contact 有没有角色卡。
- 当前阶段 User Persona 读取 SillyTavern 当前激活 Persona 的实际文本；后续若实现每 World/Contact 独立 Persona 锁定，应把 Persona 快照纳入 Identity Context，而不是重新绑定到正文读取开关。


## moli183 / v0.5.26 — 群聊 World Boundary 与角色设置收口
- 群聊/围读会按 World Instance 隔离：酒馆主页（正文外）只显示当前正文外 scope 的群聊；A 正文只显示 A 群；B 正文只显示 B 群。历史其他正文群不得跨世界出现在列表。
- 围读会正文读取增加底层硬边界：只有“群绑定 scope 是具体 `:chat:` 正文”且“当前 Tavern scope 与群绑定 scope 完全相同”时才允许读取正文。正文外围读会不读取正文；后台/Automation 即使误触发也不得偷读另一正文。
- 酒馆角色的角色卡属于 Character Identity 必读事实，不再显示“自动跟随角色卡”提示/开关；旧 `cardProfile` 值不再能阻止 Role Fidelity 注入。角色设定页保留世界书与自定义附加 Prompt。
- 添加酒馆角色的用户可见类型改名：`正文角色` → `跟随正文`；`全局角色` → `现实陪伴`。类型只决定世界归属语义，不允许用当前屏幕正文替代该角色自己的 World Instance。

## Unified Awareness v1（v0.5.27）
### 事实、观察、认知、行动必须分层
- World Event：系统事实账本。
- Observed Context：人物被允许临时看到的材料；不自动成为亲历（典型：Global「旁观正文」）。
- Character Awareness：人物有证据能够确定知道的事实。
- Character Continuity：该人物跨 App 可延续的自身经历/已知事实视图。
- Character Decision：人物在某个合法唤醒点面对新事实和相关经历所做的行动判断。
- consumed：某 contact 的某 decision entrypoint 已处理该事实；不代表事实删除或人物遗忘。

### NPC 正文 Perspective Projection
自建 NPC 属于明确正文 World。正文原文可以作为投影器的系统证据，但不能直接等同 NPC Knowledge。进入 NPC 手机连续性的正文事实必须经过 perspective projection：明确在场/参与、可看到、可听到、亲历或被明确告知。仅出现 NPC 名字、不在场私密事件、他人内心、上帝旁白秘密不得写入 NPC Awareness。禁止关键词窗口冒充视角过滤。

### Anonymous definite identity
系统真实 `realContactId` 不等于人物知道。匿名人物本人可知道自己的匿名身份；其他人物只有在确定证据链结算后进入 `identityKnownBy`。v1 支持社区中 User 对明确接收者作直接等同告知，并在社区刷新结算后生效。猜测、语气相似、同帖出现均不能自动升级为 definite knowledge。

### Unified Decision v1
统一的是人物判断输入语义，不是强行合并各 App 执行 Store。第一阶段输入为：wake reason / newly known facts / relevant continuity / initiative tendency / allowed actions / recent actions。动作继续复用成熟出口。知道 ≠ 在意 ≠ 行动；先判断是否行动，再在允许出口中选择；主动程度不是 Math.random 概率门。

### NPC认知（v0.5.28）
NPC 的正文知识分为“历史事实来源”和“人物当前认知”。最近正文、柏宝书长期剧情属于来源；只有经过 Perspective Projection 的 personalFacts、NPC合理会知道/受影响的重大 worldChanges，以及明确的认知 invalidations 才进入人物认知。正文没有出现 NPC 名字不代表世界重大变化不可知；反之，正文出现 NPC 名字也不代表 NPC 在场或知道。

资料卡「记忆 → NPC认知」展示的不是调试副本，而是生成时实际读取的 Character Awareness。User 可直接修正；保存后该文本成为新的人工校正基线，旧自动投影不得覆盖它，后续新正文仍可追加新的认知变化。时间线重置、失忆等默认不物理删除历史事实，而以认知变更说明当前 NPC 不再拥有/适用哪些记忆。复杂叙事最终以 User 校正为准。

自建 NPC 必须自动读取其绑定正文 World 与柏宝书长期剧情作为认知素材，不提供“关闭正文/柏宝书认知”的产品开关；但两者都不得未经 Awareness 过滤直接注入 NPC 私聊。Global「现实陪伴」的旁观正文属于 Observed Context，不适用此规则，也不得自动沉淀为亲历。

### Unified Decision entrypoint consumption（v0.5.29）
World Event 的 `consumedBy` 是“某人物的某个判断入口已经处理过”，不是事实被全局删除。social-event 与 proactive-private 必须使用不同 consumer key；事件即使在某入口 SKIP，也仍保留在 Character Continuity 中。人物决策顺序固定为：先判断是否行动，再判断在哪个当前允许的出口行动。

### 聊天列表身份短标签
自建 NPC：`NPC`。正文绑定角色：`正文`。Global：沿用`陪伴`语义。列表不得把正文标题、Branch 编号等 World Instance 内部标签拼到角色身份后缀。


## 社区交互增量与反馈规则（moli184）
- 知乎问题详情只保留一个统一刷新入口，与邀请/转发/常驻/收藏同列；刷新必须增量追加，禁止替换已有回答或评论。一次刷新允许新增回答、已有回答下新增评论/回复，或两者同时发生。旧 `extra.answer + post.comments` 首次写入时迁移为正式 answer，避免第一次刷新清空旧内容。
- 知乎回答评论折叠必须可展开/收起；User 发布回答或回答下回复后立即写入 Store 并重绘。
- 小红书评论刷新与四个社区动作同列，不使用额外灰色圆框；小红书帖子作者再次回复时显示“作者”，天涯帖子作者再次回复时继续显示“楼主”。
- 明确邀请/@ 的刷新结算适用于所有社区板块：人物可以基于自身认知选择公开回应或 SKIP；SKIP 必须返回可见原因。社区事件引发的主动私聊是独立判断，只在该 Contact 的“主动私聊”权限开启时允许 SEND；未私聊也给出简短原因，禁止无声吞掉明确结算。
- 邀请角色参与社区讨论时，公开决定必须在同一层选择 `REPLY_REAL / REPLY_ANONYMOUS / SKIP`。匿名不是已经决定回应后的装饰选项，而是谨慎人物参与讨论的正式替代路径；若唯一顾虑是暴露真实身份，应考虑匿名回应。只有即使匿名也不愿参与时才选择 SKIP，并说明原因。提示框应明确显示实名回应、匿名回应或未回应原因。
- Community Prompt 显式注入 User Persona 身份边界：配角/女配等其他人物设定不得被当作 User 的系统事实。网友仍可造谣/误解，但必须作为未经证实的社区说法，而不是身份串线。
- 自创页新增 `${user}主页` 产品入口：匿名账号可编辑/保存；社区私信建立持久化入口和收件箱数据结构。本轮只建立开口，不宣称私信认知闭环已完成。
- 删除 Contact 的既有 `purgeContactPhoneFootprint` 已审计：`cleanPublicWeb` 会删除该 Contact 作为作者的社区帖子，并从其他帖子中清理其评论、知乎回答及回答评论；这一语义继续保持“删除人物 = moli 小手机中的该人物痕迹彻底清空”。
- User 匿名昵称目前仍保留旧兼容行为；后续与 User主页/匿名Identity 一起统一，不在本轮局部改写旧昵称迁移。

## 社区帖子转发入口与记忆隔离（v0.5.30 后增量）
- 社区帖子转发到微信时，聊天消息保存的是指向原帖的结构化引用入口，核心字段为 `postId / section / platform / title / snapshotAt`；新转发不再复制帖子正文或完整评论区进消息数据。
- 转发卡片属于 User 可见 UI Payload，所有社区板块统一只显示平台与标题，不显示正文预览。卡片是否显示正文与模型能否读取帖子事实是两条独立表现层。
- 角色生成私聊或群聊回复时，Generation 根据 `postId` 从当前 World Scope 的社区 Store 解析原帖，并读取正文、配图语义、回答、楼层、评论和回复等结构化事实。模型不能自行访问前端 Store；该解析层是后台入口的实际实现。
- 每次转发以 `snapshotAt` 作为角色认知水位。角色只能把转发时已经存在的内容视为看过；之后新增的回答或评论不得因引用仍存在而自动泄露。后续内容只有在再次转发、被 @、被邀请、人物实际重新参与等合法认知入口结算后才能进入该人物认知。
- 自动聊天记忆压缩不得展开、复述或总结引用入口后的完整帖子、回答、楼层与评论区；只允许简要记录“User 转发了某帖”以及双方围绕某一主题产生了何种有延续价值的讨论。
- 历史 `communityForward.content` 快照继续兼容读取；不要求迁移或清空旧消息。新消息只写引用字段，导出时必须保留 `snapshotAt`，避免恢复后丢失认知水位。


## v0.5.31 / moli187
- 明确社区互动（邀请评论、邀请回答、@）不再判断是否回应：角色必须回应，只在实名 / 匿名之间选择；模型缺失公开正文时视为生成失败并保留待结算，不伪装成角色 SKIP。
- 通讯录「群聊」按当前 World Instance 过滤，与聊天列表的 World 边界一致：A 正文只见 A 群，正文外只见正文外群。
- 修复删除 Contact 时 World scope 定位：从 Contact 绑定 World + 全部私聊实例解析 scope，删除 NPC/正文人物时其所属 World 的社区、朋友圈、World Event、匿名认知与「我们的墙」scope 数据一并清除，避免 UI 仍显示旧社区帖子。
- 知乎顶部新增「＋」发布入口，复用现有 User 发帖链。
- Character↔Character Awareness 第一段接入社区明确互动：角色公开回答/回复后，直接相关的角色参与者获得该公开互动的已知 World Event；不是把整帖复制给所有角色。帖子转发的 snapshotAt 仍作为私聊查看“转发当时帖子内容”的受控上下文来源。


## v0.5.32 / moli188
- 修复主动行为决策的“上下文素材泄漏成第一条私聊”：Automation 的非 JSON 回退不再把任意模型原文当作 PRIVATE_CHAT；只有显式 `<msg>` 才允许作为兼容回退。柏宝书、记忆总结、正文、Prompt 回显因此不会被直接落成微信气泡。
- 小红书楼中楼展示去除模型正文中重复的“回复 @某人：”前缀；回复关系由结构化 `replyToCommentId` 负责展示，避免 UI 再显示第二遍。
- 知乎发布按钮从社区顶层导航彻底移除，改为知乎问题列表内容区左上角的蓝色圆形“＋”；详情页“增加回答”上移到操作栏右侧，并把原粗分隔缩成细线。
- Character↔Character Awareness 继续扩展到直接 @ 回应：公开回应发生后，与该回复直接相关的角色参与者获得自己的已知 World Event；仍不把整帖广播给所有联系人。
- Community 身份混淆本轮只审计、不修改。实际代码显示：当最近正文存在时，`generatePublicWebRefresh()` 当前只把最近 14 条 / 12000 字正文作为故事 context；Role Fidelity / 世界书补充仅在 recent body 为空时进入 fallback。因此“长世界书没读全”并非唯一可能，现有 Context Builder 确实存在“有最近正文时不同时合并角色资料/世界书”的结构性缺口。下一阶段先设计 World Context Pack，再改生成链，避免直接堆几万字导致 token/身份混淆。


## Community World Context / Awareness 分层（v0.5.33）
- Community 的世界生成上下文必须同时拥有：稳定人物身份锚点、当前正文状态、相关世界书/柏宝书历史；不能因为 recent body 非空就跳过 Identity/Lore。
- NPC 资料卡与正文角色 Role Fidelity 是 Identity Anchor，优先解决“谁是谁”；它们不是人物经历。
- Community 知道 World 事实，不等于每个角色知道。角色知识必须通过 Awareness。
- 角色真正参与帖子或 User 明确转发帖子时，建立带 `snapshotAt` 的帖子认知水位；旧水位不能自动读取未来新增楼层/评论。
- 角色间私密内容只进入直接参与者 Continuity，禁止因为数据库可访问而广播给第三人。


## v0.5.34 / moli190 — runtime regression + Community identity hardening
- Fixed group-chat runtime regression `scopeKey is not defined`: `batchRoleProfile()` had referenced `scopeKey` / `id` without receiving them. Group generation now passes the resolved conversation scope explicitly; this restores both 围读会 and ordinary group generation.
- Provider rejection text is treated as an API failure, not as an assistant utterance. Known Gemini/provider safety/error payloads are rejected before message parsing, so they cannot be saved as character chat bubbles or continuity.
- Community World Context now resolves activated world-book material per World-bound contact instead of selecting only one contact's world book. NPC profile/Role Fidelity remains the identity anchor; each NPC's own activated world-book entries can supplement missing identity facts.
- Removed the final extra truncation pass around the assembled Community Identity Anchor and Recent World State. Source-specific safety budgets remain for now; do not remove all limits blindly because provider context windows are finite. Future work should replace scattered fixed caps with one provider-aware context budget.
- IMPORTANT: one-refresh/one-API Community settlement is NOT falsely marked closed here. Current queued invite/@ settlement and ambient refresh are still separate generation paths. Next package must unify them into one batch settlement request before further Awareness expansion.

### Storage v2：Conversation 容量边界（v0.5.44）
- `localStorage` 只适合轻量配置/索引，不再作为无限增长的全局微信 Conversation 正式数据库。
- 全局 Conversation 使用 IndexedDB `moli-phone-db/global-conversations`。
- 旧 `moli-phone:global-conversations:v1` 必须自动无损迁移；顺序固定为 COPY → VERIFY → SWITCH → CLEANUP，验证成功前不得删除旧值。
- 迁移失败必须保留旧数据并显式报错，禁止为了恢复发送自动清空用户聊天。
- 先解决已证实的 Conversation 爆仓；其他增长型 store 以后根据占用诊断分批迁移，不进行无证据的大规模存储重写。


## Storage v2 hardening (moli205 / v0.5.49)
- Long-lived user data must not depend on localStorage capacity. Contacts and chat wallpaper payloads are now managed by the large-storage adapter and migrate to IndexedDB with the other growing stores.
- Image/Data-URL payloads such as chat wallpapers are explicitly treated as large data; do not introduce new direct localStorage writes for images or other unbounded payloads.
- Startup requests persistent browser storage when supported (`navigator.storage.persist()`); failure or denial is non-fatal and must never block the phone.
- The storage layer exposes origin quota/usage plus logical per-key sizes for future Storage Center diagnostics.
- Invariant: fallback/no-chat remains transient; stable business persistence requires a stable chat scope.
- Do not document external implementation references or provenance for this storage design. Repository documentation records only moli's own architecture and invariants.


## Storage v2 audit closure (v0.5.51)
- Storage Center now renders user-facing data categories instead of raw internal keys.
- localStorage is continuously audited: any moli item >= 128 KB is flagged as a possible growing-data leak.
- Current invariant: long-lived/growing business data belongs in IndexedDB; localStorage is reserved for lightweight settings/runtime markers.
- Do not reintroduce fallback/no-chat as formal persistence.


## moli208 / v0.5.52 — Community interaction reliability
- Community comment/reply long-press is isolated from whole-post deletion: comment action consumes the contextmenu event before post action. Tianya, Xiaohongshu and Zhihu answer comments use explicit comment IDs; deleting a comment never calls the post-delete path.
- Community identity ownership is stricter: every identity fact belongs only to its named character; uncertain attribution is omitted rather than migrated to a similar character.
- The previously observed multi-invite anomaly is intentionally not changed in this version because it is not currently reproducible; do not reintroduce batch settlement merely to address a historical symptom.


## v0.5.53 / moli209 — 聊天壁纸体积优化
- 新导入的聊天壁纸在写入 Storage v2 前先在本地浏览器缩放：最长边最多 2048px，并优先转为 WebP。
- 从质量 0.88 开始，仅当结果仍过大时逐级降低到约 0.64；目标是单张壁纸 Base64 字符串尽量低于约 0.9MB，而不是固定低质量压缩。
- 图片处理完全在本地完成，不上传外部服务；原图文件本身不被修改。
- 仅优化“之后新选择/重新选择”的壁纸。已有 IndexedDB 壁纸不在启动时静默重压，避免未经 User 操作改变现有图像。
- Storage v2 与 localStorage 审计规则保持不变。下一主线回到 Character Knowledge / 跨 App 人物认知闭环。


## v0.5.54 — Character Knowledge bridge (WeChat → Community cognition)
- 微信中的明确第一人称身份披露可以成为角色认知证据：当最近转发/可解析的 Community 上下文中只有一个 User 自己的匿名身份，User 明确说“那个匿名的是我/我发的”等时，只把该事实写入当前私聊角色或群成员的 Awareness/Character Continuity。
- 不复制微信正文到 Community，不使用模型猜测身份，不因同帖参与自动泄露；指代不唯一时不落知识。
- 已知身份通过 World Event + identity awareness 保存，后续同一角色在 Community/微信连续性中可读取；未获证据的其他角色仍不知道。
- 保持“手机内部人物认知连续性”与“我们的墙→正文”边界独立。


## v0.5.55 / moli211 — Zhihu answer action isolation
- Zhihu answers now have their own long-press action target. Long-pressing an answer deletes that answer only; long-pressing a nested comment/reply still deletes only that comment/reply.
- Answer action consumes the contextmenu event before whole-post deletion, preserving the Post Action / Answer Action / Comment Action boundary.
- Character Knowledge v0.5.54 remains intact; this patch deliberately does not broaden uncertain chat statements into factual knowledge.


## moli212 · Character Knowledge v2（2026-09-19）
- 微信中的“人物知道”继续采用认识论分层：**角色听见 User 的明确陈述 ≠ 角色相信该陈述 ≠ 客观世界事实**。
- 对具有明确陈述形态的 User 消息，记录 `USER_EXPLICIT_STATEMENT` World Event，仅表示当前私聊角色/在场群成员亲耳听见；不向未在场角色传播。
- 该事件以 `epistemicStatus=user-assertion / beliefState=unresolved / worldTruth=false` 保存，并进入该角色自己的 Cross-App Character Continuity。后续 Community 角色生成可通过既有 private generation continuity 路径自然使用，但不得改写成系统真相。
- 问句、普通寒暄、模糊聊天不提升为长期人物认知；同一明确陈述有去重键，避免重复发送造成认知膨胀。
- moli210 的“匿名身份=User”明确披露仍属于可确定身份知识，保持原有专门闭环；普通陈述不得冒充这种确定身份事实。

## Community natural participation invariant (moli213)
- A character who has already participated in a Community thread may receive a decision opportunity only when a new relevant User event exists for that thread.
- The opportunity is per-character and uses that character's own continuity/knowledge. It may yield public reply, permitted private message, both, or SKIP.
- SKIP consumes only that decision opportunity. It must not erase awareness or continuity.
- Explicit invite/@ remains a separate stronger path. Do not merge natural participants, targeted characters and ordinary internet actors into one batch model settlement.

### Character Knowledge：行为经历必须保存“做了什么”而非只有“做过” (v0.5.58)
角色在 Community 公开回答/回复后，该行为属于角色自己的跨 App 经历。World Event 应保存实际公开内容及身份方式，使同一角色稍后回到微信或其他手机入口时能够自然记得自己的发言；不得仅保存抽象的“参与了讨论”。这不是 App 文本复制：只记录该角色亲自实施的行为结果及必要 provenance。

匿名身份在角色上下文中必须使用可读的人物名称，不能暴露内部 contact id。身份映射仍严格服从 knownBy：系统知道真实身份，不等于所有角色知道。

## Unified Phone Context（v0.5.59）
跨 App 连续性的所有权属于 Character，而不是 App。微信、Community、朋友圈以及未来新增 App 只是同一人物经历发生的不同地点。

统一读取规则：生成某个角色时，由 Phone Context Builder 根据 `characterId + 当前话题 + 权限/awareness` 组装该角色本人已经亲历、看过、听见或确定知道的手机世界上下文。来源 App 不构成遗忘边界。

隐私/认知边界仍必须成立：角色未参加的私聊不可见；未获得的匿名真实身份不可见；系统真相不得因为帖子对象里存在真实 ID 而泄漏；User 陈述与客观事实保持认识论区分。

**禁止 App-to-App 桥接扩散：** 后续不得为“微信→微博”“微博→Community”等组合建立专用角色认知桥。新 App 只需要：①产生规范事实/awareness/experience；②生成角色时调用 Unified Phone Context。小手机→正文仍由“我们的墙”控制，不因本规则自动注入正文。

## Community Prompt responsibility boundary (v0.5.60)
- Program responsibilities: identify the current character, provide facts/experience/knowledge, expose allowed actions, enforce permissions and parse output format.
- Character behavior authority: character card/personality + User-editable Community preset + current facts. Runtime code must not add a hidden “second personality” that teaches the character how cautious, interested or active it should be.
- Anonymous public identity uses data minimization: the model receives the public nickname/content it can actually observe. Local UI ownership is not character knowledge and must not be promoted into “anonymous nickname = User” truth.
- AI may author a post as User when the generation entry permits it; AI must not author User comments, answers or replies. User-entered UI content is unaffected.
- Machine action enums/JSON/XML-like tags are interface protocol, not behavior policy.


## v0.5.61 / moli217 — Community self-experience anchoring
- Community natural participation now passes the current character's own prior authored lines from the same post as an explicit self-experience block. This prevents a later refresh from re-reading the character's own earlier wording as if it came from an unrelated third party.
- This is deliberately narrow: it does not infer or store hidden identities for other anonymous users. User anonymous comments remain public nickname + public text only unless the character actually learns more through experience.
- A character's own anonymous Community action remains their own experience: anonymity hides the actor from other participants, not from the actor themself.
- Community knowledge snapshots now use the public author name rather than UI-only `uiName`, so an anonymous role's UI decoration cannot leak the hidden real-name label into another character's cognition.
- No changes to Community autonomy opportunity/candidate gating, v0.5.60 Prompt cleanup, proactive-chat tendency, or the 6–8 generation counts.


## moli218 / v0.5.62 — Unified Context + Community 自由 @
- Community 编辑器右上角 `@` 不再打开角色下拉框；点击只向编辑框插入 `@`。提示文字说明可直接填写角色本名、角色在当前帖子使用过的小号公开 ID、以及当前帖子出现过的路人 ID；提示点击或继续输入后消失。
- `本名↓` / `@↓` 的箭头移除，保留 `本名` / `@`。
- Community 各板块共用编辑器取消整页背景模糊；编辑器自身提高不透明度，保证文字不穿透，同时允许 User 边看帖子边编辑。
- 自由 @ 解析遵守“公开身份负责显示，内部身份只负责路由”：`@角色本名` 路由真实角色并强制本名回应；`@角色已在本帖使用的小号` 路由同一真实角色但强制继续以该公开小号回应；内部 contactId 不得因此暴露给其他角色。
- 当前帖子已出现的普通路人也可被 @。路人保持帖子内连续性：生成时携带该公开 ID 在本帖此前真实发言，不创建联系人、不进入跨 App Character Experience。
- 明确邀请 / @ 的角色结算入口补入 Unified Phone Context，使角色在 Community 回应时可读取属于自己的微信/手机经历；不新增 WeChat→Community 专用桥。
- 继续坚持：控制信息，不控制推理。程序只负责权限、事实与路由，不替角色建立额外的身份推理评分或认知图。


## v0.5.63 / moli219 — 三地人物经历闭环
- 微信、Community、朋友圈继续各自保存事实与执行队列，但真实角色生成统一读取 `buildPhoneContext`；不建立 App-A → App-B 的成对桥。
- 角色自己的朋友圈刷新与公共朋友圈批量刷新都显式注入 Unified Phone Context，包含该角色亲历的微信、Community 与朋友圈事实。
- Community 的自然参与、明确邀请与自由 @ 继续在真实角色生成入口注入同一人物上下文；自由 @ 的后台路由身份不等于其他角色的认知身份。
- 验收以最终生成请求实际携带人物经历为准，不能只以 store/builder 已存在为准。
- Community 编辑框：帖子不模糊；编辑板灰调提高、不再使用突兀细线边框；本名/@ 保持上方布局，@说明紧随 @，点击/继续输入后消失；发送按钮行为不变。
- 原则：App 只是地点，角色才是记忆主人；控制信息，不控制推理。


## v0.5.64 / moli220 — 微博第一版
- Community 顶栏新增「微博」，位于「知乎」与「自创」之间；微博 App 图标直接进入该板块。
- 第一版只保留「首页 / 热搜」：首页混合关注账号、同城、实时内容；热搜显示热搜榜与热门微博。
- 微博独立 Prompt：实时公共舆论场、话题标签、媒体/大V/普通网友/知情人混流、转发链、评论生态；热门/同城/实时/明星/搞笑不做硬频道，而作为按世界观自然取舍的内容关键词。
- 微博卡片使用纯 UI 稳定字头像，不把头像送入模型；附图继续使用文字图片描述，不调用图片生成。
- 新增微博持续关注账号：User 可手动输入「ID；设定」创建常驻关注账号；任何已出现微博 ID 均可点关注。对自然出现的 ID，关注时由模型只根据已出现公开内容总结一条账号画像，不得凭空补全；以后刷新会把关注档案提供给微博生成。
- Character 仍高于微博普通账号：角色本名/小号继续走 Character Experience。角色已经实际使用过的小号会作为已有账号事实提供给后续社区参与；再次使用小号时优先延续已有 ID，只有人物确有理由时才新建。
- 三层连续性保持：Character 跨 App；已关注微博账号跨微博刷新；未关注路人仅保留局部讨论连续性。
- 微博内部路由身份不等于人物认知；User UI 可显示角色小号归属，但公开世界只接收公开 ID。


## moli221 / v0.5.65 · 微博第一版收口
- 微博继续独立于「社区推荐」：社区推荐仍只混合天涯/小红书/知乎/自创；微博刷新属于整块微博时间线。
- 微博刷新改为只增不删：旧微博与既有互动保留，新一轮微博追加进入历史；刷新不再重建整个微博板块。
- 微博顶部改为居中的「首页 / 热搜」，右上保留刷新并增加仿微博红底「＋」发布入口；移除顶部独立关注入口，关注统一放在作者卡片。
- 热搜页顶部增加搜索框。点击后出现白底方框，可直接搜索关键词并生成相关实时微博，也可按 ID / 身份（可选）/ 人设 / 语言风格 自创持续关注人。身份不是必填字段，只作为账号公开定位。
- 发布微博页支持正文、@、# 与 1~9 张文字图片描述；图片在 UI 中以灰底 1~9 宫格呈现，模型只接收描述。
- 首页长微博由前端截断为预览并显示「…正文」，完整正文只保存一份，进入详情显示完整内容。
- 微博详情页采用独立「微博正文」布局；去掉真实微博无关的打赏/广告/外部平台区，保留 moli 的社区工具，并增加微博自己的转发 / 评论 / 赞操作区。
- 搜索/刷新生成仍使用同一微博世界上下文与关注账号档案；搜索是本次内容意图，不建立新的平行记忆系统。


### v0.5.66 / moli222 微博首轮实机打磨
- 微博刷新每次 6~8 条，普通时间线保留最新 20 条，常驻内容不计入上限。
- 微博正文增加评论刷新；首页与评论刷新在请求期间持续旋转。
- 图片/视频使用方形媒体描述格，格式为“图片：（内容）”“视频：（内容）”。
- 关注完全由 User 决定；首页与正文共用同一关注状态。
- 微博评论支持楼中楼关系、博主标签、转发/回复/赞入口；初始评论按冷热自然变化。
- Community 所有板块继续沿用自由 @：角色本名、角色已出现小号、当前帖子已知路人 ID；@ 注释放在编辑文本区域内。


## v0.5.67 / moli223 微博实机 UI 修正
- 单张媒体不再铺满正文宽度；图片/视频统一使用微博式方形媒体格。
- 微博首页与评论刷新在请求期间持续旋转，补齐实际 keyframes。
- 转发微博保留来源账号视觉信息；新转发同时记录公开来源账号。
- 微博正文与评论区改回普通字号/字重；评论含头像、微博式动作图标、首条楼中楼预览与“共 n 条回复”展开。
- 关注切换改为立即落库/立即刷新 UI，账号画像分析异步补充，避免点击关注时像无响应。
- 发微博编辑器移除常驻图片描述框；底栏图标化。图片/视频点击后使用独立编辑框，右下角添加；添加后以媒体格展示并可继续加。图片最多 9 张，视频最多 1 个。
- 发微博输入 @ 时显示与 Community 一致的自由 @ 说明：角色本名、已出现小号 ID、当前帖子已知路人 ID。


### moli224 / v0.5.68 · 微博最终美化收尾
- 微博首页发布时间改为按 createdAt 动态显示：刚刚 / N分钟前 / N小时前 / N天前 / 日期时间，刷新重绘后自然更新。
- 转发原博来源识别放宽：优先结构化 repostAuthor，并可从转发正文/转发链中的 @ID 回退恢复来源头像与ID。
- 评论操作三图标收紧间距。
- 发微博 @ 提示改为紧跟 @ 的行内提示；文案为“可写角色名字（强制角色用大号回应）、角色的小号、已出现过的路人ID”，点击提示即消失。
- 本轮不改默认关注初始化，留待后续“关注人列表/管理关注人”功能节点统一处理。


## v0.5.69 微博持续账号 / 私信
- 微博关注账号可标记为 🔥持续互动账号；🔥账号保存微博私信与实际互动记忆，不因此获得微信、正文或其他人物秘密。
- 微博增加私信/关注入口：关注列表可查看账号画像、编辑设定、切换🔥并发私信。
- 关注账号仍是微博网络账号；未来可显式升级为“网友”联系人，但本版本不自动升级。
- @提示仅在需要时出现一次；Community 评论编辑器提示与 @ 按钮同排，避免遮挡输入文字。


## v0.5.70 / moli221 — Community规则分层 + 微博Preset正式化

- 将所有板块共同遵守的硬规则收口为 `Community 通用契约`：刷新只追加不重写、User发言边界、@身份连续、路由身份不等于人物认知、真实经历才进入人物连续性、失败行为不得记成已发生。
- `本人账号与楼主身份` 不再重复“AI可代User发帖但不可代评论/回答/回复”，该规则由全局契约统一负责。
- 新增独立 `微博` Community Prompt 条目：首页=关注/同城/实时混流；热搜=集中话题+热门内容；账号生态、媒体描述、评论、转发链、关注账号与🔥持续互动能力均有明确职责。
- 微博生成器内部 Prompt 缩减为机器执行协议（字段、JSON、路由边界），不再维护第二套微博风格，避免Preset与代码内Prompt互相打架。
- 设计分层固定为：Community全局契约 → 板块机制/功能 → 板块风格Preset。后续新增板块不得复制共同铁律。
- 本轮不继续美化微博私信/关注入口，也不实现“网友→微信联系人”；这些留在微博主体功能稳定后集中处理。


## moli222 / v0.5.71 微博公共区第一阶段
- 微博顶层入口固定为：首页｜超话｜热搜。首页负责关注/同城/实时混流；热搜负责热点与热门微博；超话负责正文已知角色之间的CP粉丝社区。
- 微博刷新继续严格追加，不清空历史。不同顶层入口把自己的生成意图送入同一微博生成链，不另建独立社交系统。
- 超话只使用当前世界已知正文角色组合关系，允许CP粉、唯粉、毒唯、对家、拆家、乐子人、考据党、产粮用户等生态；公开事实与粉丝猜测严格分层。
- 继续沿用微博既有评论/回复/自由@、转发链、关注账号、稳定ID、小号连续性和Unified Character Experience；程序路由身份不等于角色认知。
- 关注/🔥/私信保留现有原型，本阶段不升级“网友”第四人物来源。


## v0.5.72 · 微博网络联系人 / 消息中心第一版
- 微博原“私信 / 关注”原型改为正式双页入口：`关注人｜消息`；🔥持续网友优先排序。
- 关注人列表显示头像、ID、简短账号设定与“已关注”；取关必须二次确认，并删除该账号的持续认知记忆与微博私信记录。
- 关注人资料页支持编辑设定、更换本地头像、进入私信、切换🔥。从帖子主页关注时继续异步生成简短公开账号画像。
- 消息页保留“@我的”“评论”聚合入口，私信会话显示未读红点/数量；打开对应入口后记为已读。
- 右上角 `⊕` 可新建粉丝群；第一版完成群创建、成员选择与本地消息容器，后续网络人物批量生成再接群内AI发言。
- 私信页顶部提示改为“添加小火花，成为持续网友吧！”，🔥显示在账号ID旁。未关注的普通微博路人也可进入消息列表；在自然情境下可主动 @User / 私信 User。🔥账号获得更熟稔、更主动的机会，但不是强制行动。
- 微博刷新生成协议新增可选 `privateMessage`：普通路人仅在有自然动机时使用；收到路人私信不会自动把对方变成关注人，User点击🔥时才会建立关注并成为持续网友。
- 后台路由/关注/🔥状态仍不等于其他人物的认知；人物只获得自己实际经历的信息。

## Community User IDs / Weibo network UI (v0.5.73)
- User 的公开Community身份由“全社区ID管理”统一维护，可保存多个“大号/小号”；这是跨板块公开身份池，不再以自创板块单个“匿名账号”字段表达。
- 微博网络中心是Community全局私信/持续网友布局的试验基线：关注人/消息为顶部主分栏；账号显示不自动加@符号。
- 私信采用“先发送、后请求回复”的显式两步交互，避免每条User消息自动消耗一次API。
- 表情按钮仅预留UI，不在本版本引入表情包数据结构。


### v0.5.74 startup hotfix
- Fixed startup import failure: `src/ui/phone-panel.js` imports `deleteCustomCommunities`, so the canonical `src/storage/public-web-store.js` now exports the matching batch-delete helper. The prior patch had the helper only in the obsolete/root duplicate store file, which does not satisfy the runtime import.


## v0.5.75 UI close-out
- 自创主页的社区身份入口改为居中的下划线 `ID card`，ID 管理直接在主页卡内展开，不再弹独立弹窗；支持大号/小号，长按删除提示固定在展开框底部。
- Community 评论/回复编辑器统一使用“大号 / 小号 / 楼主”；选择“小号”时在右侧展开已有小号列表，选中后以 `ID：xxx` 显示当前公开身份。
- 网络消息页和私聊页继续压缩顶部高度；私聊返回键叠放到左上，不再为返回标题单独占一整行。🔥提示只是 ID 下方的临时提示，成为持续网友后完全消失。
- 消息会话支持长按清空该会话；私聊中的单条消息支持长按删除。
- 本轮为 UI 收口；后续停止继续做纯美化，回到 Community / Network Actor 功能闭环。
