# fourth-wall-phone SPEC

> 当前状态：设计阶段  
> 版本：v0.2  
> 原则：本文件只记录当前确认后的最终规则。旧方案若被推翻，直接以本文件最新内容为准。

---

## 1. 项目定位

`fourth-wall-phone` 是一个面向 SillyTavern / 酒馆助手环境的“场外通讯系统”。

它不是正文聊天本身，而是一个独立于主剧情之外的手机式通讯层，用来让：

- 第四面墙人格
- 编剧
- 攻略
- 红笔编辑
- 酒馆角色
- 用户自定义联系人
- 多人群聊

在“正文之外”与用户交流、吐槽、点评、讨论剧情。

核心体验目标：

1. 像微信一样管理私聊和群聊。
2. 场外内容默认不进入正文。
3. 用户需要时可通过“采纳”把建议注入下一轮正文。
4. 酒馆角色必须尽量保持正文中的人设、关系状态、语言风格与长期记忆。
5. 场外系统使用独立 API，不干扰正文主 API。
6. 多存档严格隔离，避免不同剧情档互相串线。

---

## 2. 当前基础

现有 Lite 原型已经具备：

- 可拖拽悬浮球
- 简单聊天窗口
- 手动聊天
- 读取当前 SillyTavern 上下文
- 独立聊天历史
- 简化版提示词
- `generateRaw` / `generateQuietPrompt`
- 上下文层数等基础设置

后续开发保留悬浮球入口，但逐步替换 Lite 的简单聊天结构。

---

## 3. 首页与入口

### 3.1 悬浮球

点击悬浮球后，进入微信式“聊天列表首页”。

### 3.2 首页顶部

顶部不放搜索按钮。

右上角只保留两个入口：

#### `＋`

弹出：

- 同步酒馆角色
- 添加联系人
- 发起群聊

#### `设置`

进入全局设置。

当前至少包含：

- 独立场外 API
- 自动吐槽全局总开关
- 自动点评全局总开关
- 提示词与预设

后续全局功能继续从这里扩展。

---

## 4. 内置联系人

以下联系人首次启用插件时自动存在：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

它们：

- 自动进入全局通讯录
- 自动为当前酒馆存档创建私聊会话
- 自动出现在当前存档的聊天首页

内置联系人不需要用户手动添加。

### 4.1 内置联系人删除规则

内置联系人不做真正物理删除。

可以：

- 隐藏
- 停用
- 恢复默认提示词

但不能像普通联系人一样彻底删除系统定义。

---

## 5. 自定义联系人

### 5.1 创建方式

`＋ → 添加联系人 → 新建空白人物`

不设置联系人类型。

原因：

- 人格提示词已经定义“这个人是谁”
- 不需要额外再用“NPC / 陪聊 / 作者 / 其他”等标签限制行为

### 5.2 新建字段

新建空白联系人时只需要：

- 名称
- 头像
- 简介 / 一句话描述
- 人格提示词

不提供：

- 联系人类型
- 是否允许加入群聊
- 是否读取正文

### 5.3 上下文读取

所有联系人默认自动读取当前存档上下文。

不同联系人根据自身数据结构自动获得不同上下文包，不要求用户手动配置“能不能看正文”。

创建成功后：

- 加入全局通讯录
- 自动为当前存档创建私聊
- 自动进入当前聊天首页

---

## 6. 同步酒馆角色

### 6.1 添加流程

`＋ → 同步酒馆角色`

弹出当前酒馆角色选择列表。

支持：

- 多选
- 已添加角色标记
- 点击“添加”后批量导入

添加完成后：

- 进入全局通讯录
- 自动为当前存档创建私聊
- 直接出现在当前聊天首页

### 6.2 不采用自动全量同步删除

不能因为酒馆角色卡被删除，就自动删除手机联系人。

源角色不存在时：

- 联系人继续保留
- 所有场外聊天历史继续保留
- 显示“来源失效 / 来源角色不可用”
- 后续可重新绑定角色

### 6.3 自动跟随刷新

酒馆联系人会自动跟随刷新其“来源数据”，包括：

- 酒馆角色原名
- 原角色头像
- 角色设定
- 性格
- 场景
- Example Dialogue
- 其他角色卡相关数据

但是自动同步只能更新来源层。

不能覆盖用户手动层。

---

## 7. 联系人资料模型

酒馆角色联系人同时保存：

- 联系人 ID
- 酒馆来源角色 ID / 可稳定引用信息
- 酒馆角色原名
- 酒馆原头像
- 用户备注名
- 用户自定义头像（可选）
- 用户简介 / 一句话描述
- 人格提示词相关配置
- 来源状态
- 最近同步状态

### 7.1 名称显示规则

优先级：

`用户备注名 > 联系人显示名 > 酒馆角色原名`

酒馆角色原名永远保留，不会因为用户改备注而丢失。

### 7.2 头像显示规则

优先级：

`用户自定义头像 > 酒馆来源头像`

如果用户手动换过头像：

- 酒馆角色卡以后换头像，不覆盖用户自定义头像

用户可选择：

- 恢复跟随角色卡头像

### 7.3 联系人详情页

入口：

`私聊右上角 … → 聊天信息 / 联系人资料`

建议结构：

- 头像
- 当前显示名
- 原角色名（酒馆角色）
- 简介
- 备注名
- 头像
- 简介
- 人格与提示词
- 酒馆角色信息
- 来源状态
- 重新绑定角色
- 恢复跟随头像 / 名称
- 删除联系人（非内置）

---

## 8. 联系人与会话分离

这是核心数据规则。

### 8.1 联系人是全局的

以下内容跨酒馆存档共享：

- 联系人本体
- 内置人格
- 自定义联系人
- 酒馆联系人来源信息
- 联系人备注
- 用户自定义头像
- 联系人人格 Prompt
- 提示词配置

### 8.2 会话按酒馆存档隔离

以下内容必须按当前酒馆聊天 / 存档隔离：

- 私聊历史
- 群聊
- 群聊历史
- 自动吐槽状态
- 自动点评状态
- 自动点评正文回合计数
- 未读消息
- 当前档会话列表
- 与当前档相关的角色动态状态
- 当前档场外上下文

### 8.3 多存档规则

同一个酒馆角色可以在多个存档中存在不同场外聊天。

例如：

- 祈年 / A 档 → A 档私聊
- 祈年 / B 档 → B 档私聊

两者绝不共享聊天记录。

### 8.4 切换酒馆存档

切换酒馆存档后：

- 手机聊天首页切换到该存档自己的会话空间
- 通讯录仍然全局存在

如果新档从未和某联系人聊过：

- 他仍在通讯录
- 不自动出现在新档聊天首页
- 用户从通讯录点进去后，才建立该档的新私聊

### 8.5 内置人格例外

内置人格：

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

在每个新存档第一次进入时，都自动创建当前档私聊并显示在首页。

---

## 9. 聊天首页

首页采用微信式聊天列表。

每个聊天项显示：

- 头像 / 群头像
- 联系人名 / 群名
- 最后一条消息预览
- 最后消息时间
- 未读数字 / 红点
- 置顶状态

排序：

1. 置顶聊天优先
2. 其余按最新消息时间降序

### 9.1 未读

后台产生的：

- 自动吐槽
- 自动点评
- 其他自动场外消息

如果当前会话没有打开，则计入未读。

当前正在打开的会话：

- 不增加未读

打开聊天后：

- 清空该会话未读

---

## 10. 私聊

### 10.1 基本布局

微信式私聊布局。

顶部：

- 返回
- 联系人名称
- 右上角 `…`

右上角进入：

- 聊天信息
- 联系人资料
- 当前聊天设置

### 10.2 私聊消息

场外人格消息与用户消息正常显示气泡。

### 10.3 自动点评

私聊不提供自动点评。

### 10.4 自动吐槽

私聊设置：

- 自动吐槽：开 / 关
- 触发概率：0–100%

---

## 11. 群聊

### 11.1 创建

`＋ → 发起群聊`

用户选择联系人创建。

群名：

- 用户自定义

群头像：

- 模拟微信群头像
- 将所有当前群成员头像拼接成九宫格 / 多人格头像组合

### 11.2 群成员

支持：

- 后续加人
- 踢人

群成员排序：

- 不作为 AI 发言权重
- 无业务意义

### 11.3 群聊消息 UI

采用真实微信式群聊布局：

- 每条人格消息显示头像
- 每条人格消息显示昵称
- 显示消息气泡
- 连续同一人发言也不合并头像和昵称

### 11.4 删除群成员

成员离开 / 被踢出后：

- 过去的群聊历史仍保留其旧消息
- 不清理历史记录

---

## 12. 群聊发言编排器

群聊分成两种模式：

1. 普通群聊
2. 自动点评

### 12.1 普通群聊

用户主动发送群聊消息时：

- 不强制所有人回复
- 不设置固定 1～3 人上限
- 最大自然上限就是群成员数量

AI 根据：

- 当前话题相关度
- 角色立场
- 人格关系
- 是否被 @
- 是否有明显插话价值

判断谁说话。

### 12.2 第一位发言者

普通群聊第一位：

- 按相关度决定
- 不纯随机

### 12.3 @

用户 @ 某人格：

- 被 @ 人格必须参与

但：

- 未被 @ 的其他成员仍可自然插话

### 12.4 后续成员必须接前文

同一轮内：

第二位看到：

- 用户消息
- 第一位的新回复

第三位看到：

- 用户消息
- 第一位
- 第二位

以此类推。

允许：

- 赞同
- 反驳
- 补充
- 调侃
- 转移话题
- 自然接话

禁止所有人彼此不可见地独立答题。

---

## 13. 群聊 API 生成策略

采用：

**轻编排 + 逐人生成**

### 13.1 编排请求

先发一次轻量请求。

只负责决定：

- 本轮谁说
- 发言顺序

编排阶段不加载每个人完整角色卡，只给足够判断相关度的短描述。

例如：

- 名称
- 简短身份
- 简短性格
- 与当前话题关系
- 是否被 @

### 13.2 发言生成

真正轮到某人格说话时：

单独发一次生成请求。

该请求只加载该人格自己的完整上下文。

例如：

`Speaker 1 → 完整人格包 → 生成`

然后：

`Speaker 2 → 自己的完整人格包 + Speaker 1 新消息 → 生成`

依次继续。

### 13.3 目的

该方案优先保证：

- 人格隔离
- 角色声线稳定
- 不串人设
- 后发言者真正读取前面新消息
- 酒馆角色尽量像正文里的本人

不采用“一次请求同时扮演所有群成员”作为默认模式。

---

## 14. 自动吐槽（Commentary）

### 14.1 定位

自动吐槽是概率型主动行为。

特点：

- 短
- 即时
- 情绪化
- 像看到正文后的第一反应

不是正式剧情分析。

### 14.2 全局开关

系统设置提供：

- 自动吐槽总开关

关闭总开关：

- 只暂停
- 不删除每个会话自己的配置

重新开启：

- 恢复原配置

### 14.3 每会话配置

私聊 / 群聊分别独立保存：

- 自动吐槽开关
- 触发概率 0–100%

### 14.4 不使用业务冷却

不提供用户可设置的冷却时间。

100% 应意味着：

- 每个符合条件的新事件都允许触发

### 14.5 技术去重

底层保留短暂同事件去重锁：

- 只防止同一个酒馆事件被重复广播
- 不属于“冷却”
- 不暴露设置

### 14.6 触发事件

至少包括：

- AI 正文生成完成
- 用户编辑自己的正文消息
- 用户编辑 AI 正文消息

每次正文事件后：

- 每个开启自动吐槽的会话独立进行概率判定

同一正文事件可以同时触发多个私聊 / 群聊。

### 14.7 群聊自动吐槽

如果群聊概率命中：

- 使用普通群聊编排规则
- 不强制全员
- AI 判断本轮谁适合短暂吐槽

---

## 15. 自动点评（Review）

### 15.1 定位

自动点评是周期型群聊行为。

特点：

- 每 N 个正文回合触发
- 全员参与
- 更完整、更分析型
- 与自动吐槽完全独立

### 15.2 只存在于群聊

私聊没有自动点评。

### 15.3 群聊设置

每个群聊保存：

- 自动点评开 / 关
- 每 N 个正文回合触发一次

### 15.4 全员必须参与

自动点评触发后：

- 当前群内每一个人格至少发言一次

### 15.5 发言顺序

第一位：

- 随机

后续：

- 必须看见前面的本轮新消息
- 根据已有讨论继续接话

允许：

- 赞同
- 反驳
- 补充
- 改变讨论重点
- 二次发言

### 15.6 二次发言

每人至少一次，但不限制恰好一次。

如果讨论自然需要：

- 已经发言过的人可以再次回应

### 15.7 单轮安全上限

隐藏规则：

`单轮最大场外消息数 = 当前群成员数 × 2`

不作为用户设置项。

### 15.8 正文回合定义

一个正文回合 = 一次有效 AI 正文回复。

用户消息本身：

- 不单独计一个自动点评回合

### 15.9 删除与重 Roll

自动点评计数根据当前有效正文状态判断。

规则：

- 删除有效 AI 正文 → 回合回退
- 删除多条 → 按实际状态回退
- 重 Roll 同一条 AI 正文 → 不额外 +1
- 如果重 Roll 的位置正好位于 N 边界，可针对新版本正文重新触发对应点评

---

## 16. 不提供“重新生成”按钮

场外聊天 UI 不提供：

- AI 消息重新生成
- 群聊整轮重新生成
- 自动点评重新生成按钮

这是明确取消的功能。

---

## 17. 消息长按菜单

长按消息支持：

- 引用
- 复制
- 转发
- 多选
- 删除
- 采纳（适用时显示）

不提供：

- 重新生成

### 17.1 转发

转发时：

- 弹出聊天列表
- 可转发到其他私聊 / 群聊

建议采用：

- 带来源信息的转发卡片 / 转发消息样式

避免直接复制成普通文本导致来源混淆。

### 17.2 多选

多选模式至少支持：

- 批量删除
- 批量转发

---

## 18. 删除与清空规则

### 18.1 删除联系人

删除普通联系人时：

同时删除：

- 联系人资料
- 与该联系人相关的所有存档私聊数据
- 与其专属联系人数据

必须二次确认。

警告文案应明确：

“将同时删除该联系人在所有存档中的场外聊天记录，无法恢复。”

### 18.2 群聊历史中的旧成员消息

删除联系人 / 踢出群成员：

- 不自动删除其过去群聊消息

### 18.3 清空聊天记录

清空某个聊天记录时：

只删除：

- 当前会话历史

保留：

- 联系人
- 人格 Prompt
- 自动吐槽设置
- 自动点评设置
- 其他会话配置

### 18.4 恢复默认人格

“恢复默认人格 / 恢复默认提示词”只重置人格配置。

不会删除：

- 已有场外聊天历史
- 会话
- 群聊历史

例如：

第四面墙可恢复 LittleWhiteBox 默认 Meta Protocol。

自定义空白联系人没有“系统默认模板”，因此不提供这一恢复项。

---

## 19. 提示词与预设

系统设置提供：

`提示词与预设`

采用两层结构：

### 19.1 全局生成框架

管理所有场外生成共享的提示词组装结构。

### 19.2 联系人配置

选择联系人后管理该人格专属配置。

下拉列表按组展示：

#### 内置人格

- 第四面墙
- 编剧
- 攻略
- 红笔编辑

#### 酒馆角色

- 当前已同步的角色

#### 自定义联系人

- 用户新建人物

从：

`设置 → 提示词与预设 → 联系人配置 → 某联系人`

与：

`联系人资料 → 人格与提示词`

打开的是同一份数据。

不能复制两份配置。

---

## 20. 全局生成框架

默认逻辑块：

1. 场外系统协议
2. 当前任务类型
3. 用户 Persona
4. 当前发言人格
5. 角色保真包（酒馆角色专用）
6. 当前世界 / 相关世界书
7. 当前存档正文上下文
8. 时间与消息间隔
9. 当前场外会话历史
10. 本轮用户消息 / 正文触发事件
11. 群聊本轮已产生的新消息
12. 输出规则

这些逻辑块：

- 可查看
- 可开关
- 可拖动排序
- 可按权限编辑

核心系统项不能轻易物理删除。

---

## 21. 酒馆角色保真（Role Fidelity Pack）

目标：

**手机里的酒馆角色，要像正文里那个正在活着的角色。**

不是只读取静态角色卡。

### 21.1 核心来源

酒馆角色生成时优先组合：

1. 角色身份与硬设定
2. 当前存档剧情事实 / 关系状态
3. 当前角色动态状态
4. Example Dialogue / 角色语言示例
5. 最近正文中的真实说话样本
6. 当前场外聊天历史
7. 本轮消息

### 21.2 角色卡与当前剧情关系

角色卡负责：

- 这个人是谁

当前正文 / 记忆负责：

- 这个人现在变成了什么样
- 当前关系走到哪一步
- 当前有哪些承诺、冲突、秘密、状态

### 21.3 语言声纹

系统应尽量提取最近正文中该角色的真实表达方式。

重点学习：

- 用词习惯
- 句子长短
- 称呼
- 标点
- 是否常反问
- 情绪表达强弱
- 是否主动解释
- 是否回避直白表达
- 幽默方式
- 线上说话相对现实是否更松弛

要求：

**学习正文中的角色声纹，但不要照抄正文的第三人称小说叙述格式。**

### 21.4 Example Dialogue

角色卡的 Example Dialogue 作为高价值角色语言来源。

角色设定说明“他是什么样的人”。

Example Dialogue 更直接说明“他具体怎么说话”。

---

## 22. 柏宝书 Memory Provider

支持接入：

`ST-BaiBai-Book`

仓库：

https://github.com/baibai-git/ST-BaiBai-Book

### 22.1 定位

柏宝书作为：

**可选增强记忆提供器**

不能作为硬依赖。

### 22.2 检测

如果存在：

`globalThis.STBaiBaiBook`

则启用柏宝书公开只读 API。

如果不存在：

- 插件继续正常运行
- 使用自己的最近正文读取机制

### 22.3 推荐使用的公开能力

优先考虑：

- `getInjectedHistory()`
- `getSnapshot()`
- `getContextAtFloor()`
- 必要时 `getHistory()`
- 必要时 `getVar()`

### 22.4 长期记忆

优先使用：

`getInjectedHistory()`

原因：

- 与柏宝书正常记忆注入口径一致
- 可跳过仍在滑动窗口内的近期正文
- 避免“最近原文 + 最近摘要”重复占 token

组合方式：

`柏宝书长期记忆 → 最近正文原文 → 当前场外聊天`

### 22.5 当前状态

`getSnapshot()` 可用于增强：

- 当前故事时间
- 地点
- 主角状态
- 变量
- 物品
- 计划
- 场景
- NPC
- 其他当前剧情状态

### 22.6 精确楼层上下文

处理旧楼层相关事件时，可使用：

`getContextAtFloor({ floor })`

以获得该楼：

- 前置历史
- 楼层摘要
- 楼层前状态
- 楼层后状态

避免拿当前 500 楼状态去错误解释第 100 楼事件。

### 22.7 覆盖完整性

如果柏宝书返回：

`coverage.complete === false`

不能把它当成完整记忆。

此时：

- 继续保留最近正文原文
- 不假设记忆源毫无缺口

---

## 23. 不同联系人上下文组装

所有联系人默认读取当前存档上下文，但按类型自动组装。

### 23.1 酒馆角色

可使用：

- 角色卡
- 性格
- 场景
- Example Dialogue
- 相关世界书
- 柏宝书记忆
- 柏宝书当前状态
- 当前存档最近正文
- 最近角色语言样本
- 当前场外历史

### 23.2 第四面墙

使用：

- LittleWhiteBox Meta Protocol
- 当前存档长期记忆
- 最近正文
- 当前场外历史
- 当前触发任务

### 23.3 编剧 / 攻略 / 红笔编辑

使用：

- 各自专属 Prompt
- 当前存档剧情摘要 / 长期记忆
- 最近正文
- 当前场外历史
- 当前任务

### 23.4 自定义联系人

使用：

- 自定义人格提示词
- 当前存档长期记忆 / 当前状态
- 最近正文
- 当前场外历史

---

## 24. 独立场外 API

### 24.1 与正文 API 分离

所有场外功能统一使用独立 API 层：

- 私聊
- 群聊
- 自动吐槽
- 自动点评
- 编剧
- 攻略
- 红笔编辑
- 酒馆角色

### 24.2 统一适配层

结构：

`场外功能 → Generation / Agent 层 → Provider Adapter → 实际 API`

不同功能不各自直接写网络请求。

### 24.3 当前版本

第一阶段：

- 所有联系人共用一套全局场外 API

后续底层预留：

`联系人 API / 模型覆盖 > 系统默认场外 API`

但当前 UI 不开放联系人级 API。

### 24.4 Provider 方向

底层不要写死单一 OpenAI 请求格式。

设计上允许：

- OpenAI
- OpenAI-compatible
- Claude
- Gemini
- 其他后续 Provider

### 24.5 API 设置页面

预计包含：

- Provider / API 类型
- API 地址
- API Key
- 模型
- 刷新模型列表
- 测试连接
- 流式生成

第一版具体 Provider 范围后续再定。

### 24.6 流式与中断

统一 Generation 层支持：

- 流式输出
- 停止生成
- 错误处理
- 请求取消

---

## 25. 第四面墙来源

第四面墙人格基于 LittleWhiteBox 的第四面墙设计。

需要保留：

- 完整 Meta Protocol
- 原版自动 commentary 思路
- 时间与消息间隔
- 流式
- 停止
- 上下文清洗
- 提示词可编辑
- 恢复 LittleWhiteBox 默认 Prompt

明确不做：

- LittleWhiteBox 的 180 秒用户级 commentary 冷却
- 图片生成
- TTS / Voice
- Fullscreen
- 图像增强
- Voice enhancer

---

## 26. “采纳”机制

场外内容默认不会进入正文。

后续加入：

`采纳`

适合：

- 编剧建议
- 攻略建议
- 红笔编辑意见
- 其他场外建议

未定细节：

- 以什么 Prompt 注入正文
- 一次性还是持续
- 注入多久
- 群聊消息如何采纳
- 多条采纳如何合并

---

## 27. 第三方参考与发布边界

项目计划公开发布，因此从现在起按可发布标准开发。

### 27.1 LittleWhiteBox

仓库：

https://github.com/RT15548/LittleWhiteBox

其项目使用 Apache License 2.0，并额外要求 attribution。

如果项目引用、修改或分发 LittleWhiteBox 文件，应在 README / Credits 中加入原作者署名。

建议：

`Based on LittleWhiteBox by biex`

发布前还需要整理：

- License
- NOTICE
- attribution

### 27.2 Acsus-Paws-Puffs

仓库：

https://github.com/Hmkovo/Acsus-Paws-Puffs

其许可证为 All Rights Reserved，并明确限制：

- 未授权修改
- 二次开发
- 分发
- 把其代码用于其他项目

因此公开版本中：

**不能直接复制其代码、CSS、默认提示词等实现。**

可以：

- 研究功能设计
- 研究交互流程
- 理解算法思想
- 然后按本项目自己的架构重新实现

### 27.3 柏宝书

仓库：

https://github.com/baibai-git/ST-BaiBai-Book

本项目只计划调用其公开只读 API：

`globalThis.STBaiBaiBook`

优先使用公开文档中支持的接口，不依赖其内部私有实现。

---

## 28. 暂不纳入第一版的功能

不优先做：

- 钱包
- 转账
- 会员
- 模拟生活
- 天气生活系统
- 复杂图片系统
- 表情包生态
- Fullscreen
- TTS
- 图片生成
- 聊天背景深度美化
- 大量 QQ / 微信附加玩法

项目不能膨胀成完整手机模拟器。

核心仍然是：

**场外角色通讯 + 群聊讨论 + 自动吐槽 + 自动点评 + 角色保真。**

---

## 29. 当前已确认的开发顺序

建议顺序：

1. 数据结构与存档隔离
2. 联系人与同步酒馆角色
3. 私聊会话
4. 群聊与成员管理
5. 群聊轻编排 + 逐人生成
6. 自动吐槽
7. 自动点评
8. Role Fidelity Pack
9. 柏宝书 Memory Provider
10. 独立场外 API
11. 提示词与预设
12. 转发 / 多选 / 采纳
13. 最终 iPhone / 微信 UI 美化

---

## 30. 尚待继续讨论

当前未最终拍板：

1. 私聊输入区按钮布局
2. 聊天顶部具体按钮
3. 转发卡片最终样式
4. “采纳”的注入格式与生命周期
5. 联系人详情页最终视觉布局
6. API 设置页最终字段与首发 Provider
7. 编剧正式 Prompt
8. 攻略正式 Prompt
9. 红笔编辑正式 Prompt
10. 自定义联系人 Prompt 编辑体验
11. 全局生成框架每个条目的最终 UI
12. 第一版最终 UI 美化
13. README / Credits / LICENSE / NOTICE 的最终发布整理

---

## 31. 维护约定

从现在开始：

- 已确认功能进入 `SPEC.md`
- 后续如果规则被推翻，直接修改 `SPEC.md`
- 不在代码里偷偷保留与 SPEC 冲突的旧逻辑
- 开发时以最新 `SPEC.md` 为唯一设计源
- 大功能确认后再集中更新 SPEC，避免每个小问题都改一次文件

